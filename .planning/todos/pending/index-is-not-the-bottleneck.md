---
created: 2026-09-21
severity: medium
source: 2026-09-21 인덱스 10그룹 증설 실측 (잡 a19789b5 · 53분 · 크레딧 0)
evidence: 인덱스 30그룹 -> 40그룹 후에도 상품 연결 51 불변
---

# 인덱스를 늘려도 연결은 안 는다 — 상한은 **스캔 범위**다

## 무슨 일이 났나

파랑 배너가 `상품 연결 51/6062 · 인덱스 미보유 5978행` 을 띄우고
**"인덱스 구축을 끝내고 조인 스캔을 다시 돌리면 줄어든다"** 고 안내한다.
그 말을 믿고 인덱스를 30그룹 → 40그룹으로 늘렸다(9,500행 · 53분 · 미조회 0).

**연결은 51에서 1건도 안 늘었다.**

## 원인

```
인덱스 = 어디를 볼지 아는 지도
스캔   = 실제로 가서 보는 행위   ← 여기가 진짜 상한
```

`post_bulsaja_scan` 과 `post_bulsaja_index` 는 둘 다 대상을 **③원인분석·⑤효자확정
194행으로 하드코딩 축소**한다(D-18 최적화 — 2시간 7분을 깎은 그 결정).

그래서 ③⑤ 194행 안에서 `"불사자 인덱스에 아직 없다"` 는 **0건**이다.
인덱스는 이 범위에선 **처음부터 병목이 아니었다.**
새로 채운 10그룹은 ③⑤ **밖** 행들을 위한 것인데, 스캔이 그 행들을 안 훑는다.

## 194행의 실제 내역 (2026-09-21)

| 버킷 | 수 | 내용 |
|---|---|---|
| 연결됨 | 51 | 🔴31 · 🟡20 |
| 광고청소 (사람 몫) | 102 | 번호 추출실패 57 · 번호없음 45 |
| 시스템 | 41 | "마켓그룹에 이 상품이 없다" 33 · 제외설정(13-2) 8 |

**연결률을 올리는 건 인덱스가 아니라 광고 쪽 청소 102건이다.**

## 화면 문구가 오해를 만든다

파랑 배너의 `5978행` 은 "인덱스만 채우면 풀린다" 로 읽힌다. 실제로는
**스캔 범위를 같이 넓혀야** 풀린다. 분모 `6062` 는 번호해소 전체인데
분자 `51` 은 스캔한 194행 안에서 나온 수다 — **분모와 분자의 모집단이 다르다.**

이 페이즈가 `추출실패`/`미조회` 를 가른 것과 같은 원칙이 필요하다:
**"인덱스가 없어서 못 봤다" 와 "애초에 볼 대상이 아니었다" 는 다른 사실이다.**

## Phase 5 로 넘기는 것

Phase 5 는 ③⑤ 밖의 🔴 상품도 골라야 한다. 그때 스캔 범위를 넓히는데,
**범위를 넓히는 순간 D-18 이 깎아 낸 비용이 도로 붙는다.** 미리 결정할 것:

- 스캔 범위를 무엇으로 넓힐 것인가 (전체 6,062행? 🔴 후보만?)
- 넓힌 범위의 인덱스 비용은? (40그룹은 지금 깔려 있다 — 그래서 이 53분은 선행투자다)
- 파랑 배너 분모를 스캔 범위 기준으로 바꿀 것인가

## 남긴 자산

2026-09-21 기준 인덱스 **40그룹** 보유(③⑤ 30 + 확장 10).
Phase 5 에서 범위를 넓힐 때 이 10그룹은 다시 안 훑어도 된다.
