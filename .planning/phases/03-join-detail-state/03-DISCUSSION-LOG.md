# Phase 3 Discussion Log

**Date:** 2026-09-21

## 논의한 회색지대

제시한 6개 중 **1번(조인 키)** 과 **5번(크레딧 무한루프)** 이 실측으로 해소됐다.
나머지 4개(미해소 표시 위치 · 팬아웃 경고 시점 · 상태 조회 방식 · 계정 가드)는
Claude 재량 또는 계획 단계로 넘겼다.

## 이 세션에서 실제로 측정한 것

| 측정 | 결과 |
|---|---|
| ③+⑤ 작업 대상 후보 | **194행** (7계정, 2026-09-20 회차) |
| 광고그룹 번호 추출 | 69개 중 **68개 성공**, 1개 누락(`소형이동식오피스` → 실제 25-2) |
| 그룹번호 ↔ 부킹 마켓그룹 매칭 | 63개 중 **44개 매칭**, 19개(`5-x~12-x`) 미매칭 |
| 상품명 조회 정확도 | 6건 중 **4건(67%)** — 2건 실패(사본 2건 반환 / 0건) |
| `mallProductId` ↔ `uploadedSuccessUrl.smartstore` | **정확 일치 확인** |
| `find_by_code`(mallProductId) | **4건 전부 0** — 안 된다 |
| `find_by_code`(불사자코드) | **사본 2건 반환** — 팬아웃 키로 성립 |
| `imageTranslated` 실측 타입 | 문자열 `"1"` |
| `detail_batch.py` 실패 원인 | 48행 윈도 경로 하드코딩. 모듈은 실재 |

## 방향이 바뀐 지점

용팀장이 제시한 방법은 **"그룹 번호로 좁힌 뒤 상품명으로 조회"** 였다.
앞 절반(그룹 번호)은 채택했고, 뒤 절반(상품명)은 실측 67% 라 버렸다 —
물갈이 사본이 같은 단어를 순서만 섞어 올라가 있어 이름 기반은 조용히 틀린 상품을 집는다.
대신 `mallProductId` 정확 일치를 찾아 대체했다. 결과적으로 더 단순하고 정확해졌다.

## 용팀장 지시 (원문)

> "구매_가공완료 되어있는건 추가 작업은 하지말고, 내가 그걸 인지할 수 있게 방법을 알려줘.
> 그럼 내가 추가 작업을 할지말지 판단할게."

→ D-08. 자동 제외하되 화면에서 회색으로 보이고 직접 선택 가능.

## 계획 전에 답이 필요한 것

`5-x ~ 12-x` 마켓그룹이 용팀장 불사자 계정에 있는지 **미확인**.
맞다면 조인이 계정 2개를 다 봐야 하고 ENG-08 이 "전환"까지 가야 한다.
