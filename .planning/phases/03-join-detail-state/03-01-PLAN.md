---
phase: 03-join-detail-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webapp/settings.py
  - workspace.toml
  - .gitignore
  - webapp/tests/test_paths.py
  - webapp/tests/test_board.py
  - webapp/tests/test_jobs.py
  - webapp/tests/conftest.py
  - webapp/tests/fixtures/result_traps.json
  - webapp/tests/fixtures/bulsaja_groups_traps.json
  - webapp/tests/fixtures/join_traps.json
  - webapp/tests/fixtures/result_join_real.json
  - webapp/tests/fixtures/anonymize_join.py
  - webapp/tests/test_cli_shim.py
  - .claude/skills/bulsaja-detail-page/scripts/detail_batch.py
autonomous: true
requirements: [STATE-01]

must_haves:
  truths:
    - "`.venv/bin/python3 .claude/skills/bulsaja-detail-page/scripts/detail_batch.py --help` 가 이 맥북에서 exit 0 이다 — Core Value 경로의 첫 관문이 열린다 (STATE-01 / 성공기준 4)"
    - "설정 8키가 `settings.DEFAULTS` 에 있고 실값은 `workspace.toml [webapp]` 에만 있다 — 기대 닉네임·제외그룹이 코드·주석 어디에도 없다 (D-21 / D-18)"
    - "`settings.cfg('expected_bulsaja_nick', required=True)` 가 기동 시 KeyError 로 터지지 않는다 — toml 에 값이 들어갔기 때문이다"
    - "아직 존재하지 않는 런타임 파일 3종이 계정 리터럴 가드 리스트에 **미리** 들어 있다 — 나중에 넣으면 가드가 있는 척만 한다"
    - "테이블 가드가 `{jobs, ss_index}` 로 넓어졌고, **왜 캐시가 아닌지**가 그 테스트 docstring 에 적혀 있다 (D-20)"
    - "픽스처가 함정 8종을 담고, 진짜 마켓그룹명·계정 닉네임은 담지 않는다"
  artifacts:
    - path: "webapp/settings.py"
      provides: "expected_bulsaja_nick · profile_path · mcp_min_interval · mcp_retry_after · mcp_batch_size · index_excluded_groups · done_tags · profile_max_age_min"
      contains: "index_excluded_groups"
    - path: "workspace.toml"
      provides: "[webapp] 테이블 — 기대 닉네임과 D-18 제외 마켓번호의 유일한 자리"
      contains: "[webapp]"
    - path: "webapp/tests/fixtures/join_traps.json"
      provides: "조인 잡 산출물 모양 + 상태 판정 함정 4종 + 미조회/불일치/팬아웃/표시규칙"
    - path: "webapp/tests/fixtures/result_join_real.json"
      provides: "실회차 ③⑤ 194행 익명화본 — 해상률 회귀의 모수"
    - path: "webapp/tests/test_cli_shim.py"
      provides: "detail_batch.py --help 가 exit 0 (STATE-01 회귀)"
  key_links:
    - from: ".claude/skills/bulsaja-detail-page/scripts/detail_batch.py"
      to: ".claude/skills/bulsaja-category-fix/scripts/bulsaja_mcp.py"
      via: "__file__ 기준 상대 경로 계산 후 sys.path.insert (run_names.py:40-46 관용구)"
      pattern: "os.path.normpath"
    - from: "webapp/settings.py"
      to: "workspace.toml [webapp]"
      via: "paths.read_workspace_toml().get('webapp') 얕은 덮어쓰기"
      pattern: "expected_bulsaja_nick"
---

## Phase Goal

> ⚠ ROADMAP `**Goal:**` 이 user-story 형식(`As a … I want to … so that …`)이 아니다.
> 정규화하려면 `/gsd mvp-phase 03` 을 먼저 돌려라. 아래는 ROADMAP 원문 그대로다 — 지어내지 않았다.

광고 판정 행을 불사자 상품에 실제로 잇고 상세 상태를 3단계로 판정해, "유입은 있는데 상세가 중국어 원본인 상품" 목록을 화면에서 정확히 뽑아낸다.

<objective>
**Wave 0 — 이 페이즈가 딛고 설 바닥을 먼저 깐다. 그리고 막혀 있던 Core Value 경로 하나를 이 플랜에서 바로 뚫는다.**

세 가지다:
1. 설정 8키 + `workspace.toml [webapp]` 신설 — 기대 닉네임·제외그룹이 코드에 안 박히게 (D-21 / D-18)
2. 픽스처·가드 확장 — 뒤 플랜들이 만들 파일을 **미리** 감시 대상에 넣고, 함정을 심은 픽스처를 깐다
3. `detail_batch.py` 윈도 경로 3줄 치환 — 이 맥북에서 `--help` 가 exit 0 이 된다 (STATE-01)

Purpose: 3번은 그 자체로 사용자가 얻는 것이다(막혀 있던 CLI 가 이 맥북에서 돈다).
1·2번은 뒤 다섯 플랜 전부가 이것 없이는 리터럴 가드에 걸리거나 기동 즉시 KeyError 로 죽는다.
Output: 설정 8키 · `[webapp]` 테이블 · 픽스처 5종 · 가드 3종 확장 · shim + 회귀 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-join-detail-state/03-CONTEXT.md
@.planning/phases/03-join-detail-state/03-RESEARCH.md
@.planning/phases/03-join-detail-state/03-PATTERNS.md
@.planning/phases/03-join-detail-state/03-VALIDATION.md

<interfaces>
이 플랜이 굳히는 계약. 03-02 ~ 03-06 이 전부 이 이름들을 전제로 쓴다.

webapp/settings.py — DEFAULTS 에 더할 8키 (기존 6키는 건드리지 않는다):
  "expected_bulsaja_nick": ""        # ENG-08. **빈 문자열이어야 한다** — 실값은 workspace.toml 에만
  "profile_path": "webapp-profile.json"   # 계정 확인 산출물 (저장소 루트 상대)
  "profile_max_age_min": 120         # 이보다 오래된 계정 확인은 안 믿는다 (ENG-08 사전점검)
  "mcp_min_interval": 0.26           # 초당 4회 하한 (RateLimit-Policy 240;w=60 실측)
  "mcp_retry_after": 21              # 서버 Retry-After: 20 + 1초
  "mcp_batch_size": 50               # find_by_code 상한 · 목록 페이지 크기
  "index_excluded_groups": []        # D-18. **마켓번호 문자열 리스트.** 빈 리스트 = 제외 없음
  "done_tags": ["구매_가공완료"]      # D-08 기작업 태그. 늘어날 수 있으므로 리스트

workspace.toml 에 신설할 테이블 (이 파일은 .gitignore 대상이고 webapp/** 밖이라 가드 범위 밖):
  [webapp]
  expected_bulsaja_nick = "<현재 붙어 있는 불사자 계정 닉네임>"
  index_excluded_groups = ["13-2"]    # D-18 — 마켓번호로 적는다. 그룹ID 를 적지 마라

webapp/tests/conftest.py 에 더할 픽스처 4개 (기존 fake_result_json 과 같은 모양):
  result_traps          -> dict   # 광고 쪽 함정 ③⑤ 행
  bulsaja_groups_traps  -> dict   # 불사자 마켓그룹 목록 함정
  join_traps            -> dict   # 조인 잡 산출물 모양 + 상태/팬아웃/표시규칙 함정
  result_join_real      -> dict   # 실회차 ③⑤ 194행 익명화본

조인 잡 산출물(`join_<job_id>.json`) 스키마 — 03-04 의 CLI 가 쓰고 03-02 의 순수 함수가 읽는다:
  {
    "run_dir": "<회차명>",
    "생성시각": "<ISO8601>",
    "계정": {"닉네임": str, "크레딧": str, "확인시각": "<ISO8601>"},
    "마켓그룹": [{"groupId": str, "그룹명": str}, ...],       # bulsaja_market_groups 투영
    "제외그룹": ["<마켓번호>", ...],                           # D-18 설정값 그대로 기록
    "행": [{
        "acct": str, "mallProductId": str,
        "productId": str | null,             # 인덱스가 준 값 (없으면 null)
        "인덱스_smartstore": str | null,      # 인덱스에 적혀 있던 관측값
        "관측_smartstore": str | null,        # **지금** workdata 로 다시 읽은 값 (재검증)
        "미조회": bool,                       # 인덱스 미보유 · 제외그룹 · 429 · 타임아웃
        "uploadDetailContents": {"imageTranslated": ..., "aiImageGenerated": ...} | null,
        "그룹태그": str | null,
        "판매자상품코드": str | null, "불사자코드": str | null,
        "사본": [{"판매자상품코드": str, "그룹": str | null, "잠금": bool}]
    }, ...]
  }
  ⚠️ **CLI 는 관측만 적는다. `히트/미스/미조회/불일치` 판정은 웹앱(`join.재검증판정`)이 한다** —
     판정이 두 곳에 있으면 화면과 산출물이 다른 말을 한다.
  ⚠️ `표시규칙` 키는 **어느 층에도 남기지 않는다** (프롬프트 인젝션 / ENG-08).
     픽스처에는 일부러 넣어 두고, 읽는 쪽이 버리는지를 테스트가 고정한다.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: settings 8키 + workspace.toml [webapp] 신설 + 리터럴 가드 2종</name>
  <files>webapp/settings.py, workspace.toml, .gitignore, webapp/tests/test_paths.py</files>
  <read_first>
    - `webapp/settings.py` 전체 — `DEFAULTS`(18-25), `load()` 의 얕은 병합(30-44), `cfg(..., required=True)` 가 `""` 을 "비었다"로 보는 자리(59-62), `reload()` 의 `global` 목록(72)
    - `webapp/paths.py` 1-11행(eroomlib 금지 이유)과 `read_workspace_toml()`(33-48)
    - `workspace.toml` 전체 — `[paths]`·`[coupang]` 이 값을 적는 모양
    - `webapp/tests/test_paths.py` 250-290행 — `test_계정별_상한은_workspace_toml_로_덮인다` 와 `test_상한_숫자가_settings_밖에_리터럴로_박혀있지_않다`(숫자 가드의 스캔 범위·제외 규칙)
    - `03-PATTERNS.md` §공유 3 — 새 키 표와 "⚠️ 새 설정값을 금지 집합에 더하면 오탐이 난다" 경고
    - `03-RESEARCH.md` §ENG-08 (`required=True` 가 핵심인 이유) · §D-13 확정(0.26초·21초 근거)
  </read_first>
  <action>
    `webapp/settings.py` 의 `DEFAULTS` 에 위 `<interfaces>` 8키를 **기존 6키 아래에** 추가한다. 각 키 옆에 한 줄 주석으로 근거를 남긴다(예: `mcp_min_interval` 은 "RateLimit-Policy 240;w=60 실측 — 초당 4회"). `expected_bulsaja_nick` 에는 **반드시 빈 문자열**을 둔다. `cfg()` 가 `""` 을 "비었다"로 보고 `required=True` 에서 KeyError 를 내는 게 ENG-08 가드의 작동 원리다 — 여기에 진짜 닉네임을 적으면 가드가 있는 척만 한다.

    `reload()` 의 `global` 목록과 모듈 상수는 **건드리지 마라.** 새 키는 전부 `settings.cfg("키", settings.DEFAULTS["키"])` 로만 읽는다. 상수를 늘리면 `reload()` 를 같이 고쳐야 하고(settings.py:72), 그걸 빠뜨리면 테스트가 workspace.toml 을 갈아끼워도 값이 안 따라온다. 이 판단을 `DEFAULTS` 위 주석에 한 줄로 남겨라.

    `workspace.toml` 맨 아래에 `[webapp]` 테이블을 신설한다. `expected_bulsaja_nick` 에는 **지금 이 맥북의 `bulsaja` MCP 에 붙어 있는 계정 닉네임**을 적는다(`03-RESEARCH.md` §실측 종합표의 `bulsaja_my_profile` 결과에 그 값이 있다). `index_excluded_groups = ["13-2"]` 로 D-18 을 적는다 — **마켓번호 문자열**이다. 그룹 ID 나 그룹명을 적지 마라. 테이블 위에 주석으로 "이 값들이 여기 있는 이유: webapp/** 안에 적으면 리터럴 가드에 걸리고, 설정을 고쳐도 동작이 안 바뀌는 가짜 가드가 된다" 를 남겨라.

    `.gitignore` 에 `webapp-profile.json` 한 줄을 `webapp.db*` 옆에 더한다 (계정 확인 산출물 — 런타임 파일이고 커밋 대상이 아니다).

    `webapp/tests/test_paths.py` 에 테스트 3개를 더한다. **기존 `test_상한_숫자가_settings_밖에_리터럴로_박혀있지_않다` 의 `금지` 집합에는 아무것도 더하지 마라** — `50`·`21` 같은 작은 정수를 넣으면 `width: 50` 같은 무관한 코드가 전부 걸려 오탐이 난다(PATTERNS §공유 2(b) 경고). 대신 **문자열 값 전용 가드**를 따로 둔다:

    - `test_불사자_기대닉네임이_리터럴로_박혀있지_않다` — `settings.cfg("expected_bulsaja_nick", required=True)` 로 값을 읽고, `webapp/**` 의 `*.py`/`*.html`/`*.js` 본문에 그 문자열이 0건인지 본다. 제외는 기존 가드와 같다(`settings.py`, `tests` 경로, `vendor`). 값이 비어 있으면 KeyError 로 터지는 게 맞다 — 그게 T-1-12 의 정신이다. docstring 에 그 사실을 적어라.
    - `test_제외그룹이_리터럴로_박혀있지_않다` — `settings.cfg("index_excluded_groups", [])` 의 각 원소 문자열이 런타임 파일에 0건 (D-18).
    - `test_새_설정키가_workspace_toml_로_덮인다` — 기존 `test_계정별_상한은_workspace_toml_로_덮인다` 를 본떠, tmp 에 `[webapp]` 을 써 놓고 `settings.reload()` 뒤 `cfg("mcp_min_interval")`·`cfg("index_excluded_groups")` 가 덮인 값을 주는지 본다. **덮지 않은 키는 DEFAULTS 가 그대로 산다**(얕은 병합)도 같이 단언해라.

    테스트 이름에 `리터럴` 이 들어가야 한다 — `03-VALIDATION.md` 가 `pytest webapp/tests/test_paths.py -k 리터럴` 로 이 행들을 집는다.
  </action>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_paths.py -x -q && .venv-web/bin/python3 -c "from webapp import settings; assert settings.cfg('expected_bulsaja_nick', required=True); assert settings.cfg('index_excluded_groups') == ['13-2']; assert settings.DEFAULTS['expected_bulsaja_nick'] == ''; print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv-web/bin/pytest webapp/tests/test_paths.py -x -q` 가 exit 0
    - `.venv-web/bin/pytest webapp/tests/test_paths.py -k 리터럴 -x` 가 exit 0 이고 **3개 이상** 수집된다 (`-q` 출력의 passed 수로 확인)
    - `.venv-web/bin/python3 -c "from webapp import settings; print(settings.DEFAULTS['expected_bulsaja_nick'])"` 의 출력이 **빈 줄**이다
    - `grep -c '^\[webapp\]' workspace.toml` 이 1
    - `grep -v '^#' webapp/settings.py | grep -c index_excluded_groups` 가 1 이상
    - `grep -rn "$(.venv-web/bin/python3 -c 'from webapp import settings;print(settings.cfg("expected_bulsaja_nick", required=True))')" webapp --include=*.py --include=*.html --include=*.js | grep -v '/tests/' | grep -v settings.py` 가 **0줄**
  </acceptance_criteria>
  <done>설정 8키가 DEFAULTS 에 있고 실값은 workspace.toml [webapp] 에만 있다. 리터럴 가드 2종이 새 문자열 값을 상시 감시한다. `.gitignore` 에 `webapp-profile.json` 이 있다.</done>
</task>

<task type="auto">
  <name>Task 2: 픽스처 5종 + conftest 픽스처 4개 + 가드 리스트 2종 확장</name>
  <files>webapp/tests/fixtures/result_traps.json, webapp/tests/fixtures/bulsaja_groups_traps.json, webapp/tests/fixtures/join_traps.json, webapp/tests/fixtures/result_join_real.json, webapp/tests/fixtures/anonymize_join.py, webapp/tests/conftest.py, webapp/tests/test_board.py, webapp/tests/test_jobs.py</files>
  <read_first>
    - `webapp/tests/fixtures/result_min.json` — 진짜 계정 4개 + 가짜 `zzfake` 가 섞인 모양. **새 픽스처가 베낄 정본**
    - `webapp/tests/conftest.py` 1-57행 — 모듈 docstring 의 "tmp 밖을 못 만진다" 원칙, `fake_result_json` 픽스처 모양(매번 새로 파싱)
    - `webapp/tests/test_board.py` 3-31행(모듈 docstring 의 "픽스처에 일부러 심어 둔 함정 4종" 열거 + `런타임_파일들` 리스트), 73-98행(`test_계정을_코드에_박지_않는다` 가 리스트를 어떻게 훑는지 — `f.is_file()` 로 없는 파일을 건너뛴다)
    - `webapp/tests/test_jobs.py` 460-471행 — `test_보드캐시와_대상별락_테이블을_만들지_않는다` (D-20 으로 일부러 고칠 대상)
    - `03-PATTERNS.md` §`webapp/tests/fixtures/` — 🔴 가짜 이름 규약 · Phase 3 판 함정 목록 (a)~(f)
    - `03-RESEARCH.md` §D-12 확정(키 부재 vs false, `'0'`/`'1'`/`False`/`1` 실측), §팬아웃 실측(`find_by_code` 응답 모양), §ENG-08 의 `표시규칙` 경고
    - `03-VALIDATION.md` §Wave 0 Requirements — 픽스처 ①~④ 와 "가짜 이름을 섞어라" 경고
  </read_first>
  <action>
    **(a) `result_traps.json`** — 광고 판정 결과(`result.json`) 모양의 작은 함정 픽스처. `result_min.json` 과 같은 3층 구조(`accounts.<alias>.rules.<규칙키>[]`)를 쓰되 ③원인분석·⑤효자확정만 담는다. 계정 alias 는 **전부 가짜**(`zzaa`·`zzbb`)로 짓는다. 행마다 `adId`·`adGroup`·`title`·`mallProductId` 를 넣고, 광고그룹명에 실측 포맷 3종과 함정을 심는다:
      - `판매상품_15-2_zzfakecompany` (언더바 접두)
      - `20-3 zzfakecompany` (공백 접두)
      - `1000개_22-1_zzfakecompany` (숫자 접두 — `1000` 이 오탐되지 않는지)
      - `zzfake이동식오피스` (번호 없음 → 추출실패)
      - `판매상품_9-9_zzfakecompany` (번호는 있는데 불사자에 없음 → 번호없음)
      - `판매상품_13-2_zzfakecompany` (D-18 제외 그룹 → 미조회)
      - 같은 번호 `17-3` 을 쓰는 광고그룹 **2개**(다른 계정) → 번호 기준 중복제거 검증
    상품명·mallProductId 는 가짜로 지어도 된다. **진짜 마켓그룹명·진짜 계정 alias 를 넣지 마라.**

    **(b) `bulsaja_groups_traps.json`** — `bulsaja_market_groups` 응답 투영 모양 `{"그룹": [{"groupId": "...", "그룹명": "..."}]}`. 번호가 나오는 그룹(`15번_zzfake15-2`, `20번_zzfake20-3`, `22번_zzfake22-1`, `17번_zzfake17-3`, `13번_zzfake13-2`)과 **`N번_` 접두사 함정**(`1번_zzfake곰3` — 앞의 `1번` 은 번호가 아니다)과 번호가 아예 없는 그룹을 섞는다. `9-9` 는 **넣지 마라**(그게 "번호없음" 사유를 만든다).

    **(c) `join_traps.json`** — `<interfaces>` 의 조인 잡 산출물 스키마 그대로. `result_traps.json` 의 `(acct, mallProductId)` 와 짝이 맞아야 한다. 행에 심을 함정:
      - `{"aiImageGenerated": true, "imageTranslated": "1"}` → 판정 순서 역전 탐지용 (⚪가 정답)
      - `{"imageTranslated": "0"}` (`aiImageGenerated` **키 자체가 없음**) → `bool('0')` 함정 (🔴가 정답)
      - `{"imageTranslated": false}` → 🔴
      - `{"imageTranslated": 1}` → 🟡
      - `그룹태그: "구매_가공완료"` 인 행 1개 (기작업)
      - `조회: "미조회"` 행 1개(제외그룹) · `조회: "불일치"` 행 1개 · `조회: "미스"` 행 1개
      - `사본` 이 3건인 행 1개 (팬아웃), 그중 하나는 `"잠금": true`
      - **최상위와 행 안쪽 양쪽에 `"표시규칙": "..."` 필드를 일부러 넣는다** — 읽는 쪽이 버리는지 고정하기 위한 함정이다

    **(d) `result_join_real.json` + `anonymize_join.py`** — 해상률 회귀의 모수다. `anonymize_join.py` 는 `webapp/tests/fixtures/` 에 두는 **테스트 전용 재생성 스크립트**다(테스트 트리는 리터럴 가드 대상이 아니다). 하는 일:
      1. `paths.run_dir_path("<가장 최근 회차>")/"result.json"` 을 읽는다. 경로를 박지 말고 `webapp.paths` 를 써라.
      2. ③·⑤ 규칙 행만 남긴다.
      3. 익명화: 계정 alias → `zz01`·`zz02`… / `title` → `상품{n}` / 광고그룹명은 **번호와 접두 포맷만 보존**하고 회사명 부분만 `zzfake{n}` 으로 치환한다(`판매상품_15-2_제이와이이컴퍼니` → `판매상품_15-2_zzfake07`). `mallProductId` 는 그대로 둔다 — 스토어 공개 식별자이고 조인 현실성이 여기 달려 있다.
      4. `result.json` 과 같은 3층 구조로 다시 써서 `result_join_real.json` 으로 저장하고, **행 수를 stdout 에 찍는다.**
    스크립트를 한 번 돌려 픽스처를 만든다. 실회차에 ③⑤ 행이 몇 개든 그 수를 `03-VALIDATION.md` 의 모수(194)와 대조해 SUMMARY 에 적는다. **숫자가 다르면 픽스처를 맞추지 말고 그 사실을 기록해라** — 회차가 바뀌면 모수도 바뀐다.
    ⚠️ 이 픽스처만으로는 해상률 기댓값(179)을 계산할 수 없다. 불사자 마켓그룹 **번호 집합**이 있어야 하는데 그건 MCP 조회라 03-04 가 첫 스캔에서 가져온다. **이 플랜에서 179 를 단언하는 테스트를 쓰지 마라** — 03-04 가 `bulsaja_groups_real.json` 을 뜬 뒤에 붙인다.

    **(e) `conftest.py`** — 픽스처 4개(`result_traps`·`bulsaja_groups_traps`·`join_traps`·`result_join_real`)를 `fake_result_json` 과 **같은 모양**(매번 새로 파싱)으로 더한다. 모듈 docstring 에 "Phase 3 픽스처는 진짜 마켓그룹명·계정 닉네임을 담지 않는다 — 담으면 리터럴 가드와 충돌한다" 를 한 줄 더한다.

    **(f) `test_board.py` 의 `런타임_파일들` 확장** — `webapp/join.py`, `webapp/state.py`, `webapp/bulsaja_index.py` 를 리스트에 더한다. **파일이 아직 없어도 된다** — 그 테스트는 `f.is_file()` 로 건너뛴다(test_board.py:91-92). 리스트 위 주석에 "파일보다 가드를 먼저 넣는다 — 나중에 넣으면 그 사이에 박힌 리터럴을 아무도 못 본다" 를 남겨라.

    **(g) `test_jobs.py` 의 테이블 가드 확장 (D-20)** — `test_보드캐시와_대상별락_테이블을_만들지_않는다` 의 허용 집합을 `{"jobs", "ss_index"}` 로 넓히고, **docstring 에 왜 이 예외가 생겼는지** 를 적는다: *보드 캐시는 `result.json` 에서 언제든 재생성 가능한 투영이고, `ss_index` 는 3시간 반을 태워야 다시 얻는 외부 관측 기록이다 — 성격이 다르다. 대상별 잠금 테이블은 여전히 Phase 2 다.* 조용히 고치면 다음 사람이 "보드 캐시 금지"가 풀린 줄 안다. 테스트 이름도 의미에 맞게 `test_스키마는_jobs_와_ss_index_뿐이다` 로 바꾸고, `assert 이름들 - {"jobs", "ss_index"} == set()` 로 둔다.
  </action>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_board.py webapp/tests/test_jobs.py -x -q && .venv-web/bin/python3 -c "
import json, pathlib
F = pathlib.Path('webapp/tests/fixtures')
j = json.loads((F/'join_traps.json').read_text(encoding='utf-8'))
r = json.loads((F/'result_traps.json').read_text(encoding='utf-8'))
g = json.loads((F/'bulsaja_groups_traps.json').read_text(encoding='utf-8'))
real = json.loads((F/'result_join_real.json').read_text(encoding='utf-8'))
assert '표시규칙' in json.dumps(j, ensure_ascii=False), '표시규칙 함정이 없다'
조회 = {x['조회'] for x in j['행']}
assert {'히트','미스','미조회','불일치'} <= 조회, 조회
그룹명 = [x['adGroup'] for a in r['accounts'].values() for rows in a['rules'].values() for x in rows]
assert any('1000개_' in n for n in 그룹명) and any(n.count('-')==0 for n in 그룹명), 그룹명
assert len(g['그룹']) >= 6
n = sum(len(rows) for a in real['accounts'].values() for rows in a['rules'].values())
print('실회차 ③⑤ 행수:', n)
print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - 위 명령이 exit 0 이고 마지막 줄이 `PASS`
    - `.venv-web/bin/pytest webapp/tests -q` 전량 통과 (기존 186종이 안 깨진다)
    - `grep -v '^#' webapp/tests/test_board.py | grep -c 'bulsaja_index.py'` 가 1 이상 (가드 리스트에 들어갔다)
    - `grep -c 'ss_index' webapp/tests/test_jobs.py` 가 2 이상 (허용 집합 + docstring 설명)
    - `webapp/tests/fixtures/result_join_real.json` 의 계정 키가 전부 `zz` 로 시작한다:
      `.venv-web/bin/python3 -c "import json;d=json.load(open('webapp/tests/fixtures/result_join_real.json'));assert all(k.startswith('zz') for k in d['accounts']), d['accounts'].keys();print('PASS')"`
  </acceptance_criteria>
  <done>픽스처 5종이 함정 8종을 담고 진짜 이름을 0건 담는다. 가드 2종이 아직 없는 파일과 새 테이블을 미리 감시한다.</done>
</task>

<task type="auto">
  <name>Task 3: detail_batch.py 윈도 경로 3줄 치환 + --help 회귀 테스트 (STATE-01)</name>
  <files>.claude/skills/bulsaja-detail-page/scripts/detail_batch.py, webapp/tests/test_cli_shim.py</files>
  <read_first>
    - `.claude/skills/bulsaja-detail-page/scripts/detail_batch.py` 1-60행 — docstring(체크포인트 규약·크레딧 단가), 40-43행 인코딩 관용구, **45-48행의 윈도 경로 하드코딩(고칠 자리)**, 31행의 `run_yong.py` 안내(같이 고칠 자리)
    - `.claude/skills/product-name/scripts/run_names.py` 34-47행 — **그대로 베낄 shim 관용구.** `SCRIPT_DIR`/`SKILLS_DIR`/`BULSAJA_SCRIPTS` 계산과 `sys.path.insert` 순서
    - `webapp/tests/test_cli_patch.py` 1-35행 — 웹앱 테스트가 CLI 를 만지는 유일하게 허가된 방식(테스트 프로세스 한정 `sys.path` 삽입), 200-230행 — `subprocess.run([str(CLI_PY), ...])` 로 CLI 를 실제로 띄우는 모양
    - `webapp/argv.py` 27-41행 — `PY_CLI`(= `.venv/bin/python3`) 가 왜 현재 인터프리터가 아닌지
    - `03-RESEARCH.md` §STATE-01 — 재현·원인·3줄 수정·검증이 전부 적혀 있다
    - `.claude/skills/bulsaja-detail-page/SKILL.md` 60-70행 — `run_yong.py` 가 죽은 경로임이 이미 적혀 있다(문서는 이미 맞다, 코드 docstring 만 틀렸다)
  </read_first>
  <action>
    `detail_batch.py` 45-48행의 하드코딩을 `run_names.py:40-47` 관용구로 치환한다. **로직은 한 줄도 바꾸지 마라** — import 경로 계산만이다. `os` 는 35행에 이미 import 돼 있으므로 import 추가가 없다. 치환 후 주석으로 "**경로를 박지 않는다** — 이 파일 위치에서 스킬 루트를 거슬러 올라가 찾는다. 같은 관용구: `product-name/scripts/run_names.py:40-46`" 를 남겨라.

    같이 고칠 것: docstring 31행의 `용쌤 계정: python run_yong.py <python절대경로> detail_batch.py ...`. 이 맥북에는 그 MCP 서버 항목이 없어서 그대로 따라 하면 실패한다(`SKILL.md` 가 이미 "죽은 경로"로 적어 뒀다). 그 줄을 지우고 **계정 닉네임을 적지 않은 채** "불사자 MCP 항목이 하나뿐인 환경에서는 래퍼 없이 그대로 돌린다" 로 바꿔라.

    `webapp/tests/test_cli_shim.py` 를 새로 쓴다. 모듈 docstring 에 "이 파일은 CLI 가 **이 맥북에서 import 단계를 통과하는지**만 본다 — 크레딧 0, 쓰기 0, 네트워크 0" 을 적는다. 테스트:
    - `test_상세배치가_이_맥북에서_뜬다` — `subprocess.run([str(PY_CLI), str(DETAIL_BATCH), "--help"], capture_output=True, timeout=60)` 의 `returncode == 0` 이고 stdout 에 `--run-dir` 가 있다. 실패 메시지에 `stderr` 마지막 20줄을 실어라(원인이 보여야 한다).
    - `test_윈도_경로가_남아있지_않다` — `detail_batch.py` 본문에 `C:\\` 드라이브 문자 경로가 0건. 소스 문자열 검사지만 이건 **경로 리터럴 재발 방지**라 문자열 검사가 맞는 도구다.
    ⚠️ 인터프리터는 `sys.executable` 을 쓰지 마라 — `.venv-web` 으로 CLI 를 띄우면 검증이 무의미하고, `test_argv.py:175-187` 이 웹앱 런타임에서 그 속성 사용을 금지한다. `webapp.argv.PY_CLI` 를 import 해서 쓰거나 `paths.repo_root()/".venv"/"bin"/"python3"` 를 직접 계산해라.
  </action>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_cli_shim.py -x -q && .venv/bin/python3 .claude/skills/bulsaja-detail-page/scripts/detail_batch.py --help > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python3 .claude/skills/bulsaja-detail-page/scripts/detail_batch.py --help` 가 **exit 0** 이고 usage 를 찍는다 (STATE-01 / ROADMAP 성공기준 4)
    - `.venv-web/bin/pytest webapp/tests/test_cli_shim.py -x -q` 가 exit 0
    - `grep -c 'C:\\\\Users' .claude/skills/bulsaja-detail-page/scripts/detail_batch.py` 가 0
    - `grep -v '^#' .claude/skills/bulsaja-detail-page/scripts/detail_batch.py | grep -c 'run_yong.py'` 가 0
    - `git diff --stat .claude/skills/bulsaja-detail-page/scripts/detail_batch.py` 의 변경 줄수가 **10줄 이하** (로직 무변경 shim 이라는 증거)
  </acceptance_criteria>
  <done>막혀 있던 Core Value 경로가 이 맥북에서 import 단계를 통과한다. 회귀가 스위트에 남는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `workspace.toml` → 웹앱 프로세스 | PC별 설정 파일. 값이 비면 ENG-08 가드가 사라진다 |
| 저장소 소스 → 테스트 가드 | 가드 리스트에 없는 파일은 감시되지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Elevation | `settings.cfg("expected_bulsaja_nick")` | mitigate | `DEFAULTS` 를 빈 문자열로 두고 `required=True` 로 읽어 KeyError 를 내게 한다. 실값은 `workspace.toml` 에만 (Task 1) |
| T-3-02 | Tampering | 리터럴 가드 무력화 | mitigate | 새 런타임 파일 3종을 **파일 생성 전에** `런타임_파일들` 에 넣는다 (Task 2f) |
| T-3-03 | Tampering | `ss_index` 신설로 "보드 캐시 금지" 원칙이 조용히 풀림 | mitigate | 허용 집합을 명시적으로 `{jobs, ss_index}` 로 넓히고 예외 사유를 docstring 에 고정 (Task 2g / D-20) |
| T-3-04 | Info Disclosure | 픽스처에 진짜 계정 닉네임·마켓그룹명 유출 | mitigate | 전 픽스처를 `zz*` 가짜 이름으로. 익명화 스크립트를 저장소에 남겨 재생성 가능하게 (Task 2d) |
| T-3-05 | Tampering | npm/pip/cargo 설치 | accept | **이 페이즈는 새 외부 패키지를 하나도 설치하지 않는다** (RESEARCH §Package Legitimacy Audit — 설치 대상 0건). 설치 태스크가 생기면 그 순간 설계가 틀린 것이다 |
</threat_model>

<verification>
```bash
.venv-web/bin/pytest webapp/tests -q                      # 기존 186종 + 신규 전량
.venv-web/bin/pytest webapp/tests/test_paths.py -k 리터럴 -x
bash webapp/tests/no_commit_guard.sh
.venv/bin/python3 .claude/skills/bulsaja-detail-page/scripts/detail_batch.py --help
```
</verification>

<success_criteria>
- `detail_batch.py --help` 가 이 맥북에서 exit 0 (STATE-01 · 성공기준 4)
- `settings.cfg("expected_bulsaja_nick", required=True)` 가 값을 돌려준다 (기동 즉시 KeyError 가 아니다)
- 기대 닉네임·제외 마켓번호 문자열이 `webapp/**` 런타임 파일에 0건
- 픽스처 5종이 존재하고 `표시규칙`·판정 함정 4종·미해소 3종·팬아웃을 담는다
- `webapp/tests` 전량 green
</success_criteria>

<output>
Create `.planning/phases/03-join-detail-state/03-01-SUMMARY.md` when done.
실회차 ③⑤ 행수 실측값을 SUMMARY 에 반드시 적어라 — 03-04 의 해상률 기댓값이 그 수를 분모로 쓴다.
</output>
