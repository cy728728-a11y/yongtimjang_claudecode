# Phase 3 — 다음 세션 이어서

**멈춘 지점:** 계획 **완료**. plan-checker 통과. **실행 전.**

## 바로 할 것

```
/gsd-execute-phase 3
```

계획 단계에서 물어볼 건 다 물어봤다. **묻지 말고 바로 실행에 들어가면 된다.**

## 실행 구조 — 7플랜 / 6웨이브

| 웨이브 | 플랜 | 내용 | 자율 |
|---|---|---|---|
| 1 | `03-01` | 기반 — 설정 키·픽스처 5종·리터럴 가드 확장·`detail_batch.py` shim | yes |
| 2 | `03-02` ∥ `03-03` | 순수 판정(`state.py`·`join.py`) ∥ 인덱스 저장소+잡 배선 (**병렬**) | yes |
| 3 | `03-04` | 불사자 CLI 3종 (`bulsaja_rate.py`·`ss_index_build.py`·`bulsaja_scan.py`) | yes |
| 4 | `03-05` | 실스캔으로 해상률 확정 + 라우트 + 버튼 | yes |
| 5 | `03-06` | 보드 상태 열·배너 2종·청소 목록·CDP 검증 | yes |
| 6 | `03-07` | **실탄** — 인덱스 완주 + 보드 확인 | **no · 체크포인트 2개** |

## 실행 중 사람이 필요한 지점 (딱 여기뿐)

**`03-07` 이 유일한 비자율 플랜이다.** 첫 인덱스 구축 **3시간 32분** 을 실제로 돌린다.
- `caffeinate -i` 로 감싸게 돼 있다 (맥북 잠들면 폴링이 끊긴다)
- 그룹 단위 체크포인트라 **중단·재개 된다.** 한 번에 끝내려 하지 마라
- 돌리기 전에 불사자 연결 계정이 **부킹(용쌤)** 인지 확인 — 아니면 잡이 스스로 거부한다

## 실행 중 나올 수 있는 것

**`03-05` 의 `test_실회차_해상률` 기댓값이 179/194 와 다르게 나올 수 있다.**
불사자 마켓그룹 번호 집합이 MCP 조회라 오프라인에서 기댓값을 못 만들었다.
→ **픽스처를 숫자에 맞추지 마라.** 실측값을 단언하고 왜 달라졌는지 사유를 적는 게 플랜의 지시다.
   (맞추는 순간 테스트가 거짓말을 시작한다)

## 이어받을 상태

- **Phase 1 완료** (9/9). 웹앱 동작 중 — `CT_DEV_TOKEN=devtoken123 ./webapp/run-webapp.sh`
- **Phase 2 는 이월** — Phase 5 계획 시 SAFE-04~07 중 크레딧 태우기 전에 끌어올 게 있는지 재확인
- 광고 데이터: `2026-09-20` 회차, 7계정
- **광고그룹 정리 완료** (2026-09-21, 용팀장이 2회 직접 수행). 69개 → 58개.
  번호 없는 그룹 0개 · 불사자에 없는 번호 0개. **해상률 179/194, 다음 회차 실질 100%**

## 읽을 문서 순서

1. `03-CONTEXT.md` — 결정 **D-01 ~ D-21**. 이게 최우선이다
2. `03-RESEARCH.md` — **최상단 🔴 갱신 공지를 먼저 읽어라.** 본문 옛 숫자(92/194 · 31그룹 · 4h25m)를 쓰면 틀린다
3. `03-PATTERNS.md` — 최상단 🔴 발견 3개 + 파일별 analog·줄번호·발췌
4. `03-VALIDATION.md` — 검증 26행 · Wave 0 갭 · 수동검증 3건

## 절대 깨면 안 되는 것 (플랜에 다 박혀 있지만 재확인용)

- **미해소 사유를 한 칸에 담지 마라.** `추출실패`/`번호없음`(광고 오류) vs `미조회`(인덱스 미보유·429).
  섞이면 용팀장이 **멀쩡한 광고그룹을 지우러 간다.** 이 페이즈 최대 오진이다
- **판정 순서:** `aiImageGenerated`(키 부재로 온다, `False` 아님) 가 `imageTranslated` 보다 **우선**.
  뒤집히면 ⚪가 🟡로 내려앉고 Phase 5 가 크레딧을 다시 태운다. `bool('0')` 은 True — `bool()` 금지
- **D-19:** `webapp/` 아래 어떤 파일도 `eroomlib`·`requests` 를 import 하지 않는다.
  `.venv-web` 에 `requests` 가 없다. MCP 접촉은 전부 CLI 자식 프로세스
- **새 런타임 파일은 `test_board.py` 의 `런타임_파일들` 에 등록해야** 리터럴 가드가 실제로 작동한다
- **429 를 성공으로 접지 마라.** 초당 4회, `Retry-After: 20`
- **`--workers 1`** 유지
- 해상률이 지금 100% 라고 **미해소 경로를 빼지 마라.** 물갈이로 다시 어긋난다

## 열린 항목 (Phase 3 와 무관)

1. `01-HUMAN-UAT.md` — 되돌리기 실탄 미검증. 싸게 닫는 법: 5건만 되돌렸다 즉시 재인상
2. 실제 인상 472건 유지 중 (cy728, 되돌리지 않음 — 용팀장 지시)
