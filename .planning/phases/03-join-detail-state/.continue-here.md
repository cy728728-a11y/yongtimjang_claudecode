# Phase 3 — 다음 세션 이어서

**멈춘 지점:** 실행·수정·재검증 **전부 완료.** 검증이 `human_needed` 라 **ROADMAP 완료만 안 찍혔다.**
막힌 것은 **JOIN-02 의 사람 진술 두 줄**뿐이다. 코드 작업은 없다.

## 바로 할 것 — 둘 중 하나

**A. 두 줄을 답한다 (권장 · 5분)** → `03-HUMAN-UAT.md` 채우고 ROADMAP 완료 → Phase 4

1. 보드의 **미해소 청소 목록(19그룹)** 에 적힌 광고그룹명·번호만 들고
   네이버 검색광고에서 **그 그룹을 실제로 찾을 수 있었나?**
2. **빨강 배너**("광고 쪽에서 내가 지울 것" · 883행/19그룹) 와
   **파랑 배너**("시스템이 못 읽은 것" · 상품 연결 51/6062) 가 **헷갈렸나?**
   ← 이 페이즈 최대 오진 지점. 헷갈리면 멀쩡한 광고그룹을 지우러 간다

**B. 그냥 넘긴다** → `/gsd:verify-work 3` 으로 나중에 닫는다. `/gsd:progress` 에 계속 뜬다.

답을 받으면: `03-HUMAN-UAT.md` result 채움 → `gsd-sdk query phase.complete 03` → 커밋 → 푸시 → Phase 4.

## 현재 상태 (2026-09-21 푸시 완료 `ecf77e7`)

| 항목 | 값 |
|---|---|
| 플랜 | **7/7 완료** |
| 테스트 | **355 passed** (340 → CR 수정으로 +15) |
| 코드 검증 | **10/10 must-have** (`03-VERIFICATION.md` · `human_needed`) |
| 인덱스 | 30,546행 / 30그룹 / 미조회 0 · 첫 완주 2시간 8분 · 429 0회 |
| 보드 | 상품 연결 51 (🔴31 · 🟡20 · ⚪0) · 기작업 29 · 사본 42(최대 23) |
| 크레딧 | **0** (페이즈 전체) |
| 서버 | 8765 · 토큰 고정(`CT_DEV_TOKEN`) · `webapp-server.log` |

URL: `http://127.0.0.1:8765/?t=P-8dEcLYRJjXo4urok3aAwcvk0SEjjj9pX0FwuB0dy0`
죽었으면: `CT_DEV_TOKEN=... nohup ./webapp/run-webapp.sh >> webapp-server.log 2>&1 &`

## 이번에 고친 Critical 3건 — 셋 다 같은 병이었다

**불사자 MCP 의 툴 레벨 오류는 예외가 아니라 평범한 dict 로 온다**(`{"_text": "MCP error -32602: ..."}`).
호출부가 `.get("항목") or []` 로 읽으면 **"오류"가 "물어봤더니 없더라"로 둔갑한다.**
막으려고 만든 `항목꺼내기` 가 **3곳 중 1곳에만** 붙어 있었다.

| | 증상 | 수정 |
|---|---|---|
| CR-01 | 마켓그룹 조회 오류 → 전 행 `번호없음`(광고청소). 멀쩡한 그룹 8개를 "지워라"로 띄움 | `ec2d5e7` 2층 |
| CR-02 | 팬아웃 실패 → "사본 0건·기작업 아님" → Phase 5 크레딧 재지불 | `1ee701a` 3층 |
| CR-03 | workdata 오류 → `unresolved=0`(성공) 영구 기록 → 재개가 영원히 건너뜀 | `268b3dc` |

**지금 구조:** `ss_index_calls.목록꺼내기` 가 공통 본체, `항목꺼내기`·`그룹꺼내기`·`워크데이터꺼내기` 셋이 전부 그걸 부른다.
**원칙 — 이걸 깨지 마라:** 해석("모양 아니면 예외")과 정책("0개면 거부")을 가른다.
**"못 읽었다"와 "0개라 답했다"는 다른 사실이다.**

## 남은 것 (Phase 3 완료를 막지 않음)

1. 🔴 **그룹 단계 429 가 "완주"로 둔갑** — 용팀장이 "기록만" 결정. `.planning/todos/pending/429-group-stage-silent-completion.md`
   운용 회피책: **인덱스 잡과 스캔 잡을 동시에 띄우지 마라** (레이트리밋이 프로세스마다 걸린다)
2. 🟡 **`join.py` 번호 정규식에 숫자 경계 없음** — `2024-12_이벤트_7-1` → `24-12` 를 집는다(실측).
   픽스처 쪽 `anonymize_join.py:44` 는 경계가 있다. **런타임과 픽스처가 다른 규칙이다.** 실데이터 미발현
3. 🟡 **옛 스캔 산출물의 `사본: []`** 는 "없음"과 "못 물어봤음"을 구분 못 한다. 다음 스캔부터 정상

## 절대 깨면 안 되는 것

- **미해소 사유를 한 칸에 담지 마라.** `추출실패`/`번호없음`(광고 오류) vs `미조회`(시스템)
- **판정 순서:** `aiImageGenerated` 가 `imageTranslated` 보다 우선. **`bool()` 금지**(`bool('0')` 은 True)
- **D-19:** `webapp/` 아래 `eroomlib`·`requests` import 0건. MCP 접촉은 전부 CLI 자식 프로세스
- **화면에 `미스` 를 그대로 쓰지 마라.** `group_health(...)["완결"]` 로 갈라 "인덱스 불완전(시스템)"
- **429 를 성공으로 접지 마라.** 초당 4회, `Retry-After: 20`
- **`--workers 1`** 유지
- 해상률이 100% 라고 **미해소 경로를 빼지 마라.** 물갈이로 다시 어긋난다

## 페이즈 밖 열린 항목

1. `01-HUMAN-UAT.md` — 되돌리기 실탄 미검증. 싸게 닫는 법: 5건만 되돌렸다 즉시 재인상
2. 실제 인상 472건 유지 중 (되돌리지 않음 — 용팀장 지시)
3. **Phase 2 는 이월** — Phase 5 계획 시 SAFE-04~07 중 크레딧 태우기 전에 끌어올 게 있는지 재확인
