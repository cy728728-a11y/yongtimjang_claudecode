# Phase 3 — 다음 세션 이어서

**멈춘 지점:** `03-07` **Task 3 사람 게이트 대기.** 7플랜 중 6개 완료 + 03-07 은 4태스크 중 3개 진행(Task 1·2 완료, Task 4 부분).

## 바로 할 것

서버가 떠 있으면 브라우저로 보드를 열고 **Task 3 6항목**을 확인한 뒤 "승인" 또는 고칠 점을 답한다.
그 다음 `/gsd-execute-phase 3` → 03-07 Task 4 마감 → SUMMARY → 페이즈 검증으로 이어진다.

서버가 죽었으면 다시 띄운다 (토큰은 재시작마다 바뀐다 — `webapp-server.log` 마지막 `→ http://...` 줄이 입구):

```
nohup ./webapp/run-webapp.sh > webapp-server.log 2>&1 &
```

## 지금까지 확정된 것

| 항목 | 값 |
|---|---|
| 인덱스 | **30,546행 / 30그룹 / 미조회 0** · 소요 2시간 8분 · **429 0회** |
| 보드 상품 연결 | 0 → **51** (🔴31 · 🟡20 · ⚪0) |
| 기작업 / 사본 | 29행 / 42행(최대 23건) |
| 미해소 청소 목록 | **19그룹 883행** |
| 🔴 13-2 검사 | **PASS** — 197행 전부 `미조회`. `번호없음` 0건 |
| 테스트 | 340 passed |
| 불사자 계정 | 부킹(용쌤) · 크레딧 933,172 (이 페이즈는 0 소모) |

증거: `.planning/phases/03-join-detail-state/03-07-evidence/` 4파일.

## Task 3 에서 용팀장이 답해야 하는 것

1. 🔴 필터 **31건**이 "유입은 있는데 상세가 중국어 원본인 상품"으로 읽히는가 (Phase 5 의 입력)
2. 기작업 29행이 회색이되 목록에 남고, `필터 전체 선택` 이 그걸 빼는가 / 손으로 체크하면 들어가는가
3. 사본 2건 이상인 행 하나를 불사자에서 눈으로 대조
4. **미해소 청소 목록(19그룹)의 광고그룹명·번호만 들고 네이버 광고에서 그 그룹을 찾을 수 있는가** ← JOIN-02, 되든 안 되든 한 줄
5. **두 배너("광고 쪽에서 내가 지울 것" 빨강 883행/19그룹 vs "시스템이 못 읽은 것" 파랑 51/6062)가 헷갈리는가** ← 되든 안 되든 한 줄
6. ⚪ 행 대조 → **건너뛴다** (⚪ 0건이라 대조할 게 없다)

## 실탄에서만 드러난 것 (Task 2 에서 고침 — 되돌리지 마라)

1. **`caffeinate -i` 가 실제로 안 붙고 있었다.** 모델에 `prefix` 필드가 있고 조립 테스트도 green 인데 `jobs._build_argv` 가 안 넘겼다. 2시간짜리 잡이 절전에 무방비였다
2. **`groupId` 를 문자열로 보내 30그룹이 9.2초에 "성공"했다.** 서버가 `-32602 expected number` 를 줬고 그 응답에 `항목` 키가 없어 `r.get("항목") or []` 가 "상품 0건"으로 읽었다. **크레딧 0이라 경보가 없다** — 조용히 빈 인덱스를 만들 뻔했다
3. **스캔이 `mode=summary` 라 사본·기작업이 전 행 0이었다.** `uploadBulsajaCode` 가 summary 14키엔 없고 full 38키엔 있다

03-04 Known Stubs 2건 모두 닫힘: 전체 개수 필드명 = **`총상품수`**, 배치 인자명 = **`codes`** 맞음.

## 절대 깨면 안 되는 것

- **미해소 사유를 한 칸에 담지 마라.** `추출실패`/`번호없음`(광고 오류) vs `미조회`(인덱스 미보유·429). 섞이면 멀쩡한 광고그룹을 지우러 간다. 이 페이즈 최대 오진
- **판정 순서:** `aiImageGenerated` 가 `imageTranslated` 보다 우선. `bool()` 금지 (`bool('0')` 은 True)
- **D-19:** `webapp/` 아래 `eroomlib`·`requests` import 0건. MCP 접촉은 전부 CLI 자식 프로세스
- **화면에 `미스` 를 그대로 쓰지 마라.** `group_health(...)["완결"]` 로 갈라 "인덱스 불완전(시스템)"
- **429 를 성공으로 접지 마라.** 초당 4회, `Retry-After: 20`
- **`--workers 1`** 유지
- 해상률이 100% 라고 **미해소 경로를 빼지 마라.** 물갈이로 다시 어긋난다
- 새 런타임 파일은 `test_board.py` 의 `런타임_파일들` 에 등록 (지금은 트리 전체를 훑는다)

## 열린 항목 (Phase 3 와 무관)

1. `01-HUMAN-UAT.md` — 되돌리기 실탄 미검증. 싸게 닫는 법: 5건만 되돌렸다 즉시 재인상
2. 실제 인상 472건 유지 중 (되돌리지 않음 — 용팀장 지시)
3. **Phase 2 는 이월** — Phase 5 계획 시 SAFE-04~07 중 크레딧 태우기 전에 끌어올 게 있는지 재확인

## 의도적으로 안 고친 것 (2026-09-21 용팀장 결정: "그냥 두고 기록만")

**그룹 목록 조회 단계의 HTTP 429 가 "완주"로 둔갑한다.**
→ `.planning/todos/pending/429-group-stage-silent-completion.md` 에 전문.
Phase 3 범위 밖으로 넘겼다. 첫 구축을 다시 돌릴 일이 생기면(물갈이 후) 그때 문다.

**당분간 운용 회피책: 인덱스 잡과 스캔 잡을 동시에 띄우지 마라.**
레이트리밋이 프로세스마다 걸려서 합산 상한을 넘긴다(가설, 미검증 — 첫 완주는 혼자 돌았고 429 0회).

## 서버 상태 (2026-09-21 기준)

- PID 갱신됨 · 토큰 고정(`CT_DEV_TOKEN`)이라 URL 그대로:
  `http://127.0.0.1:8765/?t=P-8dEcLYRJjXo4urok3aAwcvk0SEjjj9pX0FwuB0dy0`
- `running` 으로 굳어 있던 잡 2건(`f5c960da` index · `218104ba` scan)은 `_reap` 으로 `orphaned` 처리 완료.
  **버튼이 409 로 막히지 않는다.**
