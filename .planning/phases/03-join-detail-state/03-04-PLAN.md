---
phase: 03-join-detail-state
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - .claude/skills/bulsaja-detail-page/scripts/bulsaja_rate.py
  - .claude/skills/bulsaja-detail-page/scripts/ss_index_build.py
  - .claude/skills/bulsaja-detail-page/scripts/bulsaja_scan.py
  - webapp/tests/test_index.py
  - webapp/tests/test_cli_shim.py
autonomous: true
requirements: [ENG-08, JOIN-03, JOIN-04]

must_haves:
  truths:
    - "호출 간격이 초당 4회를 넘지 않는다 — 실측 `RateLimit-Policy: 240;w=60` 을 가짜 시계로 검증한다"
    - "429 를 **삼키지 않는다** — `Retry-After` 만큼 기다리고 재시도하고, 끝내 못 받으면 `미조회` 로 명시 기록한다 (Pitfall 2·3)"
    - "인덱스 구축이 그룹 단위로 중단·재개된다. 재개 기준은 페이지 번호가 아니라 **이미 처리한 productId 집합**이다 (Pitfall 6)"
    - "인덱스 CLI 는 `ss_index` 테이블을 **만들지 않는다** — 스키마 정본은 `webapp/jobs.py` 의 DDL 하나다"
    - "스캔 CLI 는 관측만 적는다 — `히트/미스/불일치` 판정을 하지 않는다"
    - "두 CLI 모두 시작 즉시 `bulsaja_my_profile` 로 계정을 확인하고, 기대 계정이 아니면 **아무것도 하지 않고** 종료한다 (ENG-08 2층 방어)"
    - "불사자 응답의 `표시규칙` 이 산출물 파일에 남지 않는다"
    - "진행 출력이 `flush=True` 줄 단위라 3시간짜리 로그가 0바이트로 머물지 않는다"
  artifacts:
    - path: ".claude/skills/bulsaja-detail-page/scripts/bulsaja_rate.py"
      provides: "호출간격(최소간격, now_fn, sleep_fn) · 안전호출(fn, 재시도대기, sleep_fn) — stdlib 만. 주입 가능한 시계"
      min_lines: 50
    - path: ".claude/skills/bulsaja-detail-page/scripts/ss_index_build.py"
      provides: "마켓그룹 단위 인덱스 구축 · SQLite 체크포인트 · 미조회 명시 기록"
      min_lines: 150
    - path: ".claude/skills/bulsaja-detail-page/scripts/bulsaja_scan.py"
      provides: "계정 확인(--profile-only) · 회차 조인 스캔 → join_<job>.json"
      min_lines: 170
  key_links:
    - from: "ss_index_build.py / bulsaja_scan.py"
      to: ".claude/skills/bulsaja-category-fix/scripts/bulsaja_mcp.py"
      via: "__file__ 기준 상대 경로 shim (run_names.py:40-46 관용구). detail_batch.py 와 같은 모양"
      pattern: "os.path.normpath"
    - from: "ss_index_build.py"
      to: "webapp.db ss_index"
      via: "stdlib sqlite3 INSERT OR REPLACE — 건마다 커밋. 테이블 생성은 하지 않는다"
      pattern: "INSERT OR REPLACE INTO ss_index"
    - from: "webapp/tests/test_index.py"
      to: "bulsaja_rate.py"
      via: "테스트 전용 sys.path 삽입 (test_cli_patch.py:21-31 이 허가한 방식)"
      pattern: "bulsaja_rate"
---

## Phase Goal

> ⚠ ROADMAP `**Goal:**` 이 user-story 형식이 아니다. 정규화는 `/gsd mvp-phase 03`. 아래는 ROADMAP 원문이다.

광고 판정 행을 불사자 상품에 실제로 잇고 상세 상태를 3단계로 판정해, "유입은 있는데 상세가 중국어 원본인 상품" 목록을 화면에서 정확히 뽑아낸다.

<objective>
**불사자 MCP 를 만지는 코드 전부가 이 플랜에 있다. 그리고 여기 말고는 어디에도 없다 (D-19).**

CLI 2종 + 공용 레이트리밋 모듈 1개. 셋 다 `.venv`(requests 있음)에서 돌고, 웹앱은 이들이 써 둔
SQLite 행과 JSON 파일을 읽을 뿐이다.

**이 플랜이 막는 사고 둘 (둘 다 실측 근거가 있다):**
- `eroomlib` 의 백오프는 0.4/0.8/1.6/3.2초 = 합쳐 6초인데 서버는 `Retry-After: 20` 을 요구한다.
  그대로 두면 3시간짜리 잡이 **시작 40초 만에** `HTTP 429` 로 죽는다 (Pitfall 2 — 이 페이즈 최대 위험).
  공용 라이브러리를 고치면 스킬 12개가 같이 바뀌므로, **호출자가 애초에 초당 4회를 안 넘기는** 길을 간다
  (리서치의 953건 완주가 이 방식으로 돌았다 — 검증된 방법이다).
- 429 로 빠진 상품을 조용히 건너뛰면 화면에서 "미해소 — 번호가 불사자에 없음" 으로 보이고,
  용팀장이 **멀쩡한 광고그룹을 지우러 간다.** 되돌릴 수 없다 (Pitfall 3).

Purpose: 광고 행을 불사자 상품에 잇는 데 필요한 관측을 안전하게 긁어 온다.
Output: `bulsaja_rate.py` · `ss_index_build.py` · `bulsaja_scan.py` + 레이트리밋 검증 + `--help` 스모크
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-join-detail-state/03-CONTEXT.md
@.planning/phases/03-join-detail-state/03-RESEARCH.md
@.planning/phases/03-join-detail-state/03-PATTERNS.md
@.planning/phases/03-join-detail-state/03-VALIDATION.md
@.planning/phases/03-join-detail-state/03-01-SUMMARY.md
@.planning/phases/03-join-detail-state/03-03-SUMMARY.md

<interfaces>
`03-03` 의 `BulsajaArgv` 가 조립하는 argv 의 **반대편**이다. 플래그 이름이 한 글자라도 다르면 잡이 죽는다.

ss_index_build.py (subcommand "index"):
  --db <path>            (필수) webapp.db
  --groups <path>        (필수) groupId 문자열 JSON 배열. **빈 배열이면 exit 2** (전량 아님)
  --out <path>           (필수) 진행 요약 JSON
  --profile-out <path>   (필수) 계정 확인 결과 JSON
  --expect-nick <닉네임>  (필수) 불일치면 아무것도 안 하고 exit 3
  --min-interval <float> (필수)  --retry-after <int> (필수)  --batch-size <int> (필수, 페이지 크기)
  --limit <int>          (선택) 그룹당 처리 상한. 0/미지정이면 전량

bulsaja_scan.py (subcommand "scan" / "profile"):
  --profile-only                계정 확인만 하고 --profile-out 에 쓰고 exit 0
  --run-dir <회차명>      (scan 필수, 기록용)
  --targets <path>       (scan 필수) `"<계정alias>|<mallProductId>"` 문자열 JSON 배열
  --out <path>           (scan 필수) join_<job>.json
  --profile-out / --expect-nick / --db / --min-interval / --retry-after / --batch-size  (전부 필수)

산출물 스키마는 `03-01-PLAN.md` §interfaces 의 조인 잡 산출물 정의가 정본이다. 그대로 쓴다.

bulsaja_rate.py (stdlib 만. MCP 를 import 하지 않는다 — 그래야 `.venv-web` pytest 가 직접 때린다):
  class 호출간격:
      def __init__(self, 최소간격: float, now_fn=time.monotonic, sleep_fn=time.sleep)
      def 대기(self) -> float          # 실제로 잔 시간(초). 첫 호출은 0
  def 안전호출(fn, *, 재시도대기: float, 횟수: int = 6, sleep_fn=time.sleep, 로그=print)
        -> tuple[object | None, bool]  # (결과, 미조회여부). 429 가 아닌 예외는 그대로 올린다
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bulsaja_rate.py — 초당 4회 상한 + 429 를 삼키지 않는 재시도</name>
  <files>.claude/skills/bulsaja-detail-page/scripts/bulsaja_rate.py, webapp/tests/test_index.py</files>
  <read_first>
    - `03-RESEARCH.md` §Code Examples 954-1003행 — **인덱스 구축 루프 완성 코드.** 최소간격 0.26 · 429 시 21초 대기 · `else: 기록(pid, None, 미조회=True)` 가 이 모듈의 원형이다
    - `03-RESEARCH.md` §Pitfall 2 (백오프 6초 vs `Retry-After: 20`) · §Pitfall 3 (미조회가 광고 오류로 둔갑) · §D-13 확정 ③④ (`RateLimit-Policy: 240;w=60`, 실측 3.7건/초)
    - `.claude/lib/eroomlib/bulsaja.py` 147-190행 — `_post` 의 백오프(`0.4 × 2^attempt`)와 예외 메시지 모양. **`RuntimeError` 문자열에 `429` 가 들어간다는 것이 우리가 잡을 신호다**
    - `.claude/lib/eroomlib/snapshot.py` 191-192행 — `sleep_fn=time.sleep` 주입 관용구(저장소 선례)
    - `webapp/tests/test_cli_patch.py` 21-33행 — **테스트 프로세스에만 허용된 `sys.path` 삽입.** 그 주석을 그대로 인용해 이 모듈을 import 한다
    - `webapp/tests/test_jobs.py` 474-478행 — "문자열 검사로 때우지 않는다"
  </read_first>
  <behavior>
    - `test_초당4회_상한` — 가짜 시계로 20번 연속 호출했을 때 **모든 인접 호출 간격이 `min_interval` 이상**이다. 소스에 `0.26` 이 있는지 보는 게 아니라 주입한 시계의 기록을 잰다
    - `test_이미_충분히_지났으면_안_잔다` — 직전 호출 후 `min_interval` 보다 오래 지났으면 `대기()` 가 0 을 돌려주고 `sleep_fn` 이 안 불린다
    - `test_429는_미조회로_남는다` — 항상 `RuntimeError("... HTTP 429 ...")` 를 던지는 `fn` 을 주면 `안전호출` 이 `(None, True)` 를 돌려준다. **예외를 밖으로 던지지 않고, 성공으로 접지도 않는다**
    - `test_429가_아닌_예외는_올라간다` — `ValueError` 는 그대로 전파된다. 모르는 실패를 미조회로 뭉개면 원인이 사라진다
    - `test_429뒤_성공하면_결과를_준다` — 첫 호출만 429, 두 번째 성공이면 `(결과, False)` 이고 `sleep_fn` 이 `재시도대기` 로 한 번 불린다
    - `test_재시도대기는_서버요구를_따른다` — `sleep_fn` 에 들어온 값이 `재시도대기` 와 같다(호출자가 설정으로 21초를 넘긴다). 하드코딩된 짧은 백오프가 아니다
    - `test_로그가_429를_말한다` — 주입한 `로그` 콜백이 429 사실을 문자열로 받는다. **무신호가 더 위험한 신호다**(RESEARCH §Pitfall 2)
  </behavior>
  <action>
    `.claude/skills/bulsaja-detail-page/scripts/bulsaja_rate.py` 를 새로 만든다. **stdlib `time` 말고는 아무것도 import 하지 마라** — `bulsaja_mcp`·`eroomlib`·`requests` 를 건드리는 순간 `.venv-web` 의 pytest 가 이 모듈을 import 조차 못 하고, 그러면 레이트리밋 규율이 3시간짜리 잡을 실제로 돌려봐야만 검증되는 물건이 된다.

    모듈 docstring 에 실측 근거를 적는다: `RateLimit-Policy: 240;w=60` · `Retry-After: 20` (HTTP 429 응답 헤더 원문, 2026-09-21) · 단일 세션 지속 3.7건/초 · **동시 세션 4개·8개 모두 429**(토큰 단위 정책이라 세션을 늘려도 총량이 같다). "왜 `eroomlib` 의 백오프를 안 고치나" 도 한 줄: 공용 라이브러리라 스킬 12개가 같이 바뀐다 — 호출자가 안 넘기는 쪽이 싸고 이미 953건으로 검증됐다.

    `호출간격` 은 `now_fn`/`sleep_fn` 을 **인자로 받는다**(기본 `time.monotonic`/`time.sleep`). `time.time` 이 아니라 `monotonic` 을 쓰는 이유를 주석에 남겨라 — 3시간짜리 루프 중에 시스템 시계가 조정되면 `time.time` 기반 간격은 음수가 되거나 몇 시간을 잔다. `대기()` 는 실제로 잔 시간을 돌려준다(테스트가 잴 수 있게).

    `안전호출(fn, *, 재시도대기, 횟수=6, sleep_fn, 로그)` — `fn()` 을 부르고 `RuntimeError` 의 문자열에 `"429"` 가 있으면 `로그(...)` 후 `sleep_fn(재시도대기)` 하고 재시도한다. `횟수` 를 다 쓰면 `(None, True)` 를 돌려준다. **`continue` 로 조용히 넘어가는 경로를 만들지 마라** — 호출부가 미조회를 반드시 기록하게 `(None, True)` 로 강제한다. 429 가 아닌 예외는 잡지 않는다(`raise` 를 그대로 통과). 그 판단을 주석에 적어라: 모르는 실패를 미조회로 뭉개면 원인이 사라지고, 미조회는 화면에서 "인덱스 불완전" 으로 읽혀 **사람이 고칠 수 없는 문제**가 된다.

    `webapp/tests/test_index.py` 에 behavior 7종을 덧붙인다. 파일 맨 위에 `test_cli_patch.py:21-31` 의 주석을 인용해 `sys.path` 삽입을 정당화하고 `import bulsaja_rate` 한다. **가짜 시계**는 리스트에 시각을 쌓는 간단한 객체로 만들어라 — `sleep_fn` 이 불리면 현재 시각을 그만큼 앞으로 민다. 실제 `time.sleep` 을 한 번도 부르지 않아야 한다(테스트 전체가 0.01초 안에 끝난다).
  </action>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_index.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - VALIDATION 노드 2종이 존재하고 통과한다:
      `.venv-web/bin/pytest "webapp/tests/test_index.py::test_초당4회_상한" "webapp/tests/test_index.py::test_429는_미조회로_남는다" -x` 가 exit 0
    - `.venv-web/bin/pytest webapp/tests/test_index.py -q` 가 **1초 안에** 끝난다 (실제 sleep 을 안 탄다):
      `time .venv-web/bin/pytest webapp/tests/test_index.py -q` 의 real 이 1s 미만
    - `grep -cE '^(import|from) ' .claude/skills/bulsaja-detail-page/scripts/bulsaja_rate.py` 가 1 이고 그 줄이 `import time` 이다
    - `.venv-web/bin/python3 -c "
import sys, pathlib
sys.path.insert(0, str(pathlib.Path('.claude/skills/bulsaja-detail-page/scripts').resolve()))
import bulsaja_rate; print('import OK from .venv-web')"` 가 성공한다 (MCP 의존이 0 이라는 증거)
  </acceptance_criteria>
  <done>레이트리밋 규율과 429 처리가 3시간짜리 잡을 돌리지 않고도 검증된다. 미조회를 삼키는 경로가 구조적으로 없다.</done>
</task>

<task type="auto">
  <name>Task 2: ss_index_build.py — 그룹 단위 인덱스 구축 (체크포인트 · 미조회 명시 기록)</name>
  <files>.claude/skills/bulsaja-detail-page/scripts/ss_index_build.py, webapp/tests/test_cli_shim.py</files>
  <read_first>
    - `.claude/skills/bulsaja-detail-page/scripts/detail_batch.py` 1-60행(docstring 에 실측 근거·체크포인트 규약·사용법을 적는 모양 · 인코딩 관용구 · shim), 205-235행(argparse), 270-290행(`flush=True` 줄 출력)
    - `.claude/skills/product-name/scripts/run_names.py` 34-47행 — shim 관용구 정본
    - `.claude/lib/eroomlib/snapshot.py` 213-235행 — `collect_group` 의 페이지 순회와 **`seen` 집합으로 중복을 거르는** 원리(Pitfall 6 의 근거). 단 이 헬퍼는 `그룹` 태그를 버리므로 여기서는 원본 호출을 쓴다
    - `.claude/lib/eroomlib/snapshot.py` 165-192행 — `ProductMCP.workdata()` 투영이 **`uploadedSuccessUrl` 을 버린다**(Pitfall 1). `call_tool` 원본을 써야 하는 이유
    - `.claude/skills/bulsaja-category-fix/scripts/bulsaja_mcp.py` 55-60행 — `BulsajaMCP` 가 상속으로 얻는 것(`open`/`call_tool`/`close`)
    - `webapp/jobs.py` 76-101행 — `ss_index` DDL (03-03 산출물). **여기 있는 컬럼에만 쓴다. CREATE 를 복제하지 마라**
    - `03-RESEARCH.md` §인덱스 잡이 반드시 지켜야 할 것 1~5 · §Code Examples 954-1003 · §Pitfall 6 · §Pitfall 1
    - `03-CONTEXT.md` D-18 — 제외는 설정값이고 그룹 ID·이름을 코드·주석에 박지 않는다
  </read_first>
  <action>
    `.claude/skills/bulsaja-detail-page/scripts/ss_index_build.py` 를 쓴다. 뼈대는 `detail_batch.py` 다 — 같은 인코딩 관용구, 같은 shim, 같은 argparse 스타일, 같은 `flush=True` 규율.

    docstring 에 **체크포인트 규약과 비용 실측**을 적는다: 36그룹 47,105 상품 ≈ 3시간 32분(3.7건/초) · 재개 단위는 **처리한 productId 집합** · 크레딧 0(읽기 전용 조회) · 중단해도 안전.
    같은 자리에 `향후` 한 줄도 남겨라 — **커머스API 로 가면 30초다**(`channelProductNo → sellerManagementCode` → `find_by_code` 배치). 막는 건 오직 자격증명(스토어당 앱 1개 상한 × 몰 50개)이고 v1 범위 밖이다. 근거는 `webapp/bulsaja_index.py` docstring 에 있으니 거기를 가리켜라 — 비용이 다시 아플 때 이 비교표가 없으면 처음부터 다시 조사하게 된다.
    **같은 인덱스 잡이 두 개 동시에 돌면 각자 초당 4회를 지켜도 합산 8회/초라 429 가 쏟아진다.** 그건 `jobs.create_job` 의 `SINGLETON_KINDS` 가드(03-03)가 막는다 — 이 CLI 는 그 전제를 믿고 자기 몫의 간격만 지킨다. 그 분업을 docstring 에 한 줄 적어라. `13번_용쌤13-2` 같은 **구체적인 그룹 이름·ID 를 docstring 에도 적지 마라** — D-18 제외는 설정값이고, 주석에 남은 이름은 다음 사람에게 하드코딩으로 읽힌다.

    흐름:
    1. `--groups` 를 읽는다. **파일이 없거나 배열이 비었으면 `exit 2`** 로 죽으면서 "빈 목록은 전량이 아니다" 를 말한다. 이 경로가 `jobs._build_argv` 의 예외와 짝을 이루는 2층 방어다.
    2. `BulsajaMCP()` 를 열고 `bulsaja_my_profile` 을 **한 번** 부른다. `닉네임` 이 `--expect-nick` 과 다르면 `exit 3` — 그 전에 SQLite 에 아무것도 쓰지 않는다. 결과를 `--profile-out` 에 원자적으로 쓴다(`표시규칙` 제거 후, `tmp → os.replace`). 닉네임·크레딧·확인시각만 싣고 이메일은 싣지 마라(SAFE-03 의 정신 — 화면에 뜰 값이 아니다).
    3. `--db` 에 `sqlite3.connect` 하고 `SELECT name FROM sqlite_master WHERE name='ss_index'` 로 **테이블 존재만 확인**한다. 없으면 `exit 4` 로 "웹앱을 한 번 띄워 스키마를 만들어라" 를 말한다. **`CREATE TABLE` 을 여기 쓰지 마라** — 스키마 정본은 `webapp/jobs.py` 의 DDL 하나다(두 벌이 되면 컬럼이 조용히 갈라진다).
    4. 그룹마다:
       - `SELECT product_id FROM ss_index WHERE market_group_id = ?` 로 **이미 처리한 집합**을 만든다. 이게 재개 기준이다. 페이지 번호로 잡지 마라 — 목록 순서의 정렬 기준이 문서화돼 있지 않고 `mallProductId` 단조성이 61.9%다(Pitfall 6).
       - `bulsaja_market_group_products` 를 `page`/`pageSize=--batch-size` 로 순회해 `productId` 목록과 응답의 `전체개수`(→ `group_total`)를 모은다. `항목` 이 비면 멈춘다. 페이지 호출도 `호출간격.대기()` 를 탄다.
       - 처리 안 된 pid 마다: `호출간격.대기()` → `안전호출(lambda: mcp.call_tool("bulsaja_product_workdata", {"productId": pid, "mode": "summary"}), 재시도대기=--retry-after, ...)`.
         **`mode="summary"` 다.** `full` 을 쓸 이유가 없다(실측: summary 에도 `uploadedSuccessUrl` 이 나온다, 11,967자 vs 13,856자).
         **`ProductMCP.workdata()` 헬퍼를 쓰지 마라** — 그 투영이 `uploadedSuccessUrl` 을 버려서 인덱스 히트율이 0%가 된다(Pitfall 1).
       - 결과에서 `data.uploadedSuccessUrl.smartstore` 를 꺼내 `INSERT OR REPLACE INTO ss_index (...) VALUES (...)` 로 **한 건씩 커밋**한다. 28,000건짜리 그룹이 있어서 메모리에 쌓으면 중단 시 통째로 날아간다.
       - 미조회(`안전호출` 이 `(None, True)`)면 `smartstore=NULL, unresolved=1` 로 **행을 남긴다.** 건너뛰지 마라 — 조용히 빠지면 그 행이 화면에서 "미해소"가 되고 미해소는 "광고 쪽 오류"로 읽힌다(Pitfall 3).
       - 진행 로그를 `flush=True` 로 찍는다: `[그룹 3/36] 1200/2484 (미조회 2)` 같은 모양. 50건마다 한 줄이면 충분하다. `PYTHONUNBUFFERED` 가 잡에서 주입되지만(`jobs.py:294`) 터미널 직접 실행에는 그게 없으므로 **둘 다** 필요하다.
    5. `--out` 에 진행 요약을 원자적으로 쓴다: 그룹별 `{groupId, 행수, 미조회, 총상품수, 완결}` + 총계 + 소요초. 웹앱의 `bulsaja_index.group_health()` 가 같은 값을 DB 에서 다시 낼 수 있어야 한다(두 값이 어긋나면 DB 가 정본이다 — 그 사실을 주석에 적어라).
    6. 종료코드: 0 정상 · 2 대상 없음 · 3 계정 불일치 · 4 스키마 없음. 그 표를 docstring 에 적는다.

    `webapp/tests/test_cli_shim.py` 에 `test_인덱스빌더가_뜬다` 를 더한다 — `--help` 가 exit 0 이고 stdout 에 `--groups` 가 있다. 모듈 최상위에서 MCP 를 import 하므로 `--help` 만으로 import 사슬 전체가 증명된다(크레딧 0, 쓰기 0, 네트워크 0).
  </action>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_cli_shim.py -x -q && .venv/bin/python3 .claude/skills/bulsaja-detail-page/scripts/ss_index_build.py --help > /dev/null && .venv/bin/python3 -c "
import json, subprocess, sys, tempfile, pathlib
d = pathlib.Path(tempfile.mkdtemp())
(d/'g.json').write_text('[]', encoding='utf-8')
r = subprocess.run(['.venv/bin/python3', '.claude/skills/bulsaja-detail-page/scripts/ss_index_build.py',
                    '--db', str(d/'x.db'), '--groups', str(d/'g.json'), '--out', str(d/'o.json'),
                    '--profile-out', str(d/'p.json'), '--expect-nick', 'zzfake',
                    '--min-interval', '0.26', '--retry-after', '21', '--batch-size', '50'],
                   capture_output=True, text=True, timeout=60)
assert r.returncode == 2, (r.returncode, r.stdout[-500:], r.stderr[-500:])
assert not (d/'x.db').exists(), 'DB 를 만들었다 — 빈 목록에서 아무것도 하면 안 된다'
print('PASS 빈 목록은 전량이 아니다')"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python3 .claude/skills/bulsaja-detail-page/scripts/ss_index_build.py --help` 가 exit 0 이고 `--groups`·`--expect-nick`·`--min-interval` 을 모두 보여준다
    - 빈 `--groups` 배열에서 **exit 2** 이고 DB 파일이 생기지 않는다 (위 명령이 `PASS 빈 목록은 전량이 아니다` 를 찍는다)
    - `grep -c 'CREATE TABLE' .claude/skills/bulsaja-detail-page/scripts/ss_index_build.py` 가 **0** (스키마 정본은 하나)
    - `grep -c 'INSERT OR REPLACE INTO ss_index' .claude/skills/bulsaja-detail-page/scripts/ss_index_build.py` 가 1 이상
    - `grep -c 'unresolved' .claude/skills/bulsaja-detail-page/scripts/ss_index_build.py` 가 2 이상 (기록 + 재개 집계)
    - `grep -c 'mode.*summary' .claude/skills/bulsaja-detail-page/scripts/ss_index_build.py` 가 1 이상, `grep -c '\.workdata(' .claude/skills/bulsaja-detail-page/scripts/ss_index_build.py` 가 **0** (Pitfall 1)
    - `grep -c 'flush=True' .claude/skills/bulsaja-detail-page/scripts/ss_index_build.py` 가 3 이상
    - 그룹명·그룹ID 리터럴 0건: `grep -cE '(용쌤|번_|13-2)' .claude/skills/bulsaja-detail-page/scripts/ss_index_build.py` 가 0
  </acceptance_criteria>
  <done>인덱스 구축 CLI 가 뜨고, 대상 없이는 아무것도 하지 않으며, 미조회를 명시적으로 남긴다.</done>
</task>

<task type="auto">
  <name>Task 3: bulsaja_scan.py — 계정 확인 + 회차 조인 스캔 (관측만 적는다)</name>
  <files>.claude/skills/bulsaja-detail-page/scripts/bulsaja_scan.py, webapp/tests/test_cli_shim.py</files>
  <read_first>
    - `.claude/skills/bulsaja-detail-page/scripts/ss_index_build.py` (Task 2 산출물) — shim·프로필 확인·원자적 쓰기·종료코드 표를 **그대로 재사용한다**
    - `.claude/skills/bulsaja-detail-page/scripts/bulsaja_rate.py` (Task 1 산출물)
    - `03-RESEARCH.md` §팬아웃 (JOIN-03) 실측 — `find_by_code` 응답 필드(`판매자상품코드`·`불사자코드`·`그룹`·`잠금`·`업로드된마켓`), **배치 상한 50**, 2단 흐름(`판매자상품코드 → 불사자코드 → 사본 전체`), 그리고 **CONTEXT 정정**: `uploadBulsajaCode` 는 불사자코드가 아니라 **판매자상품코드**다
    - `03-RESEARCH.md` §D-12 확정 실측한 것 — `uploadDetailContents` 키 구성 4패턴
    - `03-RESEARCH.md` §ENG-08 — `표시규칙` 경고(모든 도구 응답에 붙는다)
    - `03-01-PLAN.md` §interfaces — **조인 잡 산출물 스키마 정본**
    - `webapp/jobs.py` 136-140행(`_web_dir`) · 340-350행(`_write_targets` 의 원자적 쓰기 관용구 `tmp → os.replace`)
    - `webapp/join.py` (03-02 산출물) — 이 산출물을 읽는 쪽. **여기서 판정을 하지 않는다는 것**을 확인하는 용도
  </read_first>
  <action>
    `.claude/skills/bulsaja-detail-page/scripts/bulsaja_scan.py` 를 쓴다. Task 2 와 같은 뼈대(인코딩·shim·프로필 확인·원자적 쓰기·종료코드).

    **모드 둘:**
    - `--profile-only` — MCP 를 열고 `bulsaja_my_profile` 한 번 부르고 `--profile-out` 에 쓰고 exit 0. 닉네임이 달라도 **파일은 쓰고** exit 3 으로 끝낸다(화면이 "지금 붙어 있는 계정이 다르다"를 말할 수 있어야 한다). 0.14초짜리 호출이다.
    - 스캔 — 아래 흐름.

    흐름:
    1. 프로필 확인(위와 동일). 불일치면 `exit 3`, 스캔은 시작도 하지 않는다.
    2. `--targets` 를 읽는다. `"<계정alias>|<mallProductId>"` 문자열 배열이다. 비었으면 `exit 2`. `|` 로 한 번만 나눈다(alias 에는 `|` 가 못 들어간다 — `webapp/argv.py:46` 의 `Alias` 패턴이 보장).
    3. `bulsaja_market_groups` 를 **한 번** 부른다(0.11초, 86개). 응답에서 `groupId`·`그룹명` 만 뽑아 산출물의 `마켓그룹` 에 싣는다. **번호 추출·매칭을 여기서 하지 마라** — 그건 웹앱(`join.market_number`/`group_index`)의 일이고, 여기서 다시 하면 진실이 둘이 된다.
    4. `--db` 를 **읽기 전용**(`file:...?mode=ro`)으로 열어 `smartstore IN (...)` 로 인덱스를 조회한다. `--batch-size` 단위로 나눠 질의한다. 테이블·파일이 없으면 **전 행을 `미조회=True` 로 두고 계속 간다**(인덱스 구축 전에도 스캔은 돌아야 한다 — 그래야 번호층 해상률과 광고 청소 목록이 먼저 나온다). 그 사실을 로그에 한 줄 남겨라.
    5. 인덱스 히트가 있는 행마다: `호출간격.대기()` → `안전호출(workdata mode=summary)` → `data.uploadedSuccessUrl.smartstore` 를 `관측_smartstore` 에, `data.uploadDetailContents` 를 그대로(키 부재 포함) `uploadDetailContents` 에, `uploadBulsajaCode` 를 `판매자상품코드` 에 싣는다. 미조회면 `미조회=True`.
       **`히트`/`불일치` 를 계산하지 마라.** 이 파일은 관측만 적는다 — 판정은 `webapp/join.재검증판정` 한 곳이다.
    6. 팬아웃(JOIN-03): 모인 `판매자상품코드` 를 **50개 배치**로 `bulsaja_product_find_by_code` 에 넣어 `불사자코드`·`그룹`(태그)을 얻고, 다시 `불사자코드` 를 50개 배치로 넣어 **사본 전체**를 얻는다. 행마다 `사본` 리스트(`판매자상품코드`·`그룹`·`잠금`)를 채운다. **행마다 한 번씩 부르지 마라** — 194행이면 194호출이 되어 레이트리밋 예산의 40%를 먹는다(실측: 50코드 배치가 0.53초).
    7. 산출물 dict 전체를 **재귀적으로 훑어 `표시규칙` 키를 제거**한 뒤 `--out` 에 원자적으로 쓴다(`tmp → os.replace`). 웹앱도 한 번 더 버리지만(방어적 이중화), 파일에 남기지 않는 게 1차 방어다.
    8. 진행 로그 `flush=True`. 마지막 줄에 `행 194 · 히트관측 N · 미조회 M · 사본조회 K건` 같은 요약을 찍어 SSE 에서 바로 읽히게 한다.

    `webapp/tests/test_cli_shim.py` 에 `test_스캔이_뜬다` 를 더한다 — `--help` exit 0 + stdout 에 `--targets`·`--profile-only` 가 보인다.
  </action>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_cli_shim.py -x -q && .venv/bin/python3 .claude/skills/bulsaja-detail-page/scripts/bulsaja_scan.py --help > /dev/null && .venv/bin/python3 -c "
import subprocess, tempfile, pathlib
d = pathlib.Path(tempfile.mkdtemp()); (d/'t.json').write_text('[]', encoding='utf-8')
r = subprocess.run(['.venv/bin/python3', '.claude/skills/bulsaja-detail-page/scripts/bulsaja_scan.py',
                    '--run-dir', '2026-01-01', '--targets', str(d/'t.json'), '--out', str(d/'j.json'),
                    '--db', str(d/'x.db'), '--profile-out', str(d/'p.json'), '--expect-nick', 'zzfake',
                    '--min-interval', '0.26', '--retry-after', '21', '--batch-size', '50'],
                   capture_output=True, text=True, timeout=90)
assert r.returncode in (2, 3), (r.returncode, r.stdout[-400:], r.stderr[-400:])
assert not (d/'j.json').exists(), '대상이 없는데 산출물을 썼다'
print('PASS 대상 없음/계정 불일치에서 산출물을 만들지 않는다')"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python3 .claude/skills/bulsaja-detail-page/scripts/bulsaja_scan.py --help` 가 exit 0 이고 `--targets`·`--profile-only`·`--expect-nick` 을 보여준다
    - 위 자동 명령이 `PASS 대상 없음/계정 불일치에서 산출물을 만들지 않는다` 를 찍는다
    - `grep -cE '(히트|불일치)' .claude/skills/bulsaja-detail-page/scripts/bulsaja_scan.py` 가 **0** (판정을 안 한다 — 관측만 적는다)
    - `grep -c '표시규칙' .claude/skills/bulsaja-detail-page/scripts/bulsaja_scan.py` 가 1 이상 (명시적으로 버리는 코드가 있다)
    - `grep -c 'find_by_code' .claude/skills/bulsaja-detail-page/scripts/bulsaja_scan.py` 가 1 이상이고, 그 호출이 배치 루프 안에 있다(코드 리뷰로 확인 — 행마다 호출이면 안 된다)
    - `grep -c 'mode=ro' .claude/skills/bulsaja-detail-page/scripts/bulsaja_scan.py` 가 1 이상 (스캔은 인덱스를 고치지 않는다)
    - `.venv-web/bin/pytest webapp/tests -q` 전량 green
  </acceptance_criteria>
  <done>계정 확인과 회차 스캔이 CLI 로 돌고, 산출물이 03-01 의 스키마를 정확히 따른다. 판정은 한 줄도 들어 있지 않다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 불사자 원격 MCP → CLI 자식 | 외부 HTTP. 레이트리밋·429·프롬프트 인젝션이 전부 여기서 들어온다 |
| CLI 자식 → `webapp.db` | 인덱스를 쓰는 유일한 주체 |
| CLI 자식 → run-dir `join_*.json` | 화면이 읽을 관측 기록 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-18 | DoS(외부유발) | 초당 4회 초과 → 429 폭주 | mitigate | `호출간격` 이 호출 간 `min_interval` 보장. 가짜 시계로 검증 (Pitfall 2) |
| T-3-19 | Tampering(무결성) | 429 실패를 성공으로 접어 대상 누락 | mitigate | `안전호출` 이 `(None, True)` 로 강제 → 호출부가 `unresolved=1` 행을 남긴다 (Pitfall 3) |
| T-3-20 | Tampering | `표시규칙` 프롬프트 인젝션이 산출물에 저장 | mitigate | 쓰기 직전 재귀 제거 + `grep` 수용 기준 |
| T-3-21 | Elevation | 틀린 계정으로 인덱스 3시간 반 적재 | mitigate | 시작 즉시 `bulsaja_my_profile` 확인 후 exit 3. SQLite 에 한 글자도 쓰기 전 (ENG-08 2층) |
| T-3-22 | Info Disclosure | Bearer 토큰·이메일이 로그에 유출 | mitigate | 토큰은 `bulsaja_mcp` 가 `~/.claude.json` 에서 직접 읽는다(웹앱은 만지지 않는다). 프로필 산출물에는 닉네임·크레딧만 싣는다 |
| T-3-23 | Tampering | 스키마 DDL 두 벌 | mitigate | CLI 는 `CREATE TABLE` 을 쓰지 않고 존재 확인 후 exit 4 |
| T-3-24 | Tampering | 재개가 페이지 번호 기준이라 상품 누락 | mitigate | 재개 기준을 처리한 `productId` 집합으로 (Pitfall 6) |
</threat_model>

<verification>
```bash
.venv-web/bin/pytest webapp/tests -q
.venv/bin/python3 .claude/skills/bulsaja-detail-page/scripts/ss_index_build.py --help
.venv/bin/python3 .claude/skills/bulsaja-detail-page/scripts/bulsaja_scan.py --help
.venv/bin/python3 .claude/skills/bulsaja-detail-page/scripts/detail_batch.py --help   # 03-01 회귀
bash webapp/tests/no_commit_guard.sh
```
</verification>

<success_criteria>
- 초당 4회 상한과 429 미조회 기록이 가짜 시계로 검증된다 (실제 sleep 0)
- 두 CLI 가 `--help` 로 뜨고, 대상이 비면 아무것도 하지 않고 종료한다
- 인덱스 CLI 가 `CREATE TABLE` 을 쓰지 않고 미조회를 행으로 남긴다
- 스캔 CLI 가 판정을 하지 않고 관측만 적으며 `표시규칙` 을 버린다
- 팬아웃 조회가 50코드 배치로 돈다
</success_criteria>

<output>
Create `.planning/phases/03-join-detail-state/03-04-SUMMARY.md` when done.
</output>
