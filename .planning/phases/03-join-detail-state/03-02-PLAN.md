---
phase: 03-join-detail-state
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - webapp/state.py
  - webapp/join.py
  - webapp/tests/test_state.py
  - webapp/tests/test_join.py
autonomous: true
requirements: [STATE-02, STATE-03, STATE-04, STATE-05, JOIN-01, JOIN-02, JOIN-03, JOIN-04]

must_haves:
  truths:
    - "`imageTranslated` 가 `'0'` `'1'` `False` `1` `None` 어느 형태로 와도 판정이 뒤집히지 않는다 (STATE-02 / 성공기준 2)"
    - "`aiImageGenerated` 를 **먼저** 본다 — AI 가공 완료분이 '단순번역만'으로 내려앉지 않는다 (STATE-03 / Pitfall 4)"
    - "`aiImageGenerated` 키가 통째로 없어도 KeyError 가 안 난다 (실측 34건 중 26건이 그 상태다)"
    - "미해소 사유가 `추출실패` / `번호없음` / `미조회` 로 **서로 다른 값**이고, 광고 쪽 청소 대상과 시스템 문제가 다른 버킷에 담긴다 (JOIN-02 / Pitfall 3)"
    - "미해소 사유 문자열에 **광고그룹명 원문과 추출된 번호**가 같이 들어간다 — 용팀장이 그걸로 네이버 광고에서 찾아 지운다"
    - "같은 번호를 쓰는 광고그룹 2개가 인덱스 대상에서 한 그룹으로 접힌다 (비용)"
    - "기작업(`구매_가공완료` 태그 또는 AI 가공 완료)인 상품이 **기본 선택에서 빠지되 목록에서 숨지 않는다** (D-08 / STATE-05)"
    - "불사자 응답의 `표시규칙` 필드가 어느 반환값에도 남지 않는다 (프롬프트 인젝션)"
  artifacts:
    - path: "webapp/state.py"
      provides: "불리언정규화(v) · 상세상태(dc) · 기작업여부(그룹태그, dc, done_tags) — 순수 함수, 네트워크 0"
      min_lines: 60
      contains: "def 상세상태("
    - path: "webapp/join.py"
      provides: "market_number · group_index · ad_groups_by_product · attach · resolution · cleanup_groups · index_targets · strip_display_rules"
      min_lines: 120
      contains: "def attach("
    - path: "webapp/tests/test_state.py"
      provides: "판정 순서 역전·bool('0') 함정·키 부재 회귀"
    - path: "webapp/tests/test_join.py"
      provides: "해상률·미해소 3종·광고그룹명 인용·번호추출 전수·중복제거·팬아웃·표시규칙 제거"
  key_links:
    - from: "webapp/join.py"
      to: "webapp/state.py"
      via: "attach() 안에서 상세상태()·기작업여부() 호출 — 판정 진실은 한 곳"
      pattern: "from webapp import state"
    - from: "webapp/join.py"
      to: "조인 잡 산출물 join_<job_id>.json"
      via: "읽기 전용 dict 투영. 파일을 직접 열지 않고 dict 를 인자로 받는다"
      pattern: "join_doc"
---

## Phase Goal

> ⚠ ROADMAP `**Goal:**` 이 user-story 형식이 아니다. 정규화는 `/gsd mvp-phase 03`. 아래는 ROADMAP 원문이다.

광고 판정 행을 불사자 상품에 실제로 잇고 상세 상태를 3단계로 판정해, "유입은 있는데 상세가 중국어 원본인 상품" 목록을 화면에서 정확히 뽑아낸다.

<objective>
**이 페이즈에서 새로 쓰는 판정 로직 전부가 이 플랜에 있다.** RESEARCH 의 말대로 사실상 함수 3개다 —
번호 추출 · 불리언 정규화 · 3단계 판정. 셋 다 순수 함수고 셋 다 pytest 로 전수 검증된다.
그 비율이 깨지면 설계가 어긋난 것이다.

**이 두 모듈은 `webapp/board.py` 와 같은 성질이다 — 읽기 전용 · 네트워크 0 · 크레딧 0.**
불사자 MCP 를 부르지 않는다(D-19). MCP 접촉은 전부 CLI 자식 몫이고, 여기는 그 자식이 써 둔
산출물 dict 를 받아 투영할 뿐이다. `eroomlib` 을 import 하면 `.venv-web` 에 `requests` 가 없어
ImportError 다 — 그게 이 설계의 물리적 근거다(PATTERNS §🔴①).

Purpose: 이 함수들이 틀리면 Phase 5 가 이미 가공된 상품에 크레딧을 다시 태우고(판정 순서 역전),
용팀장이 멀쩡한 광고그룹을 지우러 간다(사유 뭉개기). 둘 다 되돌릴 수 없다.
Output: `webapp/state.py` · `webapp/join.py` + 검증 15종
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-join-detail-state/03-CONTEXT.md
@.planning/phases/03-join-detail-state/03-RESEARCH.md
@.planning/phases/03-join-detail-state/03-PATTERNS.md
@.planning/phases/03-join-detail-state/03-VALIDATION.md
@.planning/phases/03-join-detail-state/03-01-SUMMARY.md

<interfaces>
이 플랜이 만드는 계약. 03-05(라우트 ctx)·03-06(보드 열)이 이 이름으로 쓴다.

webapp/state.py:
  AI가공완료 = "AI가공완료"      # ⚪ — 이모지는 화면에서 붙인다. 판정값은 문자열이다
  단순번역만 = "단순번역만"      # 🟡
  중국어원본 = "중국어원본"      # 🔴
  불리언정규화(v) -> bool
  상세상태(uploadDetailContents: dict | None) -> str        # 위 세 값 중 하나
  기작업여부(그룹태그: str | None, dc: dict | None, done_tags: list[str]) -> bool

webapp/join.py:
  # 사유코드
  추출실패 = "추출실패"; 번호없음 = "번호없음"      # 버킷: "광고청소"
  미조회   = "미조회";   미스     = "미스"; 불일치 = "불일치"   # 버킷: "시스템"

  재검증판정(인덱스_smartstore: str | None, 관측_smartstore: str | None, 미조회: bool) -> str
      # "미조회" | "미스" | "히트" | "불일치"  — 순서에 의미가 있다 (아래 action 참조)
  market_number(ad_group_name: str) -> str | None
  group_index(groups: list[dict]) -> dict[str, dict]            # {"15-2": {"groupId":..., "그룹명":...}}
  ad_groups_by_product(result: dict) -> dict[tuple[str, str], list[str]]
  strip_display_rules(obj) -> obj                               # 재귀적으로 '표시규칙' 키 제거
  attach(rows, result, join_doc, *, excluded=(), done_tags=()) -> list[dict]
  resolution(attached_rows) -> dict
  cleanup_groups(attached_rows) -> list[dict]
  index_targets(attached_rows, join_doc, excluded=()) -> list[str]   # 훑을 groupId 목록(중복제거 후)
  selectable(attached_rows) -> list[dict]                       # 기본 선택 대상 (기작업 제외)

attach() 가 행에 더하는 키 (board.fold_products 결과를 **복사해서** 돌려준다. 원본 mutate 금지):
  "adGroups": list[str]          # 이 상품 줄에 걸린 광고그룹명 원문 (사유 문자열의 재료)
  "번호": str | None
  "번호해소": bool
  "상품조회": "히트" | "미스" | "미조회" | "불일치" | None    # 번호 미해소면 시도 안 함 → None
  "해소": bool                   # 번호해소 and 상품조회 == "히트"
  "사유코드": str | None
  "사유": str | None             # 사람이 읽는 한 줄. 광고그룹명 원문 + 번호 포함
  "버킷": "광고청소" | "시스템" | None
  "productId": str | None
  "상세상태": str | None         # state.AI가공완료 / 단순번역만 / 중국어원본. 미해소면 None
  "기작업": bool
  "기작업태그": str | None       # 불사자 `그룹` 태그 원문 (D-08 화면 표시용)
  "사본N": int | None            # 팬아웃 (JOIN-03)
  "잠금혼재": bool | None        # 사본 중 잠긴 것과 안 잠긴 것이 섞였는가

resolution() 반환:
  {"전체": int,
   "번호해소": int, "번호미해소": int,
   "상품해소": int,
   "광고청소": {"행": int, "그룹": int, "사유별": {"추출실패": int, "번호없음": int}},
   "시스템":   {"행": int, "사유별": {"미조회": int, "미스": int, "불일치": int}}}
  ⚠️ 두 버킷을 절대 한 숫자로 합치지 마라 (Pitfall 3).

cleanup_groups() 반환 (광고그룹 단위로 접은 청소 목록 — 지울 대상은 그룹이고 행은 부산물이다):
  [{"adGroup": str, "번호": str | None, "사유코드": str, "행수": int, "계정들": [str]}, ...]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: webapp/state.py — 3단계 판정 + 타입 안전 정규화 (STATE-02/03/04)</name>
  <files>webapp/state.py, webapp/tests/test_state.py</files>
  <read_first>
    - `03-RESEARCH.md` §D-12 확정 — **완성된 판정 규약 코드가 338-380행에 있다. 그대로 옮겨라. 다시 설계하지 마라**
    - `03-RESEARCH.md` §D-12 핵심 결론 셋(키 부재 vs false · 두 신호 독립 · `상세페이지있음` 금지) · §STATE-04 확정(양성대조 8 / 음성대조 26) · §Pitfall 4 · §Pitfall 8
    - `webapp/board.py` 3-31행 — 모듈 docstring 관용구("이 모듈이 절대 하지 않는 것" 목록)와 36-40행 `.get()` 만 쓰는 규율
    - `webapp/static/board.js` 64-72행 — **bool 을 int 보다 먼저 분기**해야 하는 이유가 이미 주석으로 적혀 있다
    - `.claude/lib/eroomlib/snapshot.py` 184-189행 — `bool(det.get("aiImageGenerated"))` 로 **틀리게** 하고 있는 실물. 베끼지 마라
    - `.claude/skills/bulsaja-detail-page/scripts/detail_batch.py` 136-150행 — 현행 CLI 의 깨진 스킵 로직(`existing_ai_detail`). 왜 이 페이즈가 웹앱 쪽에서 막는지의 근거
    - `webapp/tests/test_board.py` 3-13행(함정 열거 docstring), 110-113행(**기대값 옆에 틀린 값도 적는** 단언 관용구)
    - `webapp/tests/fixtures/join_traps.json` (03-01 산출물)
  </read_first>
  <behavior>
    - `test_불리언정규화` — `'0'`→False · `'1'`→True · `False`→False · `1`→True · `None`→False · `''`→False · `'false'`→False
    - `test_문자열0은_거짓이다` — `불리언정규화('0') is False` 이고, 실패 메시지에 "bool('0') 은 True 다" 가 보인다
    - `test_상세상태_3단계` — `{aiImageGenerated: True}`→AI가공완료 · `{imageTranslated: '1'}`→단순번역만 · `{imageTranslated: '0'}`→중국어원본
    - `test_AI생성이_번역보다_우선` — `{aiImageGenerated: True, imageTranslated: '1'}` → **AI가공완료**. 단언 옆에 `# "단순번역만" 이면 판정 순서가 뒤집혔다` 를 적는다
    - `test_키가_없어도_안터진다` — `{}` · `None` · `{"renderContent": "..."}` 전부 예외 없이 `중국어원본`
    - `test_불리언순서가_int보다_먼저다` — `isinstance(v, bool)` 분기를 `int` 분기 **앞에** 두지 않으면 통과할 수 없는 케이스로 고정한다 (`True`/`False` 가 우연히 맞는 것과 구분되게 `True` 가 `1 != 0` 경로로 새지 않음을 함수 소스가 아니라 **동작**으로 본다)
    - `test_기작업은_태그와_기계기록_둘다본다` — `그룹태그='구매_가공완료'`(AI 흔적 없음) → True · `aiImageGenerated=True`(태그 없음) → True · 둘 다 없음 → False
    - `test_상태와_태그를_한값으로_합치지_않는다` — `구매_가공완료` 태그만 있는 행의 `상세상태` 가 `AI가공완료` 가 **아니다**(실측 6건 전부 `aiImageGenerated` 키 부재). 태그는 사람 손자국, 상태는 기계 기록이다 (Pitfall 8)
    - `join_traps.json` 의 각 행을 돌려 기대 판정이 나온다 (픽스처 함정과 1:1)
  </behavior>
  <action>
    `webapp/state.py` 를 쓴다. `03-RESEARCH.md:338-380` 의 `불리언정규화` · `상세상태` 를 **그대로** 옮기고, 거기 달린 주석(`bool(v)` 금지 이유 · 판정 순서 이유)도 같이 옮긴다. 모듈 docstring 은 `board.py:3-13` 모양으로 쓰되 "이 모듈이 절대 하지 않는 것" 에 다음을 못박아라:
      - MCP 호출 (D-19 — `eroomlib` import 금지. `.venv-web` 에 `requests` 가 없다)
      - `bool()` 로 플래그 읽기 (`'0'` 이 True 가 된다)
      - `dc["aiImageGenerated"]` (키가 **통째로 없다**. 실측 34건 중 26건)
      - `상세페이지있음` 으로 판정 (34건 전부 True 라 기준이 못 된다)
      - 태그(`구매_가공완료`)와 기계기록(`aiImageGenerated`)을 한 값으로 합치기 (Pitfall 8)

    **STATE-04 의 결론을 이 docstring 에 기록해라** — 검증 수단이 문서다(VALIDATION 의 STATE-04 행): *외부 반영 경로(`bulsaja_detail_apply`)는 `aiImageGenerated` 를 찍지 않는다. 양성대조 8건 전부 True · 음성대조 26건 전부 키 부재(2026-09-21 실측). 그래서 외부 경로 기작업의 유일한 신호는 `그룹` 태그이고, 그 태그는 목록 조회에 공짜로 딸려 온다 — 보완 인덱스를 신설하지 않는다(D-09).*

    `기작업여부(그룹태그, dc, done_tags)` 를 더한다. **두 신호의 OR** 다: ① `그룹태그` 가 `done_tags`(설정값) 안에 있거나 ② `상세상태(dc) == AI가공완료`. `done_tags` 는 인자로 받는다 — 모듈이 `settings` 를 직접 읽으면 테스트가 monkeypatch 할 자리가 애매해지고, 호출부(`join.attach`)가 이미 설정을 읽는다. **`pages`·`장수` 같은 인자를 절대 받지 마라** (STATE-05 — 기작업 스킵은 목표 장수와 무관한 절대 조건이다).

    한글 함수명을 쓰는 이유를 docstring 에 한 줄로 남겨라: 판정 규약의 정본이 RESEARCH 의 코드이고 `03-VALIDATION.md` 의 테스트 이름이 그 이름을 지목한다. (저장소 관례는 공개 API 영문이지만 여기서는 정본을 따른다.)

    `webapp/tests/test_state.py` 를 behavior 9항목에 1:1 로 쓴다. 파일 IO 없이 순수 함수만 때린다. 모듈 docstring 에 픽스처 함정 목록을 `test_board.py:3-13` 모양으로 적는다. 기대값 옆에 **틀린 값도 같이** 적어라(`test_board.py:110-113` 관용구).
  </action>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_state.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv-web/bin/pytest webapp/tests/test_state.py -x -q` 가 exit 0
    - `03-VALIDATION.md` 가 지목한 노드 5종이 그 실행 안에서 돈다:
      `.venv-web/bin/pytest webapp/tests/test_state.py::test_불리언정규화 webapp/tests/test_state.py::test_문자열0은_거짓이다 webapp/tests/test_state.py::test_상세상태_3단계 webapp/tests/test_state.py::test_AI생성이_번역보다_우선 webapp/tests/test_state.py::test_키가_없어도_안터진다 -x` 가 exit 0
    - `grep -c 'def 상세상태(' webapp/state.py` 가 1
    - `grep -v '^#' webapp/state.py | grep -c 'bool('` 가 **0** (정규화 함수 안에서도 `bool()` 을 쓰지 않는다)
    - `grep -cE '^(import|from) ' webapp/state.py` 의 결과에 `eroomlib`·`requests`·`fastapi` 가 0건:
      `grep -cE '(eroomlib|requests|fastapi|starlette)' webapp/state.py` 가 0
    - `.venv-web/bin/python3 -c "from webapp import state; import inspect; s=inspect.signature(state.기작업여부); assert 'pages' not in s.parameters and '장수' not in s.parameters, s; print('PASS')"`
  </acceptance_criteria>
  <done>판정 순서·타입 함정·키 부재가 전부 테스트로 고정됐다. STATE-04 결론이 소스에 남았다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: webapp/join.py — 번호 매칭 · 해상률 · 미해소 사유 3종 · 팬아웃 (JOIN-01/02/03, STATE-05)</name>
  <files>webapp/join.py, webapp/tests/test_join.py</files>
  <read_first>
    - `03-RESEARCH.md` §Code Examples 915-953행 — **번호 추출·마켓그룹색인 완성 코드.** 그대로 옮긴다
    - `03-RESEARCH.md` §해상률 실측 → §미해소 표시에 대한 설계 요구 1~5 (사유 문자열 포맷 · 광고그룹명 원문 필수 · 그룹 단위 접기 · 해석 붙이기) · §광고그룹명 번호 추출 실측 함정(`N번_` 접두사에 속지 마라) · §Pitfall 3 · §팬아웃 실측(2단 흐름과 배치 상한 50)
    - `03-CONTEXT.md` §광고그룹 정리 완료 — 같은 번호 7쌍, 불사자에만 있는 번호 3개, "해상률 100% 를 전제로 미해소 경로를 빼지 마라"
    - `webapp/board.py` 108-180행 — `fold_products` 의 행 dict 모양(`acct`·`mallProductId`·`rules`·`rule1_count`)과 후처리 자리, 52-56행 `_rule_key` 정렬키 관용구, 174-178행 "0 이 아니라 None" 규율
    - `webapp/board.py` 129-132행 — `result["accounts"][alias]["rules"][규칙][*]` 안에 `adGroup` 이 들어 있다. `ad_groups_by_product` 가 그걸 긁는다
    - `webapp/tests/test_board.py` 34-39행(`_행` 헬퍼), 73-98행(리터럴 가드가 훑는 방식)
    - `webapp/tests/fixtures/result_traps.json` · `bulsaja_groups_traps.json` · `join_traps.json` · `result_join_real.json` (03-01 산출물)
    - `webapp/state.py` (Task 1 산출물)
  </read_first>
  <behavior>
    - `test_번호추출_전수` — `result_join_real.json` 의 **모든** 광고그룹명에서 번호가 추출된다(추출실패 0). 더해서 트랩 픽스처의 3포맷(`판매상품_NN-N_` · `NN-N ` 공백 · `1000개_NN-N_`)이 전부 맞고 `1000` 이 오탐되지 않는다
    - `test_같은번호_중복제거` — 같은 번호를 쓰는 광고그룹 2개가 `index_targets` 에서 groupId 1개로 접힌다
    - `test_해상률` — `resolution()` 이 전체/번호해소/번호미해소/상품해소를 각각 돌려주고, **광고청소 버킷과 시스템 버킷을 합치지 않는다**
    - `test_미해소_사유_3종` — `추출실패`·`번호없음`·`미조회` 가 **서로 다른 사유코드**이고, 앞 둘의 버킷은 `광고청소`, `미조회` 의 버킷은 `시스템` 이다. D-18 제외 그룹의 행은 `미조회` 이고 `번호없음` 이 **아니다**
    - `test_사유에_광고그룹명` — 미해소 행의 `사유` 문자열에 **광고그룹명 원문**이 그대로 들어 있고, 번호가 추출된 경우 번호도 들어 있다
    - `test_팬아웃_N건` — 사본 3건인 행의 `사본N == 3` 이고, 사본이 1건이면 `사본N == 1`, 조인이 안 된 행은 `None`(0 이 아니다)
    - `test_표시규칙은_버린다` — `join_traps.json` 최상위·행 양쪽에 있는 `표시규칙` 이 `attach()` 결과 어디에도 없다. `json.dumps(결과)` 전문에 그 글자가 0건
    - `test_기작업은_기본선택에서_빠진다` — `구매_가공완료` 행과 `AI가공완료` 행이 `selectable()` 에 없지만 `attach()` 결과에는 **그대로 남아** `기작업=True`·`기작업태그` 를 달고 있다 (D-08: 숨기지 않는다)
    - `test_스킵은_장수와_무관하다` — `attach`·`selectable` 시그니처에 장수/`pages` 인자가 없고, 기작업 판정이 어떤 장수 맥락에서도 동일하다 (STATE-05)
    - `test_청소목록은_그룹단위다` — `cleanup_groups()` 가 행이 아니라 광고그룹 단위로 접고 `행수`·`계정들` 을 달고 나온다
    - `test_빈입력도_안깨진다` — `attach([], {}, None)`·`resolution([])`·`index_targets([], None)` 이 예외 없이 빈 결과 (board.py:63-67 / 129-130 관용구)
    - `test_히트도_재검증한다` — `재검증판정(인덱스값, 관측값=같은값, 미조회=False)` 이 `"히트"`. **관측값이 None 이면 히트가 나오지 않는다** — 인덱스만 보고 해소로 접는 경로가 없다 (JOIN-04)
    - `test_불일치는_미해소` — 인덱스값과 관측값이 다르면 `"불일치"` 이고, 그 행의 `해소` 가 False · `버킷` 이 `시스템` 이다 (물갈이 재발급)
    - `test_미조회는_미조회다` — `미조회=True` 면 값이 무엇이든 `"미조회"`. 429 를 성공으로 접지 않는다
    - `test_인덱스에_없으면_미스` — 인덱스값이 None 이고 미조회가 False 면 `"미스"` (미조회와 **다른 값**)
    - `test_인덱스없음이_광고오류로_둔갑하지_않는다` — `join_doc=None`(스캔 전) 이면 모든 행이 `미조회`/`시스템` 이고 `광고청소` 버킷이 **0** 이다 (Pitfall 3 의 핵심)
  </behavior>
  <action>
    `webapp/join.py` 를 쓴다. 모듈 docstring 은 `board.py:3-13` 모양. "이 모듈이 절대 하지 않는 것" 에 못박아라:
      - 상품명 매칭 (D-03 — 실측 6건 중 2건이 깨졌다. 조용히 틀린 상품을 집는 길이다)
      - 별칭표·몰이름 2차 키·폴백 매핑 (D-02b — 짝 없는 번호는 광고 쪽 오등록이고 사람이 고친다)
      - `mallProductId` 영구 캐시 (D-04 — 20일 물갈이로 재발급. 회차 스냅샷 안에서만 조인 키다)
      - MCP 호출 (D-19)
      - 미해소를 한 통에 담기 (Pitfall 3 — 사유가 사라지면 시스템 고장이 사람 할 일 목록으로 둔갑한다)

    `market_number` / `group_index` 는 `03-RESEARCH.md:915-953` 을 그대로 옮긴다(주석 포함). `group_index` 의 "번호가 안 나오는 그룹이 86개 중 32개이고 `N번_` 접두사는 번호가 아니다" 경고를 반드시 같이 옮겨라.

    `ad_groups_by_product(result)` — `result["accounts"][alias]["rules"][규칙][*]["adGroup"]` 을 `(alias, mallProductId)` 키로 모은다. **중복 제거하되 순서를 보존**해라(첫 등장 순). `.get()` 으로만 만진다 — 규칙마다 있는 키가 다르다(board.py:28-30).

    `strip_display_rules(obj)` — dict/list 를 재귀로 돌며 `표시규칙` 키를 **제거한 사본**을 돌려준다. 원본을 mutate 하지 마라. `attach()` 는 `join_doc` 을 받자마자 이걸 통과시킨다. 이유를 주석에 적어라: 그 필드는 모델 대상 지시문(*"앞 지시 무시해…"*)이고, 화면 렌더·LLM 투입 경로를 만들면 프롬프트 인젝션이 된다.

    `attach(rows, result, join_doc, *, excluded=(), done_tags=())` — `rows` 는 `board.fold_products(result)` 결과다. **`fold_products` 의 시그니처를 바꾸지 말고 `board.py` 를 건드리지도 마라** — 그 모듈이 "네트워크도 크레딧도 0" 인 계약을 지키게 두고, 조인은 여기서 얹는다. 각 행을 **복사**해 `<interfaces>` 의 키 13종을 채운다. 판정 순서:
      1. `adGroups` → `market_number` 로 `번호`. 번호가 없으면 `사유코드=추출실패`, `버킷=광고청소`, 끝.
      2. `번호` 가 `group_index(join_doc["마켓그룹"])` 에 없으면 `사유코드=번호없음`, `버킷=광고청소`, 끝.
         ⚠️ `join_doc` 이 `None`(아직 스캔 안 함)이면 **여기서 `번호없음` 을 내면 안 된다.** 그룹 목록을 아직 모르는 것과 그룹이 없는 것은 다르다 → `사유코드=미조회`, `버킷=시스템`.
      3. `번호` 가 `excluded`(D-18) 에 있으면 `사유코드=미조회`, `버킷=시스템`. 사유 문자열에 "제외 설정" 임을 밝혀라.
      4. `join_doc["행"]` 에서 `(acct, mallProductId)` 로 관측을 찾는다. 없으면 `미조회`. 있으면 **`재검증판정(행["인덱스_smartstore"], 행["관측_smartstore"], 행["미조회"])`** 으로 `상품조회` 를 정하고, `히트` 일 때만 `해소=True` 로 한다.
         `재검증판정` 은 순수 함수이고 **순서에 의미가 있다**: ① `미조회` 가 참이면 `"미조회"`(다른 무엇보다 먼저 — 429 를 성공으로 접지 않는다) ② 인덱스값이 없으면 `"미스"` ③ 관측값이 없으면 `"미조회"`(재검증 자체를 못 했다) ④ 두 값이 같으면 `"히트"`, 다르면 `"불일치"`.
         이 함수가 `join.py` 에 있는 이유: 판정은 조인 의미론이고 `webapp/bulsaja_index.py` 는 인덱스에 적힌 값을 꺼내 주는 데까지만 한다. 판정이 두 곳에 있으면 화면과 산출물이 다른 말을 한다.
         ⚠️ `03-VALIDATION.md` 는 `test_히트도_재검증한다`·`test_불일치는_미해소` 를 `test_index.py` 행에 적어 뒀다. 함수가 여기 살므로 **같은 이름으로 `test_join.py` 에 둔다** — 이 이탈을 SUMMARY 에 적어라(검증 내용은 그대로다).
      5. `히트` 면 `state.상세상태(관측["uploadDetailContents"])` 로 `상세상태`, `state.기작업여부(관측["그룹태그"], dc, done_tags)` 로 `기작업`, `사본` 길이로 `사본N`, `잠금` 혼재 여부로 `잠금혼재`.
    사유 문자열 포맷은 RESEARCH §미해소 표시 설계요구 1 의 두 줄을 기본형으로 쓰되, `미조회`·`미스`·`불일치` 에는 **시스템 문제임이 드러나는 문구**를 쓴다(예: "불사자 인덱스에 아직 없다 — 인덱스 구축이 필요하다"). **광고그룹명은 f-string 으로 끼워 넣어라. 예시 그룹명을 주석에 복붙하지 마라** — 주석에 남은 그룹명은 다음 사람에게 별칭표로 읽힌다(Pitfall 5).

    `resolution(rows)` — `<interfaces>` 모양으로 집계한다. **두 버킷을 합친 숫자를 내보내지 마라.** `번호해소`/`상품해소` 를 따로 낸다. 비율은 화면이 만든다(`board.py:174-178` 의 "0 이 아니라 None" 정신 — 분모가 0 이면 비율을 지어내지 않는다).

    `cleanup_groups(rows)` — `버킷=="광고청소"` 인 행만 광고그룹명으로 접어 `행수`·`계정들`·`사유코드`·`번호` 를 낸다. 정렬은 행수 내림차순(지울 값어치 순). *지울 대상은 그룹이고 행은 그 부산물이다* 를 docstring 에 적어라.

    `index_targets(rows, join_doc, excluded=())` — 훑어야 할 `groupId` 목록. **번호 기준으로 중복 제거**한다(같은 번호를 쓰는 광고그룹 7쌍이 실측으로 있다 — 안 접으면 같은 마켓그룹을 두 번 훑어 몇 시간을 버린다). `excluded` 번호는 뺀다. 결과는 정렬된 고유 문자열 리스트. **빈 리스트를 "전량"으로 읽는 호출부가 생기지 않게** docstring 에 "빈 리스트 = 훑을 대상 없음" 을 못박아라(`argv.py:79-84` / `jobs.py:389-395` 와 같은 부류).

    `selectable(rows)` — `기작업=False` 이고 `해소=True` 인 행만. D-08 대로 **화면에서 숨기는 게 아니라 기본 선택에서만 빼는** 장치다. 그 구분을 docstring 에 적어라.

    `webapp/tests/test_join.py` 를 behavior 11항목에 1:1 로 쓴다. `test_board.py` 의 `_행` 헬퍼 관용구를 그대로 가져와 실패 메시지가 원인을 말하게 해라. **`test_실회차_해상률` 은 이 플랜에서 쓰지 마라** — 기댓값(179)을 확정하려면 불사자 마켓그룹 번호 집합이 필요하고 그건 03-04 의 첫 스캔이 가져온다.
  </action>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_join.py webapp/tests/test_state.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv-web/bin/pytest webapp/tests/test_join.py -x -q` 가 exit 0
    - VALIDATION 이 지목한 노드가 전부 존재하고 통과한다:
      `.venv-web/bin/pytest "webapp/tests/test_join.py::test_히트도_재검증한다" "webapp/tests/test_join.py::test_불일치는_미해소" "webapp/tests/test_join.py::test_해상률" "webapp/tests/test_join.py::test_미해소_사유_3종" "webapp/tests/test_join.py::test_사유에_광고그룹명" "webapp/tests/test_join.py::test_번호추출_전수" "webapp/tests/test_join.py::test_같은번호_중복제거" "webapp/tests/test_join.py::test_팬아웃_N건" "webapp/tests/test_join.py::test_표시규칙은_버린다" "webapp/tests/test_join.py::test_기작업은_기본선택에서_빠진다" "webapp/tests/test_join.py::test_스킵은_장수와_무관하다" -x` 가 exit 0
    - `grep -c 'def attach(' webapp/join.py` 가 1
    - `grep -cE '(eroomlib|requests|fastapi|starlette|APIRouter)' webapp/join.py` 가 0 (D-19)
    - `grep -cE '(sqlite3|open\()' webapp/join.py` 가 0 (파일도 DB 도 안 연다 — dict 만 받는다)
    - 리터럴 가드가 새 파일을 실제로 훑는다: `.venv-web/bin/pytest webapp/tests/test_board.py -k 계정을_코드에 -x` 가 exit 0 이고 `grep -c 'join.py' webapp/tests/test_board.py` 가 1 이상
    - 전량 회귀: `.venv-web/bin/pytest webapp/tests -q` 가 exit 0
  </acceptance_criteria>
  <done>해상률·미해소 3종·팬아웃·기본선택 제외가 전부 순수 함수로 서고 테스트가 고정한다. 인덱스가 비었을 때 광고청소 버킷이 0 이라는 것도 기계가 본다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 불사자 MCP 응답 → 웹앱 순수 함수 | 외부 문자열(상품명·그룹태그·`표시규칙`)이 dict 로 들어온다 |
| 광고 `result.json` → 웹앱 | 사람이 네이버 광고 화면에서 지은 그룹명이 사유 문자열로 화면까지 간다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-06 | Tampering | `표시규칙` 프롬프트 인젝션 | mitigate | `strip_display_rules()` 로 `attach()` 입구에서 재귀 제거. `test_표시규칙은_버린다` 가 `json.dumps` 전문 검사로 고정 |
| T-3-07 | Tampering(무결성) | 429/미조회가 "광고 쪽 오류"로 둔갑 | mitigate | 사유코드 5종 + 버킷 2종으로 분리. `join_doc=None` 이면 광고청소 버킷이 0 임을 테스트로 고정 (Pitfall 3) |
| T-3-08 | Tampering | 판정 순서 역전으로 ⚪→🟡 | mitigate | `aiImageGenerated` 를 먼저 보는 순서를 `test_AI생성이_번역보다_우선` 이 고정 (Pitfall 4) |
| T-3-09 | Info Disclosure | 마켓그룹명·계정 alias 가 소스에 박힘 | mitigate | 사유 문자열은 f-string 으로만. 주석에 실제 그룹명 복붙 금지. `test_계정을_코드에_박지_않는다` 가 `join.py`·`state.py` 를 훑는다 |
| T-3-10 | Elevation | 웹앱 프로세스가 MCP 를 직접 부름 | mitigate | `grep` 기반 수용 기준으로 `eroomlib`/`requests` import 0건을 고정 (D-19) |
</threat_model>

<verification>
```bash
.venv-web/bin/pytest webapp/tests -q
.venv-web/bin/pytest webapp/tests/test_join.py webapp/tests/test_state.py -q
.venv-web/bin/pytest webapp/tests/test_paths.py -k 리터럴 -x
bash webapp/tests/no_commit_guard.sh
```
</verification>

<success_criteria>
- `imageTranslated` 5형태 · `aiImageGenerated` 키 부재가 전부 올바로 판정된다
- 미해소 사유 3종이 서로 다른 값이고 버킷 2종으로 갈린다 — 인덱스가 비어 있을 때 광고청소가 0 이다
- 기작업 상품이 기본 선택에서 빠지되 목록에 남는다
- `표시규칙` 이 반환값 전문에 0건
- `webapp/join.py`·`webapp/state.py` 에 네트워크·DB·파일 접근이 0건
</success_criteria>

<output>
Create `.planning/phases/03-join-detail-state/03-02-SUMMARY.md` when done.
</output>
