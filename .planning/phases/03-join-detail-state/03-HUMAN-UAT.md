---
status: partial
phase: 03-join-detail-state
source: [03-VERIFICATION.md, 03-07-PLAN.md Task 3]
started: 2026-09-21
updated: 2026-09-21
---

<!-- uat-auto: done -->
<!-- uat-verifier 자동 판정 2026-09-21 — #1 BLOCKED(네이버 로그인 필요, 보드 필드는 전량 채집) · #2 구조 PASS(직관 판단만 사람 몫) -->

## Current Test

#1 — 용팀장이 네이버 검색광고에서 직접 대조 (아래 "사용자 확인 절차")

## Tests

### 1. 미해소 사유만 보고 네이버 광고에서 그 그룹을 찾을 수 있는가 (JOIN-02 ④)
expected: 보드의 미해소 청소 목록(19그룹)에 적힌 **광고그룹명 원문과 번호**만 들고
네이버 검색광고 화면에서 그 그룹을 실제로 찾을 수 있다.
못 찾으면 사유 문구에 무엇이 더 필요한지가 이 기능의 쓸모를 결정한다.
result: BLOCKED(네이버 로그인 필요) — 자동 판정 불가(AI 네이버 접속 금지). 대신 보드가 실제로
뿌리는 필드를 전량 채집했다: 광고그룹명 **원문 전체**(19행 모두 잘림 없음) · 번호(NN-N, 19건 중
18건 존재) · 사유(`추출실패` 1건 / `번호없음` 18건) · 행수 · 계정. **캠페인명 컬럼은 없다** —
회사명이 그룹명 문자열 안에 섞여 있을 뿐(`판매상품_7-1_오운웨이컴퍼니`). 행수 합계 883 = 배너 숫자와 일치.
최난도 1건: `소형이동식오피스`(추출실패 · 번호 `—` · 703행) — 번호 없이 원문만으로 찾아야 한다.
**사용자 확인 절차**: ① 원문 2개(`소형이동식오피스`, `판매상품_7-1_오운웨이컴퍼니`)를 들고 네이버
검색광고 관리자센터를 연다 → ② 광고그룹 검색창에 원문을 그대로 붙여넣는다 → ③ 각각 1개로 특정되는지,
특히 번호 없는 `소형이동식오피스`도 헷갈림 없이 찾아지는지 알려 준다(찾음/못 찾음 + 못 찾았으면 무슨 문구가 더 필요했는지).

### 2. 두 배너가 헷갈리지 않는가 (JOIN-02 ⑤ · 이 페이즈 최대 오진 지점)
expected: 빨강 배너("광고 쪽에서 내가 지울 것" — 883행 / 19그룹)와
파랑 배너("시스템이 못 읽은 것" — 상품 연결 51/6062)를 나란히 봤을 때
**무엇이 내 일이고 무엇이 기계 일인지** 헷갈리지 않는다.
헷갈리면 멀쩡한 광고그룹을 지우러 가게 된다.
result: PASS(구조) / 직관 판단만 사람 몫 — uat-verifier 실측:
· 두 배너 모두 렌더됨. 빨강 `.stale`(살구 `rgb(253,231,192)` + 진갈색 좌보더, top 711px) /
  파랑 `.system-warn`(회색 `rgb(243,245,247)` + 파란 좌보더 `rgb(1,114,173)`, top 815px) — 색·클래스·좌표 분리, 겹침 없음
· 숫자 귀속 정확: `883행/19그룹`은 빨강 문단에만, `51/6062`는 파랑 문단에만. 883은 19행 테이블 행수 합계와 직접 합산 일치
· 해소 필터 배타성 실측: `광고청소` → 40셀 전부 🛠(번호없음/추출실패, ⚙ 0건) / `시스템` → 40셀 전부 ⚙(인덱스 미보유/없음, 🛠 0건).
  배너뿐 아니라 **행 단위 아이콘(🛠 사람 / ⚙ 기계)** 으로 이중 구분
· 금지 표현 `미스` 페이지 전체(1083줄) 0건
남은 것: 스크린샷 `~/.claude/uat/uat-artifacts/20260921-seller-control-tower/banners2.png` 를 보고
**실제로 헷갈리지 않는지** 최종 한 줄(심미·직관은 사람 몫).

## Summary

total: 2
passed: 0
issues: 0
pending: 1
skipped: 0
blocked: 1

## Gaps

없음 — 기계 검증은 10/10 통과, #2 구조 검증도 PASS. 남은 둘은 "동작하는가"가 아니라
**"쓸 만한가"** 를 묻는 항목이라 사람만 답할 수 있다(#1 네이버 로그인 · #2 직관).
용팀장이 2026-09-21 에 `"보드 확인했어, 승인"` 으로 포괄 승인했으나 항목별 진술은 아직 없다.

## 참고 — 건너뛴 항목

Task 3 의 ⑥(⚪ 행을 불사자에서 대조)은 **대조 불가**다. ⚪가 0건이다(51행이 🔴31 · 🟡20 · ⚪0).
STATE-04 결론대로 외부 경로가 `aiImageGenerated` 를 안 찍고, 실측한 `uploadDetailContents`
키가 `{imageTranslated, renderContent}` 둘뿐이었다.
