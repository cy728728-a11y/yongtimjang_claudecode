---
status: complete
phase: 03-join-detail-state
source: [03-VERIFICATION.md, 03-07-PLAN.md Task 3]
started: 2026-09-21
updated: 2026-09-21
---

<!-- uat-auto: done -->
<!-- uat-verifier 자동 판정 2026-09-21 — #2 구조 PASS(직관 판단만 사람 몫) -->
<!-- #1 은 2026-09-21 용팀장이 네이버에서 직접 확인·수정 완료 → PASS. 코드 실측으로 수정 효과 확인됨 -->

## Current Test

없음 — 2건 모두 PASS (2026-09-21 완료)

## Tests

### 1. 미해소 사유만 보고 네이버 광고에서 그 그룹을 찾을 수 있는가 (JOIN-02 ④)
expected: 보드의 미해소 청소 목록(19그룹)에 적힌 **광고그룹명 원문과 번호**만 들고
네이버 검색광고 화면에서 그 그룹을 실제로 찾을 수 있다.
못 찾으면 사유 문구에 무엇이 더 필요한지가 이 기능의 쓸모를 결정한다.
result: **PASS** — 2026-09-21 용팀장이 실제로 왕복을 닫았다. 보드의 광고그룹명 원문
`소형이동식오피스`(최난도 케이스: 번호 `—` · 추출실패 · 703행)만 들고 네이버 검색광고에서
**그 그룹을 찾아냈고**, 광고그룹명을 `25-2 소형이동식오피스` 로 **고쳤다**.
고친 것이 시스템에 먹히는지 메인 세션이 코드로 실측 확인:
· `market_number("25-2 소형이동식오피스")` → `25-2` (`_번호패턴` 이 `search` 라 접두·공백 구분도 잡는다.
  기존 `판매상품_7-1_회사명` 언더스코어 형태와 모양이 달라도 통과)
· `25-2` 가 불사자 마켓그룹에 **존재** → `5번_용쌤25-2` (groupId `1001822`, 조인 산출물 86그룹 중 번호 있는 54개에 포함)
· 단 groupId `1001822` 는 **인덱스 미보유**(인덱스 보유 30그룹에 없음)
→ **재스캔하면 703행은 `추출실패`(빨강·사람 몫)에서 `인덱스 미보유`(파랑·기계 몫)로 이동한다.**
   빨강 배너 883행/19그룹 → **180행/18그룹** 으로 줄고, 파랑 인덱스 미보유 5978행 → 6681행.
**판정 근거**: 이 항목이 물은 것은 "사유 문구만 보고 네이버에서 작업할 수 있는가" 였고,
사유 문구(`추출실패` + 광고그룹명 원문)만으로 **번호조차 없는 최악의 케이스에서 실제 작업이 완료됐다.**
사유 문구에 더 필요한 것은 없었다. 캠페인명 컬럼 부재도 장애가 되지 않았다.
**함께 수정**: `판매상품_17-3_오운웨이컴퍼니` 도 같은 방식으로 고쳤다.
**넘긴 것**: `판매상품_10-3_제이와이에프컴퍼니`(13행 · 883행의 1.5% · 계정 dldmswl1986) 는
네이버 광고그룹에 보이지 않아 **용팀장이 그냥 넘기기로 결정**(2026-09-21). 이미 지워진 광고그룹으로 보인다 —
보드 결함이 아니라 광고 쪽 실물이 없는 경우이고, 미해소로 남아도 무해하다.
**후속(Phase 3 완료를 막지 않음)**: 조인 스캔을 다시 돌려야 이 변경이 보드에 반영된다.

### 2. 두 배너가 헷갈리지 않는가 (JOIN-02 ⑤ · 이 페이즈 최대 오진 지점)
expected: 빨강 배너("광고 쪽에서 내가 지울 것" — 883행 / 19그룹)와
파랑 배너("시스템이 못 읽은 것" — 상품 연결 51/6062)를 나란히 봤을 때
**무엇이 내 일이고 무엇이 기계 일인지** 헷갈리지 않는다.
헷갈리면 멀쩡한 광고그룹을 지우러 가게 된다.
result: **PASS** — 2026-09-21 용팀장 진술: **"안헷갈려"**.
행동으로도 뒷받침된다 — 같은 세션에서 빨강 배너의 청소 목록을 보고 네이버 광고로 가서
광고그룹명 2건(`소형이동식오피스` → `25-2 소형이동식오피스`, `판매상품_17-3_오운웨이컴퍼니`)을
실제로 고쳤다. 빨강을 "내 일"로 읽고 실행했다는 뜻이다.
구조 검증(uat-verifier 실측):
· 두 배너 모두 렌더됨. 빨강 `.stale`(살구 `rgb(253,231,192)` + 진갈색 좌보더, top 711px) /
  파랑 `.system-warn`(회색 `rgb(243,245,247)` + 파란 좌보더 `rgb(1,114,173)`, top 815px) — 색·클래스·좌표 분리, 겹침 없음
· 숫자 귀속 정확: `883행/19그룹`은 빨강 문단에만, `51/6062`는 파랑 문단에만. 883은 19행 테이블 행수 합계와 직접 합산 일치
· 해소 필터 배타성 실측: `광고청소` → 40셀 전부 🛠(번호없음/추출실패, ⚙ 0건) / `시스템` → 40셀 전부 ⚙(인덱스 미보유/없음, 🛠 0건).
  배너뿐 아니라 **행 단위 아이콘(🛠 사람 / ⚙ 기계)** 으로 이중 구분
· 금지 표현 `미스` 페이지 전체(1083줄) 0건
스크린샷: `~/.claude/uat/uat-artifacts/20260921-seller-control-tower/banners2.png`

## Summary

total: 2
passed: 2
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

없음 — 기계 검증 10/10, #1 실사용 PASS(네이버 왕복 완주), #2 PASS("안헷갈려").
**JOIN-02 는 이것으로 닫힌다.**

**#1 이 남긴 것 — 이 페이즈가 실제로 값을 했다는 증거**
`추출실패` 사유 + 광고그룹명 원문만으로 703행짜리 최난도 그룹을 네이버에서 찾아 고쳤고,
그 수정이 코드에서 그대로 해소된다. "사유를 보고 광고 화면에서 작업할 수 있는가"에 실측 YES.

## 참고 — 건너뛴 항목

Task 3 의 ⑥(⚪ 행을 불사자에서 대조)은 **대조 불가**다. ⚪가 0건이다(51행이 🔴31 · 🟡20 · ⚪0).
STATE-04 결론대로 외부 경로가 `aiImageGenerated` 를 안 찍고, 실측한 `uploadDetailContents`
키가 `{imageTranslated, renderContent}` 둘뿐이었다.
