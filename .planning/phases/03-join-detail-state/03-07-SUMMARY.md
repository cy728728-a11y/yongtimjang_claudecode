---
phase: 03-join-detail-state
plan: 07
subsystem: index-live-run
tags: [join-04, join-01, state-04, eng-06, d-18, pitfall-6, 실탄, 재개, 체크포인트]

# Dependency graph
requires:
  - phase: 03-join-detail-state
    plan: 04
    provides: "ss_index_build.py · bulsaja_scan.py · bulsaja_rate.py · 재개 기준(productId 집합)"
  - phase: 03-join-detail-state
    plan: 05
    provides: "불사자 잡 3종 라우트 · 계정 배너 · 버튼 3개 · 인덱스 대상 30그룹 계산"
  - phase: 03-join-detail-state
    plan: 06
    provides: "보드 상태 열·필터·배너 2종·청소 목록 — 인덱스가 차면 그대로 값이 오른다"
provides:
  - "`ss_index_resume.py` — 재개 계산 2함수(`처리완료` · `남은대상`). import 0줄이라 `.venv-web` pytest 가 직접 부른다"
  - "`ss_index_calls.py` — MCP 호출 규약(groupId 숫자 강제 · 오류응답 판별). import 0줄"
  - "`webapp/jobs.py` `_build_argv` 가 `BulsajaArgv.prefix` 를 실제로 앞에 붙인다 (caffeinate -i · ENG-06)"
  - "`bulsaja_scan.py` 가 `mode=full` 로 읽는다 — `uploadBulsajaCode` 가 summary 응답에 없다"
  - "**실물 인덱스 30,546행 / 30그룹 / 미조회 0** — Phase 5 의 대상 목록이 이 위에 선다"
  - "보드 상품 연결 51건 = 🔴 31 · 🟡 20 · ⚪ 0 (Phase 5 의 입력)"
  - "`03-VALIDATION.md` 마감 — pending 표기 0건 · Sign-Off 6/6 · 미해결 5건 명시"
affects: [phase-04-banner, phase-05-detail-page]

# Tech tracking
tech-stack:
  added: []   # 새 외부 패키지 0건
  patterns:
    - "3시간짜리 루프의 규율은 루프를 돌려서 검증할 수 없다 — 재개 계산을 import 0줄 모듈로 떼어 1초 테스트로 고정한다"
    - "크레딧 0인 작업은 실패해도 경보가 없다 — 그래서 '성공했는데 0건' 을 의심하는 테스트가 따로 필요하다"
    - "조립 테스트가 green 이어도 호출부가 그 필드를 안 넘기면 아무 일도 안 일어난다 (prefix 가 그랬다)"
    - "MCP 응답의 오류 모양을 '빈 결과' 로 읽지 않는다 — `r.get(키) or []` 는 오류를 0건으로 접는다"
    - "재개는 '건너뛴다' 와 '해소로 승격되지 않는다' 를 한 테스트에서 같이 단언한다"
    - "사람이 하지 않은 말을 검증 문서에 적지 않는다 — 포괄 승인은 포괄 승인으로만 기록한다"

key-files:
  created:
    - .claude/skills/bulsaja-detail-page/scripts/ss_index_resume.py
    - .claude/skills/bulsaja-detail-page/scripts/ss_index_calls.py
    - .planning/phases/03-join-detail-state/03-07-evidence/
  modified:
    - .claude/skills/bulsaja-detail-page/scripts/ss_index_build.py
    - .claude/skills/bulsaja-detail-page/scripts/bulsaja_scan.py
    - webapp/jobs.py
    - webapp/tests/test_index.py
    - webapp/tests/test_argv.py
    - .planning/phases/03-join-detail-state/03-VALIDATION.md

key-decisions:
  - "재개 계산을 `ss_index_resume.py` 로 **추출만** 했다(로직 무변경). 3시간짜리 루프를 돌려서는 재개 규율을 검증할 수 없고, CLI 전체를 태우면 MCP 가 필요하다. `bulsaja_rate.py` 와 같은 선 — import 0줄이라 `.venv-web` pytest 가 바로 부른다"
  - "`test_미조회도_처리한것이다` 를 `test_미조회는_다시_시도하되_해소로_승격되지_않는다` 로 바꿨다 (플랜 이탈). 03-04 의 이탈 #1 이 옳다 — 미조회를 '처리완료'로 세면 429 로 빠진 상품이 **영원히 굳는다**"
  - "`groupId` 를 숫자로 강제하는 규약을 `ss_index_calls.py` 로 따로 뺐다. 빌더가 오류 응답을 직접 해석하지 않게 해서, '오류를 빈 결과로 읽는' 실수가 한 자리에만 있게 만들었다"
  - "그룹 단계 429 구멍은 **안 고쳤다.** 용팀장이 Phase 3 범위 밖으로 넘겼다(2026-09-21 \"그냥 두고 기록만\"). 대신 `.planning/todos/pending/` 에 전문과 고칠 단계 4개를 띄웠다"
  - "Task 3 을 **포괄 승인으로만** 기록했다. 플랜이 요구한 4번·5번 항목별 진술은 받지 못했고, 실행자가 대신 적지 않았다 — 그게 T-3-40(검증 문서를 추정으로 채움) 그 자체다"

patterns-established:
  - "실탄 전에 '시작 전 상태' 를 찍고 끝난 뒤 다시 찍어 차이를 증거 파일로 남긴다 (`03-07-evidence/` 7파일)"
  - "음성 대조를 계속 실제로 본다 — 차집합을 빼고 재개 테스트 3종이 FAIL 하는 것을 보고 되돌렸다"

requirements-completed: [JOIN-01, JOIN-04, STATE-04, ENG-06]

# Metrics
duration: 258min
completed: 2026-09-21
---

# Phase 3 Plan 07: 실탄 인덱스 구축 + 페이즈 검증 마감 Summary

**2시간 8분짜리 인덱스를 실서버에서 실제로 돌려 30,546행 / 30그룹 / 미조회 0 으로 완주했고,
중단 → 재개가 상품 3,688건을 건너뛰는 것을 실물로 봤고, 그 결과로 보드의 상품 연결이
0 → 51 (🔴 31 · 🟡 20 · ⚪ 0) 로 올라왔다. 그리고 실탄에서만 드러나는 버그 3건을 고쳤다 —
셋 다 자동 스위트가 green 인 채로 조용히 틀리고 있던 것들이다.**

크레딧 소모 **0** · 불사자 쓰기 **0** (전부 읽기 전용 조회).

## Performance

- **Duration:** 약 4시간 18분 (10:10 → 14:28). **그중 2시간 8분이 인덱스 완주 대기**
- **Tasks:** 4 / 4 (Task 2·3 은 사람 게이트)
- **Tests:** 327 → **340 passed** (신규 13) · full 11.35초 · `test_index.py` 40건
- **Files:** 신규 2 + 증거 7파일 · 수정 6
- **크레딧 소모:** **0**

---

## 🔴 이 플랜의 한 문장

**크레딧이 0인 작업은 틀려도 아무도 안 알려준다.**

자동 스위트 340건이 green 인 상태에서 아래 셋이 동시에 조용히 틀리고 있었다.
셋 다 "돈이 안 드는 읽기 작업" 이라 경보가 울릴 자리가 없었다.
**실탄을 안 돌렸으면 Phase 5 가 빈 목록 위에서 크레딧을 태웠다.**

---

## 실탄에서만 드러나 고친 것 3건

### ① `caffeinate -i` 가 **실제로 안 붙고 있었다** (ENG-06 / T-3-37) — `1afe86d`

- **증상:** 없다. 조용하다. 2시간짜리 잡이 맥북 절전에 무방비였다
- **왜 안 잡혔나:** `BulsajaArgv` 모델에 `prefix` 필드가 있고 **조립 테스트도 green** 이었다.
  그런데 `jobs._build_argv` 가 그 필드를 **안 넘겼다.**
  모델이 값을 들고 있는 것과 호출부가 그걸 쓰는 것은 다른 일이다
- **고침:** `_build_argv` 가 `prefix` 를 argv 앞에 붙인다. `test_argv.py` 에 회귀 추가
- **교훈(패턴에 올림):** 조립 테스트가 green 이어도 **호출부가 그 필드를 안 넘기면 아무 일도 안 일어난다**

### ② `groupId` 를 문자열로 보내 **30그룹이 9.2초에 "성공"했다** — `52872ec`

이게 셋 중 제일 위험했다.

- **증상:** 인덱스 잡이 exit 0 · `끝 — 그룹 30개` 로 **성공**하고 9.2초에 끝났다.
  `ss_index` 는 비어 있었다
- **무슨 일이었나:**
  1. `groupId` 를 문자열로 보냈다 → 서버가 `-32602 expected number` 를 돌려줬다
  2. 그 **오류 응답에는 `항목` 키가 없다**
  3. 빌더의 `r.get("항목") or []` 가 그걸 **"상품 0건"** 으로 읽었다
  4. 0건이니 아무것도 안 하고 "완료" 로 넘어갔다. 30번 반복
- **왜 경보가 없었나:** **크레딧이 0이라 태울 돈이 없다.** 실패를 알려줄 계량기가 없다.
  로그도 `[그룹 N/30] 상품 0건` 이라 "그 그룹이 비었나 보다" 로 읽힌다
- **고침:** `ss_index_calls.py` 신설 (import 0줄) — `groupId` 를 `int` 로 강제하고,
  **오류 응답 판별을 한 자리에** 뒀다. 빌더가 오류 응답을 직접 해석하지 않는다
- **회귀 4종:** `test_그룹아이디는_숫자로_보낸다` · `test_오류응답을_빈그룹으로_읽지_않는다` ·
  `test_진짜_빈_그룹은_정상이다`(오류와 진짜 0건을 갈라야 한다) ·
  `test_인덱스빌더가_오류응답을_직접_해석하지_않는다`

> `r.get(키) or []` 는 **오류를 0건으로 접는 관용구**다. 이 저장소 어디서든 다시 보이면 의심해라.

### ③ 스캔이 `mode=summary` 라 **사본·기작업이 전 행 0** 이었다 — `f7ef4a6`

- **증상:** 사본 조회 1단이 0건. 기작업도 0행. 보드의 사본·기작업 열이 통째로 비었다
- **원인:** `uploadBulsajaCode` 가 **summary 응답 14키에는 없고 full 응답 38키에는 있다**
- **고침:** 스캔은 `mode=full`, 인덱스는 `mode=summary`(그쪽은 필요한 키가 다 있다).
  `test_스캔은_full_로_읽고_인덱스는_summary_로_읽는다` 가 둘이 어긋나는 걸 고정한다
- 고친 뒤 실측: 사본 조회 1단 판매자상품코드 **57건** · 2단 묶음 키 **55건** ·
  기작업 **29행** · 사본 1건 이상 **42행**(최대 23)

---

## 03-04 Known Stubs 2건 — 둘 다 닫혔다

03-04 가 "실서버에서만 드러난다" 며 남긴 둘을 이번 실탄에서 실측했다.

| # | 가정 | 실측 결과 |
|---|---|---|
| 1 | `bulsaja_market_group_products` 의 전체 개수 필드 이름 (`전체개수·총상품수·총개수·전체` 4종 시도) | **`총상품수`** 가 맞다. 진행 로그에 `(서버 집계 3546건)` 이 실제 숫자로 찍혔고, 30그룹 전부 `행수 == 서버집계` 로 `완결` 판정됐다 |
| 2 | `bulsaja_product_find_by_code` 의 배치 인자 이름 = `codes` | **`codes` 가 맞다.** 단 ③ 때문에 입력 코드 자체가 안 모이고 있었다 — mode 를 고친 뒤 1단 57건 / 2단 55건으로 실제로 돌았다 |

`group_health` 의 "총상품수가 None 이면 행수만으로 완결 판단" 폴백은 **한 번도 안 탔다.**
지어내지 않은 쪽을 고른 게 맞았다.

---

## 무엇을 했나

### Task 1 — 재개 회귀 4종 + 재개 계산 분리 (`2112c68`)

`ss_index_resume.py` 신설. **import 0줄**(`bulsaja_rate.py` 와 같은 선)이라
`.venv-web` pytest 가 `sys.path` 삽입만으로 직접 부른다. 함수 2개:

```
처리완료(cx, 그룹아이디) -> set[str]     # SELECT product_id WHERE market_group_id = ?
남은대상(전체목록, 처리완료집합) -> list  # 순서 보존 차집합
```

`ss_index_build.그룹훑기` 가 그 둘을 쓴다 — **로직은 한 줄도 안 바꿨고 추출만 했다.**
왜 떼었는지를 주석에 남겼다: **3시간짜리 루프의 재개 규율은 루프를 돌려서 검증할 수 없다.**

| 테스트 | 무엇을 고정하나 |
|---|---|
| `test_재개는_처리한것을_건너뛴다` | 처리완료 3 + 전체 5 → 남은 대상이 정확히 2건이고, 그 2건이 처리완료에 **없는** 것들이다 |
| `test_재개는_페이지번호를_안_쓴다` | 전체 목록 **순서를 뒤섞어도** 남은 대상 집합이 같다 (Pitfall 6) |
| `test_미조회는_다시_시도하되_해소로_승격되지_않는다` | ⬅ **플랜 이탈** — 아래 |
| `test_재개모듈은_아무것도_import하지_않는다` | 이 모듈이 stdlib 밖으로 나가면 red (D-19 선) |

**음성 대조 (플랜 수용 기준):** `남은대상` 에서 차집합을 빼 전량 재처리로 고쳐 놓으니
**재개 테스트 3종이 전부 FAIL** 했다. 확인하고 되돌렸다.

### Task 2 — 첫 인덱스 구축 실행 (체크포인트 · `1afe86d`·`52872ec`·`7fe0295`·`f7ef4a6`·`ef701d4`)

용팀장 부재중 "돌려둬" 승인 아래 실행자가 라우트로 직접 눌렀다. 크레딧 0 · 쓰기 0.

#### 완주 실측

| 항목 | 실측 | 근거 |
|---|---|---|
| **소요시간** | **2시간 8분 (7,681초)** | 잡 `4180682f` 11:43:03 → 13:51:25 · status=done · exit 0 |
| **인덱스 행수** | **30,546행 / 30그룹** (smartstore 번호 있는 행 **26,551**) | `select count(*), sum(unresolved) from ss_index` → `30546\|0` |
| **미조회 수** | **0** | 같은 쿼리 |
| **429 발생** | **0회** | 진행 로그 전문에 `HTTP 429`·`Retry-After` **0건**. 최소간격 0.26초 = 초당 3.85회로 2시간 8분을 돌았다 |
| **재개가 건너뛴 것** | **1그룹 통째 + 그룹2 내 142건 = 상품 3,688건** | `01-재개-실증.txt` |
| **`[그룹 1/N]` 의 N** | **30** (플랜 본문의 36 은 옛 숫자) | 진행 로그. D-18 제외(13-2) + 라우트가 ③⑤ 행으로 먼저 좁힌다 → 41 → 31 → 30 |
| **보드 상품 연결** | **0 → 51** (/6062) | 인덱스 전후 배너 |
| **인덱스 불완전** | **84행(30그룹) → 0행(0그룹)** | 인덱스 전후 배너 |
| 🔴 **13-2 제외 그룹 검사 (T-3-38)** | **PASS** — **197행 전부 `미조회`** · **`번호없음` 0건** | `03-완주-실측.txt` |
| 상태 분포 (Phase 5 의 입력) | 🔴 **31** · 🟡 **20** · ⚪ **0** / 기작업 29 / 사본 1건 이상 42행(최대 23) / 잠금혼재 13 | CDP `V-BOARD-12` 가 독립 확인 |
| 미해소 청소 목록 | **19그룹 883행** | 보드 배너 |

**T-3-38 검사가 이 태스크의 최중요 항목이었다.** 제외 그룹이 `번호없음` 으로 뜨면
용팀장이 **멀쩡한 광고그룹을 지우러 간다** — 되돌릴 수 없다. 행에 실린 사유 원문:

> "제외 설정으로 이번 인덱스에서 안 훑은 마켓그룹이다 — 번호 '13-2' ·
> 광고그룹 '판매상품_13-2_업소용전문사무실'. **광고 쪽은 멀쩡하다**"

#### 재개 실서버 전수 증명 (JOIN-04)

Task 1 이 합성으로 고정한 것을 **실서버에서도** 봤다. 두 번.

**(a) 의도적 중단 → 재개** (`01-재개-실증.txt`)

자식 PID 2개만 `kill` 했다 — **서버는 안 건드렸다**(healthz 200 유지).
잡 `39728b1a` 가 `status=failed · exit_code=-15(SIGTERM)` 로 **정직하게** 찍혔다.
성공으로 접지 않았다. 재실행 로그:

```
[그룹 1/30] 목록 조회 중 (이미 적힌 것 3546건)
[그룹 1/30] 이번에 조회할 것 0건          ← 그룹 하나를 통째로 건너뛰었다
[그룹 2/30] 목록 조회 중 (이미 적힌 것 142건)
[그룹 2/30] 이번에 조회할 것 759건        ← 901 - 142 = 759, 산술 일치
```

**(b) 완주 후 전수 재실행 — 30그룹 전부 "조회할 것 0건" 으로 171.9초** (`05-재실행-429-그룹5-6.txt`)

첫 완주 **7,681초** → 재실행 **171.9초**. **44.7배.**
재개 기준이 페이지 번호가 아니라 **productId 집합**이라는 것이 전수로 증명됐다 (Pitfall 6).
_(이 재실행에서 그룹 5·6 이 429 로 죽었다 — §미해결 1번.)_

### Task 3 — 보드 최종 확인 (사람 게이트 · `808c60f`)

**용팀장 답 원문: "보드 확인했어, 승인" — 포괄 승인.**

기계 쪽 선행 확인은 전부 끝난 상태였다:
`render_check.py` 13검사 PASS · `board_cdp.sh` 38 PASS / 0 FAIL · 서버 healthz 200.

**항목별 진술은 받지 못했다.** 플랜은 아래 둘을 "되든 안 되든 한 줄씩" 요구했다:

| 항목 | 결과 |
|---|---|
| ① 🔴 필터가 "유입은 있는데 상세가 중국어 원본인 상품" 으로 읽히는가 | 포괄 승인에 포함 |
| ② 기작업 회색 · 전체선택 제외 · 손체크 포함 | 포괄 승인에 포함 (CDP V-BOARD-15/16/17 독립 확인) |
| ③ 사본 N건 1건 눈대조 | 포괄 승인에 포함 |
| ④ **사유만 보고 네이버 광고에서 그 그룹을 찾을 수 있었는가 (JOIN-02)** | ⚠️ **진술 미수령** |
| ⑤ **두 배너가 헷갈리는가** | ⚠️ **진술 미수령** |
| ⑥ ⚪ 행 1건 대조 | **대조 불가 — ⚪ 가 0건이다** |

**④⑤ 를 실행자가 대신 적지 않았다.** "사유만 보고 광고그룹을 찾을 수 있었다" 같은 문장을
만들어 넣으면 그게 거짓 기록이고, 이 페이즈가 막으라고 받은 T-3-40 그 자체다.
→ **§미해결 2번으로 남겼다.**

⑥ 의 ⚪ 0건은 **고장이 아니라 STATE-04 결론 그대로다** — 외부 경로는 `aiImageGenerated` 를
안 찍는다. 실데이터가 한 번 더 그걸 말했다: 해소 51행 중 ⚪ **0건**, `uploadDetailContents` 의
키가 `{imageTranslated, renderContent}` **둘뿐**이다.

증거: `03-07-evidence/07-Task3-보드확인-승인.txt`

### Task 4 — VALIDATION 마감 (`2908862`·`cd1db32`·`fcf691d`·`808c60f`)

`03-VALIDATION.md` 를 **실측으로만** 마감했다. 추정으로 채운 칸은 없다.

- Per-Task Verification Map **27행 전부 실행** — ✅ 23 · ⚠️ 4 · ❌ 0
- Wave 0 체크박스 8행 — `test -f` · `pytest --co` 로 실물 확인
- Manual-Only **3행 전부** · Sign-Off **6행 전부**
- `nyquist_compliant: true` · `wave_0_complete: true` · `Approval: 2026-09-21`
  — **근거는 Sign-Off 충족이지 "구멍이 없다" 가 아니다.** 프론트매터 주석에 그걸 적었다
- **pending(`⬜`) 표기 0건** — `grep -c` → **0**
- 미해결 **5건 유지** (아래)

⚠️ 4행의 정체는 **전부 "green 이지만 계약과 어긋남"** 이다(red 는 0건):
JOIN-01 기댓값 179 vs 92 · JOIN-04 파일 이탈 2건 · 그룹 단계 429.

**STATE-04 문서 확인:** `grep -c 'aiImageGenerated' webapp/state.py` → **6건**.
`webapp/state.py:31` 에 제목 그대로 "STATE-04 확정 — 외부 반영 경로는 `aiImageGenerated` 를
찍지 않는다" 가 있고, 33-36행에 양성대조 8건 / 음성대조 26건 근거가 붙어 있다.

**요구사항 10종 전부 등장 확인:**
```
$ grep -h '^requirements:' .planning/phases/03-join-detail-state/03-0*-PLAN.md \
  | tr -d '[]' | tr ',' '\n' | tr -d ' ' | sed 's/requirements://' | sort -u
ENG-08  JOIN-01  JOIN-02  JOIN-03  JOIN-04
STATE-01  STATE-02  STATE-03  STATE-04  STATE-05
```

---

## Deviations from Plan

### 1. [계획된 이탈] `test_미조회도_처리한것이다` → `test_미조회는_다시_시도하되_해소로_승격되지_않는다`

- **플랜 원문:** "`unresolved=1` 행도 '이미 훑었다'로 세어, 재개가 429 난 상품을 무한 재시도하지 않는다"
- **왜 뒤집었나:** **03-04 의 이탈 #1 이 옳다.** 미조회를 "처리완료"로 세면
  **429 로 빠진 상품이 영원히 굳는다** — 다시 돌려도 영원히 안 훑고, 화면엔 계속 미해소로 남는다.
  그 상품은 Phase 5 의 대상 목록에서 **영구히 사라진다.**
  플랜이 걱정한 "무한 재시도" 는 429 대기 21초 + 재시도 4회 상한이 이미 막고 있다
- **대신 고정한 두 성질:** ① 미조회 행은 **다음 재개에서 다시 시도된다**
  ② 재시도해도 여전히 미조회면 **해소로 승격되지 않는다**. 한 테스트에서 같이 단언한다
- **Files:** `webapp/tests/test_index.py` · **Commit:** `2112c68`

### 2. [Rule 1 - Bug] `caffeinate -i` 미부착 — §실탄 ①

`webapp/jobs.py` · `webapp/tests/test_argv.py` · `1afe86d`

### 3. [Rule 1 - Bug] `groupId` 문자열 → 30그룹 9.2초 "성공" — §실탄 ②

`ss_index_calls.py`(신규) · `ss_index_build.py` · `webapp/tests/test_index.py` · `52872ec`

### 4. [Rule 1 - Bug] 스캔의 `mode=summary` → 사본·기작업 전 행 0 — §실탄 ③

`bulsaja_scan.py` · `webapp/tests/test_index.py` · `f7ef4a6`

### 5. [범위 밖 · 용팀장 결정] 그룹 단계 429 를 안 고쳤다

발견은 이 플랜에서 했지만 고치지 않았다. **2026-09-21 용팀장 결정: "그냥 두고 기록만."**
Phase 3 범위 밖으로 넘겼다. 전문과 고칠 단계 4개를 `.planning/todos/pending/` 에 띄웠다 (`cd1db32`).

### 6. [Rule 3 - 블로킹] 증거 로그를 `.log` → `.txt` 로

`.gitignore` 의 `*.log` 가 증거 파일을 **조용히 삼켰다.** `git add` 가 아무 말 없이 무시한다.
확장자를 바꿔 커밋했다 (`ef701d4`). 증거를 남기라는 게 이 태스크의 수용 기준이라 블로킹이었다.

---

## 인증 게이트

없었다. 불사자 MCP 는 **이미 붙어 있는 계정**(부킹/용쌤 · 크레딧 933,172)으로 돌았고,
ENG-08 계정 가드가 매 잡마다 프로필을 확인했다. 토큰 재발급·재로그인 0회.

---

## 미해결 (`/gsd:verify-work` 가 읽는다 — 숨기지 않았다)

전문은 `03-VALIDATION.md` §미해결 목록. 여기선 **넘어가는 2건**만 다시 적는다.

### 🔴 1. 그룹 목록 조회 단계의 HTTP 429 가 "완주" 로 둔갑한다

2026-09-21 14:01 재실행에서 그룹 5·6 이
`⛔ 실패: 요청 실패(재시도 4회 초과): HTTP 429` 로 죽었다.
그런데 최종 요약은 **`끝 — 그룹 30개 · 누적 27775행 · 미조회 0`**.

숫자가 앞뒤를 증명한다: `27,775` + 죽은 두 그룹(760 + 2,011 = 2,771) = **30,546** = 실제 행수.
**죽은 그룹을 빼고 세면서 "완주" 라고 적었다.** `sum(unresolved)` 도 그대로 0 —
미조회 행이 한 줄도 안 생긴다. 화면에서 알아챌 방법이 없다.

- **위험:** 첫 구축 중에 같은 일이 나면 **그 그룹이 통째로 비고 화면은 "완주" 라고 말한다**
  → Phase 5 가 대상 목록을 통째로 놓친다. 이 페이즈가 못 박은
  **"429 를 성공으로 접지 마라"** 를 정확히 어기는 자리다
- **왜 테스트가 못 잡나:** `test_429는_미조회로_남는다` 는 **상품 단위** 429 만 본다
- **결정:** 2026-09-21 용팀장 — **"그냥 두고 기록만". Phase 3 범위 밖**
- **이번엔 손실 0:** 이미 채워진 그룹에서 났다. 데이터는 멀쩡하다(30,546 / 30 / 0)
- **당분간 운용 회피책: 인덱스 잡과 스캔 잡을 동시에 띄우지 마라**
  (레이트리밋이 프로세스마다 걸려 합산 상한을 넘긴다 — **가설, 미검증**.
  첫 완주는 혼자 돌았고 429 **0회**. 앞뒤가 맞는다)
- **할 일 전문:** `.planning/todos/pending/429-group-stage-silent-completion.md`
- **증거:** `03-07-evidence/05-재실행-429-그룹5-6.txt`

### 🟡 2. Task 3 의 ④·⑤ 항목별 진술 미수령 — JOIN-02 수동 검증이 절반만 닫혔다

용팀장의 답은 **"보드 확인했어, 승인"** 한 줄이었다. 플랜이 요구한
④(미해소 사유만 보고 네이버 광고에서 그 그룹을 실제로 찾을 수 있었는가)와
⑤(두 배너가 헷갈렸는가) 두 줄이 **없다.**

- **왜 중요한가:** **JOIN-02 의 쓸모는 기계로 증명되지 않는다.** CDP 는
  "청소 목록 19그룹이 그려지고 셀 값이 원본 `adGroups` 와 정확히 일치한다"(V-BOARD-20/21)
  까지만 본다. **그 문구를 들고 광고 화면에 가서 실제로 찾아본 사람은 아직 없다**
- ⑥(⚪ 행 대조)은 ⚪ **0건**이라 대조 불가 — 고장이 아니라 STATE-04 결론 그대로다
- **채우는 법:** 보드 청소 목록에서 아무 그룹이나 하나 골라, 거기 적힌 원문·번호만 들고
  네이버 광고에서 찾아본 뒤 한 줄씩 적으면 이 항목이 닫힌다
- **증거:** `03-07-evidence/07-Task3-보드확인-승인.txt`

_(나머지 3건 — 레이트리밋 프로세스별 · JOIN-01 기댓값 179 vs 92 · JOIN-04 파일 이탈 —
은 `03-VALIDATION.md` §미해결 3·4·5번에 그대로 있다.)_

---

## Known Stubs

없다. 이 플랜은 코드를 거의 안 썼고, 쓴 두 모듈(`ss_index_resume.py`·`ss_index_calls.py`)은
둘 다 회귀 테스트가 붙어 있다. 03-04 가 남긴 Known Stubs 2건은 **이번 실탄에서 닫혔다**(위 표).

## Threat Flags

없다. 이 플랜이 만진 표면은 전부 `<threat_model>` 에 등록돼 있다. 새 네트워크 엔드포인트·
인증 경로·신뢰 경계 **0건**이다.

| 위협 | 어디서 막혔나 |
|---|---|
| T-3-36 재개가 상품을 빠뜨리거나 중복 처리 | `ss_index_resume` + 회귀 4종(순서 뒤섞기 포함) + **실서버 전수 재실행 171.9초** |
| T-3-37 맥북 절전으로 3시간 잡이 끊김 | `caffeinate -i` 를 **실제로 붙였다**(안 붙고 있었다 — §실탄 ①) + `test_argv.py` 회귀 |
| T-3-38 제외 그룹이 "번호없음" → 멀쩡한 광고그룹 삭제 | **실서버 PASS** — 13-2 197행 전부 `미조회` · `번호없음` 0건 |
| T-3-39 완주 결과가 기록 안 돼 다음 회차에 재구축 | 소요시간·행수·미조회를 SUMMARY·VALIDATION·증거 7파일에 남겼다 |
| T-3-40 검증 문서를 추정으로 채움 | pending 표기 0건 + **포괄 승인을 항목별 승인으로 바꿔 적지 않았다** |

---

## 검증

```
.venv-web/bin/pytest webapp/tests -q                → 340 passed, 2 warnings in 11.35s (327 → +13)
.venv-web/bin/pytest webapp/tests/test_index.py     → 40 passed in 0.07s
bash webapp/tests/no_commit_guard.sh                → exit 0 (webapp/tests 에 --commit 리터럴 0건)
.venv-web/bin/python3 webapp/tests/fixtures/render_check.py → 렌더 확인 전량 PASS (13검사)
bash webapp/tests/board_cdp.sh                      → 38 PASS / 0 FAIL (8766 임시 서버)
curl .../healthz                                    → 200 (8765 운영 서버 · 재시작 0회)

grep -c 'aiImageGenerated' webapp/state.py          → 6
grep -c '⬜' .../03-VALIDATION.md                    → 0
요구사항 10종 grep                                    → ENG-08 · JOIN-01~04 · STATE-01~05 전부 등장

sqlite3 webapp.db 'select count(*), sum(unresolved) from ss_index' → 30546|0
```

**음성 대조:** `남은대상` 에서 차집합을 빼 전량 재처리로 고치니 재개 테스트 **3종 전부 FAIL**.
확인하고 되돌렸다. (앞 플랜들의 규율 승계 — "FAIL 을 본 적 없는 검증은 검증이 아니다")

---

## 다음 플랜에 넘기는 것

### Phase 5 (상세페이지 — 크레딧을 태우는 곳)

**대상 목록은 실물로 존재한다.** 이 플랜이 확인하려던 게 그거였고, 답은 **있다** 이다.

| 항목 | 값 |
|---|---|
| **🔴 중국어 원본** | **31건** ← 이게 Phase 5 의 1차 대상 |
| 🟡 단순번역만 | 20건 |
| ⚪ AI 가공완료 | **0건** (STATE-04 결론 그대로 — 외부 경로는 안 찍는다) |
| 기작업(`구매_가공완료`) 제외 | 29행 |
| 사본 1건 이상 | 42행 (최대 **23건** 팬아웃) |
| 인덱스 커버리지 | 30,546행 / 30그룹 / **미조회 0** (smartstore 번호 보유 26,551) |

- **대상 선정은 서버(`join.selectable`)를 정본으로 써라.** 화면의 기본 선택만 믿지 않는다
  (기작업 제외가 화면·서버 두 곳에 있다 — 03-06 이 넘긴 규율)
- **사본 42행이 크레딧 견적의 함정이다.** 한 상품을 고르면 사본 23개에 번진다.
  "이 버튼이 N건에 적용됩니다" 를 견적에 반드시 반영해라 (JOIN-03)
- **⚪ 0건은 "아직 아무것도 안 했다" 가 아니다.** 기작업 29행이 실제 가공분이고,
  불사자가 그걸 `aiImageGenerated` 로 안 찍어서 태그로만 보인다.
  **중복방지는 태그 쪽을 봐야 한다** — `aiImageGenerated` 만 보면 29건을 다시 태운다
- STATE-04 **확정 실험**(외부 반영 1건 실측)은 예정대로 Phase 5 다

### 인덱스를 다시 돌릴 사람에게

- **재실행은 싸다.** 전수 재실행이 **171.9초**다(첫 완주 7,681초의 1/45).
  물갈이 후 갱신을 겁내지 마라
- **단 §미해결 1번을 먼저 읽어라.** 그룹 단계 429 가 조용히 그룹을 통째로 날린다.
  재실행 후 `select count(*) from ss_index` 가 **줄었으면** 그게 신호다
- **인덱스 잡과 스캔 잡을 동시에 띄우지 마라** (레이트리밋이 프로세스마다 걸린다)
- 모수는 **서버가 계산한 30그룹이 정본**이다. 플랜·CONTEXT 의 36그룹/47,105건은 옛 숫자다

### 미해소 청소 목록을 쓸 사람에게 (Phase 4 이후)

- **19그룹 883행**이 광고 청소 후보로 그려져 있다. 13-2(제외 그룹)는 **거기 없다** — 설계대로다
- 다만 **§미해결 2번이 열려 있다.** 그 목록을 들고 광고 화면에 가서 실제로 찾아본 사람이
  아직 없다. **처음 쓰는 날 한 그룹만 대조해 보고 그 결과를 VALIDATION 에 적어라.**
  안 맞으면 사유 문구를 고쳐야 하고, 그건 되돌릴 수 없는 삭제로 이어지는 자리다

---

## Self-Check: PASSED

**생성 파일 존재 확인** — FOUND
- `.claude/skills/bulsaja-detail-page/scripts/ss_index_resume.py`
- `.claude/skills/bulsaja-detail-page/scripts/ss_index_calls.py`
- `.planning/phases/03-join-detail-state/03-07-evidence/` (7파일)
- `.planning/todos/pending/429-group-stage-silent-completion.md`
- `.planning/phases/03-join-detail-state/03-07-SUMMARY.md`

**커밋 존재 확인** — 10/10 FOUND (⚠️ 리베이스로 히스토리가 다시 쓰여 SHA 는 실측값이다)

| 커밋 | Task | 내용 |
|---|---|---|
| `2112c68` | 1 | 재개 회귀 4종 + `ss_index_resume` 분리 |
| `1afe86d` | 2 | `caffeinate -i` 실제 부착 (ENG-06 / T-3-37) |
| `52872ec` | 2 | `groupId` 숫자 강제 + 오류응답을 0건으로 안 읽는다 |
| `7fe0295` | 2 | 재개 실증 기록 |
| `f7ef4a6` | 2 | 스캔 `mode=full` |
| `ef701d4` | 2 | 증거 로그 `.log` → `.txt` |
| `2908862` | 4 | VALIDATION 실측 마감 (Task 3 의존 행만 남김) |
| `cd1db32` | 4 | 429 구멍을 todo 로 |
| `fcf691d` | 4 | 인수인계 노트 갱신 |
| `808c60f` | 3·4 | **Task 3 포괄 승인 기록 + VALIDATION 최종 마감 (pending 0건)** |

**삭제된 추적 파일 0건** — `git diff --diff-filter=D --name-only b4cea81..HEAD` 비어 있음

**`webapp/` 아래 `eroomlib`/`requests` import 0건 유지** — D-19 OK

**서버 무중단** — 8765 운영 서버를 죽이거나 재시작하지 않았다 (healthz 200 유지).
CDP 검증은 8766 에 임시 서버를 따로 띄워 돌린 뒤 **그 임시 서버만** 껐다.

**크레딧 0 · 불사자 쓰기 0** — 전부 읽기 전용 조회. 잔액 933,172 불변.

---
*Phase: 03-join-detail-state*
*Completed: 2026-09-21*
