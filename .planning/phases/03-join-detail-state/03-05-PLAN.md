---
phase: 03-join-detail-state
plan: 05
type: execute
wave: 4
depends_on: ["03-04"]
files_modified:
  - webapp/tests/fixtures/bulsaja_groups_real.json
  - webapp/tests/test_join.py
  - webapp/routes/jobs.py
  - webapp/routes/board.py
  - webapp/templates/board.html
  - webapp/tests/test_routes_jobs.py
autonomous: true
requirements: [ENG-08, JOIN-01, JOIN-04]

must_haves:
  truths:
    - "실회차 ③⑤ 행의 번호 해상률이 **실측 숫자로 고정**되고, 그 회귀가 스위트에 남는다 (JOIN-01)"
    - "사용자가 터미널 없이 브라우저에서 계정 확인 · 조인 스캔 · 인덱스 구축을 띄운다"
    - "화면 상단에 **현재 붙어 있는 불사자 계정과 크레딧이 항상 보인다**. 기대 계정과 다르면 경고색이다 (ENG-08 / 성공기준 6)"
    - "기대 계정이 아니면 스캔·인덱스 버튼이 409 로 거부되고, 그 사유가 화면에 뜬다"
    - "인덱스 구축 대상 그룹은 서버가 계산한다 — 화면이 그룹 목록을 만들지 않는다 (D-11 정신)"
    - "세 라우트 모두 POST 다 — GET 으로 만들면 Origin 방어가 통째로 무력화된다 (T-1-01b)"
  artifacts:
    - path: "webapp/tests/fixtures/bulsaja_groups_real.json"
      provides: "실물 번호 분포를 보존한 불사자 마켓그룹 목록 (이름은 익명) — 해상률 회귀의 짝"
    - path: "webapp/routes/jobs.py"
      provides: "POST /jobs/bulsaja/profile · /scan · /index — 얇은 핸들러 + AccountMismatchError→409"
      contains: "bulsaja/scan"
    - path: "webapp/templates/board.html"
      provides: "계정·크레딧 배너 + 불사자 버튼 3개 (htmx, JS 0줄)"
      contains: "bulsaja/scan"
  key_links:
    - from: "webapp/routes/jobs.py"
      to: "webapp/join.py index_targets"
      via: "인덱스 대상 groupId 를 서버가 계산해 create_job(only_ads=...) 로 대상 파일에 떨군다"
      pattern: "index_targets"
    - from: "webapp/routes/board.py"
      to: "webapp/bulsaja_index.py profile"
      via: "화면 상단 계정 표시. MCP 를 부르지 않고 자식이 써 둔 파일을 읽는다 (D-19)"
      pattern: "bulsaja_index.profile"
    - from: "webapp/templates/board.html"
      to: "POST /jobs/bulsaja/*"
      via: "htmx hx-post + hx-target=#job-panel (prep 버튼과 같은 배선)"
      pattern: "hx-post"
---

## Phase Goal

> ⚠ ROADMAP `**Goal:**` 이 user-story 형식이 아니다. 정규화는 `/gsd mvp-phase 03`. 아래는 ROADMAP 원문이다.

광고 판정 행을 불사자 상품에 실제로 잇고 상세 상태를 3단계로 판정해, "유입은 있는데 상세가 중국어 원본인 상품" 목록을 화면에서 정확히 뽑아낸다.

<objective>
**첫 수직 슬라이스가 여기서 닫힌다 — 버튼을 누르면 불사자를 훑고 그 결과가 회차에 남는다.**

세 단계다:
1. CLI 를 **실물로 한 번 돌려** 해상률 숫자를 확정하고 회귀 픽스처로 박는다 (읽기 전용, 크레딧 0)
2. 그 CLI 를 부르는 라우트 3개 — 얇게, POST 로, 예외는 상태코드로만 번역
3. 버튼과 계정 배너 — htmx 로 붙여 JS 를 0줄 늘린다

**이 플랜이 끝나면 용팀장이 터미널 없이 조인 스캔을 돌릴 수 있다.**
표에 상태 열이 붙는 건 다음 플랜(03-06)이지만, 잡 패널의 진행 로그와 산출물은 여기서 이미 돈다.

Purpose: 화면이 없으면 3시간 32분짜리 인덱스 구축을 사람이 터미널에서 관리하게 된다 — 그건 이 프로젝트가
없애려는 상태 그 자체다.
Output: 실측 해상률 회귀 + 라우트 3개 + 계정 배너 + 버튼 3개
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-join-detail-state/03-CONTEXT.md
@.planning/phases/03-join-detail-state/03-RESEARCH.md
@.planning/phases/03-join-detail-state/03-PATTERNS.md
@.planning/phases/03-join-detail-state/03-VALIDATION.md
@.planning/phases/03-join-detail-state/03-02-SUMMARY.md
@.planning/phases/03-join-detail-state/03-03-SUMMARY.md
@.planning/phases/03-join-detail-state/03-04-SUMMARY.md

<interfaces>
라우트 3개 (전부 POST · 토큰 필요 · `def` — `async def` 가 아니다):

  POST /jobs/bulsaja/profile   body: (없음)            → kind="bulsaja_profile"
  POST /jobs/bulsaja/scan      body: {run_dir}         → kind="bulsaja_scan",  대상 = ③⑤ 행 키
  POST /jobs/bulsaja/index     body: {run_dir}         → kind="bulsaja_index", 대상 = index_targets()

응답은 기존 `_응답(request, jobs.job_status(job_id))` 그대로 — 작업 패널 조각을 돌려준다.

예외 번역 (기존 `_작업만들기` 표에 한 줄을 **먼저** 끼운다):
  jobs.AccountMismatchError → 409   ← `except jobs.BusyError` 와 `except ValueError` **사이**가 아니라
                                       **`ValueError` 보다 먼저** 와야 한다. 상속 때문에 순서가 곧 동작이다
  jobs.BusyError            → 409
  ValueError                → 400
  KeyError                  → 500   (설정이 비었다 — 가드가 없는 채로 도는 것보다 낫다)
  RuntimeError              → 500

보드 컨텍스트에 더할 키 (routes/board.py):
  "bulsaja": {"닉네임": str|None, "크레딧": str|None, "확인시각": str|None,
              "일치": bool, "사유": str|None}     # bulsaja_index.profile()/profile_ok() 투영
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 첫 실스캔 1회 — 해상률 실측 확정 + 회귀 픽스처 (JOIN-01)</name>
  <files>webapp/tests/fixtures/bulsaja_groups_real.json, webapp/tests/test_join.py</files>
  <read_first>
    - `.claude/skills/bulsaja-detail-page/scripts/bulsaja_scan.py` (03-04 산출물) — 필요한 플래그 전부
    - `webapp/tests/fixtures/anonymize_join.py` · `result_join_real.json` (03-01 산출물) — **같은 익명화 규약을 그대로 쓴다**
    - `webapp/join.py` (03-02) — `attach`·`resolution`·`group_index`·`market_number`
    - `03-CONTEXT.md` §광고그룹 정리 완료 — 해상률 179/194 의 유래와 "남은 15행은 삭제된 광고그룹의 소재다"
    - `03-RESEARCH.md` 최상단 🔴 갱신 공지 — **92 가 아니라 179 다**
    - `03-VALIDATION.md` — `test_실회차_해상률` 행(기댓값 179/194)
    - `03-01-SUMMARY.md` — 실회차 ③⑤ 행수 실측값(분모)
  </read_first>
  <action>
    **(a) 실스캔 1회.** 읽기 전용이고 크레딧 0 이다. 순서:
      1. `.venv-web/bin/python3 -c "from webapp import jobs; jobs.init_db()"` 로 스키마를 만든다(`ss_index` 포함).
      2. 최신 회차의 `result.json` 에서 ③·⑤ 행을 골라 `"<계정alias>|<mallProductId>"` 목록을 만들고 임시 파일에 쓴다. `board.fold_products` 대신 `result.json` 을 직접 훑어도 되지만, **규칙 기호로 고르고 중복을 없애라**(같은 상품이 ③과 ⑤에 동시에 있을 수 있다).
      3. `.venv/bin/python3 .claude/skills/bulsaja-detail-page/scripts/bulsaja_scan.py --run-dir <회차> --targets <임시> --out <임시>/join.json --db webapp.db --profile-out webapp-profile.json --expect-nick "$(.venv-web/bin/python3 -c 'from webapp import settings;print(settings.cfg("expected_bulsaja_nick", required=True))')" --min-interval 0.26 --retry-after 21 --batch-size 50`
         ⚠️ 이 값들을 손으로 적지 말고 `settings.cfg()` 에서 꺼내 쓴다 — 설정이 정본임을 실행으로도 증명한다.
         인덱스가 비어 있으므로 상품층은 전부 미조회로 나온다. **그게 정상이다** — 이 태스크가 확정하려는 건 **번호층 해상률**이다.
      4. 소요시간과 요약 줄을 SUMMARY 에 적는다.
    **실행 중 429 가 나면 멈추고 원인을 적어라.** `--min-interval` 을 임의로 늘려 통과시키지 마라 — 설정값이 틀렸다는 발견이다.

    **(b) 그룹 목록 픽스처.** `join.json` 의 `마켓그룹` 을 `webapp/tests/fixtures/bulsaja_groups_real.json` 으로 익명화해 저장한다. **번호(`NN-N`)는 그대로 보존하고 나머지 이름만 `zzfake{n}` 으로** 바꾼다 — 해상률은 번호로만 결정되므로 익명화해도 재현된다. `groupId` 는 연번 가짜값으로 바꿔라(그룹 ID 는 화면에도 코드에도 남을 값이 아니다). 익명화 코드는 `anonymize_join.py` 에 함수로 더해 재생성 가능하게 둔다.

    **(c) 회귀 테스트.** `webapp/tests/test_join.py` 에 `test_실회차_해상률` 을 더한다:
      - `result_join_real.json` + `bulsaja_groups_real.json` 으로 `attach` → `resolution` 을 돌린다
      - `전체`·`번호해소`·`번호미해소` 를 **실측값으로 단언**한다
      - **VALIDATION 의 기댓값은 179/194 다.** 실행 결과가 그와 다르면 **픽스처를 맞추지 말고** 실측값으로 단언하되, 테스트 docstring 에 `VALIDATION 기댓값 179/194 · 실측 <값> · 차이 사유(회차가 바뀌었다 / 광고그룹이 또 정리됐다 등)` 를 적고 SUMMARY 에도 올려라. 숫자를 맞추려고 픽스처를 고치는 순간 이 테스트는 아무것도 지키지 않는다
      - 단언 옆에 틀린 값도 적어라: `# 92 면 광고그룹 정리 전 데이터를 쓰고 있다`
      - `광고청소` 버킷의 그룹 수와 `시스템` 버킷을 **따로** 단언해라. 인덱스가 비었으므로 `시스템` 이 전량이어야 하고 `광고청소` 는 미해소 행 수와 같아야 한다
  </action>
  <verify>
    <automated>.venv-web/bin/pytest "webapp/tests/test_join.py::test_실회차_해상률" -x -q && .venv-web/bin/pytest webapp/tests/test_join.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv-web/bin/pytest "webapp/tests/test_join.py::test_실회차_해상률" -x` 가 exit 0
    - `webapp/tests/fixtures/bulsaja_groups_real.json` 이 존재하고 실물 이름이 0건이다:
      `.venv-web/bin/python3 -c "
import json;d=json.load(open('webapp/tests/fixtures/bulsaja_groups_real.json'))
names=[g['그룹명'] for g in d['그룹']]
assert all('zzfake' in n for n in names), names[:5]
import re;nums=[n for n in names if re.search(r'\d{1,2}-\d{1,2}', n)]
print('그룹', len(names), '· 번호 있는 그룹', len(nums))"`
      → 번호 있는 그룹이 40개 이상 (실측 54 근처)
    - 스캔 실행 로그에 `HTTP 429` 가 0건 (SUMMARY 에 붙여라)
    - `webapp-profile.json` 이 생겼고 닉네임이 `settings.cfg("expected_bulsaja_nick")` 과 같다:
      `.venv-web/bin/python3 -c "
from webapp import bulsaja_index, settings
ok, why = bulsaja_index.profile_ok(settings.cfg('expected_bulsaja_nick', required=True), int(settings.cfg('profile_max_age_min', 120)))
assert ok, why; print('계정 확인 OK')"`
  </acceptance_criteria>
  <done>해상률이 추정이 아니라 실측으로 박혔고, 그 숫자가 회귀로 남았다. 계정 확인 산출물이 실물로 존재한다.</done>
</task>

<task type="auto">
  <name>Task 2: 라우트 3종 — 불사자 잡을 브라우저에서 띄운다 (ENG-08 거부 포함)</name>
  <files>webapp/routes/jobs.py, webapp/tests/test_routes_jobs.py</files>
  <read_first>
    - `webapp/routes/jobs.py` 1-45행(라우트 목록 docstring · **모든 작업 생성은 POST** 이유 · 핸들러는 얇다 · `def` vs `async def`), 60-100행(`공개필드` 화이트리스트 · `JobReq` · `요청_풀기` 가 JSON 과 폼을 둘 다 받는 방식), 187-199행(`_판정읽기`), 344-380행(**`_작업만들기` 의 예외→상태코드 표. 여기에 한 줄을 먼저 끼운다**), 574-596행(GET 핸들러를 파일 맨 아래 모으는 규약)
    - `webapp/jobs.py` — `create_job` 시그니처와 `AccountMismatchError`(03-03), `latest_done`
    - `webapp/join.py` — `index_targets`, `ad_groups_by_product`(03-02)
    - `webapp/security.py` 77-111행 — `guard` 미들웨어가 쓰기 요청에 토큰을 요구하는 자리
    - `webapp/tests/test_routes_jobs.py` 전체 — 라우트 테스트가 `TestClient` 를 쓰는 모양과 토큰 헤더 붙이는 법
    - `03-RESEARCH.md` §Pattern 2 · §ENG-08 · §Security Domain(CSRF 로 4시간 잡 기동)
  </read_first>
  <action>
    `webapp/routes/jobs.py` 의 **라우트 목록 docstring 에 3줄을 먼저 더한다**(그 표가 이 파일의 목차다):
    ```
        POST /jobs/bulsaja/profile  불사자 계정 확인 (0.14초, 읽기)      [쓰기 — 토큰 필요]
        POST /jobs/bulsaja/scan     회차 조인 스캔 (수 분, 읽기)          [쓰기 — 토큰 필요]
        POST /jobs/bulsaja/index    마켓그룹 인덱스 구축 (수 시간, 읽기)  [쓰기 — 토큰 필요]
    ```
    "읽기(불사자에 아무것도 안 쓴다)인데 왜 POST 이고 토큰이 필요한가" 를 한 줄로 적어라: 교차 사이트 단순 GET 에는 `Origin` 이 없어 `security.guard` 가 아무것도 못 거른다. 남의 페이지가 내 맥북의 3시간짜리 잡을 띄우게 둘 이유가 없다(T-1-01b).

    `_작업만들기` 의 예외 번역에 **`except jobs.AccountMismatchError` 를 `except ValueError` 보다 먼저** 넣는다. 상속 관계라 순서가 곧 동작이다 — 뒤에 두면 영원히 400 으로 새고 "때가 아니다(409)" 와 "요청이 틀렸다(400)" 의 구분이 사라진다. 메시지는 `bulsaja_index.profile_ok` 가 준 사유를 그대로 쓰되, **기대 닉네임은 싣지 않는다**. `except KeyError` 도 더해 500 으로 번역하고 "설정 `webapp.expected_bulsaja_nick` 이 비었다" 를 말한다.

    세 라우트를 POST 핸들러들 사이(기존 `post_revert_round` 뒤, GET 핸들러들 **앞**)에 둔다. GET 핸들러를 파일 맨 아래 모으는 규약을 깨지 마라 — `security_curl.sh` 의 V-SAFE-01d 가 이 파일의 GET 핸들러 본문을 훑는다.

    - `post_bulsaja_profile` — `jobs.create_job("bulsaja_profile")`. `run_dir` 도 대상도 없다. 계정 가드도 안 탄다(그게 이 잡의 목적이다).
    - `post_bulsaja_scan(req: JobReq)` — `req.run_dir` 이 필수다. `_판정읽기(run_dir)` 로 `result.json` 을 읽고 **③·⑤ 행만** 골라 `"<alias>|<mallProductId>"` 키 목록을 만든다(중복 제거). 빈 목록이면 400 으로 "이 회차에 ③⑤ 행이 없다" 를 말한다 — **빈 대상으로 잡을 만들지 마라**(`jobs.create_job` 이 빈 목록도 파일로 떨구지만, 그 스캔은 0건을 훑고 빈 산출물을 남겨 화면이 "인덱스가 비었나?" 로 오독된다). 규칙 기호는 `board.RULE_ORDER` 에서 가져오지 말고 **호출부에 상수로 두되 주석으로 근거를 남겨라**(③원인분석·⑤효자확정이 이 페이즈의 작업 대상이라는 것은 ROADMAP 의 범위 정의다).
    - `post_bulsaja_index(req: JobReq)` — `req.run_dir` 필수. `_판정읽기` + 최근 스캔 산출물(`jobs.latest_done("bulsaja_scan", run_dir)` → `result_path` 읽기)로 `board.fold_products` → `join.attach` → `join.index_targets(rows, join_doc, excluded=settings.cfg("index_excluded_groups", []))` 를 계산해 그 **groupId 목록**을 `create_job(only_ads=...)` 로 넘긴다.
      - 스캔 산출물이 없으면 **409** 로 "먼저 조인 스캔을 돌려라 — 마켓그룹 목록이 있어야 대상을 계산한다" 를 말한다. 그룹 목록 없이 인덱스를 돌리면 전 그룹이다.
      - 계산 결과가 빈 목록이면 **400** 으로 거부한다. 빈 목록이 "전량" 으로 해석되는 경로를 화면에서도 막는다(`jobs._build_argv` 가 2층 방어, CLI 의 `exit 2` 가 3층이다).
      - **화면이 그룹 목록을 만들지 않는다.** 클라이언트가 groupId 를 보내는 인자를 두지 마라 (D-11 정신 — 대상은 서버가 정한다).
      - `prefix=["/usr/bin/caffeinate", "-i"]` 를 태울 자리는 `jobs._build_argv` 다. 라우트는 kind 만 고른다.

    `webapp/tests/test_routes_jobs.py` 에 더한다:
    - `test_불사자_라우트는_토큰없이_안된다` — 세 경로 전부 토큰 없이 403 (기존 보안 테스트 관용구 재사용)
    - `test_계정불일치면_409` — 프로필을 불일치로 깔아 두고 POST → 409 이고 본문에 **기대 닉네임이 없다**
    - `test_스캔은_대상이_없으면_400`
    - `test_인덱스는_스캔없이_409`
    - `test_인덱스_대상은_서버가_만든다` — 요청 본문에 groupId 를 넣어도 무시된다(그런 필드가 아예 없다). `JobReq` 모델에 필드가 없음을 단언해라
    - `test_불사자_라우트는_GET이_아니다` — `client.get(경로)` 가 405
  </action>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_routes_jobs.py -x -q && .venv-web/bin/pytest webapp/tests -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv-web/bin/pytest webapp/tests/test_routes_jobs.py -x -q` 가 exit 0
    - `grep -c 'bulsaja/scan' webapp/routes/jobs.py` 가 2 이상 (docstring 목차 + 라우트)
    - `except` 순서가 맞다: `grep -n 'except jobs.AccountMismatchError' webapp/routes/jobs.py` 의 줄번호가 `grep -n 'except ValueError' webapp/routes/jobs.py` 의 첫 줄번호보다 **작다**
    - GET 핸들러 규약 유지: `bash webapp/tests/security_curl.sh` 를 돌릴 수 없는 환경이면, 최소한 `grep -n '@router.get' webapp/routes/jobs.py | head -1` 의 줄번호가 `grep -n '@router.post' webapp/routes/jobs.py | tail -1` 보다 **크다**
    - 전량 회귀 `.venv-web/bin/pytest webapp/tests -q` exit 0
  </acceptance_criteria>
  <done>브라우저에서 불사자 잡 3종이 뜬다. 틀린 계정·대상 없음·스캔 없음이 전부 상태코드로 갈린다.</done>
</task>

<task type="auto">
  <name>Task 3: 계정 배너 + 버튼 3개 (htmx — JS 0줄)</name>
  <files>webapp/routes/board.py, webapp/templates/board.html</files>
  <read_first>
    - `webapp/templates/board.html` 1-30행(자동 이스케이프 경고 · `.stale` CSS), 61-97행(hx-headers 로 토큰을 붙이는 한 줄 · **신선도 배너** — 새 배너가 베낄 모양), 99-150행(작업 버튼의 `hx-post`/`hx-target="#job-panel"`/`hx-swap="outerHTML"` 배선과 "hx-get 으로 바꾸지 마라" 경고), 189-192행(`_job_panel.html` include 자리)
    - `webapp/routes/board.py` 100-130행 — `ctx` 딕셔너리와 화이트리스트 투영 주석, `_load_result` 의 "폴백하지 않는다"
    - `webapp/bulsaja_index.py` — `profile()` / `profile_ok()` (03-03)
    - `03-RESEARCH.md` §ENG-08 — "표시는 보드 상단 상시. 크레딧도 같이 띄우면 Phase 5 의 견적 보고가 붙을 자리가 생긴다" · `표시규칙` 을 화면에 렌더하지 마라
    - `03-PATTERNS.md` §`webapp/templates/board.html` — `.stale` 재사용 · 미해소 배너와 "인덱스 불완전" 배너는 **생김새가 달라야 한다** · `|safe` 금지
  </read_first>
  <action>
    `webapp/routes/board.py` 의 `ctx` 에 `"bulsaja"` 키를 더한다. `bulsaja_index.profile()` 와 `profile_ok(settings.cfg("expected_bulsaja_nick", required=True), ...)` 를 불러 **필요한 값만 뽑아** 싣는다(닉네임·크레딧·확인시각·일치여부·사유). 응답 dict 를 통째로 넘기지 마라 — `routes/board.py:131-133` 의 화이트리스트 투영 관례이고, `표시규칙` 이 화면으로 새는 걸 구조적으로 막는 자리이기도 하다.
    `required=True` 가 `KeyError` 를 던질 수 있는데, 보드는 읽기 라우트라 500 이 나면 화면이 통째로 안 뜬다. **여기서만 예외를 잡아** `{"일치": False, "사유": "설정 webapp.expected_bulsaja_nick 이 비었다"}` 로 배너에 띄워라 — 설정이 빈 것을 조용히 통과시키는 게 아니라 **화면이 그 사실을 말하게** 한다(쓰기 경로인 `create_job` 은 계속 터뜨린다).
    핸들러는 `def` 그대로 둔다. 파일 읽기 하나가 는 것뿐이다 — `async def` 로 바꾸면 SSE 가 같이 끊긴다(`test_jobs.py:714` 가 기계로 집행).

    `webapp/templates/board.html`:
    - **계정 배너** — 신선도 배너 바로 위(또는 아래)에 `<p id="bulsaja-account">` 를 둔다. `{{ bulsaja.닉네임 }}` · 크레딧 · `확인시각` 을 보여주고, `bulsaja.일치` 가 거짓이면 `class="stale"` 을 붙이고 `bulsaja.사유` 를 같이 띄운다. 프로필이 아예 없으면 "계정 확인을 먼저 눌러라" 를 띄운다. **`|safe` 를 쓰지 마라** — 닉네임·크레딧 문자열은 외부(MCP) 문자열이다.
    - **버튼 3개** — 기존 작업 버튼과 같은 `hx-post`/`hx-target="#job-panel"`/`hx-swap="outerHTML"`/`hx-include="#job-run-dir"` 배선으로 붙인다. **JS 를 새로 쓰지 마라.**
      · `계정 확인` → `/jobs/bulsaja/profile` (회차 불필요)
      · `조인 스캔` → `/jobs/bulsaja/scan`
      · `인덱스 구축` → `/jobs/bulsaja/index`
    - **인덱스 버튼 옆에 비용 경고를 붙여라.** 되돌릴 수 없는 버튼은 아니지만 **수 시간**을 먹는다: "수십 분~수 시간 걸린다 · 크레딧 0 · 중간에 멈춰도 이어서 돈다". 숫자(그룹 수·소요시간)를 템플릿에 박지 마라 — 회차마다 다르고, 박으면 설정을 고쳐도 화면이 안 따라온다.
    - 세 버튼은 `{% if run_dir %}` 안에 두되 `계정 확인` 만은 **회차가 없어도 보이게** 한다(회차가 하나도 없는 상태에서 계정부터 확인할 수 있어야 한다).
    - 기존 `#job-error` 자리를 그대로 쓴다 — 409/400 응답이 거기 뜬다.
  </action>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_board.py webapp/tests/test_routes_jobs.py -x -q && .venv-web/bin/python3 -c "
from fastapi.testclient import TestClient
from webapp import settings, security
from webapp.main import app
c = TestClient(app, base_url=f'http://127.0.0.1:{settings.PORT}')
c.cookies.set(security.COOKIE_NAME, security.BOOT_TOKEN)
r = c.get('/')
assert r.status_code == 200, r.status_code
for 조각 in ('bulsaja-account', '/jobs/bulsaja/profile', '/jobs/bulsaja/scan', '/jobs/bulsaja/index'):
    assert 조각 in r.text, 조각
assert '표시규칙' not in r.text
assert 'undefined' not in r.text
print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - 위 명령이 `PASS` 를 찍는다 (보드가 200 이고 배너·버튼 3종이 실제 HTML 에 있다)
    - `grep -c 'hx-post="/jobs/bulsaja' webapp/templates/board.html` 가 3
    - `grep -c 'hx-get="/jobs/bulsaja' webapp/templates/board.html` 가 **0**
    - `git diff --stat webapp/static/board.js` 가 **변경 0줄** (이 태스크는 JS 를 안 늘린다)
    - 리터럴 가드 green: `.venv-web/bin/pytest webapp/tests/test_board.py -k 계정을_코드에 -x` 와 `.venv-web/bin/pytest webapp/tests/test_paths.py -k 리터럴 -x` 가 exit 0
    - 전량 회귀 `.venv-web/bin/pytest webapp/tests -q` exit 0
  </acceptance_criteria>
  <done>화면 상단에 현재 불사자 계정이 항상 보이고, 버튼 3개가 잡을 띄운다. JS 는 한 줄도 안 늘었다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 웹앱 POST | 다른 탭의 사이트가 3시간짜리 잡을 띄우려 할 수 있다 |
| 불사자 프로필 파일 → 화면 | 외부 문자열(닉네임·크레딧)이 템플릿으로 들어간다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-25 | Tampering(CSRF) | 교차 사이트에서 인덱스 잡 기동 | mitigate | 세 라우트 전부 POST + 기존 `security.guard`(Host/Origin/부팅토큰). `test_불사자_라우트는_GET이_아니다` 가 405 로 고정 |
| T-3-26 | Elevation | 틀린 계정으로 잡 기동 | mitigate | `create_job` 의 ENG-08 가드 + 라우트의 409 번역(`ValueError` 보다 **먼저**) |
| T-3-27 | Tampering | 클라이언트가 인덱스 대상 그룹을 지정 | mitigate | `JobReq` 에 그룹 필드를 두지 않는다. 대상은 서버가 `join.index_targets` 로 계산 (D-11) |
| T-3-28 | XSS | 닉네임·크레딧 문자열이 템플릿에 | mitigate | Jinja2 자동 이스케이프 유지(`|safe` 금지) + 화이트리스트 투영 |
| T-3-29 | Info Disclosure | 거부 사유에 기대 닉네임 노출 | mitigate | 라우트가 `profile_ok` 사유를 그대로 쓰고, 그 사유에는 기대값이 없다 |
| T-3-30 | DoS(자기유발) | 빈 대상이 "전량" 으로 해석 | mitigate | 3층 방어 — 라우트 400 · `_build_argv` ValueError · CLI `exit 2` |
</threat_model>

<verification>
```bash
.venv-web/bin/pytest webapp/tests -q
.venv-web/bin/pytest "webapp/tests/test_join.py::test_실회차_해상률" -x
CT_DEV_TOKEN=devtoken123 ./webapp/run-webapp.sh    # 수동: 배너·버튼 눈으로 확인
bash webapp/tests/security_curl.sh                  # 수동: 새 POST 라우트가 방어를 탄다
bash webapp/tests/no_commit_guard.sh
```
</verification>

<success_criteria>
- 실회차 번호 해상률이 실측으로 고정되고 회귀가 남는다 (JOIN-01)
- 브라우저에서 계정 확인 · 조인 스캔 · 인덱스 구축을 띄울 수 있다
- 화면 상단에 현재 계정·크레딧이 상시 보이고 불일치면 경고색이다 (ENG-08)
- 틀린 계정 409 · 대상 없음 400 · 스캔 없이 인덱스 409
- `board.js` 변경 0줄
</success_criteria>

<output>
Create `.planning/phases/03-join-detail-state/03-05-SUMMARY.md` when done.
실스캔 소요시간 · 429 발생 수 · 확정 해상률(분자/분모)을 반드시 적어라 — 03-06 의 배너 문구와 인덱스 비용 추정이 그 수를 쓴다.
</output>
