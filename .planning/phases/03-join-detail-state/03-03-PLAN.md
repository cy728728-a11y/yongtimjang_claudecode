---
phase: 03-join-detail-state
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - webapp/bulsaja_index.py
  - webapp/argv.py
  - webapp/jobs.py
  - webapp/tests/test_index.py
  - webapp/tests/test_argv.py
  - webapp/tests/test_jobs.py
autonomous: true
requirements: [ENG-08, JOIN-04]

must_haves:
  truths:
    - "인덱스 히트를 **재검증 없이** 쓰지 않는다 — 물갈이로 `mallProductId` 가 재발급되면 옛 관측이 조용히 틀린 상품을 가리킨다 (JOIN-04 / D-04)"
    - "재검증 불일치는 해소가 아니라 미해소로 떨어지고, 그 사유가 `번호없음` 과 다른 값이다"
    - "불사자 MCP 를 부르는 잡은 기대 계정이 확인되지 않으면 **자식을 띄우기 전에** 거부된다 (ENG-08 / 성공기준 6)"
    - "기대 계정 닉네임이 코드·주석에 없다 — `settings.cfg(..., required=True)` 로만 읽는다"
    - "인덱스 잡은 쓰기 전역 가드를 **타지 않는다** — 3시간 반 동안 입찰가 인상이 전부 409 가 되면 안 된다"
    - "`--groups` 없는 인덱스 잡은 만들어지지 않는다 — 빈 값이 '전 그룹 75,335건'으로 해석되는 경로를 예외로 터뜨린다"
    - "`ss_index` 테이블이 `webapp.db` 에 서고, 그게 보드 캐시가 아니라는 근거가 DDL 주석에 남는다 (D-20)"
  artifacts:
    - path: "webapp/bulsaja_index.py"
      provides: "lookup · group_health · 재검증판정 · profile · profile_ok — stdlib sqlite3 읽기 전용, 네트워크 0"
      min_lines: 90
      contains: "mode=ro"
    - path: "webapp/argv.py"
      provides: "BulsajaArgv — 불사자 CLI argv 조립 (같은 파일, 조립은 한 곳)"
      contains: "class BulsajaArgv"
    - path: "webapp/jobs.py"
      provides: "bulsaja_profile/bulsaja_index/bulsaja_scan kind · ss_index DDL · ENG-08 가드 · latest_done()"
      contains: "AccountMismatchError"
    - path: "webapp/tests/test_index.py"
      provides: "히트 재검증 · 불일치 미해소 · 그룹 불완전 · 프로필 만료"
  key_links:
    - from: "webapp/jobs.py create_job"
      to: "webapp/bulsaja_index.py profile_ok"
      via: "BULSAJA_KINDS 잡 생성 전 사전 점검. 라우트가 아니라 여기서 막는다 (D-17/ENG-07)"
      pattern: "profile_ok"
    - from: "webapp/jobs.py _build_argv"
      to: "webapp/argv.py BulsajaArgv"
      via: "argv 조립은 webapp/argv.py 한 곳 (T-1-10)"
      pattern: "BulsajaArgv"
    - from: "webapp/bulsaja_index.py"
      to: "webapp.db ss_index"
      via: "sqlite3 읽기 전용 URI. 파일이 없으면 만들지 않고 빈 결과"
      pattern: "ss_index"
---

## Phase Goal

> ⚠ ROADMAP `**Goal:**` 이 user-story 형식이 아니다. 정규화는 `/gsd mvp-phase 03`. 아래는 ROADMAP 원문이다.

광고 판정 행을 불사자 상품에 실제로 잇고 상세 상태를 3단계로 판정해, "유입은 있는데 상세가 중국어 원본인 상품" 목록을 화면에서 정확히 뽑아낸다.

<objective>
**Phase 1 의 잡 엔진에 불사자 잡 3종을 얹고, 그 잡들이 틀린 계정으로 도는 것을 구조로 막는다.**

이 플랜이 없으면 03-04 의 CLI 는 부를 수 있는 입구가 없고, 03-06 의 보드는 인덱스를 읽을 수 없다.

**핵심 판단 셋 (전부 근거가 코드 안에 있다):**
- 인덱스 잡은 `WRITE_KINDS` 에 **넣지 않는다.** 불사자에 아무것도 안 쓴다(읽기 전용 workdata 조회).
  넣으면 3시간 32분 동안 입찰가 인상이 전부 409 다 — "쓰기가 아닌 것까지 막는 가드는 사람이 가드를 끄게 만든다"(jobs.py:54).
- `ss_index` 는 보드 캐시가 **아니다.** 보드 캐시는 `result.json` 에서 언제든 재생성되는 투영이고,
  이건 3시간 반을 태워야 다시 얻는 외부 관측 기록이다 (D-20).
- ENG-08 거부는 **`create_job` 안**이다. 라우트가 아니다 — v2 스케줄러가 같은 함수를 부른다 (D-17/ENG-07).

Purpose: 틀린 계정으로 3시간 반짜리 인덱스를 쌓거나, 재검증 없는 옛 관측으로 Phase 5 가
엉뚱한 상품에 크레딧을 태우는 걸 막는다.
Output: `webapp/bulsaja_index.py` · `BulsajaArgv` · 잡 3종 + ENG-08 가드 + 검증 14종
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-join-detail-state/03-CONTEXT.md
@.planning/phases/03-join-detail-state/03-RESEARCH.md
@.planning/phases/03-join-detail-state/03-PATTERNS.md
@.planning/phases/03-join-detail-state/03-VALIDATION.md
@.planning/phases/03-join-detail-state/03-01-SUMMARY.md

<interfaces>
이 플랜이 만드는 계약. 03-04(CLI)·03-05(라우트)·03-06(보드)이 이 이름으로 쓴다.

webapp/bulsaja_index.py — **stdlib sqlite3 읽기 전용. 네트워크 0. eroomlib import 금지 (D-19)**
  db_path() -> Path                       # jobs.db_path() 와 같은 파일. import 순환을 피해 계산만 복제
  lookup(smartstores: list[str]) -> dict[str, dict]
      # {smartstore: {"product_id","market_group_id","observed_at","unresolved"}}
      # DB 파일이 없으면 {} — **만들지 않는다**(읽기 경로가 파일을 만들면 GET 계약이 흐려진다)
  indexed_group_ids() -> set[str]
  group_health(group_ids) -> dict[str, dict]
      # {gid: {"행수": int, "미조회": int, "총상품수": int | None, "완결": bool}}
  profile() -> dict | None                # webapp-profile.json. 없으면 None
  profile_ok(expected_nick: str, max_age_min: int) -> tuple[bool, str]   # (통과여부, 사유)

webapp/jobs.py — ss_index DDL (jobs DDL 과 같은 자리, 같은 모양)
  CREATE TABLE IF NOT EXISTS ss_index (
    product_id      TEXT PRIMARY KEY,
    smartstore      TEXT,                 -- 업로드 안 된 상품은 NULL 이다. NOT NULL 금지
    market_group_id TEXT NOT NULL,
    group_total     INTEGER,              -- 관측 시점의 그룹 전체 상품수 (완결성 판정용)
    unresolved      INTEGER NOT NULL DEFAULT 0,  -- 1 = 미조회(429/타임아웃). 성공으로 접지 않는다
    observed_at     TEXT NOT NULL
  );
  CREATE INDEX IF NOT EXISTS idx_ss_index_smartstore ON ss_index(smartstore);
  CREATE INDEX IF NOT EXISTS idx_ss_index_group ON ss_index(market_group_id);

webapp/argv.py
  SS_INDEX_BUILD = repo_root()/".claude"/"skills"/"bulsaja-detail-page"/"scripts"/"ss_index_build.py"
  BULSAJA_SCAN   = repo_root()/".claude"/"skills"/"bulsaja-detail-page"/"scripts"/"bulsaja_scan.py"
  Nick = Annotated[str, StringConstraints(pattern=r"^[^-\s][^\s]*$", min_length=1, max_length=40)]

  class BulsajaArgv(BaseModel):
      subcommand: Literal["profile", "index", "scan"]
      db: Path                 # --db      (webapp.db)
      out: Path                # --out     (join_<job>.json / 인덱스 진행요약). profile 모드에선 안 붙인다
      profile_out: Path        # --profile-out   (세 모드 공통. 자식이 계정 확인 결과를 여기 갱신한다)
      expect_nick: Nick        # --expect-nick   (ENG-08 — 자식이 스스로 한 번 더 확인한다)
      min_interval: float      # --min-interval
      retry_after: int         # --retry-after
      batch_size: int          # --batch-size
      run_dir: str | None = None   # --run-dir   (scan 전용)
      groups: Path | None = None   # --groups    (index 전용, 필수 — 없으면 전 그룹)
      targets: Path | None = None  # --targets   (scan 전용, 필수 — 없으면 대상 불명)
      limit: int = 0               # --limit     (0 이면 플래그를 붙이지 않는다)
      prefix: list[str] = []       # caffeinate -i 자리 (AdsArgv.prefix 와 같은 규약)
      def build(self) -> list[str]

webapp/jobs.py — 잡 3종
  JobKind/KINDS 에 "bulsaja_profile" · "bulsaja_index" · "bulsaja_scan" 추가
  BULSAJA_KINDS = frozenset({"bulsaja_index", "bulsaja_scan"})   # ENG-08 사전점검 대상
  WRITE_KINDS   = 변경 없음                                        # 셋 다 안 넣는다
  class AccountMismatchError(ValueError)                           # 라우트가 409 로 번역
  latest_done(kind: str, run_dir: str | None = None) -> dict | None  # 최근 성공 잡 1건
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: webapp/bulsaja_index.py — ss_index 읽기 · 재검증 판정 · 프로필 (JOIN-04)</name>
  <files>webapp/bulsaja_index.py, webapp/jobs.py, webapp/tests/test_index.py</files>
  <read_first>
    - `webapp/jobs.py` 76-101행 — `DDL` 문자열과 그 아래 `# ※` 주석 관용구("왜 이 컬럼이 이 모양인지"). **`ss_index` 를 여기 얹는다**
    - `webapp/jobs.py` 118-128행(`db_path()` 계산), 157-178행(`_conn`/`init_db`/WAL), 180-186행(`_row`)
    - `webapp/paths.py` 1-11행 · 19-48행 — stdlib 만 쓰는 경계와 `repo_root()`
    - `webapp/tests/test_jobs.py` 72-77행 — `잡판` 픽스처(DB·로그를 tmp 로 돌린다). `test_index.py` 가 그대로 쓴다
    - `webapp/tests/test_jobs.py` 474-478행 — "문자열 검사로 때우지 않는다" 규율
    - `webapp/routes/board.py` 100-114행 · `webapp/jobs.py` 603-615행(`active_job` 이 **DB 파일을 만들지 않는** 이유)
    - `03-RESEARCH.md` §D-13 확정 권장설계 (A) — 영속 인덱스 + 회차 재검증. **"D-04 를 어기지 않는다"의 근거를 주석으로 옮겨야 한다**
    - `03-RESEARCH.md` §D-04 캐시 위치 판단 표 · §ENG-08 · §Pitfall 3
    - `03-PATTERNS.md` §`webapp/bulsaja_index.py` 항목 전체 (DDL 주석 2줄 요구 포함) · §🔴② (D-20)
  </read_first>
  <behavior>
    - `test_디비가_없으면_만들지_않는다` — DB 파일이 없을 때 `lookup([...])` 이 `{}` 를 주고, 그 호출 뒤에도 파일이 **생기지 않는다**
    - `test_읽기전용이다` — `lookup` 이 연 커넥션으로는 INSERT 가 안 된다(`sqlite3.OperationalError`). 읽기 경로가 인덱스를 오염시키지 못한다
    - `test_그룹_완결성` — 같은 그룹에 `unresolved=1` 행이 하나라도 있으면 `완결=False`. 행수가 `group_total` 보다 적어도 `완결=False`
    - `test_프로필이_없으면_거부` — `profile()` 이 None 일 때 `profile_ok(...)` 가 `(False, 사유)` 이고 사유에 "계정 확인" 이 들어 있다
    - `test_프로필_닉네임_불일치` — 닉네임이 다르면 `(False, 사유)`. **사유 문자열에 기대 닉네임을 싣지 마라**(화면에 그대로 뜬다 — 다른 값이 왔다는 사실만 말한다)
    - `test_프로필이_오래되면_거부` — `확인시각` 이 `max_age_min` 을 넘으면 `(False, 사유)`
    - `test_스키마는_jobs_와_ss_index_뿐이다` — `init_db()` 뒤 테이블 집합이 정확히 그 둘 (03-01 이 넓힌 가드가 여기서 의미를 갖는다)
  </behavior>
  <action>
    `webapp/jobs.py` 의 `DDL` 문자열에 `<interfaces>` 의 `ss_index` 를 더한다. 기존 `jobs` 테이블 DDL 은 건드리지 마라. DDL 아래 `# ※` 주석 블록에 **두 줄을 반드시** 더한다(PATTERNS 요구):
      - `※ observed_at 이 있어야 D-04 를 안 어긴다` — "영구 키로 쓰는 것"과 "관측 기록을 관측시각과 함께 보관하는 것"은 다르다. 쓰기 직전 재검증이 그 선을 지킨다.
      - `※ 이건 보드 캐시가 아니다` — 보드 캐시는 `result.json` 에서 언제든 재생성되는 투영이고, 이건 3시간 반을 태워야 다시 얻는 외부 관측 기록이다. 대상별 잠금 테이블은 여전히 Phase 2 다.
    `smartstore` 에 `NOT NULL` 을 걸지 마라 — 업로드 안 된 상품이 정상적으로 NULL 이다(전체 수집상품 97만 건 중 대부분).

    `webapp/bulsaja_index.py` 를 새로 쓴다. 모듈 docstring 은 `board.py:3-13` 모양으로, **이 모듈이 절대 하지 않는 것**을 못박는다:
      - MCP 호출 (D-19 — `.venv-web` 에 `requests` 가 없다. 인덱스를 **채우는** 것은 CLI 자식 몫이고 여기는 읽기만)
      - 쓰기 (INSERT/UPDATE/DELETE 0건. 읽기 전용 URI 로 연다)
      - DB 파일 생성 (`active_job()` 과 같은 규율 — 읽기 경로가 파일을 만들면 "GET 은 상태를 안 바꾼다"가 흐려진다)
      - 인덱스 히트를 그대로 해소로 접기 (JOIN-04 — 재검증 없는 히트는 물갈이 뒤 틀린 상품을 가리킨다)

    `db_path()` 는 `jobs.db_path()` 와 **같은 3줄**을 복제한다. `jobs` 를 import 하면 `jobs → bulsaja_index → jobs` 순환이 된다(ENG-08 가드가 이 모듈을 부른다). 복제 이유를 주석에 남겨라.

    `lookup(smartstores)` — `sqlite3.connect(f"file:{db_path()}?mode=ro", uri=True)` 로 연다. 파일이 없으면 **연결 시도 전에** `is_file()` 로 확인해 `{}` 를 돌려준다. `IN (...)` 플레이스홀더는 **`settings.cfg("mcp_batch_size")` 단위로 나눠** 질의한다(SQLite 변수 상한 방어). `smartstore` 가 NULL 인 행은 조회 대상이 아니다.

    ⚠️ **재검증 판정 함수(`재검증판정`)는 이 모듈에 두지 않는다.** 그건 조인 의미론이라 `webapp/join.py`(03-02)에 있고, `히트/미스/미조회/불일치` 어휘도 거기가 정본이다. 이 모듈은 **인덱스에 적힌 값을 꺼내 주는 데까지**만 한다 — 판정이 두 곳에 있으면 화면과 산출물이 다른 말을 한다.

    `group_health(group_ids)` — 그룹별 `행수`·`미조회`·`총상품수`(`MAX(group_total)`)·`완결`. `완결` 은 `미조회==0 and (총상품수 is None or 행수 >= 총상품수)`. 이 값이 `False` 인 그룹의 행은 화면에서 **"인덱스 불완전"(시스템)** 으로 떠야지 "광고 쪽 오류"로 뜨면 안 된다(Pitfall 3) — 그 사실을 docstring 에 적어라.

    `profile()` / `profile_ok(expected_nick, max_age_min)` — `settings.cfg("profile_path")` 를 `paths.repo_root()` 기준으로 풀어 JSON 을 읽는다. 파일이 없거나 깨졌으면 `None`(예외를 올리지 않는다 — 화면이 "계정 확인을 먼저 눌러라"로 안내할 수 있어야 한다). `profile_ok` 는 `(False, 사유)` 를 돌려줄 때 **기대 닉네임을 사유에 싣지 마라**: 화면에 그대로 뜨고, 그 순간 그 값이 사실상 리터럴이 된다. "붙어 있는 계정이 기대와 다르다" 까지만 말한다. 읽은 dict 에 `표시규칙` 키가 있으면 버린다(방어적 이중화 — CLI 가 이미 제거하지만 파일이 손으로 편집될 수 있다).

    `webapp/tests/test_index.py` 를 behavior 11항목에 1:1 로 쓴다. `잡판` 픽스처(`test_jobs.py:72-77`)를 그대로 가져와 tmp DB 를 쓰고, `ss_index` 행은 테스트가 직접 INSERT 한다(쓰기 커넥션은 테스트가 따로 연다 — 런타임 모듈은 읽기 전용이라는 게 검증 대상이다). 프로필 테스트는 `monkeypatch.setattr(paths, "repo_root", lambda: tmp_path)` 로 tmp 에 프로필 파일을 깐다.
    ⚠️ **레이트리밋(초당 4회)과 429 재시도 테스트는 이 플랜에 쓰지 마라.** 그 루프는 CLI 안에 있고(03-04), `test_index.py` 에 두 테스트를 덧붙이는 것도 03-04 의 작업이다 — 여기서 흉내 내면 "우리가 짠 가짜 루프"를 검증하게 된다.
  </action>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_index.py webapp/tests/test_jobs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv-web/bin/pytest webapp/tests/test_index.py -x -q` 가 exit 0
    - VALIDATION 의 `test_히트도_재검증한다`·`test_불일치는_미해소` 는 **03-02 의 `test_join.py` 에 있다**(판정 함수가 `join.py` 에 살기 때문 — 이 이탈은 두 플랜 SUMMARY 에 적는다). 여기서 같이 돌려 green 을 확인한다:
      `.venv-web/bin/pytest "webapp/tests/test_join.py::test_히트도_재검증한다" "webapp/tests/test_join.py::test_불일치는_미해소" -x` 가 exit 0
    - `grep -c 'mode=ro' webapp/bulsaja_index.py` 가 1 이상
    - `grep -cE '(INSERT|UPDATE|DELETE|CREATE)' webapp/bulsaja_index.py` 가 **0**
    - `grep -cE '(eroomlib|requests|fastapi|starlette|APIRouter|HTTPException)' webapp/bulsaja_index.py` 가 0
    - `grep -c 'ss_index' webapp/jobs.py` 가 3 이상 (DDL + 인덱스 2종)
    - `grep -v '^#' webapp/jobs.py | grep -c '보드 캐시' ` 로는 세지 말고, DDL 주석에 `※` 두 줄이 있는지 눈으로 확인한 뒤 `grep -c '※ 이건 보드 캐시가 아니다' webapp/jobs.py` 가 1
    - 전량 회귀 `.venv-web/bin/pytest webapp/tests -q` exit 0
  </acceptance_criteria>
  <done>인덱스를 읽는 유일한 관문이 서고, 히트도 재검증을 거쳐야만 해소가 된다. 테이블이 보드 캐시가 아니라는 근거가 소스에 남았다.</done>
</task>

<task type="auto">
  <name>Task 2: webapp/argv.py — BulsajaArgv + jobs 의 kind 3종 · argv 분기 (조립은 한 곳)</name>
  <files>webapp/argv.py, webapp/jobs.py, webapp/tests/test_argv.py</files>
  <read_first>
    - `webapp/argv.py` 전체 96행 — 모듈 docstring(조립이 한 곳일 때만 사는 성질 3가지), `PY_CLI`(27-38) 가 현재 인터프리터가 아닌 이유, `Alias` 패턴(46), `AdsArgv`(49-96), **`prefix` 필드 주석(62-68 — `caffeinate -i` 자리)**, `build()` 의 "빈 리스트면 플래그 자체를 안 붙인다"(77-84), 경로 `str()` 못박기(89-91)
    - `webapp/jobs.py` 10-19행(작업 종류 표 — **갱신도 작업이다**), 43-47행(`JobKind`/`KINDS` — **둘 다 고쳐야 한다**), 49-56행(`WRITE_KINDS` 와 왜 전역인가), 365-402행(`_build_argv` 와 `revert_only` 의 "빈 값이 전량" 예외), 496-501행(`result_path` 를 만드는 자리)
    - `webapp/tests/test_argv.py` 25-27행(가드가 스캔하는 문자열을 런타임에 조립하는 관용구), 29-40행(`av[0]` 이 `.venv/bin/python3` 여야 한다), 159-187행(문자열로 명령을 만들지 않는다 · 셸 경유 금지 가드)
    - `03-RESEARCH.md` §Pattern 1 — "⚠️ `argv.AdsArgv` 는 `run_ads.py` 전용이다. 불사자 인덱스 잡은 별도 모델이 필요하다. **같은 파일에 두어라**"
    - `03-RESEARCH.md` §인덱스 잡이 반드시 지켜야 할 것 1~5 (특히 4 — `caffeinate -i`)
    - `03-PATTERNS.md` §`webapp/argv.py` 항목 · §`webapp/jobs.py` 항목의 "고칠 자리 6군데" 표
  </read_first>
  <action>
    `webapp/argv.py` 에 `SS_INDEX_BUILD`·`BULSAJA_SCAN` 경로 상수와 `Nick` 타입, `BulsajaArgv` 를 더한다. **다른 파일에 만들지 마라** — `test_argv.py:159-171` 이 "조립은 한 곳"을 기계로 집행한다.

    `Nick` 은 `Annotated[str, StringConstraints(pattern=r"^[^-\s][^\s]*$", min_length=1, max_length=40)]`. **하이픈으로 시작할 수 없다**가 핵심이다 — 닉네임이 `--force` 같은 모양이면 argv 에서 플래그로 읽힌다. `Alias` 가 영숫자만 받는 것과 같은 계열의 방어이고, 닉네임은 한글이라 문자 화이트리스트를 못 쓴다는 점만 다르다. 그 차이를 주석에 남겨라.

    `BulsajaArgv.build()` 는 `[*prefix, str(PY_CLI), str(스크립트), ...플래그]` 를 낸다. 스크립트는 `subcommand` 로 고른다: `index` → `SS_INDEX_BUILD`, `profile`/`scan` → `BULSAJA_SCAN`(같은 스크립트의 두 모드. `profile` 은 `--profile-only` 를 붙인다 — `detail_batch.py` 의 `--submit-only`/`--poll-only` 관용구와 같은 모양). 경로는 전부 `str()` 로 못박는다(`Path` 를 두면 jobs 테이블 argv JSON 직렬화가 터진다).
    `limit` 은 **0 이면 플래그 자체를 안 붙인다**(`AdsArgv` 의 빈 리스트 규율과 같다). `groups` 가 None 이면 `--groups` 를 안 붙이는데, **그 상태로 인덱스를 돌리면 전 그룹 75,335건 = 5시간 39분이다.** 그래서 `build()` 가 아니라 아래 `_build_argv` 가 먼저 터뜨린다 — 두 자리 중 한 곳에만 두고 주석으로 서로를 가리켜라.
    `min_interval`·`retry_after`·`batch_size`·`expect_nick` 은 **기본값을 두지 마라.** 호출부(`jobs._build_argv`)가 `settings.cfg()` 로 읽어 넘긴다. 기본값을 CLI 나 모델에 두면 설정을 고쳐도 안 바뀌는 가짜 설정이 된다.
    `prefix` 필드는 `AdsArgv` 와 같은 주석을 달고 둔다 — 인덱스 잡이 `caffeinate -i` 를 실제로 쓰는 첫 자리다.

    `webapp/jobs.py`:
    - 모듈 docstring 10-19행의 **작업 종류 표에 3행을 더한다** (`bulsaja_profile`/`bulsaja_index`/`bulsaja_scan` — 디스크 쓰기: 프로필 파일·`ss_index`·`join_*.json` / 불사자 쓰기: **없음(읽기 전용 조회)** / 전역 가드: **안 탄다**). 표를 안 고치면 다음 사람이 잡 종류를 `KINDS` 에서 찾아야 한다.
    - `JobKind` 와 `KINDS` **둘 다** 에 3종을 더한다. 하나만 고치면 타입만 맞고 런타임이 거부한다.
    - `WRITE_KINDS` 는 **건드리지 마라.** 그 이유를 `WRITE_KINDS` 주석 아래 한 줄로 남겨라: 불사자 잡은 불사자에 아무것도 안 쓴다. 넣으면 3시간 32분짜리 인덱스가 도는 동안 입찰가 인상·되돌리기가 전부 409 가 된다.
    - `BULSAJA_KINDS = frozenset({"bulsaja_index", "bulsaja_scan"})` 를 `WRITE_KINDS` 옆에 둔다. **`bulsaja_profile` 은 넣지 않는다** — 그 잡이 프로필을 만드는 잡이라, 가드 대상에 넣으면 첫 확인을 영영 못 한다(닭·달걀).
    - `_build_argv` 에 분기 3개를 더한다. **`bulsaja_index` 와 `bulsaja_scan` 둘 다 `targets_path` 가 필수다.** 없으면 `revert_only` 와 **똑같은 모양으로 ValueError 를 던진다**:
      · 인덱스 — "인덱스 잡에는 그룹 목록 파일이 반드시 있어야 한다 — 없으면 전 그룹(실측 75,335건 · 5시간 39분)이다"
      · 스캔 — "스캔 잡에는 대상 목록 파일이 반드시 있어야 한다 (FLOW-02 / D-11)"
      `bulsaja_index` 는 `targets_path` → `groups`, `bulsaja_scan` 은 `targets_path` → `targets` 에 싣는다. `result_path` 는 `out`, `settings.cfg()` 값들은 나머지에 싣는다. `bulsaja_profile` 은 대상 파일이 없다(계정 확인에 대상이 없다).
      대상 파일의 내용 규약: 인덱스는 **groupId 문자열 리스트**, 스캔은 **`"<계정alias>|<mallProductId>"` 문자열 리스트**다(`board.js` 의 Tabulator index 와 같은 모양). 둘 다 `jobs._write_targets` 가 쓰는 JSON 배열이라 형식이 하나다.
    - `create_job` 의 `result_path` 생성 분기(496-501행)에 불사자 kind 를 더한다. 접두는 `bulsaja_scan` → `join`, `bulsaja_index` → `index`, `bulsaja_profile` → `profile`. `BIDS_KINDS` 를 건드리지 말고 나란한 집합을 하나 더 둬라.
      ⚠️ `bulsaja_profile` 은 `run_dir` 이 없어도 돌아야 한다(회차와 무관한 계정 확인). 그 경우 `out` 은 `settings.cfg("profile_path")` 를 `repo_root()` 기준으로 푼 경로다.
    - `latest_done(kind, run_dir=None)` — `status='done'` 인 최근 1건을 `_as_dict` 로 돌려준다. `recent_jobs` 옆에 두고 같은 `_conn`/`_reap` 규율을 쓴다. 03-05·03-06 이 "마지막 조인 산출물"을 찾는 유일한 길이다. 파일 목록을 glob 으로 뒤지지 마라 — 어느 잡이 성공했는지는 레지스트리가 정본이다.

    `webapp/tests/test_argv.py` 에 테스트를 더한다:
    - `test_불사자_argv_도_cli_인터프리터를_쓴다` — `prefix` 없이 만든 argv 의 `av[0]` 이 `.venv/bin/python3` 로 끝나고 `.venv-web` 이 들어가지 않는다 (기존 29-40행 관용구 그대로)
    - `test_caffeinate_프리픽스가_앞에_붙는다` — `prefix=["/usr/bin/caffeinate","-i"]` 면 `av[0]` 이 caffeinate 이고 `av[2]` 가 스크립트 경로다
    - `test_limit_0_이면_플래그가_없다` · `test_groups_None_이면_플래그가_없다`
    - `test_닉네임에_플래그모양은_거부된다` — `expect_nick="--force"` 가 `ValidationError`
    - `test_인덱스잡은_그룹파일이_없으면_터진다` — `jobs._build_argv("bulsaja_index", ..., targets_path=None, ...)` 가 `ValueError` 이고 메시지에 "그룹" 이 들어 있다
    - `test_스캔잡은_대상파일이_없으면_터진다` — 같은 모양으로 `bulsaja_scan` 도 `ValueError` (FLOW-02 — 대상 파일은 하나이고 없으면 만들지 않는다)
    - `test_설정값이_argv_로_흐른다` — `settings` 를 tmp workspace.toml 로 덮고 `_build_argv` 를 부르면 `--min-interval` 값이 따라 바뀐다 (설정이 진짜로 동작을 바꾸는지)
  </action>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_argv.py webapp/tests/test_jobs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv-web/bin/pytest webapp/tests/test_argv.py -x -q` 가 exit 0
    - `.venv-web/bin/python3 -c "
from webapp import argv, jobs
assert 'bulsaja_index' in jobs.KINDS and 'bulsaja_scan' in jobs.KINDS and 'bulsaja_profile' in jobs.KINDS
assert not (set(jobs.WRITE_KINDS) & {'bulsaja_index','bulsaja_scan','bulsaja_profile'}), jobs.WRITE_KINDS
assert 'bulsaja_profile' not in jobs.BULSAJA_KINDS
print('PASS')"` 가 `PASS`
    - `grep -c 'class BulsajaArgv' webapp/argv.py` 가 1 이고 `grep -rc 'class BulsajaArgv' webapp | grep -v 'argv.py:0' | wc -l` 로 다른 파일에 없음을 확인
    - `grep -c 'bulsaja' webapp/jobs.py` 가 6 이상 (docstring 표 3행 + KINDS + BULSAJA_KINDS + argv 분기)
    - 전량 회귀 `.venv-web/bin/pytest webapp/tests -q` exit 0
  </acceptance_criteria>
  <done>불사자 잡 3종이 잡 엔진 계약을 그대로 타고, argv 조립은 여전히 한 곳이다. 그룹 목록 없는 인덱스 잡은 만들어지지 않는다.</done>
</task>

<task type="auto">
  <name>Task 3: ENG-08 — create_job 안의 계정 가드 (틀린 계정으로는 자식이 안 뜬다)</name>
  <files>webapp/jobs.py, webapp/tests/test_jobs.py</files>
  <read_first>
    - `webapp/jobs.py` 404-440행 — `create_job` docstring 의 **"이 함수는 HTTP 를 모른다"** 와 "순서에 의미가 있다 ①~⑥", 99-105행 `BusyError`("라우트가 409 로 번역한다"), 448-467행 — `BEGIN IMMEDIATE` 안에서 가드를 **맨 먼저** 보는 이유
    - `webapp/routes/jobs.py` 344-358행 — `_작업만들기` 의 예외 → 상태코드 번역표 (`BusyError`→409, `ValueError`→400, `RuntimeError`→500). **`AccountMismatchError` 가 `ValueError` 를 상속하면 400 으로 새 나간다** — 이 점을 어떻게 다룰지 결정해야 한다
    - `webapp/bulsaja_index.py` (Task 1 산출물) — `profile_ok`
    - `03-RESEARCH.md` §ENG-08 전체 — "거부는 쓰기 잡 생성 함수 안에서. 라우트가 아니라 `create_job` 계열에 둬야 v2 스케줄러도 같은 가드를 탄다"
    - `03-CONTEXT.md` §계정 매핑 — "ENG-08 은 '표시 + 거부' 로 충분하다. 계정 전환 로직은 범위 밖"
    - `.planning/STATE.md` Blockers — "이 맥북에 `bulsaja-yongssaem` MCP 서버 항목이 없다 — ENG-08 착수 시 확인"
  </read_first>
  <action>
    `webapp/jobs.py` 에 `AccountMismatchError` 를 `BusyError` 옆에 세운다. `ValueError` 를 상속하되 **`_작업만들기` 의 `except ValueError` 보다 먼저 잡히도록** 라우트 쪽에서 별도 `except` 를 추가할 자리를 만든다(라우트 수정은 03-05 다 — 여기서는 예외 클래스와 docstring 에 "라우트가 409 로 번역한다. `ValueError` 를 그냥 쓰면 '모르는 회차'와 구분이 안 된다" 를 적는다). `ValueError` 상속 자체는 유지해라 — 03-05 이전 상태에서도 400 으로 거부되어 **열린 채로 새지 않는다**(안전한 기본값).

    `create_job` 의 순서 ①~⑥ 사이에 **①.5** 를 끼운다. `kind in BULSAJA_KINDS` 면 `bulsaja_index.profile_ok(settings.cfg("expected_bulsaja_nick", required=True), int(settings.cfg("profile_max_age_min", ...)))` 를 보고, 실패면 `AccountMismatchError(사유)` 를 던진다. 자리가 중요하다:
      - **자식을 띄우기 전**이다. 3시간 32분짜리 인덱스를 틀린 계정으로 쌓고 나면 되돌리는 비용이 그 시간 전부다.
      - `BEGIN IMMEDIATE` 트랜잭션 **안**에 넣지 마라 — 파일 읽기이고 DB 경합과 무관하다. 트랜잭션을 열기 전에 본다.
      - `create_job` docstring 의 "순서에 의미가 있다" 목록에 이 단계를 **글로도** 더한다.
    `settings.cfg(..., required=True)` 를 쓰는 게 핵심이다. 값이 비면 KeyError 로 터져야지 조용히 폴백해 가드가 사라지면 안 된다(T-1-12 와 같은 부류). 그 KeyError 는 라우트에서 500 이 되는데, **그게 맞다** — 설정이 빈 채로 불사자 잡이 도는 것보다 낫다.

    `webapp/tests/test_jobs.py` 에 테스트를 더한다. `잡판` 픽스처 + `monkeypatch.setattr(paths, "repo_root", lambda: tmp_path)` 로 tmp 프로필을 깐다:
    - `test_계정불일치_거부` — 프로필 파일의 닉네임이 기대와 다르면 `create_job("bulsaja_scan", ...)` 이 `AccountMismatchError`. **그리고 jobs 테이블에 행이 안 생긴다**(거부가 자식보다 먼저다). 행 수 0 을 같이 단언해라
    - `test_계정확인이_없으면_거부` — 프로필 파일 자체가 없으면 같은 예외
    - `test_프로필잡은_가드를_안_탄다` — 프로필 파일이 없어도 `create_job("bulsaja_profile", ...)` 은 만들어진다(합성 argv 로 실제 spawn 을 피하지 말고, `BULSAJA_SCAN` 이 아직 없는 파일이면 spawn 이 실패하므로 **`argv_override` 로 `synthetic` 스펙을 태우지 말고** `monkeypatch` 로 `jobs.spawn` 을 가짜로 바꿔라 — 가드만 겨누는 테스트다)
    - `test_인덱스잡은_쓰기가드를_안_탄다` — 도는 `bulsaja_index` 잡이 있어도 `bids_commit` 이 `BusyError` 없이 만들어진다. 반대로 도는 `prep` 이 있어도 `bulsaja_index` 가 만들어진다
    - `test_기대닉네임이_코드에_없다` — 03-01 의 `test_paths.py` 가 이미 훑지만, `jobs.py` 가 값을 **설정에서 읽는지**를 여기서 한 번 더 본다: `settings` 의 값을 monkeypatch 로 바꾸면 거부/통과가 따라 바뀐다 (문자열 검사가 아니라 동작으로)
  </action>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_jobs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv-web/bin/pytest "webapp/tests/test_jobs.py::test_계정불일치_거부" -x` 가 exit 0
    - `.venv-web/bin/pytest webapp/tests/test_jobs.py -x -q` 가 exit 0 (기존 잡 테스트 전량이 안 깨진다)
    - `grep -c 'class AccountMismatchError' webapp/jobs.py` 가 1
    - `grep -c 'profile_ok' webapp/jobs.py` 가 1 이상
    - 기대 닉네임 리터럴이 런타임에 0건: `.venv-web/bin/pytest webapp/tests/test_paths.py -k 리터럴 -x` 가 exit 0
    - 전량 회귀 `.venv-web/bin/pytest webapp/tests -q` exit 0 · `bash webapp/tests/no_commit_guard.sh` exit 0
  </acceptance_criteria>
  <done>기대 계정이 확인되지 않으면 불사자 잡의 자식이 뜨지 않는다. 가드는 라우트가 아니라 호출 가능한 함수 안에 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 웹앱 → CLI 자식 (argv) | 조립은 `webapp/argv.py` 한 곳. 셸을 거치지 않는다 |
| `webapp-profile.json` → 웹앱 | 자식이 쓴 파일. 손으로 편집될 수 있다 |
| `ss_index` → 화면 | 3시간 반짜리 외부 관측 기록. 낡을 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-11 | Elevation | 틀린 계정으로 불사자 잡 실행 | mitigate | `create_job` 안 ①.5 단계에서 `profile_ok` 확인 후 `AccountMismatchError`. 자식을 띄우기 전이다 (ENG-08) |
| T-3-12 | Tampering | `--groups` 누락 → 전 그룹 75,335건 | mitigate | `_build_argv` 가 `revert_only` 와 같은 모양으로 ValueError. `test_인덱스잡은_그룹파일이_없으면_터진다` 가 고정 |
| T-3-13 | Tampering | 닉네임이 플래그로 해석되는 argv 주입 | mitigate | `Nick` StringConstraints 가 선행 하이픈·공백을 거부. 리스트 argv 라 셸을 안 거친다 |
| T-3-14 | Tampering(무결성) | 재검증 없는 인덱스 히트 | mitigate | `재검증판정` 이 관측값 없이는 `히트` 를 내지 않는다 (JOIN-04 / D-04) |
| T-3-15 | DoS(자기유발) | 인덱스 잡이 전역 쓰기 가드를 3시간 반 점유 | mitigate | `WRITE_KINDS` 에 넣지 않는다 + 테스트로 고정 |
| T-3-16 | Info Disclosure | 거부 사유에 기대 닉네임 노출 | mitigate | `profile_ok` 사유 문자열에 기대값을 싣지 않는다 |
| T-3-17 | Tampering | 읽기 경로가 인덱스를 오염 | mitigate | `mode=ro` URI + `test_읽기전용이다` |
</threat_model>

<verification>
```bash
.venv-web/bin/pytest webapp/tests -q
.venv-web/bin/pytest webapp/tests/test_index.py webapp/tests/test_argv.py webapp/tests/test_jobs.py -q
bash webapp/tests/no_commit_guard.sh
.venv-web/bin/python3 -c "from webapp import jobs; print(sorted(jobs.KINDS)); print(sorted(jobs.WRITE_KINDS)); print(sorted(jobs.BULSAJA_KINDS))"
```
</verification>

<success_criteria>
- 불사자 잡 3종이 `KINDS` 에 있고 `WRITE_KINDS` 에는 없다
- 계정이 확인되지 않으면 불사자 잡이 `create_job` 단계에서 거부된다 (jobs 행도 안 생긴다)
- 인덱스 히트가 재검증 없이는 해소가 되지 않고, 불일치·미조회·미스가 서로 다른 값이다
- `ss_index` 가 `webapp.db` 에 서고 스키마 가드가 `{jobs, ss_index}` 로 정확히 맞는다
- `webapp/bulsaja_index.py` 에 쓰기·네트워크가 0건
</success_criteria>

<output>
Create `.planning/phases/03-join-detail-state/03-03-SUMMARY.md` when done.
</output>
