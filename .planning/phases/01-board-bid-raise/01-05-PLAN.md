---
phase: 01-board-bid-raise
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-04"]
files_modified:
  - webapp/argv.py
  - webapp/jobs.py
  - webapp/routes/jobs.py
  - webapp/templates/board.html
  - webapp/templates/_job_panel.html
  - webapp/tests/test_argv.py
  - webapp/tests/test_jobs.py
autonomous: true
requirements: [ENG-01, ENG-07]

must_haves:
  truths:
    - "D-15: 보드에 '새로 수집' 버튼이 있고, 누르면 `prep` 이 서브프로세스로 돌기 시작하며 화면이 즉시 돌아온다(블로킹 없음)"
    - "D-17 / ENG-07: 작업 생성이 `jobs.create_job(...)` 이라는 **HTTP 를 모르는 호출 가능한 함수**에 있고, 라우트는 그걸 얇게 감싸기만 한다. v2 의 APScheduler 가 같은 함수를 부른다"
    - "ENG-01: CLI 는 `.venv/bin/python3`(웹앱의 `.venv-web` 이 아니다) + `run_ads.py` 로 **서브프로세스** 실행된다. 같은 프로세스에 올리지 않는다"
    - "argv 조립이 `webapp/argv.py` 의 `AdsArgv` 한 곳에서만 일어난다. `shell=False`, 리스트 argv — 명령 주입 경로가 없다"
    - "`--account` 를 값 없이 붙이지 않는다. 빈 리스트면 플래그 자체를 안 붙인다 — 값 없는 `--account` 는 전 계정이 된다"
    - "Phase 2 의 `caffeinate -i` 프리픽스를 `build()` 맨 앞에 한 줄로 끼울 수 있는 모양이다"
    - "Pitfall 3 최소 가드: 쓰기 잡(`bids_commit`·`revert_only`·`revert_all`·`prep`)은 **전역에서 한 번에 하나만** 돈다. 두 번째 요청은 409 로 거부되고 사유가 화면에 뜬다"
    - "jobs 테이블에 pid 와 started_at 이 남는다 — Phase 2 의 고아 작업 정리(ENG-05)가 끼워 넣을 자리"
    - "웹앱이 자격증명을 env 로 자식에게 넘기지 않는다. CLI 가 `~/.eroom/naver-ads.json` 을 직접 읽으므로 넘길 필요가 없다"
  artifacts:
    - path: "webapp/argv.py"
      provides: "AdsArgv Pydantic 모델 + build() — argv 조립의 유일한 곳"
      contains: "PY_CLI"
    - path: "webapp/jobs.py"
      provides: "SQLite DDL · create_job() · spawn() · job_status() · 쓰기 잡 전역 가드"
      min_lines: 120
      contains: "start_new_session"
    - path: "webapp/routes/jobs.py"
      provides: "POST /jobs/prep · POST /jobs/run · GET /jobs/{id} (상태 JSON, 읽기)"
    - path: "webapp/tests/test_jobs.py"
      provides: "합성 잡으로 subprocess·상태·가드 검증"
  key_links:
    - from: "webapp/jobs.py create_job"
      to: "subprocess.Popen"
      via: "env PYTHONUNBUFFERED=1 · start_new_session=True · stdout 을 로그파일로 · stdin DEVNULL"
      pattern: "start_new_session=True"
    - from: "webapp/routes/jobs.py"
      to: "webapp/jobs.py create_job"
      via: "라우트는 Pydantic 요청 모델을 풀어 create_job 을 부르고 job_id 만 돌려준다"
      pattern: "create_job"
    - from: "webapp/argv.py PY_CLI"
      to: ".venv/bin/python3"
      via: "repo_root() / '.venv' / 'bin' / 'python3' — sys.executable 을 쓰지 않는다"
      pattern: "\\.venv/bin/python3"
---

<objective>
**보드에서 버튼을 눌러 CLI 작업을 돌린다.** 브라우저를 닫아도 자식이 살고, 화면은 즉시 돌아온다.

D-15 가 `prep`("새로 수집")을 먼저 고른 이유가 여기서 실현된다 — `prep` 은 광고를 바꾸지 않는
읽기 전용인데 수 분이 걸려서, **위험 0 인 대상으로 작업 엔진을 먼저 검증**할 수 있다.
입찰가 버튼(Plan 01-07/01-08)은 이렇게 검증된 엔진 위에 올라탄다.

이 플랜의 진행 표시는 **상태 폴링**까지다. 실시간 로그 스트리밍과 "탭 닫았다 이어보기"는 Plan 01-06.

Purpose: 이후 5개 버튼(Phase 2~7 포함)이 전부 쓸 실행 엔진을 굳힌다.
Output: `webapp/argv.py` · `webapp/jobs.py` · `POST /jobs/prep`·`/jobs/run` · 보드의 "새로 수집" 버튼 · 테스트 2종
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-board-bid-raise/01-VALIDATION.md
@.planning/phases/01-board-bid-raise/01-SKELETON.md

<interfaces>
이 플랜이 만드는 계약. Plan 01-06/07/08/09 와 Phase 2~7 의 모든 버튼이 여기에 올라탄다.

webapp/argv.py:
  PY_CLI: Path      # repo_root()/".venv"/"bin"/"python3"   ← .venv-web 이 아니다
  RUN_ADS: Path     # repo_root()/".claude"/"skills"/"naver-ads-weekly"/"scripts"/"run_ads.py"
  class AdsArgv(BaseModel):
      subcommand: Literal["prep","run","apply","bids","accounts"]
      run_dir: str | None = None
      accounts: list[str] = []
      commit: bool = False
      revert: bool = False
      only_ads: Path | None = None
      preview_out: Path | None = None
      prefix: list[str] = []          # Phase 2 의 caffeinate -i 자리
      def build(self) -> list[str]

webapp/jobs.py:
  JobKind = Literal["prep","run","bids_preview","bids_commit","revert_only","revert_all","synthetic"]
  WRITE_KINDS = {"prep","bids_commit","revert_only","revert_all"}   # 전역 1개 가드 대상
  init_db() -> None                   # CREATE TABLE IF NOT EXISTS + PRAGMA journal_mode=WAL
  create_job(kind, *, run_dir=None, accounts=None, only_ads=None, commit=False,
             parent_job_id=None, argv_override=None) -> str        # job_id (uuid4). HTTP 를 모른다
  job_status(job_id) -> dict | None   # jobs 테이블 한 행 + 살아있으면 pid 확인
  recent_jobs(limit=20) -> list[dict]
  log_path_of(job_id) -> Path
  targets_path_of(job_id) -> Path     # run-dir/web/targets_<job>.json
  result_path_of(job_id) -> Path      # run-dir/web/preview_<job>.json 또는 result_<job>.json
  class BusyError(RuntimeError)       # 쓰기 잡이 이미 돌 때

jobs 테이블 컬럼 (RESEARCH §5.7 그대로):
  id · kind · run_dir · accounts · argv · pid · status · exit_code · log_path ·
  targets_path · result_path · parent_job_id · target_count · started_at · ended_at
  status: running | done | failed | orphaned
  인덱스: idx_jobs_status(status) · idx_jobs_parent(parent_job_id)
  ※ board_cache 테이블은 만들지 마라. job_locks 는 Phase 2 다.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: webapp/argv.py — argv 조립의 유일한 곳</name>
  <files>webapp/argv.py, webapp/tests/test_argv.py</files>
  <read_first>
    - `.claude/skills/naver-ads-weekly/scripts/run_ads.py` 198-222행 — 서브파서 등록과 디스패치(이 계약의 반대편). Plan 01-02 패치 후 상태
    - `.claude/lib/eroomlib/runner/onestep.py` 36행(`PY = sys.executable`)과 365-371행(`_sh` 의 argv 한 줄 출력 관례) — **`sys.executable` 은 웹앱에서 쓰면 안 되는 반례**
    - `.claude/skills/sellerlife-keyword/scripts/run_all.py` 30·36행 — 같은 반례
    - `.planning/phases/01-board-bid-raise/01-RESEARCH.md` §5.5(AdsArgv 원형) · §5.8(caffeinate 프리픽스) · §1.1 의 `--account` 함정 · § Anti-Patterns
    - `webapp/paths.py` (repo_root)
  </read_first>
  <behavior>
    - `AdsArgv(subcommand="bids", run_dir="2026-08-30").build()[0]` 이 저장소의 `.venv/bin/python3` 절대경로다 (`.venv-web` 이 **아니다**)
    - `build()[1]` 이 `run_ads.py` 절대경로, `build()[2]` 가 서브커맨드다
    - `accounts=[]` 면 `--account` 플래그가 **아예 안 붙는다** (값 없는 `--account` 는 전 계정이 된다)
    - `accounts=["cy728","ownway1"]` 이면 `--account cy728 ownway1` 로 붙는다
    - `commit=False` 면 `--commit` 문자열이 argv 어디에도 없다
    - `only_ads`/`preview_out` 은 `Path` 를 문자열로 붙인다
    - `prefix=["/usr/bin/caffeinate","-i"]` 면 그 둘이 argv **맨 앞**에 온다
    - `AdsArgv(subcommand="drop")` 은 Pydantic `ValidationError` — Literal 밖의 서브커맨드는 조립되지 않는다
    - `build()` 결과에 셸 메타문자를 해석할 여지가 없다 (리스트 argv, `shell=False`)
  </behavior>
  <action>
    `webapp/argv.py` 를 쓴다. 모듈 docstring 에 **"argv 를 조립하는 곳은 여기 하나다. 다른 파일에서 문자열로 명령을 만들지 마라 — 명령 주입 경로가 거기서 생긴다"** 를 적는다.

    `PY_CLI = paths.repo_root() / ".venv" / "bin" / "python3"` 로 못박는다. **`sys.executable` 을 쓰지 마라** — 웹앱은 `.venv-web` 에서 돌고, CLI 는 selenium·openpyxl 이 깔린 `.venv` 가 필요하다. 저장소의 다른 스크립트들(`onestep.py:36`, `run_all.py:30`)이 `sys.executable` 을 쓰는 건 그들이 같은 venv 안에서 돌기 때문이다 — 웹앱은 다르다. 이 차이를 주석으로 남겨라.

    `RUN_ADS = paths.repo_root() / ".claude" / "skills" / "naver-ads-weekly" / "scripts" / "run_ads.py"`.

    `AdsArgv` 를 Pydantic `BaseModel` 로 정의한다. 필드는 interfaces 블록 그대로. `subcommand` 는 `Literal["prep","run","apply","bids","accounts"]` — 이 화이트리스트가 명령 주입의 1차 방어선이다. `accounts: list[str]` 에는 `max_length` 를 두지 말고(계정이 4→6 으로 는다), 대신 각 alias 가 영숫자·언더스코어·하이픈만 담도록 패턴 검증을 건다.

    `build()` 는 `[*self.prefix, str(PY_CLI), str(RUN_ADS), self.subcommand]` 로 시작해서 플래그를 순서대로 덧붙인다. `run_dir` 이 있으면 `--run-dir <값>`, **`accounts` 가 비어 있지 않을 때만** `--account` 와 값들, `commit` 이면 `--commit`, `revert` 면 `--revert`, `only_ads` 면 `--only-ads <경로>`, `preview_out` 이면 `--preview-out <경로>`. `--account` 빈 리스트 함정(값 없이 붙이면 `[]` → falsy → 전 계정)은 주석으로 근거를 남겨라.

    `prefix` 필드는 Phase 2 의 `caffeinate -i` 자리다. 지금은 항상 빈 리스트지만 **존재해야** Phase 2 가 한 줄로 끝난다. 주석에 `["/usr/bin/caffeinate", "-i"]` 예시를 적어라.

    `webapp/tests/test_argv.py` 를 behavior 9개 항목에 1:1 대응하게 쓴다. **argv 를 실행하지 않고 문자열로만 검증한다** — 이게 "크레딧·광고비 0 으로 쓰기 경로를 검증하는 3층" 중 웹앱 층이다. 테스트에 `--commit` 리터럴을 쓰지 마라(`no_commit_guard.sh` 가 스캔한다). `commit=True` 를 검증할 때는 `"--" + "commit"` 처럼 나눠 쓰거나, `argv` 에 `commit` 이라는 부분문자열이 있는지로 단언해라 — 그 이유를 주석으로 남겨라.
  </action>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_argv.py -x -q && bash webapp/tests/no_commit_guard.sh</automated>
  </verify>
  <acceptance_criteria>
    - 아래 명령이 exit 0 으로 통과한다:
      ```
      .venv-web/bin/pytest webapp/tests/test_argv.py -x -q && bash webapp/tests/no_commit_guard.sh
      ```
    - `01-VALIDATION.md § Per-Task Verification Map` 의 해당 행이 지목한 **테스트 파일**이 위 명령에 들어 있고,
      그 행의 `핵심 노드` 테스트가 이 실행 안에서 돈다 (VALIDATION 은 파일 단위로 적고, 노드 ID 는 병기만 한다 —
      태스크는 파일 전체를 돌린다. 노드 하나만 돌리는 것보다 회귀 방어가 낫기 때문이다)
  </acceptance_criteria>
  <done>test_argv.py 전량 통과. `grep -rn 'sys.executable' webapp/` 0건. `--commit` 리터럴 0건.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: webapp/jobs.py — SQLite 레지스트리 · create_job · spawn · 쓰기 잡 전역 가드</name>
  <files>webapp/jobs.py, webapp/tests/test_jobs.py</files>
  <read_first>
    - `.planning/phases/01-board-bid-raise/01-RESEARCH.md` §5.1(PYTHONUNBUFFERED 실측: 0.5초 시점 0바이트 vs 14바이트) · §5.2(start_new_session 실측 2회) · §5.3(spawn 코드) · §5.5(create_job 시그니처) · §5.7(SQLite DDL) · Pitfall 3(동시 쓰기로 백업 손실 — **이 플랜이 반드시 태스크로 넣어야 한다**)
    - `.claude/skills/sellerlife-keyword/scripts/run_all.py` 33-48행 — 유일한 Popen analog. **argv/env/인코딩 관례만 가져오고 파이프 직독은 베끼지 마라**(PATTERNS §C-3: 파이프 직결은 ENG-03 을 깨뜨린다)
    - `.claude/skills/bulsaja-detail-page/scripts/run_yong.py` 7·49-56행 — env 병합(`{**os.environ, ...}`, 통째 교체 금지)과 "토큰 값은 절대 출력하지 않는다"
    - `.claude/skills/coupang-candidates/scripts/run_coupang.py` 68-74행(원자적 쓰기) · 77-94행(append + flush + fsync)
    - `webapp/argv.py`(Task 1) · `webapp/paths.py` · `webapp/settings.py`
  </read_first>
  <behavior>
    - `test_create_job_은_http_없이_돈다` — TestClient 없이 `jobs.create_job("synthetic", ...)` 을 직접 불러 job_id 를 받는다 (ENG-07 / D-17)
    - `test_자식은_서버_종료를_견딘다` — 합성 잡을 띄운 뒤 부모의 프로세스 그룹에 시그널을 보내도 자식 pid 가 살아 있다 (`start_new_session=True`)
    - `test_로그가_실시간으로_쌓인다` — 합성 잡 시작 후 **작업이 끝나기 전 중간 시점**에 로그파일 크기가 이미 0보다 크다 (`PYTHONUNBUFFERED=1`. 이 환경변수를 빼면 이 테스트가 반드시 빨개져야 한다)
    - `test_쓰기잡은_동시에_두개가_안된다` — `prep` 잡이 running 인 동안 두 번째 쓰기 잡 생성이 `BusyError` (Pitfall 3)
    - `test_미리보기는_가드를_안탄다` — `bids_preview` 는 쓰기가 없으므로 쓰기 잡이 돌고 있어도 생성된다
    - `test_argv_에_시크릿이_없다` — jobs 테이블의 argv 컬럼에 `api_key`/`secret_key` 문자열이 없다
    - `test_잡이_끝나면_exit_code_가_기록된다` — 합성 잡 종료 후 status 가 `done`, exit_code 가 0
    - `test_실패한_잡은_failed_다` — exit 1 로 끝나는 합성 잡의 status 가 `failed`
  </behavior>
  <action>
    `webapp/jobs.py` 를 쓴다. 모듈 docstring 을 S-9 관례대로 시작한다 — 어떤 kind 가 읽기고 어떤 게 쓰기인지 표로 적어라(`prep`=읽기지만 API POST 가 있다, `run`·`bids_preview`=순수 읽기, `bids_commit`·`revert_*`=쓰기).

    DB 초기화: `init_db()` 가 `PRAGMA journal_mode = WAL` 과 RESEARCH §5.7 의 DDL(`CREATE TABLE IF NOT EXISTS jobs (...)` + 인덱스 2개)을 실행한다. **`board_cache` 테이블을 만들지 마라** — 보드는 매번 `result.json` 을 투영한다. **`job_locks` 는 Phase 2 다.** 커넥션은 작업마다 새로 열고 `timeout=5` 를 준다. DB 파일 경로는 `settings` 의 `db_path` 에서 온다.

    `spawn(argv, log_path)` 를 RESEARCH § Code Examples "자식 띄우기" 그대로 쓴다. `open(log_path, "ab", buffering=0)` 으로 append-only 로그파일을 열고, `subprocess.Popen(argv, cwd=paths.repo_root(), env={**os.environ, "PYTHONUNBUFFERED": "1"}, stdout=fh, stderr=subprocess.STDOUT, stdin=subprocess.DEVNULL, start_new_session=True, close_fds=True)`. 네 인자 각각의 근거를 한국어 docstring 에 실측과 함께 남겨라:
    - `PYTHONUNBUFFERED`: 없으면 0.5초 시점 로그파일이 **0바이트**고 종료 시 한꺼번에 떨어진다(실측). CLI 전체에 `flush=True` 가 `prune.py:115` 한 줄뿐이라 CLI 수정으로는 못 푼다. 10분짜리 `prep` 의 로그가 10분 내내 빈 화면이 된다.
    - `start_new_session`: `uvicorn --reload` 재시작과 서버 종료에서 자식을 살린다(실측 2회 — pgid 분리 자식만 생존).
    - `stdin=DEVNULL`: 자식이 입력을 기다리며 영원히 멈추는 사고를 막는다.
    - `cwd=repo_root()`: `run_ads.py` 의 `lib/eroomlib` 탐색이 여기 의존한다.
    - `env` 는 **병합**이지 교체가 아니다. 그리고 **자격증명을 env 로 넘기지 마라** — `ps -E` 에 뜬다. `nvad.py` 가 파일을 직접 읽으므로 넘길 필요가 없다.
    파일 핸들 열기 실패는 `RuntimeError(f"로그 파일을 못 연다: {e}")` 로 올리고, `Popen` 예외 시 `fh.close()` 를 보장해라.

    `create_job(kind, *, run_dir=None, accounts=None, only_ads=None, commit=False, parent_job_id=None, argv_override=None)` 을 쓴다. **이 함수는 HTTP 를 모른다** — `Request`·`Response`·FastAPI 타입을 import 하지 마라. v2 의 APScheduler 가 이걸 그대로 부른다(D-17 / ENG-07). docstring 에 그 사실을 명시해라.

    순서: ① `kind in WRITE_KINDS` 면 **쓰기 잡 전역 가드**를 건다 — `SELECT COUNT(*) FROM jobs WHERE status='running' AND kind IN (...)` 가 0 이 아니면 `BusyError("이미 도는 쓰기 작업이 있다: <id> (<kind>)")`. ② `run_dir` 이 있으면 `paths.run_dir_path(run_dir)` 화이트리스트를 통과시킨다. ③ `only_ads` 목록이 있으면 `run-dir/web/targets_<job_id>.json` 에 **원자적 쓰기**(tmp → `os.replace`, `ensure_ascii=False`)로 떨군다 — 이게 FLOW-02 의 '그 파일'이다. ④ `AdsArgv(...).build()` 로 argv 를 만든다. ⑤ jobs 행을 `status='running'` 으로 INSERT 하고 argv 를 JSON 배열로 저장한다(재현·감사용, 시크릿 없음). ⑥ `spawn()` 하고 pid 를 UPDATE 한다. ⑦ `job_id` 를 돌려준다. **호출부를 블로킹하지 않는다.**

    Pitfall 3 가드에 대한 주석을 반드시 남겨라: **"ENG-04(대상별 잠금)는 Phase 2 지만 위험은 Phase 1 에 있다. `run_bids` 가 `before_bids_<alias>.json` 과 `ledger/<alias>.json` 을 읽고-병합하고-통째로 다시 쓰는데 락이 없다. 쓰기 잡 두 개가 겹치면 백업 항목이 사라지고 그 소재는 영영 되돌릴 수 없다. 여기서는 전역 1개로 막고, Phase 2 가 대상별 락으로 넓힌다."** 미리보기(`bids_preview`)와 판정(`run`)은 쓰기가 없으니 가드에서 뺀다.

    `job_status(job_id)` 는 jobs 행을 돌려주되, `status='running'` 인데 pid 가 죽었으면 종료를 감지한다. 종료 감지는 `os.kill(pid, 0)` 로 생존을 보고, 죽었으면 `os.waitpid` 가 아니라(자식이 세션 분리돼 있다) 로그파일 마지막 상태로 판단하지 말고, **`Popen` 객체를 프로세스 메모리에 딕셔너리로 들고 있다가 `poll()`** 로 exit_code 를 읽어 UPDATE 한다. `--workers 1` 이라 이 인메모리 맵이 성립한다(워커가 갈라지면 깨진다 — Pitfall 9 근거를 주석에 남겨라). 프로세스 메모리에 없는데 pid 가 죽어 있으면 `status='orphaned'` 로 두고 Phase 2 에 넘긴다.

    `recent_jobs(limit)` · `log_path_of` · `targets_path_of` · `result_path_of` 를 구현한다. 로그 디렉터리는 `settings` 의 `job_log_dir`(기본 `webapp-logs/`), 산출물은 `run-dir/web/` 아래.

    합성 잡(`kind="synthetic"`)은 `argv_override` 로 argv 를 직접 받는 경로다. **테스트 전용이다** — 운영 라우트(`webapp/routes/jobs.py`)에 노출하지 마라. docstring 에 그 사실을 적고, 라우트에서 `kind` 를 받을 때 `synthetic` 을 거부해라.

    `webapp/tests/test_jobs.py` 를 behavior 8개 항목에 1:1 대응하게 쓴다. `synthetic_job` 픽스처(Plan 01-01)를 쓴다. `test_로그가_실시간으로_쌓인다` 는 잡 시작 후 약 0.5초 시점에 `log_path.stat().st_size > 0` 을 단언한다 — **잡이 끝난 뒤에 재면 `PYTHONUNBUFFERED` 없이도 통과해버려 테스트가 무의미해진다.** 그 이유를 주석으로 남겨라. 테스트는 네트워크를 쓰지 않고 3초 안에 끝난다.
  </action>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_jobs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 아래 명령이 exit 0 으로 통과한다:
      ```
      .venv-web/bin/pytest webapp/tests/test_jobs.py -x -q
      ```
    - `01-VALIDATION.md § Per-Task Verification Map` 의 해당 행이 지목한 **테스트 파일**이 위 명령에 들어 있고,
      그 행의 `핵심 노드` 테스트가 이 실행 안에서 돈다 (VALIDATION 은 파일 단위로 적고, 노드 ID 는 병기만 한다 —
      태스크는 파일 전체를 돌린다. 노드 하나만 돌리는 것보다 회귀 방어가 낫기 때문이다)
  </acceptance_criteria>
  <done>test_jobs.py 전량 통과. `grep -c 'board_cache\|job_locks' webapp/jobs.py` 0. `grep -c 'fastapi\|Request' webapp/jobs.py` 0 (create_job 이 HTTP 를 모른다).</done>
</task>

<task type="auto">
  <name>Task 3: POST /jobs/prep · /jobs/run + 보드의 "새로 수집" 버튼 + 상태 폴링</name>
  <files>webapp/routes/jobs.py, webapp/templates/board.html, webapp/templates/_job_panel.html</files>
  <read_first>
    - `webapp/routes/jobs.py` (Plan 01-03 이 만든 빈 router 와 "모든 작업 생성은 POST 다" docstring)
    - `webapp/templates/board.html` · `_job_panel.html` (Plan 01-03/01-04 산출물)
    - `webapp/jobs.py`(Task 2) · `webapp/security.py`
    - `.planning/phases/01-board-bid-raise/01-RESEARCH.md` §5.5(HTTP 핸들러는 딱 이만큼) · §4.3(상태를 바꾸는 GET 금지) · § Security Domain V5(Pydantic 입력검증, ad_ids 길이 상한과 `^nad-` 패턴) · §1.1(`prep` 은 ~3분/계정)
    - `.claude/skills/naver-ads-weekly/SKILL.md` — `prep`/`run` 서브커맨드 운영 의미
  </read_first>
  <action>
    `webapp/routes/jobs.py` 를 채운다. **모든 작업 생성은 POST 다** — GET 으로 만들면 Origin 방어가 통째로 무력화된다(교차 사이트 단순 GET 에는 Origin 헤더가 없다).

    요청 모델을 Pydantic 으로 정의한다. `PrepReq(run_dir: str | None = None, accounts: list[str] = [])`, `RunReq` 도 같은 모양. `accounts` 각 항목에 영숫자·언더스코어·하이픈 패턴을 건다. `run_dir` 은 라우트가 아니라 `create_job` 안의 `paths.run_dir_path()` 화이트리스트가 검증한다.

    `POST /jobs/prep` 과 `POST /jobs/run` 은 **얇다**: 요청 모델을 풀어 `jobs.create_job("prep", run_dir=..., accounts=...)` 을 부르고 `{"job_id": ...}` 를 돌려준다. `BusyError` 는 `HTTPException(status_code=409, detail="이미 도는 쓰기 작업이 있다 — 끝나고 다시 눌러라")` 로 번역한다. `ValueError`(모르는 회차)는 400. 핸들러는 `async def` 가 아니라 `def` 로 선언한다.

    `kind` 를 클라이언트가 문자열로 넘기게 만들지 마라 — 라우트마다 kind 가 고정이다. 특히 `synthetic` 이 외부에서 들어올 경로를 만들지 마라.

    `GET /jobs/{job_id}` 는 **읽기 전용 상태 JSON** 이다: `{"id","kind","status","exit_code","started_at","ended_at","run_dir","target_count","elapsed_sec"}`. `elapsed_sec` 은 `started_at` 부터의 경과초 — OQ-3 확정("`bids --commit` 진행률이 없으니 화면에 경과시간으로 덮는다")이 여기서 데이터를 받는다. 이 GET 은 작업을 만들지 않는다 — 주석으로 못박아라.

    `GET /jobs` 는 `recent_jobs(20)` 을 돌려주는 읽기 라우트.

    `webapp/templates/_job_panel.html` 을 채운다. 현재 작업의 kind · 상태 · 경과시간 · exit code 를 보여주고, htmx `hx-get="/jobs/{{ job_id }}"` + `hx-trigger="every 2s"` 로 폴링한다. 상태가 `done`/`failed` 가 되면 폴링을 멈춘다(htmx 응답에서 `hx-trigger` 를 제거하거나 `HX-Trigger` 헤더로 끊는다). 실시간 로그 자리는 **빈 채로** 두고 HTML 주석에 "Plan 01-06 이 SSE 로 채운다" 를 적는다.

    `webapp/templates/board.html` 상단 신선도 배너 옆에 **"새로 수집" 버튼**을 넣는다. `hx-post="/jobs/prep"`, `hx-target="#job-panel"`, `hx-swap="innerHTML"`. 계정 선택 없이 전 계정이 기본이고, 계정 필터가 걸려 있으면 그 계정만 넘기는 옵션을 붙인다. 버튼 옆에 반말 안내를 한 줄 둔다: **`prep` 은 계정당 3분쯤 걸린다. 광고를 바꾸지는 않는다 — 소재·통계를 새로 받아올 뿐이다.** ("광고 API 에 아무것도 쓰지 않는다"로 쓰지 마라 — `prep` 은 `POST /stat-reports` 로 리포트 잡을 만든다. OQ-4 확정 문구다.)

    "판정 다시" 버튼(`hx-post="/jobs/run"`)도 같이 둔다 — `run` 은 디스크만 만지고 초 단위로 끝난다.

    두 버튼 모두 409 응답(이미 도는 작업)을 화면에 사유와 함께 띄운다. htmx 는 기본적으로 4xx 를 스왑하지 않으므로 `hx-on::after-request` 또는 `htmx:responseError` 처리로 배너를 띄워라.
  </action>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests -x -q && .venv-web/bin/python -c "
from fastapi.testclient import TestClient
from webapp import settings, security
from webapp.main import app
c = TestClient(app, base_url=f'http://127.0.0.1:{settings.PORT}')
h = {'Origin': f'http://127.0.0.1:{settings.PORT}', security.HEADER_NAME: security.BOOT_TOKEN}
assert c.post('/jobs/prep', json={}).status_code == 403
r = c.post('/jobs/prep', json={'run_dir': 'nope'}, headers=h)
assert r.status_code in (400, 409), r.status_code
print('PASS', r.status_code)
"</automated>
  </verify>
  <acceptance_criteria>
    - 아래 명령이 exit 0 으로 통과한다:
      ```
      .venv-web/bin/pytest webapp/tests -x -q && .venv-web/bin/python -c "
      from fastapi.testclient import TestClient
      from webapp import settings, security
      from webapp.main import app
      c = TestClient(app, base_url=f'http://127.0.0.1:{settings.PORT}')
      h = {'Origin': f'http://127.0.0.1:{settings.PORT}', security.HEADER_NAME: security.BOOT_TOKEN}
      assert c.post('/jobs/prep', json={}).status_code == 403
      r = c.post('/jobs/prep', json={'run_dir': 'nope'}, headers=h)
      assert r.status_code in (400, 409), r.status_code
      print('PASS', r.status_code)
      "
      ```
    - `01-VALIDATION.md § Per-Task Verification Map` 의 해당 행이 지목한 **테스트 파일**이 위 명령에 들어 있고,
      그 행의 `핵심 노드` 테스트가 이 실행 안에서 돈다 (VALIDATION 은 파일 단위로 적고, 노드 ID 는 병기만 한다 —
      태스크는 파일 전체를 돌린다. 노드 하나만 돌리는 것보다 회귀 방어가 낫기 때문이다)
  </acceptance_criteria>
  <done>토큰 없는 `POST /jobs/prep` 은 403, 모르는 회차는 400(또는 이미 도는 작업이면 409). 보드에 "새로 수집"·"판정 다시" 버튼이 있고 작업 패널이 2초 폴링한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP 요청 → argv | 사용자 입력이 자식 프로세스의 명령줄에 닿는 유일한 경로 |
| 웹앱 프로세스 → CLI 서브프로세스 | 자격증명 경계. CLI 만 시크릿을 쥔다 |
| 동시 요청 → 공유 상태 파일 | `before_bids_*.json` / `ledger/*.json` 은 read-modify-write 이고 락이 없다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-09 | Tampering (백업 손실) | `jobs.create_job` 의 쓰기 잡 가드 | mitigate | **Pitfall 3.** `kind in WRITE_KINDS` 면 `SELECT COUNT(*) FROM jobs WHERE status='running' AND kind IN (...)` 이 0 일 때만 생성. 아니면 `BusyError` → 409. `run_bids` 가 백업·ledger 를 읽고-병합하고-통째로 쓰는데 락이 없어서, 겹치면 백업 항목이 사라지고 그 소재는 영영 못 되돌린다. ENG-04(대상별 잠금) 전체는 Phase 2. 검증: `test_쓰기잡은_동시에_두개가_안된다` |
| T-1-10 | Elevation of Privilege | `webapp/argv.py` | mitigate | argv 조립이 `AdsArgv.build()` 한 곳에서만. `shell=False` + 리스트 argv + `subcommand` 를 `Literal` 화이트리스트로 제한 + 계정 alias 패턴 검증. 문자열로 명령을 만드는 코드가 저장소에 없어야 한다 |
| T-1-11 | Tampering | `create_job` 의 `run_dir` | mitigate | `paths.run_dir_path()` 화이트리스트를 `create_job` **안에서** 통과시킨다. 라우트가 깜빡해도 막힌다. `--only-ads`/`--preview-out` 경로는 **웹앱이 생성**한다 — 사용자 입력에서 경로를 받지 않는다 (ASVS V12) |
| T-1-03c | Info Disclosure | 자식 env · jobs.argv 컬럼 | mitigate | 자격증명을 env 로 넘기지 않는다(`ps -E` 노출). CLI 가 `~/.eroom/naver-ads.json` 을 직접 읽으므로 넘길 필요가 없다 — 더 안전한 쪽으로 이미 기울어 있다. argv 컬럼에 시크릿이 들어갈 설계를 만들지 않는다. 검증: `test_argv_에_시크릿이_없다` |
| T-1-01b | Tampering | 작업 생성 라우트 메서드 | mitigate | 작업을 만드는 모든 라우트가 POST. `GET /jobs/{id}` 와 `GET /jobs` 는 읽기 전용이고 `create_job`/`spawn` 을 부르지 않는다. 검증: V-SAFE-01d grep 가드 |
| T-1-22 | DoS (자해) | 인메모리 `Popen` 맵 | accept | `--workers 1` 전제. 워커가 갈라지면 exit_code 감지가 깨진다 — `run-webapp.sh` 고정과 기동 경고로 방어하고, 고아 상태(`orphaned`) 복구는 Phase 2(ENG-05) |
| T-1-23 | Tampering | 합성 잡(`synthetic`) 경로 | mitigate | `argv_override` 로 임의 argv 를 실행할 수 있는 유일한 경로다. **운영 라우트에 노출하지 않는다** — 라우트마다 kind 가 고정이고 클라이언트가 kind 를 문자열로 넘기지 못한다 |
</threat_model>

<verification>
- `.venv-web/bin/pytest webapp/tests -q` 전량 green
- `bash webapp/tests/no_commit_guard.sh` exit 0
- `grep -rnE '@(app|router)\.get' webapp/routes/jobs.py` 가 상태 조회 2개뿐이고 어느 것도 `create_job` 을 부르지 않음을 눈으로 확인
- `CT_DEV_TOKEN=devtoken123 bash webapp/tests/security_curl.sh` — 8종 PASS (이제 `POST /jobs/bids/preview` 는 아직 404 지만 403 가드는 살아 있어야 한다)
- `for t in nvad reports ads_rules ledger bids prune; do .venv/bin/python3 .claude/skills/naver-ads-weekly/scripts/test_$t.py || exit 1; done` — CLI 기준선 불변
</verification>

<success_criteria>
- `jobs.create_job(...)` 이 HTTP 없이 호출되고 job_id 를 즉시 돌려준다
- 자식이 `.venv/bin/python3` + `run_ads.py` 로, `PYTHONUNBUFFERED=1` · `start_new_session=True` 로 뜬다
- 작업 중간 시점에 로그파일이 이미 커져 있다
- 쓰기 잡이 돌고 있으면 두 번째 쓰기 잡이 409 로 거부된다
- 보드의 "새로 수집" 버튼이 작업을 띄우고 화면이 즉시 돌아오며, 작업 패널이 상태·경과시간을 2초마다 갱신한다
</success_criteria>

<output>
Create `.planning/phases/01-board-bid-raise/01-05-SUMMARY.md` when done
</output>
