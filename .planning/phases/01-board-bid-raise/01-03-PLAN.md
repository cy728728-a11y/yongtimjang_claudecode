---
phase: 01-board-bid-raise
plan: 03
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - webapp/security.py
  - webapp/main.py
  - webapp/routes/__init__.py
  - webapp/routes/board.py
  - webapp/routes/jobs.py
  - webapp/routes/health.py
  - webapp/templates/board.html
  - webapp/templates/_job_panel.html
  - webapp/templates/_preview_table.html
  - webapp/run-webapp.sh
  - webapp/tests/test_security.py
  - webapp/tests/security_curl.sh
autonomous: false
requirements: [SAFE-01, SAFE-02, SAFE-03]

must_haves:
  truths:
    - "사용자가 `webapp/run-webapp.sh` 를 돌리면 서버가 127.0.0.1:8765 에만 뜨고, 기동 로그가 붙여넣을 URL(`http://127.0.0.1:8765/?t=<토큰>`)을 한 줄 찍는다"
    - "그 URL 로 들어가면 쿠키가 심기고 주소창이 깨끗한 `/` 로 바뀐다 — 토큰이 히스토리에 남지 않는다"
    - "토큰 없이 `/` 를 열면 403 이다"
    - "`Host: evil.com` 으로 오는 요청은 400 이다 (DNS rebinding)"
    - "다른 탭의 임의 사이트가 `Origin: https://evil.com` 으로 쏘는 POST 는 올바른 토큰을 갖고 있어도 403 이다"
    - "토큰 없는 POST · 틀린 토큰 POST 는 403 이고, 올바른 Origin + 올바른 토큰 POST 는 통과한다"
    - "상태를 바꾸는 GET 엔드포인트가 하나도 없다 — Origin 방어의 전제다"
    - "화면·로그·템플릿 어디에도 광고 시크릿이 나오지 않는다. 웹앱은 `~/.eroom/naver-ads.json` 을 열지 않고, 불사자 토큰은 이 단계에서 아예 로드하지 않는다"
  artifacts:
    - path: "webapp/security.py"
      provides: "BOOT_TOKEN · guard 미들웨어 · scrub 시크릿 스크러버 · page_cookie_ok"
      contains: "compare_digest"
    - path: "webapp/main.py"
      provides: "FastAPI 조립 + 미들웨어 3층 + 라우터 등록 + StaticFiles + Jinja2Templates"
      contains: "TrustedHostMiddleware"
    - path: "webapp/run-webapp.sh"
      provides: "127.0.0.1 바인드 + workers 1 고정 기동"
      contains: "workers 1"
    - path: "webapp/templates/board.html"
      provides: "토큰을 hx-headers 로 심는 셸 레이아웃 + 잡 패널/미리보기 partial include 자리"
      contains: "hx-headers"
    - path: "webapp/tests/security_curl.sh"
      provides: "보안 8종 curl 검증 (실행 중인 서버 대상)"
  key_links:
    - from: "webapp/main.py"
      to: "starlette.middleware.trustedhost.TrustedHostMiddleware"
      via: "allowed_hosts=['127.0.0.1','localhost'], www_redirect=False"
      pattern: "TrustedHostMiddleware"
    - from: "webapp/templates/board.html"
      to: "모든 htmx 쓰기 요청"
      via: "body 태그의 hx-headers 에 X-CT-Token 을 심는다"
      pattern: "X-CT-Token"
    - from: "webapp/security.py"
      to: "webapp/settings.py PORT"
      via: "allowed_origins 를 설정 포트에서 조립 (8765 를 박지 않는다)"
      pattern: "settings.PORT"
---

<objective>
**서버가 뜨고, 나 말고는 아무도 못 들어온다.** 성공기준 5 의 절반이 여기서 끝난다.

이 플랜이 끝나면 사용자는 실제로 브라우저를 열어 `http://127.0.0.1:8765/?t=…` 로 들어가
빈 보드 셸을 본다. 토큰 없이는 403, 다른 탭에서 쏘는 POST 는 403, `Host: evil.com` 은 400 이다.

RESEARCH §4 의 코드는 이 맥북에서 curl 6케이스를 이미 통과시킨 실측본이다. 새로 설계하지 말고 그걸 조립해라.

**파일 수가 임계를 넘는 이유(12파일 / 임계 10) — 다음 phase 가 이 크기를 표준으로 착각하지 마라.**
실제 로직은 `security.py` · `main.py` 둘이다. 나머지는 **빈 스텁**이다 — 라우터 3개(`board.py`·`jobs.py`·`health.py`)와
템플릿 3개(`board.html`·`_job_panel.html`·`_preview_table.html`)는 Plan 01-04~01-08 이 채워 넣을 **자리만 먼저 파는 것**이고,
`routes/__init__.py` 는 빈 파일, `run-webapp.sh` 는 기동 한 줄이다. 보안 3층은 **앱 셸이 서는 순간 같이 서야** 의미가 있다 —
나중에 얹으면 라우트마다 뚫린 구멍을 찾아다니게 된다. 그래서 쪼개지 않는다.

Purpose: 이후 모든 버튼이 올라탈 보안 3층과 앱 셸을 굳힌다. 나중에 얹으면 라우트마다 뚫린 구멍을 찾아다녀야 한다.
Output: security.py · main.py · 라우터 3개(스텁 포함) · 템플릿 3개 · 기동 스크립트 · 보안 테스트 2종
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-board-bid-raise/01-VALIDATION.md
@.planning/phases/01-board-bid-raise/01-SKELETON.md

<interfaces>
이 플랜이 만드는 계약. Plan 01-04/05/06/07 이 여기에 라우트와 partial 을 채운다.

webapp/security.py 가 내보내는 것:
  BOOT_TOKEN: str                       — secrets.token_urlsafe(32). CT_DEV_TOKEN 환경변수가 있으면 그 값(개발용)
  COOKIE_NAME = "ct_session"
  HEADER_NAME = "X-CT-Token"
  SAFE_METHODS = {"GET", "HEAD", "OPTIONS"}
  allowed_origins() -> set[str]         — settings.PORT 에서 조립
  guard(request, call_next)             — 쓰기 메서드 전용 Origin + 토큰 검사 미들웨어
  page_cookie_ok(request) -> bool
  register_secret(value: str) -> None
  scrub(text: str) -> str               — 등록된 시크릿 값을 *** 로 치환

webapp/main.py 가 내보내는 것:
  app: FastAPI                          — docs_url=None, redoc_url=None
  templates: Jinja2Templates            — 다른 라우터 모듈이 import 해서 쓴다
  라우터 등록 순서: health → board → jobs

webapp/routes/board.py:  router (APIRouter). GET "/" 만. Plan 01-04 가 내용을 채운다.
webapp/routes/jobs.py:   router (APIRouter). Plan 01-05 가 POST/SSE 라우트를 채운다.
webapp/routes/health.py: router. GET "/healthz" — 토큰 불필요, 쓰기 아님.

템플릿 계약:
  board.html            — body 태그에 hx-headers 로 토큰을 심는 셸.
                          _job_panel.html 과 _preview_table.html 을 include 하는 자리를 이미 갖는다
  _job_panel.html       — Plan 01-05 가 SSE 배선을 채운다
  _preview_table.html   — Plan 01-06 이 미리보기 표를 채운다
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: webapp/security.py — 부팅 토큰 · Origin 가드 · 시크릿 스크러버</name>
  <files>webapp/security.py, webapp/tests/test_security.py</files>
  <read_first>
    - `.planning/phases/01-board-bid-raise/01-RESEARCH.md` §4.1~4.5 전체 — curl 6케이스 실측 통과본, TrustedHostMiddleware 가 포트를 떼고 매칭한다는 소스 확인, 쿠키 vs 헤더 역할 분리 근거, 누출 지점 전수조사(소스 전체에서 시크릿은 `nvad.py:46-48` 3줄뿐)
    - `.claude/skills/bulsaja-detail-page/scripts/detail_batch.py` 69-78행 `sanitize()` — 저장소의 유일한 스크러버 analog (값 축약 + 깊이 제한)
    - `.claude/skills/naver-ads-weekly/scripts/bids.py` 65·86행 `str(res)[:150]` — 에러 축약 관례
    - `webapp/settings.py` (Plan 01-01 산출물) — PORT 를 여기서 읽는다
  </read_first>
  <behavior>
    - `Host: evil.com` 으로 오는 GET / → 400
    - `Host: 127.0.0.1:8765` 는 통과 (미들웨어가 포트를 떼고 매칭한다)
    - 토큰 없는 POST /jobs/bids/preview → 403
    - 틀린 토큰 POST → 403
    - `Origin: https://evil.com` + 올바른 토큰 POST → 403 (Origin 이 토큰보다 먼저 걸린다)
    - `Origin: http://127.0.0.1:8765` + 올바른 토큰 POST → 403 이 아니다
    - Origin 헤더가 없고 `Sec-Fetch-Site: cross-site` 인 POST → 403
    - GET 은 토큰·Origin 검사를 타지 않는다 (쿠키 게이트는 라우트 레벨)
    - `scrub()` 이 등록된 시크릿 문자열을 별표로 치환한다
    - `test_시크릿이_새지_않는다` — `~/.eroom/naver-ads.json` 의 첫 계정 secret_key 값이 GET / 응답 본문과 `webapp/templates/` 전체에서 0건
  </behavior>
  <action>
    `webapp/security.py` 를 쓴다. 모듈 최상단 한국어 docstring 을 저장소 S-9 관례대로 "무엇이 읽기고 무엇이 쓰기인지"로 시작한다.

    `BOOT_TOKEN` 은 `os.environ.get("CT_DEV_TOKEN") or secrets.token_urlsafe(32)` 로 정한다. 운영 기본은 랜덤이고 서버 재시작마다 바뀐다 — 그게 의도다(`--reload` 개발 중 편의를 위해서만 환경변수로 고정). 토큰을 파일로 떨구지 마라. 메모리에만 둔다.

    `allowed_origins()` 는 `http://127.0.0.1:<settings.PORT>` 와 `http://localhost:<settings.PORT>` 두 개를 담은 set 을 돌려준다. 8765 를 리터럴로 쓰지 마라 — 포트가 바뀌면 방어가 조용히 깨진다.

    `guard(request, call_next)` 를 `@app.middleware("http")` 로 붙일 async 함수로 쓴다. 순서가 중요하다. `request.method not in SAFE_METHODS` 일 때만 검사하고, 첫째 `origin is not None and origin not in allowed_origins()` 면 `PlainTextResponse("origin 거부", status_code=403)`, 둘째 `origin is None and request.headers.get("sec-fetch-site") not in (None, "same-origin")` 이면 `PlainTextResponse("sec-fetch-site 거부", status_code=403)`, 셋째 `secrets.compare_digest(request.headers.get("x-ct-token") or "", BOOT_TOKEN)` 이 거짓이면 `PlainTextResponse("토큰 거부", status_code=403)`. 토큰 비교에 `==` 를 쓰지 마라 — `secrets.compare_digest` 다(타이밍 공격, 비용 0). 함수 docstring 에 "쓰기 메서드만 검사한다. 상태를 바꾸는 GET 을 만들지 않는 것이 이 방어의 전제다" 를 남긴다.

    왜 쿠키가 아니라 헤더로 쓰기를 인가하는지를 주석으로 남긴다: 쿠키는 브라우저가 자동으로 붙여서 CSRF 방어가 되지 않는다. 커스텀 헤더는 교차 오리진에서 붙일 수 없다(CORS preflight 가 필요한데 CORS 를 아예 안 열었다). 쿠키는 페이지 접근, 헤더는 쓰기 인가다.

    `page_cookie_ok(request)` 는 `secrets.compare_digest(request.cookies.get(COOKIE_NAME, ""), BOOT_TOKEN)`.

    `register_secret(value)` / `scrub(text)` — `_SECRETS: set[str]` 를 모듈 전역에 두고 등록된 값만 별표 3개로 치환한다. Phase 1 에서는 웹앱이 시크릿을 손에 쥐지 않으므로 `_SECRETS` 가 비어 있는 것이 정상이며 `scrub()` 은 사실상 항등 함수다. 그럼에도 이 함수가 존재하는 이유를 주석으로 남겨라: 자식 프로세스가 예기치 않게 시크릿을 찍을 가능성에 대비한 로그 쓰기 시점 훅이고, Plan 01-05 의 로그 tail 이 여기를 통과시킨다. 비용은 문자열 replace 한 번이다. 휴리스틱 마스킹(길이 40자 이상 base64 스러운 토큰을 자동으로 자르기)은 하지 마라 — 상품명·URL 오탐으로 화면이 깨진다.

    `webapp/tests/test_security.py` 를 behavior 10개 항목에 1:1 대응하게 쓴다. conftest 의 `client` 픽스처(TestClient)를 쓰되 `base_url` 을 `http://127.0.0.1:<settings.PORT>` 로 지정해야 Host 헤더가 화이트리스트를 통과한다. `test_시크릿이_새지_않는다` 는 `~/.eroom/naver-ads.json` 이 없으면 `pytest.skip`, 있으면 첫 계정의 secret_key 값을 읽어 GET / 응답 본문과 `webapp/templates/` 하위 파일 전체에서 찾는다(0건이어야 한다). 이 테스트가 실제 시크릿 파일을 읽지만 값을 절대 출력하지 않는다 — assert 메시지에 값을 넣지 마라.
  </action>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_security.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 아래 명령이 exit 0 으로 통과한다:
      ```
      .venv-web/bin/pytest webapp/tests/test_security.py -x -q
      ```
    - `01-VALIDATION.md § Per-Task Verification Map` 의 해당 행이 지목한 **테스트 파일**이 위 명령에 들어 있고,
      그 행의 `핵심 노드` 테스트가 이 실행 안에서 돈다 (VALIDATION 은 파일 단위로 적고, 노드 ID 는 병기만 한다 —
      태스크는 파일 전체를 돌린다. 노드 하나만 돌리는 것보다 회귀 방어가 낫기 때문이다)
  </acceptance_criteria>
  <done>test_security.py 전량 통과. `grep -c compare_digest webapp/security.py` 가 2 이상이고, `grep -c 8765 webapp/security.py` 가 0 이다.</done>
</task>

<task type="auto">
  <name>Task 2: webapp/main.py · 라우터 3개 · 템플릿 셸 · 기동 스크립트</name>
  <files>webapp/main.py, webapp/routes/__init__.py, webapp/routes/health.py, webapp/routes/board.py, webapp/routes/jobs.py, webapp/templates/board.html, webapp/templates/_job_panel.html, webapp/templates/_preview_table.html, webapp/run-webapp.sh</files>
  <read_first>
    - `.planning/phases/01-board-bid-raise/01-RESEARCH.md` § Code Examples "보안 3층 조립 (실측 통과본)" · §4.4 부팅 토큰 라우트 · § System Architecture Diagram · § Recommended Project Structure · Pitfall 9
    - `.claude/lib/eroomlib/review_page.py` 35-62행 — 저장소의 HTML escape 관례(값마다 `html.escape(..., quote=True)` 를 명시)
    - `.claude/skills/coupang-candidates/scripts/run_coupang.py` 3-13행 — 모듈 docstring 에서 읽기/쓰기 단계를 갈라 적는 S-9 관례
    - `webapp/security.py` (Task 1 산출물) · `webapp/settings.py` · `webapp/paths.py`
  </read_first>
  <action>
    `webapp/main.py` 를 쓴다. 모듈 docstring 을 S-9 관례대로 시작한다 — 어떤 라우트가 읽기고 어떤 게 쓰기인지 표로 적고 "GET 은 절대 상태를 바꾸지 않는다" 를 명시한다.

    `app = FastAPI(docs_url=None, redoc_url=None)` — 1인 로컬 도구에 OpenAPI UI 는 공격면만 늘린다. debug 를 켜지 마라(트레이스백 노출, ASVS V7).

    미들웨어를 이 순서로 붙인다. `app.add_middleware(GZipMiddleware, minimum_size=1000)` — 보드 547KB 가 95KB 로 줄어든다(실측). `app.add_middleware(TrustedHostMiddleware, allowed_hosts=["127.0.0.1", "localhost"], www_redirect=False)` — `allowed_hosts` 에 포트를 적지 마라(미들웨어가 포트를 떼고 매칭한다). 와일드카드는 미들웨어를 끄는 것과 같으니 절대 쓰지 마라. 그 다음 `@app.middleware("http")` 로 `security.guard` 를 붙인다.

    `templates = Jinja2Templates(directory=...)` 를 모듈 전역에 두고 `app.mount("/static", StaticFiles(directory=...), name="static")` 을 붙인다. 디렉터리 경로는 `Path(__file__).parent` 기준으로 잡고 절대경로를 박지 마라. Jinja2 자동 이스케이프에 기대되 `| safe` 필터를 쓰지 마라 — 상품명에 중국어 원문·특수문자가 섞여 들어온다.

    부팅 시 `os.environ.get("WEB_CONCURRENCY")` 가 `"1"` 이 아니면서 설정돼 있으면 경고를 찍는다(Pitfall 9). 그리고 기동 로그에 `http://127.0.0.1:<PORT>/?t=<BOOT_TOKEN>` 한 줄을 찍는다 — 사용자가 붙여넣을 URL 이다.

    라우터를 health → board → jobs 순으로 `include_router` 한다. 이 플랜에서 세 라우터 모듈을 전부 만든다. `board.py` 와 `jobs.py` 는 스텁이지만 파일과 `router` 심볼이 있어야 Plan 01-04/01-05 가 `main.py` 를 다시 안 건드리고 내용만 채운다(인터페이스 우선 순서).

    `webapp/routes/health.py`: `GET /healthz` 가 `{"ok": True, "run_dirs": len(paths.scan_run_dirs())}` 를 돌려준다. 읽기 전용, 토큰 불필요. 화면이 고장났을 때 서버가 살았는지 보는 창이다. 핸들러는 `async def` 가 아니라 `def` 로 선언한다 — Starlette 이 스레드풀에서 돌려 이벤트 루프를 막지 않는다.

    `webapp/routes/board.py`: `GET /` 하나만. RESEARCH §4.4 의 라우트를 그대로 구현한다. 쿼리 파라미터 `t` 가 오고 `secrets.compare_digest(t, BOOT_TOKEN)` 이 참이면 `RedirectResponse("/", status_code=303)` 을 돌려주되 `set_cookie(COOKIE_NAME, BOOT_TOKEN, httponly=True, samesite="strict", path="/")` 를 심는다 — 토큰을 쿼리스트링과 브라우저 히스토리에 남기지 않는다. 쿠키가 이미 맞으면 `templates.TemplateResponse("board.html", ...)` 에 `request` · `token` · `run_dirs` · `freshness` 를 넘긴다. 둘 다 아니면 `PlainTextResponse("토큰이 필요하다 — 서버 기동 로그의 URL 로 들어와라", status_code=403)`. 이 라우트는 GET 이지만 서버 상태를 바꾸지 않는다(쿠키는 브라우저 상태다) — 그 구분을 주석으로 남겨라. Plan 01-04 가 여기에 보드 데이터를 채운다.

    `webapp/routes/jobs.py`: 빈 `router = APIRouter()` 와 모듈 docstring 만. docstring 에 "Plan 01-05 가 POST /jobs/* 와 GET /jobs/{id}/stream 을 채운다. 모든 작업 생성은 POST 다 — GET 으로 만들면 Origin 방어가 통째로 무력화된다" 를 적는다.

    `webapp/templates/board.html`: Pico.css 와 Tabulator CSS 를 `/static/vendor/` 에서 링크하고 htmx 2.0.10 · htmx-ext-sse 2.2.4 · Tabulator 6.5.3 을 같은 경로에서 script 로 건다(CDN 런타임 의존 금지 — vendoring 한 파일을 쓴다). body 태그에 `hx-headers` 속성으로 `X-CT-Token` 을 심는다. 이 한 줄이 모든 htmx 쓰기 요청에 헤더를 붙인다. 본문에는 제목(셀러 관제탑), 신선도 배너 슬롯, `_job_panel.html` include, `_preview_table.html` include, 보드 테이블이 들어갈 `div#board` 를 둔다. 화면 문구는 반말이다(CLAUDE.md 협업 규칙).

    `webapp/templates/_job_panel.html` 과 `_preview_table.html`: 지금은 빈 자리 표시 partial. 각각 HTML 주석으로 어느 플랜이 채우는지 적는다.

    `webapp/run-webapp.sh`: `set -euo pipefail` 로 시작하고 저장소 루트로 이동한 뒤 `.venv-web/bin/python -m uvicorn webapp.main:app --host 127.0.0.1 --port "${CT_PORT:-8765}" --workers 1 "$@"` 를 실행한다. workers 1 은 협상 불가다 — 스크립트 주석에 Pitfall 9 근거(잡 레지스트리와 SSE 구독자가 프로세스별로 갈라져 "가끔 로그가 안 뜬다"가 간헐 발생)를 남긴다. `chmod +x` 한다.
  </action>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_security.py -x -q && .venv-web/bin/python -c "from webapp.main import app; p=[r.path for r in app.routes]; assert '/' in p and '/healthz' in p, p; print('PASS')" && grep -q 'workers 1' webapp/run-webapp.sh && ! grep -rqn 'jsdelivr\|unpkg' webapp/templates/ && ! grep -rqn '| safe' webapp/templates/</automated>
  </verify>
  <acceptance_criteria>
    - 아래 명령이 exit 0 으로 통과한다:
      ```
      .venv-web/bin/pytest webapp/tests/test_security.py -x -q && .venv-web/bin/python -c "from webapp.main import app; p=[r.path for r in app.routes]; assert '/' in p and '/healthz' in p, p; print('PASS')" && grep -q 'workers 1' webapp/run-webapp.sh && ! grep -rqn 'jsdelivr\|unpkg' webapp/templates/ && ! grep -rqn '| safe' webapp/templates/
      ```
    - `01-VALIDATION.md § Per-Task Verification Map` 의 해당 행이 지목한 **테스트 파일**이 위 명령에 들어 있고,
      그 행의 `핵심 노드` 테스트가 이 실행 안에서 돈다 (VALIDATION 은 파일 단위로 적고, 노드 ID 는 병기만 한다 —
      태스크는 파일 전체를 돌린다. 노드 하나만 돌리는 것보다 회귀 방어가 낫기 때문이다)
  </acceptance_criteria>
  <done>서버 앱이 import 되고 `/` 와 `/healthz` 라우트가 존재한다. 기동 스크립트에 workers 1 이 박혀 있고, 템플릿에 CDN URL 과 `| safe` 가 0건이다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 실제 서버에 붙어 보안 8종 curl 검증 + 브라우저 1회 확인</name>
  <files>webapp/tests/security_curl.sh</files>
  <read_first>
    - `.planning/phases/01-board-bid-raise/01-VALIDATION.md` § 보안 3종 curl 검증 — 8개 명령 전문(2026-09-19 실측 통과)
    - `webapp/main.py` · `webapp/security.py` (Task 1-2 산출물)
  </read_first>
  <action>
    `webapp/tests/security_curl.sh` 를 만든다. `01-VALIDATION.md § 보안 3종 curl 검증` 의 8개 검사(V-SAFE-01a/01b/01c/01d, V-SAFE-02a/02b/02c, V-SAFE-03)를 **그 문서에 적힌 명령 그대로** 옮기고, 하나라도 FAIL 이면 스크립트가 exit 1 이 되게 묶는다. 토큰은 `CT_DEV_TOKEN` 환경변수로 받는다(`.boot-token-for-test` 파일을 만들지 마라 — 토큰을 디스크에 남기지 않는다). 포트는 `CT_PORT` 기본 8765.

    V-SAFE-02c(정상 쓰기가 통과하는지)를 빼먹지 마라 — 403 만 확인하면 "전부 막힘"을 성공으로 오독한다. 단, Plan 01-05 가 `POST /jobs/bids/preview` 를 만들기 전까지 이 라우트는 404 다. 404 는 400 미만이 아니므로, 이 단계에서는 V-SAFE-02c 의 판정 기준을 "403 이 아닐 것"으로 두고, Plan 01-06 완료 후 `-lt 400` 으로 조인다는 사실을 스크립트 주석에 적어라.

    V-SAFE-03 은 `~/.eroom/naver-ads.json` 의 첫 계정 secret_key 를 읽어 응답 본문과 `webapp-logs/` 에서 찾는다. 값을 stdout 에 찍지 마라.

    그 다음 사람이 직접 확인한다.
  </action>
  <what-built>
    127.0.0.1 전용 FastAPI 서버 + Host 화이트리스트 + 쓰기 메서드 전용 Origin 가드 + 부팅 토큰(쿠키는 페이지 접근, X-CT-Token 헤더는 쓰기 인가) + GZip + vendoring 정적파일 + 빈 보드 셸.
  </what-built>
  <how-to-verify>
    1. 터미널 A에서 `CT_DEV_TOKEN=devtoken123 ./webapp/run-webapp.sh` 를 돌린다. 기동 로그 마지막 줄에 `→ http://127.0.0.1:8765/?t=devtoken123` 이 찍히는지 본다.
    2. 터미널 B에서 `CT_DEV_TOKEN=devtoken123 bash webapp/tests/security_curl.sh` 를 돌린다. **8줄 전부 PASS** 여야 한다. 하나라도 FAIL 이면 여기서 멈춘다.
    3. 브라우저에서 `http://127.0.0.1:8765/` 를 그냥 연다 → `토큰이 필요하다 — 서버 기동 로그의 URL 로 들어와라` (403).
    4. 기동 로그의 `?t=` URL 로 들어간다 → 보드 셸이 뜨고 **주소창이 `http://127.0.0.1:8765/` 로 깨끗해졌는지** 확인한다 (토큰이 히스토리에 안 남는다).
    5. 브라우저 개발자도구 Network 탭에서 페이지 소스를 열어 `X-CT-Token` 값 말고 **광고 시크릿 문자열이 안 보이는지** 훑는다.
    6. `lsof -nP -iTCP:8765 -sTCP:LISTEN` 출력에 `127.0.0.1:8765` 만 있고 `0.0.0.0` 이 없는지 확인한다.
  </how-to-verify>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_security.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv-web/bin/pytest webapp/tests/test_security.py -x -q` 통과
    - `CT_DEV_TOKEN=devtoken123 bash webapp/tests/security_curl.sh` 가 8줄 전부 PASS 하고 exit 0
    - `lsof -nP -iTCP:8765 -sTCP:LISTEN` 에 `0.0.0.0` 이 없다
    - 브라우저 주소창에 `?t=` 가 남지 않는다 (사람 확인)
  </acceptance_criteria>
  <resume-signal>"approved" 라고 답하거나, PASS 하지 않은 항목 번호와 실제 출력을 알려줘</resume-signal>
  <done>보안 8종 전량 PASS + 브라우저 4단계 육안 확인 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저(다른 탭 포함) → 로컬 서버 | 이 단계의 **유일한** 신뢰경계. 악성 웹페이지가 같은 브라우저 안에서 우리 서버를 때릴 수 있다 |
| DNS → 로컬 서버 | 공격자가 자기 도메인을 127.0.0.1 로 재바인딩할 수 있다 |
| 서버 → 화면·로그·템플릿 | 자격증명이 사람 눈과 디스크에 닿는 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Spoofing / Tampering | `webapp/main.py` 미들웨어 스택 | mitigate | 3중 방어. (a) `--host 127.0.0.1` 바인드를 `run-webapp.sh` 에 고정, (b) `TrustedHostMiddleware(allowed_hosts=["127.0.0.1","localhost"], www_redirect=False)` → `Host: evil.com` 400 (DNS rebinding), (c) 쓰기 메서드 전용 Origin 화이트리스트 → 타 사이트 POST 403. 검증: V-SAFE-01a/01b/01c |
| T-1-01b | Tampering | 부수효과 GET | mitigate | 상태를 바꾸는 엔드포인트를 GET 으로 만들지 않는다. 교차 사이트 단순 GET(img/script/link)에는 Origin 헤더가 없어 (c) 가 통째로 무력화된다. 검증: V-SAFE-01d grep 가드 + `routes/jobs.py` docstring 규칙 |
| T-1-02 | Elevation of Privilege | `security.guard` 토큰 검사 | mitigate | `secrets.token_urlsafe(32)` 부팅 토큰을 `X-CT-Token` **커스텀 헤더**로 검사. 커스텀 헤더는 교차 오리진에서 붙일 수 없다(CORS 미개방). 비교는 `secrets.compare_digest`. 쿠키(`ct_session`, HttpOnly + SameSite=Strict)는 페이지 접근에만 쓴다. 검증: V-SAFE-02a/02b/02c |
| T-1-03 | Info Disclosure | 응답 · 템플릿 · 로그 | mitigate | 웹앱이 `~/.eroom/naver-ads.json` 을 **열지 않는다**(계정 목록이 필요하면 Plan 01-02 의 `run_ads.py accounts` 를 subprocess 로). 자식 env 에 자격증명을 넘기지 않는다(`ps -E` 노출 방지). `docs_url=None`·debug 미사용으로 트레이스백 비노출. `security.scrub()` 이 로그 쓰기 시점 훅으로 대기. Phase 1 은 불사자 토큰을 아예 로드하지 않는다. 검증: V-SAFE-03 + `test_시크릿이_새지_않는다` |
| T-1-16 | Info Disclosure | 토큰이 URL·히스토리에 잔류 | mitigate | `GET /?t=...` 는 쿠키를 심고 즉시 303 으로 깨끗한 `/` 로 털어낸다. 토큰을 디스크 파일(`.boot-token-for-test`)로 떨구지 않는다 |
| T-1-17 | Tampering | 템플릿 XSS | mitigate | Jinja2 자동 이스케이프 + `| safe` 금지(grep 가드). 상품명에 중국어 원문·특수문자가 섞여 들어온다 |
| T-1-18 | DoS (자해) | `--workers N>1` | mitigate | `run-webapp.sh` 에 `--workers 1` 고정 + 기동 시 `WEB_CONCURRENCY` 경고. 워커가 갈라지면 잡 레지스트리·SSE 구독자가 프로세스별로 나뉘어 "가끔 로그가 안 뜬다"로 나타난다 |
</threat_model>

<verification>
- `.venv-web/bin/pytest webapp/tests/test_security.py -x -q`
- `CT_DEV_TOKEN=devtoken123 bash webapp/tests/security_curl.sh` — 8줄 전량 PASS
- `grep -rnE '@(app|router)\.get' webapp/routes/ | wc -l` 로 GET 라우트 목록을 확인하고, 그 어느 것도 작업을 생성하지 않음을 눈으로 본다
- `for t in nvad reports ads_rules ledger bids prune; do .venv/bin/python3 .claude/skills/naver-ads-weekly/scripts/test_$t.py || exit 1; done` — CLI 기준선 불변
</verification>

<success_criteria>
- 서버가 127.0.0.1 에만 뜬다 (`lsof` 에 `0.0.0.0` 없음)
- `Host: evil.com` → 400, 타 Origin 쓰기 → 403, 토큰 없음/틀림 → 403, 정상 쓰기 → 403 아님
- 토큰 없이 `/` → 403, `?t=` URL → 303 후 깨끗한 `/` 에서 보드 셸
- 시크릿 문자열이 응답·템플릿·로그에서 0건
- `--workers 1` 이 기동 경로 한 곳에 고정돼 있다
</success_criteria>

<output>
Create `.planning/phases/01-board-bid-raise/01-03-SUMMARY.md` when done
</output>
