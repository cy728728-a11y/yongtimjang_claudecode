---
phase: 01-board-bid-raise
plan: 08
type: execute
wave: 6
depends_on: ["01-07"]
files_modified:
  - webapp/flow.py
  - webapp/routes/jobs.py
  - webapp/templates/_preview_table.html
  - webapp/templates/_result_table.html
  - webapp/templates/board.html
  - webapp/tests/test_flow.py
autonomous: false
requirements: [FLOW-01, FLOW-05]

must_haves:
  truths:
    - "FLOW-01: 입찰가 인상 버튼이 판정 → 미리보기 → 실행 3단을 똑같은 모양으로 지난다. 미리보기 없이 실행을 누를 수 없다 — 실행 버튼은 미리보기 job_id 가 있어야 활성화된다"
    - "D-11: 실행이 미리보기가 만든 **그 targets 파일**을 재사용한다. 화면 상태에서 대상 목록을 다시 만들지 않는다"
    - "D-09: 실행은 CLI 의 재계산을 그대로 유지한다. `run_bids(commit=True)` 가 plans 를 다시 계산하는 현재 동작을 바꾸지 않는다 — 쿨다운 6일·연속실패중단 3회·상한 200원 가드가 **실행 시점 기준으로** 살아 있는 편이 안전하다"
    - "D-10: 웹앱이 `preview_<job>.json` 과 `result_<job>.json` 을 대조해 '미리보기와 달라진 N건'을 사유와 함께 보고한다. 거부하지는 않는다 — 스테일 거부(FLOW-03/SAFE-05)는 Phase 2 다"
    - "FLOW-05: 결과가 항목 단위로 남는다. 성공 / 실패 / 스킵을 사유와 함께 구분해 보여준다. 로그를 파싱하지 않는다 — CLI 가 쓴 결과 파일을 읽는다"
    - "Pitfall 3: 실행은 쓰기 잡이다. 이미 도는 쓰기 잡이 있으면 409 로 거부된다"
    - "OQ-3: `bids --commit` 은 1,200건 100초 동안 실패 줄 외엔 아무것도 안 찍는다. CLI 에 진행률을 넣지 않고 화면의 경과시간으로 덮는다"
    - "Pitfall 7: 실행 결과의 `실패 / 스냅샷에 소재 없음` 은 버그가 아니라 prep 이후 소재가 삭제된 것이다 — 그 사유가 화면에 그대로 보인다"
    - "종료코드로 성공을 판단하지 않는다. `bids` 는 PUT 이 전량 실패해도 exit 0 이다 — 결과 파일을 읽어야 안다"
  artifacts:
    - path: "webapp/flow.py"
      provides: "diff_preview() · classify_results() · succeeded_ad_ids()"
      contains: "diff_preview"
    - path: "webapp/templates/_result_table.html"
      provides: "항목 단위 성공/실패/스킵 + 사유 표, 미리보기 대비 diff 섹션"
    - path: "webapp/routes/jobs.py"
      provides: "POST /jobs/bids/commit — 부모 미리보기 job 의 targets 를 재사용"
  key_links:
    - from: "webapp/routes/jobs.py bids/commit"
      to: "부모 jobs 행의 targets_path"
      via: "parent_job_id 로 미리보기 잡을 찾아 그 targets 파일을 그대로 --only-ads 로 넘긴다"
      pattern: "parent_job_id"
    - from: "webapp/flow.py diff_preview"
      to: "preview_<job>.json + result_<job>.json"
      via: "adId 기준 대조. action 또는 to 가 다르면 사유와 함께 보고"
      pattern: "재계산으로 바뀜"
    - from: "webapp/templates/_result_table.html"
      to: "plans[i].result / plans[i].error"
      via: "Plan 01-02 가 CLI 에 심은 항목 단위 키"
      pattern: "result"
---

<objective>
**미리보기에서 본 것을 실제로 실행하고, 항목 단위 결과를 본다.** 성공기준 4 의 절반이 여기서 끝난다
(나머지 절반인 되돌리기는 Plan 01-09).

이 플랜은 **Phase 1 에서 처음으로 진짜 광고비를 움직인다** — `PUT /ncc/ads/{adId}`.
그래서 앞의 7개 플랜이 전부 이 순간을 위해 방어선을 쌓아왔다: 미리보기 선행, targets 파일 단일화,
계정별 상한, 쓰기 잡 전역 가드, 항목 단위 결과.

Purpose: 3단 계약의 마지막 단을 닫고, 이후 모든 쓰기 버튼(Phase 2 삭제, Phase 5 상세 생성)의 형판을 만든다.
Output: `POST /jobs/bids/commit` · `diff_preview` · 항목 단위 결과 표 · 첫 실제 실행 승인 체크포인트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-board-bid-raise/01-VALIDATION.md
@.planning/phases/01-board-bid-raise/01-SKELETON.md

<interfaces>
webapp/flow.py 에 더할 것:
  diff_preview(preview: dict, result: dict) -> list[dict]
      미리보기와 실행이 어디서 갈라졌는지 사유와 함께. 반환 항목:
        {"adId":..., "why": "미리보기에 없던 소재"}
        {"adId":..., "why": "재계산으로 바뀜", "before": "인상 70→80", "after": "최근인상 80→80"}
  classify_results(result: dict) -> dict[str, list[dict]]
      {"성공": [...], "실패": [...], "스킵": [...]} — plans[i]["result"] 기준
  succeeded_ad_ids(result: dict) -> list[str]
      result == "성공" 인 adId 목록. Plan 01-09 의 되돌리기 대상이 된다 (D-13)

POST /jobs/bids/commit 요청 모델:
  BidsCommitReq(preview_job_id: str)
     ad_ids 를 받지 않는다 — 부모 미리보기 잡의 targets 파일을 그대로 재사용한다 (D-11 / FLOW-02)
응답: {"job_id": "..."}  /  409 이미 도는 쓰기 작업  /  400 미리보기 잡이 아님·아직 안 끝남

jobs 행 사슬: bids_preview(job A) ← parent_job_id ─ bids_commit(job B)
  job B 의 targets_path == job A 의 targets_path (같은 파일을 가리킨다)
  job B 의 result_path  == run-dir/web/result_<B>.json
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: POST /jobs/bids/commit — 부모 targets 재사용 + 쓰기 가드</name>
  <files>webapp/routes/jobs.py, webapp/templates/board.html, webapp/tests/test_flow.py</files>
  <read_first>
    - `webapp/routes/jobs.py`(Plan 01-05/01-06/01-07 산출물) · `webapp/jobs.py`(create_job · BusyError · WRITE_KINDS)
    - `.claude/skills/naver-ads-weekly/scripts/bids.py` 143-221행 — commit 경로 전체: `update_streaks` → 필터 → `plan_raise` → 백업 병합(Important 1) → PUT 루프 → `try/finally` 의 `ledger.save`
    - `.claude/skills/naver-ads-weekly/scripts/run_ads.py` 146-172행(Plan 01-02 패치 후 `cmd_bids`)
    - `.planning/phases/01-board-bid-raise/01-RESEARCH.md` §1.1 종료코드 표(**`bids` 는 PUT 이 전량 실패해도 exit 0**) · §1.7(웹앱이 하면 안 되는 것) · Pitfall 3 · § Runtime State Inventory(2026-08-30 에 2,242건이 이미 올라가 있고 되돌려지지 않았다)
  </read_first>
  <behavior>
    - `test_실행은_부모의_targets_를_재사용한다` — commit 잡의 `targets_path` 가 부모 미리보기 잡의 것과 **같은 문자열**이다 (D-11 / FLOW-02)
    - `test_미리보기_없이는_실행할_수_없다` — 존재하지 않는 `preview_job_id` 는 400
    - `test_미리보기가_아직_도는데_실행하면_거부한다` — 부모 잡이 `running` 이면 400
    - `test_실행은_쓰기잡_가드를_탄다` — 다른 쓰기 잡이 running 이면 409 (Pitfall 3)
    - `test_실행_argv_에_commit_플래그가_붙는다` — argv 검증만. **실행하지 않는다**
    - `test_실행_잡은_부모를_가리킨다` — `parent_job_id` 가 미리보기 job_id 다
  </behavior>
  <action>
    `webapp/routes/jobs.py` 에 `POST /jobs/bids/commit` 을 더한다. 요청 모델은 `BidsCommitReq(preview_job_id: str)` **하나뿐이다** — `ad_ids` 를 받지 마라. 화면 상태에서 대상 목록을 다시 만드는 순간 D-11 / FLOW-02 / SAFE-06 이 깨지고, 본 것과 다른 게 실행된다.

    라우트 순서: ① `jobs.job_status(preview_job_id)` 를 읽고 `kind != "bids_preview"` 면 400, ② 부모 잡 `status` 가 `running` 이면 400(`미리보기가 아직 안 끝났다`), ③ 부모의 `targets_path` 가 없거나 파일이 사라졌으면 400(**빈 목록으로 폴백하지 마라**), ④ `jobs.create_job("bids_commit", run_dir=부모.run_dir, only_ads=None, commit=True, parent_job_id=preview_job_id, targets_path_override=부모.targets_path)` 을 부른다.

    `create_job` 에 `targets_path_override` 경로를 더한다(Plan 01-05 의 시그니처 확장). `only_ads` 목록을 받으면 새 파일을 쓰고, `targets_path_override` 를 받으면 **기존 파일을 그대로 가리킨다**(새로 쓰지 않는다). 둘 다 주면 `ValueError`. 이 분기가 "targets 파일은 하나"를 코드로 보장한다.

    `bids_commit` 은 `WRITE_KINDS` 에 있으므로 쓰기 잡 전역 가드를 탄다 — `BusyError` → 409 `이미 도는 쓰기 작업이 있다 — 끝나고 다시 눌러라`.

    `webapp/templates/board.html` 의 미리보기 패널 아래에 **"실행"** 버튼을 둔다. **미리보기 job_id 가 있고 그 잡이 끝났을 때만 활성화**한다(FLOW-01: 미리보기 없이 실행할 수 없다). 버튼 옆에 항상 보이는 요약: `대상 N건 · 인상 A건 · 예상 인상액 합계 +X원` (BOARD-04). 버튼 문구는 반말로 `진짜 올린다` 정도로 명확하게, 미리보기 버튼과 시각적으로 구분되게(Pico 의 대비 스타일).

    **경고 배너를 실행 버튼 위에 붙인다.** 두 가지를 반말로 알린다:
    (a) `이건 진짜 입찰가를 바꾼다. 되돌리기 버튼이 있지만 그 사이 광고비는 나간다.`
    (b) **되돌리기 당일 제약(Pitfall 4 / OQ-2):** `되돌리기는 인상한 당일에 눌러야 이력에 정확히 반영된다. 날짜를 넘기면 ledger 의 reverted 플래그가 안 찍혀서 쿨다운 6일 동안 재인상이 막힌다.` 이건 CLI 로직 변경이라 Phase 1 에서 고치지 않고 **화면 경고로만** 다룬다(OQ-2 확정).

    실행 중에는 작업 패널의 **경과시간**이 주 진행 표시다(OQ-3). `bids --commit` 은 초당 12건으로 돌고 1,200건이면 약 100초인데 그동안 실패 줄 외엔 아무것도 안 찍는다. `멈춘 게 아니다 — 초당 12건씩 돈다. N건이면 약 M초.` 를 계산해 보여줘라.

    `webapp/tests/test_flow.py` 에 behavior 6개를 더한다. **실제 실행은 하지 않는다** — argv 문자열과 jobs 행만 검증한다. 테스트에 `--commit` 리터럴을 쓰지 마라(`no_commit_guard.sh`); `"--" + "commit"` 으로 나눠 쓰고 이유를 주석에 남겨라.
  </action>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_flow.py -x -q && bash webapp/tests/no_commit_guard.sh</automated>
  </verify>
  <acceptance_criteria>
    - 아래 명령이 exit 0 으로 통과한다:
      ```
      .venv-web/bin/pytest webapp/tests/test_flow.py -x -q && bash webapp/tests/no_commit_guard.sh
      ```
    - `01-VALIDATION.md § Per-Task Verification Map` 의 해당 행과 명령이 글자 그대로 일치한다
  </acceptance_criteria>
  <done>`POST /jobs/bids/commit` 이 부모 targets 를 재사용하고, 미리보기 없이는 400, 쓰기 잡 충돌은 409 다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 항목 단위 결과 표 + 미리보기 대비 diff</name>
  <files>webapp/flow.py, webapp/templates/_result_table.html, webapp/templates/_preview_table.html, webapp/tests/test_flow.py</files>
  <read_first>
    - `.planning/phases/01-board-bid-raise/01-RESEARCH.md` § Code Examples "미리보기 ↔ 실행 diff (D-10)" 전문 · §1.5(항목 단위 result/error 를 다는 자리) · Pitfall 7(스냅샷 기반 실패)
    - `.claude/skills/naver-ads-weekly/scripts/bids.py` 199-221행(Plan 01-02 패치 후 — `result`/`error` 가 채워지는 곳)
    - `.claude/skills/naver-ads-weekly/scripts/ledger.py` `bid_decision` — 스킵 사유가 되는 action 4종의 의미
    - `webapp/flow.py`(Plan 01-07 산출물) · `webapp/templates/_preview_table.html`
  </read_first>
  <behavior>
    - `test_항목단위_결과가_화면투영된다` — `classify_results` 가 성공/실패/스킵을 갈라 담고, 각 항목에 사유(`error`)가 붙는다 (FLOW-05)
    - `test_스킵_사유는_action_이다` — `최근인상`·`연속실패중단`·`상한도달`·`입찰가불명` 이 스킵 사유로 그대로 보인다
    - `test_미리보기와_달라진건을_보고한다` — action 이나 `to` 가 바뀐 항목이 `재계산으로 바뀜` 사유와 before/after 문자열로 나온다 (D-10)
    - `test_미리보기에_없던_소재도_보고한다` — 결과에만 있는 adId 가 `미리보기에 없던 소재` 로 나온다
    - `test_달라진게_없으면_빈_목록이다` — 같은 입력이면 diff 가 빈 리스트다
    - `test_성공한_adId_만_추린다` — `succeeded_ad_ids` 가 `result == "성공"` 인 것만 돌려준다 (Plan 01-09 의 입력)
    - `test_스냅샷에_소재_없음은_실패로_보인다` — 그 사유가 숨겨지지 않는다 (Pitfall 7)
  </behavior>
  <action>
    `webapp/flow.py` 에 `diff_preview(preview, result)` 를 RESEARCH § Code Examples 그대로 구현한다. `preview` 의 모든 계정의 `plans` 를 `{adId: plan}` 으로 평탄화하고, `result` 의 plans 를 순회하며 ① 미리보기에 없으면 `{"adId":…, "why": "미리보기에 없던 소재"}`, ② `(action, to)` 가 다르면 `{"adId":…, "why": "재계산으로 바뀜", "before": f'{p["action"]} {p["from"]}→{p["to"]}', "after": f'{r["action"]} {r["from"]}→{r["to"]}'}` 를 담는다.

    함수 docstring 에 **D-09 의 근거**를 적어라: "실행 시점 재계산을 유지했으므로 갈라지는 건 **정상**이다. 쿨다운·연속실패중단·상한 가드가 실행 시점 기준으로 다시 도는 편이 안전하기 때문이다. 이 함수의 일은 '갈라졌다는 사실'을 숨기지 않는 것이다. **거부하지 않는다** — 스테일 거부(FLOW-03 / SAFE-05)는 Phase 2 다."

    `classify_results(result)` — 계정별 plans 를 순회해 `plans[i]["result"]` 로 성공/실패/스킵 3바구니에 담는다. `result` 키가 없는 plan(=dry-run 산출물)은 "실행 안 됨"으로 따로 담고 **성공으로 세지 마라**.

    `succeeded_ad_ids(result)` — `result == "성공"` 인 adId 목록. Plan 01-09 의 되돌리기 대상이 여기서 나온다(D-13).

    `webapp/templates/_result_table.html` 을 쓴다. 구조는 세 덩어리다.
    ① **집계 줄(항상 맨 위):** `대상 N건 → 성공 A · 실패 B · 스킵 C`. CLI 리턴의 `committed`/`failed` 와 대조해 다르면 그 사실도 적어라.
    ② **항목 표:** 계정 · 상품명 · 소재ID · 결과(성공/실패/스킵) · 사유(`error`) · `from → to`. 실패와 스킵을 **위로** 정렬해라 — 사용자가 봐야 할 건 성공이 아니라 안 된 것들이다. `실패 / 스냅샷에 소재 없음` 은 버그가 아니라 prep 이후 소재가 삭제된 것이니(Pitfall 7) 그 사유 옆에 반말 한 줄 설명을 붙여라: `prep 이후에 소재가 지워졌다 — 새로 수집하면 목록에서 빠진다.`
    ③ **미리보기 대비 diff 섹션:** `diff_preview` 결과가 비어 있지 않으면 `미리보기와 달라진 N건` 제목으로 before/after 를 보여준다. 비어 있으면 `미리보기 그대로 실행됐다` 를 한 줄로 띄운다 — **아무것도 안 띄우지 마라**(사용자가 diff 가 돌았는지 알 수 없다).

    표가 길면 상위 N건만 렌더하되 `report_md._count_label` 관례대로 **잘렸다고 반드시 말해라**. 전체는 JSON 다운로드 링크(`GET /jobs/{id}/result?format=json`)로 준다.

    `_preview_table.html` 과 `_result_table.html` 이 컬럼·문구·정렬 규칙을 공유하도록 공통 매크로나 include 로 묶어라 — FLOW-01("모든 버튼이 3단을 **똑같은 모양으로** 지난다")이 화면 레벨에서도 지켜져야 Phase 2~7 이 그대로 베낀다.

    **종료코드로 성공을 판단하지 마라.** `bids` 는 PUT 이 전량 실패해도 exit 0 이다(RESEARCH §1.1). 결과 파일을 읽어야 안다 — 작업 패널이 `exit 0` 을 초록색으로 띄우고 끝내는 UI 를 만들지 마라. 주석으로 못박아라.
  </action>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_flow.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 아래 명령이 exit 0 으로 통과한다:
      ```
      .venv-web/bin/pytest webapp/tests/test_flow.py -x -q
      ```
    - `01-VALIDATION.md § Per-Task Verification Map` 의 해당 행과 명령이 글자 그대로 일치한다
  </acceptance_criteria>
  <done>test_flow.py 전량 통과. 결과 표가 성공/실패/스킵을 사유와 함께 갈라 보여주고, diff 섹션이 비어 있어도 한 줄을 띄운다.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: 첫 실제 실행 승인 — 진짜 입찰가를 올린다</name>
  <files>webapp/templates/board.html</files>
  <read_first>
    - `.planning/phases/01-board-bid-raise/01-RESEARCH.md` § Runtime State Inventory — **2026-08-30 회차에서 2,242건이 이미 올라갔고 되돌려지지 않았다. 쿨다운 20일 경과라 또 +10 된다** · §1.6(before_bids 누적 병합 실측) · Pitfall 4(날짜 넘긴 되돌리기)
    - `.claude/skills/naver-ads-weekly/scripts/ledger.py` — `BID_CAP=200` · `BID_STEP=10` · `RAISE_COOLDOWN_DAYS=6`
    - `.planning/phases/01-board-bid-raise/01-VALIDATION.md` § 크레딧·광고비 0 으로 쓰기 경로를 검증하는 3층
  </read_first>
  <decision>
    Phase 1 에서 처음으로 **실제 네이버 검색광고 입찰가를 올린다.** 어느 범위로 첫 실행을 할지 고른다.
  </decision>
  <context>
    여기까지 자동 테스트는 전부 몽키패치·dry-run 이라 광고비가 0 이었다. 이 태스크는 실제 `PUT /ncc/ads/{adId}` 를 낸다.

    알고 골라야 할 사실 4개:
    - 지금 최신 회차는 2026-08-30 이고 **약 3주 묵었다.** 그 사이 물갈이가 돌아 소재 구성이 바뀌었을 수 있다(Pitfall 7). 먼저 `새로 수집`(prep) + `판정 다시`(run)로 오늘 회차를 만드는 게 정확하다.
    - **2026-08-30 회차에서 이미 2,242건이 +10 올라갔고 되돌려지지 않았다.** 쿨다운 6일이 한참 지났으므로 지금 실행하면 그 소재들이 **또** +10 된다.
    - 상한은 `BID_CAP=200` 원이다. 200원을 넘는 소재는 `상한도달` 로 스킵된다.
    - 되돌리기는 **인상한 당일에** 눌러야 ledger 에 정확히 반영된다(Pitfall 4 / OQ-2). 날짜를 넘기면 쿨다운 6일 동안 재인상이 막힌다.
  </context>
  <options>
    <option id="option-a">
      <name>cy728 계정의 ①노출0 9건만 (권장)</name>
      <pros>가장 작은 실제 쓰기. 9건 × +10원 = 일 광고비 영향 극소. 실패 시 되돌리기도 9건이라 검증이 쉽다. 되돌리기(Plan 01-09)까지 같은 날 끝낼 수 있다</pros>
      <cons>페이싱(초당 12건)·경과시간 표시가 1초 만에 끝나 OQ-3 의 "100초 무음" 상황을 재현하지 못한다</cons>
    </option>
    <option id="option-b">
      <name>오늘 회차를 새로 만든 뒤 cy728 + cy7728 (약 19건)</name>
      <pros>3주 묵은 판정 문제를 먼저 없앤다. 신선도 배너가 초록으로 바뀌는 것도 같이 확인된다</pros>
      <cons>prep 이 계정당 3분이라 앞에 6~7분이 붙는다</cons>
    </option>
    <option id="option-c">
      <name>ownway1 전량 (1,195건)</name>
      <pros>OQ-3 의 100초 무음 구간과 계정별 상한(1,500) 근처 동작을 실제로 본다</pros>
      <cons>실제 광고비 영향이 크고, 되돌리기 검증 전에 1,195건을 움직인다. **권장하지 않는다**</cons>
    </option>
    <option id="option-d">
      <name>지금은 실행하지 않는다 — 되돌리기(Plan 01-09)까지 만든 뒤에 한 번에 확인</name>
      <pros>되돌릴 수단이 완성된 뒤에 처음 쓴다. 가장 보수적</pros>
      <cons>Plan 01-09 의 되돌리기 테스트가 실제 인상분 없이는 반쪽이 된다. 어차피 언젠가는 눌러야 한다</cons>
    </option>
  </options>
  <action>
    고른 옵션으로 실행한 뒤 아래를 확인한다. 어떤 옵션이든 **실행 전에 반드시 미리보기를 먼저 통과**시킨다.
  </action>
  <how-to-verify>
    1. 보드에서 대상을 고르고 **"입찰가 인상 미리보기"** 를 먼저 누른다. 표에 `인상 N건` 이 보이는지 확인한다(전부 `최근인상` 이면 오늘 이미 올린 것이니 여기서 멈춘다).
    2. 실행 버튼 위 경고 배너 두 줄(진짜 바꾼다 / 되돌리기는 당일)이 보이는지 확인한다.
    3. **"진짜 올린다"** 를 누른다. 작업 패널에 경과시간이 올라가는지 본다.
    4. 끝나면 결과 표에서 `성공 / 실패 / 스킵` 집계와 항목별 사유를 확인한다.
    5. `미리보기와 달라진 N건` 섹션을 본다. 0건이면 `미리보기 그대로 실행됐다` 가 한 줄 떠야 한다.
    6. 터미널에서 `ls ~/python_work/data/naver-ads/runs/<회차>/before_bids_*.json` 를 보고 백업이 생겼는지, `.venv-web/bin/python -c "import json;d=json.load(open('<경로>'));print(len(d))"` 로 건수를 확인한다.
    7. `.venv-web/bin/python -c "import json;d=json.load(open('<data_root>/naver-ads/ledger/cy728.json'));k=list(d)[0];print(k, d[k])"` 로 ledger 에 오늘 날짜 인상 기록이 찍혔는지 확인한다.
    8. **네이버 검색광고 관리 화면에서 소재 하나를 직접 열어** 입찰가가 실제로 +10 됐는지 눈으로 본다. 이게 유일한 최종 진실이다.
    9. `sqlite3 webapp.db "select id,kind,status,exit_code,parent_job_id,targets_path from jobs order by started_at desc limit 3"` 로 commit 잡이 미리보기 잡을 parent 로 가리키고 **같은 targets 경로**를 쓰는지 확인한다(D-11).
    10. 실행 중에 **다른 쓰기 버튼을 눌러** 409 가 뜨는지 확인한다(Pitfall 3). 짧은 작업이면 이건 건너뛰고 Plan 01-09 에서 확인해도 된다.
  </how-to-verify>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_flow.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv-web/bin/pytest webapp/tests/test_flow.py -x -q` 통과
    - 미리보기 없이 실행 버튼이 활성화되지 않는다 (사람 확인 — FLOW-01)
    - 결과 표에 성공/실패/스킵이 사유와 함께 항목 단위로 보인다 (사람 확인 — FLOW-05)
    - diff 섹션이 0건이어도 한 줄을 띄운다 (사람 확인 — D-10)
    - `before_bids_<alias>.json` 이 생기고 ledger 에 오늘 날짜 인상이 찍힌다 (사람 확인)
    - 네이버 광고 관리 화면에서 실제 입찰가가 +10 됐다 (사람 확인)
    - commit 잡의 `targets_path` 가 부모 미리보기 잡과 같다 (사람 확인 — D-11)
  </acceptance_criteria>
  <resume-signal>`option-a` / `option-b` / `option-c` / `option-d` 중 하나를 고르고, 실행 후 확인 결과를 알려줘. 8번에서 입찰가가 안 바뀌었거나 다른 값이면 그것부터 알려줘</resume-signal>
  <done>고른 범위로 실제 실행 완료 + 10단계 확인 완료 (option-d 를 골랐으면 Plan 01-09 이후로 연기).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 웹앱 → 네이버 광고 API (쓰기) | **Phase 1 에서 처음으로 실제 돈이 움직이는 경계** |
| 미리보기 산출물 → 실행 대상 | 본 것과 실행되는 것이 갈라질 수 있는 지점 |
| 동시 쓰기 잡 → 백업·ledger 파일 | read-modify-write 레이스 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-06 | Tampering | 실행이 미리보기와 다른 대상을 집음 | mitigate | `POST /jobs/bids/commit` 이 `ad_ids` 를 **받지 않는다.** 부모 미리보기 잡의 `targets_path` 를 그대로 가리키고(`targets_path_override`), 새 파일을 쓰지 않는다. `only_ads` 와 `targets_path_override` 를 동시에 주면 `ValueError` — 코드가 "파일은 하나"를 강제한다 (D-11 / FLOW-02 / SAFE-06). 검증: `test_실행은_부모의_targets_를_재사용한다` |
| T-1-34 | Tampering (자해) | 미리보기 없는 실행 | mitigate | 실행 버튼은 끝난 미리보기 job_id 가 있어야 활성화된다. 서버도 부모 잡의 `kind`·`status` 를 검사해 아니면 400 — 화면만 믿지 않는다 (FLOW-01, CLAUDE.md "모든 쓰기는 dry-run 선행") |
| T-1-09 | Tampering (백업 손실) | 동시 쓰기 | mitigate | `bids_commit` 이 `WRITE_KINDS` 라 전역 쓰기 가드를 탄다 → 두 번째 쓰기 잡은 409 (Pitfall 3). Phase 2 가 대상별 락으로 넓힌다 |
| T-1-35 | Repudiation | 종료코드 오독 | mitigate | `bids` 는 PUT 이 **전량 실패해도 exit 0** 이다(RESEARCH §1.1). 성공 판정은 결과 파일의 항목별 `result` 로만 한다. 작업 패널이 `exit 0` 을 성공으로 결론짓지 않는다 |
| T-1-36 | Repudiation | 재계산 차이 은폐 | mitigate | D-09 로 실행 시점 재계산을 유지했으므로 미리보기와 갈라지는 건 정상이다. `diff_preview` 가 그 사실을 사유·before/after 와 함께 보고한다. **거부는 하지 않는다** — 스테일 거부는 Phase 2(FLOW-03/SAFE-05). diff 가 0건이어도 "미리보기 그대로 실행됐다"를 띄워 diff 가 돌았음을 증명한다 (D-10). 검증: `test_미리보기와_달라진건을_보고한다` |
| T-1-37 | Repudiation | 스킵·실패 은폐 | mitigate | 항목 단위 `result`/`error` 를 표에 그대로 싣고 **실패·스킵을 위로 정렬**한다. `실패 / 스냅샷에 소재 없음` 은 prep 이후 소재 삭제(Pitfall 7)라는 설명을 붙인다. 로그를 파싱하지 않는다(D-03). 검증: `test_항목단위_결과가_화면투영된다` |
| T-1-38 | Tampering (이력 어긋남) | 날짜 넘긴 되돌리기 | accept | `ledger.record_reverted:69` 가 `raises[-1]["date"] == today` 일 때만 `reverted` 를 찍는다. 고치려면 CLI 로직 변경 + ledger 테스트 재작성이라 **Phase 2**로 미룬다(OQ-2 확정). Phase 1 은 실행 버튼 위 경고 배너로만 다룬다 |
| T-1-39 | Tampering (예상 밖 대량) | 3주 묵은 판정으로 실행 | mitigate | 신선도 배너(Plan 01-04)가 경과일을 경고색으로 띄우고, 이 태스크의 체크포인트가 "먼저 오늘 회차를 만들 것"을 선택지로 명시한다. 그리고 계정별 상한(D-08)이 마지막 그물이다 |
</threat_model>

<verification>
- `.venv-web/bin/pytest webapp/tests -q` 전량 green
- `bash webapp/tests/no_commit_guard.sh` exit 0
- `for t in nvad reports ads_rules ledger bids prune; do .venv/bin/python3 .claude/skills/naver-ads-weekly/scripts/test_$t.py || exit 1; done`
- `CT_DEV_TOKEN=devtoken123 bash webapp/tests/security_curl.sh` 8종 PASS
- 첫 실제 실행 체크포인트 10단계 (Task 3)
</verification>

<success_criteria>
- 미리보기 → 실행이 한 흐름으로 이어지고, 미리보기 없이는 실행할 수 없다
- 실행이 미리보기가 만든 그 targets 파일을 재사용한다
- 결과가 항목 단위(성공/실패/스킵 + 사유)로 남고 화면에 보인다
- 미리보기와 달라진 항목이 사유와 함께 보고된다 (0건이면 0건이라고 말한다)
- 쓰기 잡이 겹치면 409 로 거부된다
- 실제 네이버 광고 화면에서 입찰가 변경이 확인된다
</success_criteria>

<output>
Create `.planning/phases/01-board-bid-raise/01-08-SUMMARY.md` when done
</output>
