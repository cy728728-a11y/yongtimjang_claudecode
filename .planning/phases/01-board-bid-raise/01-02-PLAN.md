---
phase: 01-board-bid-raise
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - .claude/skills/naver-ads-weekly/scripts/bids.py
  - .claude/skills/naver-ads-weekly/scripts/run_ads.py
  - webapp/tests/test_cli_patch.py
autonomous: true
requirements: [FLOW-02, FLOW-05]

must_haves:
  truths:
    - "D-01: `run_ads.py bids` 가 `--only-ads <파일>` 로 대상 소재를 좁힐 수 있다. 판정·입찰가 계산·백업 병합·ledger 갱신 로직은 한 줄도 바뀌지 않았다"
    - "D-01 재해석(OQ-1 확정): 필터는 `cmd_bids` 가 아니라 **`run_bids` 안쪽, `update_streaks` 호출 뒤**에 걸린다. `cmd_bids` 에 걸면 `zero_ids = {r['adId'] for r in rows}` 가 축소돼 나머지 상품의 연속실패 카운팅이 통째로 빠지고 `_last_streak_update=today` 는 찍혀 같은 날 전량 실행도 건너뛴다"
    - "D-02: `--preview-out <파일>` 이 계정별 `plans` **전량**을 JSON 으로 쓴다. stdout 은 여전히 상위 10건만 찍지만 화면은 이제 stdout 을 파싱하지 않는다"
    - "D-03: 실행 결과가 `plans[i]['result']`(성공/실패/스킵)와 `plans[i]['error']`(사유)로 항목 단위로 남는다. 로그 파싱으로 FLOW-05 를 충족시키지 않는다"
    - "`--only-ads` 파일이 깨지면 **exit 1** 이다. 전량 실행으로 폴백하지 않는다 — 5건인 줄 알았던 게 2,242건이 되는 게 이 명령의 가장 비싼 실수다"
    - "OQ-4 확정: `run_ads.py accounts` 가 alias·customer_id 만 stdout 에 찍는다. 웹앱이 `~/.eroom/naver-ads.json` 을 두 번째로 갖지 않는다 — SAFE-03 을 테스트가 아니라 구조로 보장한다"
    - "기존 CLI 테스트 100개(6파일)가 그대로 exit=0 이다 — 새 인자는 전부 기본값 `None` 이라 기존 호출부가 안 깨진다"
  artifacts:
    - path: ".claude/skills/naver-ads-weekly/scripts/bids.py"
      provides: "run_bids(..., only_ads=None) · run_revert(..., only_ads=None) · plan dict 의 result/error 키"
      contains: "only_ads"
    - path: ".claude/skills/naver-ads-weekly/scripts/run_ads.py"
      provides: "--only-ads · --preview-out 플래그, accounts 서브커맨드, _load_only_ads, _dump_preview"
      contains: "--preview-out"
    - path: "webapp/tests/test_cli_patch.py"
      provides: "회귀-02/03/04 + 항목단위 결과 검증"
  key_links:
    - from: "run_ads.cmd_bids"
      to: "bids.run_bids(..., only_ads=only)"
      via: "기본인자 전달. 리턴값을 더 이상 버리지 않고 outputs[alias] 에 모은다"
      pattern: "only_ads=only"
    - from: "bids.run_bids"
      to: "update_streaks"
      via: "필터는 update_streaks 호출 뒤, plan_raise 리스트 컴프리헨션 직전 단 한 줄"
      pattern: "update_streaks\\(led"
---

<objective>
기존 CLI 에 **주입구 2개 + 읽기 전용 서브커맨드 1개**를 뚫는다. 총 20줄 미만이고,
검증된 판정·계산·백업·ledger 로직은 한 줄도 건드리지 않는다.

이것 없이는 성공기준 2("행을 골라 미리보기")가 **구조적으로 성립하지 않는다** —
지금 `bids` 는 계정 전량을 돌 수밖에 없고, `cmd_bids` 는 `run_bids` 의 리턴값을 버리며,
dry-run stdout 은 상위 10건만 찍고 `… 외 N건` 으로 접는다.

Purpose: 웹앱이 CLI 로직을 재구현하지 않고도 "고른 것만" 처리하고 "전량 결과"를 표로 만들 수 있게 한다.
Output: `bids.py`·`run_ads.py` 원위치 패치 + `webapp/tests/test_cli_patch.py` + CLI 100 tests 불변 증명
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-board-bid-raise/01-VALIDATION.md
@.planning/phases/01-board-bid-raise/01-SKELETON.md

<interfaces>
<!-- 이 플랜이 확정하는 CLI 계약. Plan 01-05 의 AdsArgv 와 01-06/01-07 의 파일 사슬이 여기에 맞춰진다. -->

run_ads.py bids 서브커맨드:
  --run-dir <이름>            (기존)
  --account <alias...>        (기존, nargs="*" — 값 없이 주면 전 계정이 된다)
  --commit                    (기존)
  --revert                    (기존)
  --only-ads <경로>           (신규) 대상 adId 목록 JSON — 배열 또는 {"adIds":[...]}
  --preview-out <경로>        (신규) 계정별 run_bids/run_revert 리턴값을 이 JSON 파일에 쓴다

run_ads.py accounts 서브커맨드 (신규, 읽기 전용, 인자 없음):
  stdout: [{"alias": "cy728", "customer_id": "4158478"}, ...]   # 시크릿 없음
  exit 0 (계정 0개여도 빈 배열 + exit 0)

--preview-out 산출물 스키마:
  {"<alias>": {"plans": [...], "counts": {...}, "committed": int, "failed": int?, "aborted": str?}, ...}
  plans[i]: {"adId","title","action","from","to","useGroupBid"}   (기존, 불변)
          + {"result": "성공"|"실패"|"스킵", "error": str}          (신규)
  action 5종: 인상 · 최근인상 · 연속실패중단 · 상한도달 · 입찰가불명
  run_revert 리턴: {"targets": int, "restored": int, ...}

bids.py 파이썬 시그니처:
  run_bids(acct, run_dir, rows, commit=False, log=print, only_ads=None) -> dict
  run_revert(acct, run_dir, commit=False, log=print, only_ads=None) -> dict
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `bids.py` — `only_ads` 필터 2곳 + 항목 단위 result/error</name>
  <files>.claude/skills/naver-ads-weekly/scripts/bids.py</files>
  <read_first>
    - `.claude/skills/naver-ads-weekly/scripts/bids.py` 전량(282줄) — 특히 89행 `update_streaks` 정의, 127행 `run_bids` 시그니처, 146행 `update_streaks(led, {r["adId"] for r in rows}, ...)`, 160행 `plans = [plan_raise(...)]`, 165-173행 dry-run 분기와 상위 10건 접기, 175-193행 **Important 1 백업 병합**, 199-221행 commit 루프와 `try/finally` 의 `ledger.save`, 224행 `run_revert`, 250행 `targets = [...]`
    - `.claude/skills/naver-ads-weekly/scripts/ledger.py` — `bid_decision` 액션 5종 · `BID_CAP=200` · `BID_STEP=10` · `RAISE_COOLDOWN_DAYS=6` · `FAIL_STREAK=3` · `record_reverted:69`
    - `.claude/skills/naver-ads-weekly/scripts/test_bids.py` 전량 — 특히 166·200행(`run_revert` 호출), 248·262행(`run_bids` 호출) — 새 인자가 기본값 `None` 이라 안 깨지는 근거
    - `.planning/phases/01-board-bid-raise/01-RESEARCH.md` §1.3 · §1.5 · §Pitfall 5
  </read_first>
  <behavior>
    - `run_bids(..., only_ads=None)` 은 기존과 **바이트 단위로 같은** 결과를 낸다 (회귀-02)
    - `run_bids(..., only_ads={"a"})` 를 줘도 `update_streaks` 는 `rows` **전량**의 adId 집합을 본다 (회귀-03 / T-1-04)
    - `only_ads` 로 걸러진 뒤에 `plan_raise` 가 돈다 → `plans` 길이 = 교집합 크기
    - commit 경로에서 `action != "인상"` 인 plan 은 `result="스킵"`, `error=action` 을 갖는다
    - commit 경로에서 스냅샷에 소재가 없으면 `result="실패"`, `error="스냅샷에 소재 없음"`
    - `apply_raise` 실패 시 `result="실패"`, `error=<축약된 사유>`
    - `apply_raise` 성공 시 `result="성공"`, `error=""`
    - dry-run 경로의 plan 에는 `result` 키가 **없다** (아직 실행하지 않았으므로 "성공"이라고 말하면 거짓말이다)
    - `run_revert(..., only_ads={"a","b","c"})` 의 `targets` 가 정확히 3 이고, `before_bids_<alias>.json` 파일 내용은 **바뀌지 않는다**
    - `run_revert(..., only_ads=None)` 은 백업 전량을 대상으로 잡는다 (기존 동작 불변)
  </behavior>
  <action>
    `run_bids` 시그니처를 `def run_bids(acct, run_dir, rows, commit=False, log=print, only_ads=None):` 로 바꾼다. **새 kwarg 는 `log` 뒤에** 붙인다 — 이 파일 전체(`bids.py:89,127,224` · `collect.py:67` · `reports.py:72`)의 관례다.

    필터는 **딱 한 줄**이고 **위치가 전부다.** 146행 `update_streaks(led, {r["adId"] for r in rows}, recovered_ids, today, log=log)` **뒤**, 160행 `plans = [plan_raise(r, led, today=today) for r in rows]` **앞**에 `if only_ads is not None: rows = [r for r in rows if r["adId"] in only_ads]` 를 넣는다. 바로 위에 한국어 주석으로 근거를 남겨라: **"update_streaks 는 rows 전량을 봐야 한다 — 여기를 앞에서 거르면 나머지 소재의 연속실패 카운팅이 이번 회차에 통째로 빠지고, `_last_streak_update=today` 가 찍혀 같은 날 도는 전량 실행마저 건너뛴다 (Pitfall 5)."**

    165-173행의 dry-run 로그 출력(상위 10건 + `… 외 N건` + `(dry-run — --commit 을 주면 실제로 바꾼다)`)은 **그대로 둔다.** `--preview-out` 이 전량을 따로 쓸 뿐이다.

    175-193행의 백업 병합(**Important 1** — 같은 키는 먼저 것을 유지)은 **한 글자도 건드리지 마라.** `only_ads` 로 `rows` 가 줄면 `plans` 도 줄어 이번 백업에 안 담기는 adId 가 생기지만, 기존 키가 보존되므로 안전하다. 그리고 이게 D-12~D-14("이 작업분만 되돌리기")가 성립하는 근거다 — 주석으로 남겨라.

    199-221행 commit 루프에 `result`/`error` 키를 더한다. `action != "인상"` 분기에서 `p["result"], p["error"] = "스킵", p["action"]` (스킵 사유가 곧 action 이다: 최근인상·연속실패중단·상한도달·입찰가불명). `ad_by_id.get(p["adId"])` 가 None 이면 `fail += 1` 뒤 `p["result"], p["error"] = "실패", "스냅샷에 소재 없음"` — 이건 **스냅샷 기반 실행의 정상적 실패**다(prep 이후 소재가 삭제된 경우, Pitfall 7). `apply_raise` 성공이면 `p["result"], p["error"] = "성공", ""`, 실패면 `p["result"], p["error"] = "실패", err` 로 하고 기존 `log(f"    ✗ {p['adId']} {err}")` 줄은 그대로 남긴다. **`ok`/`fail` 카운트 증감 순서와 `ledger.record_raise` 호출 위치를 바꾸지 마라.** **`try/finally` 안의 `ledger.save(led_path, led)` 는 이 파일의 핵심 안전장치다 — 구조를 손대지 마라.** `error` 문자열은 저장소 관례대로 잘라 담는다(`str(err)[:150]`).

    `run_revert` 시그니처를 `def run_revert(acct, run_dir, commit=False, log=print, only_ads=None):` 로 바꾸고, 250행을 `targets = [ad_id for ad_id, attr in backup.items() if attr is not None and (only_ads is None or ad_id in only_ads)]` 로 바꾼다. **백업 파일은 읽기만 한다** — `run_revert` 가 `before_bids_<alias>.json` 을 쓰지 않는다는 사실을 주석으로 못박아라(D-13).

    한국어 docstring 을 저장소 §S-1 관례대로 갱신한다: 한 줄 요약 → 빈 줄 → **왜 이 인자가 존재하는지**(웹앱이 고른 소재만 처리하기 위함) + Pitfall 번호 인용.
  </action>
  <verify>
    <automated>for t in nvad reports ads_rules ledger bids prune; do .venv/bin/python3 .claude/skills/naver-ads-weekly/scripts/test_$t.py || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - 아래 명령이 exit 0 으로 통과한다:
      ```
      for t in nvad reports ads_rules ledger bids prune; do .venv/bin/python3 .claude/skills/naver-ads-weekly/scripts/test_$t.py || exit 1; done
      ```
    - `01-VALIDATION.md § Per-Task Verification Map` 의 해당 행이 지목한 **테스트 파일**이 위 명령에 들어 있고,
      그 행의 `핵심 노드` 테스트가 이 실행 안에서 돈다 (VALIDATION 은 파일 단위로 적고, 노드 ID 는 병기만 한다 —
      태스크는 파일 전체를 돌린다. 노드 하나만 돌리는 것보다 회귀 방어가 낫기 때문이다)
  </acceptance_criteria>
  <done>CLI 6개 테스트 파일 전부 exit=0 (100 tests 기준선 불변). `grep -n "only_ads" bids.py` 가 시그니처 2줄 + 필터 1줄 + revert 조건 1줄 = 4곳에만 나타난다.</done>
</task>

<task type="auto">
  <name>Task 2: `run_ads.py` — `--only-ads` · `--preview-out` · `accounts` 서브커맨드</name>
  <files>.claude/skills/naver-ads-weekly/scripts/run_ads.py</files>
  <read_first>
    - `.claude/skills/naver-ads-weekly/scripts/run_ads.py` 전량(224줄) — 특히 29-57행(`data_root`/`run_dir_of` 와 "절대경로를 박지 마라" 주석), 146-172행 `cmd_bids`, 160-164행 **Important 10 읽기 방어 패턴**, 166-171행 **Important 2 조용한 스킵 금지**, 198-222행 `main()` 서브파서 등록과 디스패치 dict
    - `.claude/skills/naver-ads-weekly/scripts/nvad.py` 21행(`CRED` 상수) · 26-32행 `load_accounts()` · 41-51행 `headers()` — **`api_key`/`secret_key` 를 코드에 쓰지 않는 화이트리스트 투영의 정본**
    - `.claude/skills/naver-ads-weekly/scripts/detail_batch.py` 가 아니라 `.claude/skills/coupang-candidates/scripts/run_coupang.py` 68-74행 — 원자적 쓰기(tmp → `os.replace`) 관용구
    - `.planning/phases/01-board-bid-raise/01-RESEARCH.md` §1.3 · §3.4 · §1.1 종료코드 표
  </read_first>
  <action>
    `main()` 의 bids 서브파서 블록(208-213행) 끝에 두 줄을 더한다: `s.add_argument("--only-ads", help="대상 adId 목록 JSON 파일 — 이 목록에 있는 소재만 처리한다")` 와 `s.add_argument("--preview-out", help="계정별 계획/결과를 이 JSON 파일에 쓴다")`. 헬프 문자열은 기존 관례대로 한국어 반말 서술형이다.

    `accounts` 서브커맨드를 등록한다: `sub.add_parser("accounts")` — 인자 없음. 디스패치 dict(219-220행)에 `"accounts": cmd_accounts` 를 더한다.

    `cmd_accounts(args)` 를 새로 쓴다. `nvad.load_accounts()` 의 결과를 **화이트리스트 투영**해 `[{"alias": a.get("alias"), "customer_id": str(a.get("customer_id"))} for a in ...]` 만 `json.dumps(..., ensure_ascii=False)` 로 stdout 에 찍고 `return 0`. **`api_key`·`secret_key` 라는 문자열을 이 함수에 쓰지 마라** — 블랙리스트가 아니라 화이트리스트여야 키가 추가돼도 안 샌다. 계정이 0개여도 `[]` 를 찍고 exit 0 (웹앱이 "설정이 비었다"를 구분할 수 있게). 한국어 docstring 에 **"웹앱이 `~/.eroom/naver-ads.json` 을 두 번째로 갖지 않게 하려고 존재한다. 시크릿은 절대 출력하지 않는다 (SAFE-03)"** 를 남긴다.

    `_load_only_ads(path)` 를 새로 쓴다. `path` 가 falsy 면 `None`. 아니면 `json.loads(Path(path).read_text(encoding="utf-8"))` 후 `set(ids if isinstance(ids, list) else ids["adIds"])`. **이 함수 자체는 예외를 잡지 않는다** — 호출부가 잡는다.

    `_dump_preview(path, outputs)` 를 새로 쓴다. `path` 가 falsy 면 아무것도 안 한다. 아니면 **원자적 쓰기**(`tmp = str(path) + ".tmp"` → `json.dump(..., ensure_ascii=False, indent=1)` → `os.replace(tmp, path)`). `ensure_ascii=False` 는 예외 없이 붙인다 — 한국어 상품명이 `\uXXXX` 로 깨지면 사람이 못 읽는다. 실패는 try-except 로 잡아 `print(f"--preview-out 쓰기 실패: {type(e).__name__}: {e}")` 하고 **exit 1 을 돌려준다** — 산출물이 없으면 화면이 결과를 못 읽으니 조용히 성공으로 끝나면 안 된다.

    `cmd_bids` 를 고친다. 함수 맨 앞(run_dir·accts 결정 직후)에서 `--only-ads` 를 읽되, **읽기 실패는 중단이다**: `try: only = _load_only_ads(args.only_ads)` / `except Exception as e: print(f"--only-ads 읽기 실패 — 전량 실행으로 폴백하지 않는다: {type(e).__name__}: {e}"); return 1`. 이 방어 수준은 160-164행 **Important 10** 과 같아야 한다 — 돈이 나가는 명령이다. 예외 문자열 포맷 `f"{type(e).__name__}: {e}"` 는 저장소 표준 관용구(9회 반복)다.

    `outputs = {}` 딕셔너리를 만들고, revert 분기에서는 `outputs[alias] = bids.run_revert(acct, run_dir, commit=args.commit, only_ads=only)`, 일반 분기에서는 `outputs[alias] = bids.run_bids(acct, run_dir, v.get("rules", {}).get("①노출0", []), commit=args.commit, only_ads=only)` 로 **리턴값을 더 이상 버리지 않는다**(D-02 가 지적한 바로 그 줄). 두 분기 모두 return 직전에 `_dump_preview(args.preview_out, outputs)` 를 부르고, 그 리턴이 실패면 1 을 돌려준다.

    166-171행의 **Important 2**(자격증명 없는 계정도 `[{alias}] 자격증명 없음 — 건너뛴다` 로 로그에 남긴다)는 그대로 둔다. `--revert` 분기가 `result.json` 을 요구하지 않는 **Important 4** 구조도 그대로 둔다.

    `import os` 가 파일에 없으면 13-17행 import 블록에 더한다.
  </action>
  <verify>
    <automated>.venv/bin/python3 .claude/skills/naver-ads-weekly/scripts/run_ads.py accounts | .venv-web/bin/python -c "import json,sys;d=json.load(sys.stdin);assert isinstance(d,list);assert all(set(x)=={'alias','customer_id'} for x in d), d;print('PASS', len(d))" && ! grep -n "secret_key\|api_key" .claude/skills/naver-ads-weekly/scripts/run_ads.py && for t in nvad reports ads_rules ledger bids prune; do .venv/bin/python3 .claude/skills/naver-ads-weekly/scripts/test_$t.py || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - 아래 명령이 exit 0 으로 통과한다:
      ```
      .venv/bin/python3 .claude/skills/naver-ads-weekly/scripts/run_ads.py accounts | .venv-web/bin/python -c "import json,sys;d=json.load(sys.stdin);assert isinstance(d,list);assert all(set(x)=={'alias','customer_id'} for x in d), d;print('PASS', len(d))" && ! grep -n "secret_key\|api_key" .claude/skills/naver-ads-weekly/scripts/run_ads.py && for t in nvad reports ads_rules ledger bids prune; do .venv/bin/python3 .claude/skills/naver-ads-weekly/scripts/test_$t.py || exit 1; done
      ```
    - `01-VALIDATION.md § Per-Task Verification Map` 의 해당 행이 지목한 **테스트 파일**이 위 명령에 들어 있고,
      그 행의 `핵심 노드` 테스트가 이 실행 안에서 돈다 (VALIDATION 은 파일 단위로 적고, 노드 ID 는 병기만 한다 —
      태스크는 파일 전체를 돌린다. 노드 하나만 돌리는 것보다 회귀 방어가 낫기 때문이다)
  </acceptance_criteria>
  <done>`run_ads.py accounts` 가 alias/customer_id 만 담은 JSON 배열을 찍고, `run_ads.py` 소스에 `secret_key`·`api_key` 문자열이 0건이며, CLI 100 tests 가 그대로 통과한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `webapp/tests/test_cli_patch.py` — 회귀 3종 + 항목단위 결과 + 실데이터 스모크</name>
  <files>webapp/tests/test_cli_patch.py</files>
  <read_first>
    - `.claude/skills/naver-ads-weekly/scripts/test_bids.py` 80-115행(몽키패치 setUp/tearDown) · 210-267행(`TestRunBidsBackupMerge` 의 tempfile run-dir 과 `_write_ads`) — 이 플랜의 pytest 테스트가 베낄 원형
    - `.claude/skills/naver-ads-weekly/scripts/bids.py` (Task 1 패치 후 상태)
    - `webapp/tests/conftest.py` · `webapp/tests/fixtures/targets_one.json` · `targets_broken.json` (Plan 01-01 산출물)
    - `.planning/phases/01-board-bid-raise/01-VALIDATION.md` § Per-Task Verification Map 의 회귀-02/03/04 · FLOW-05 행 — **테스트 함수 이름이 표와 글자 그대로 같아야 한다**
  </read_first>
  <behavior>
    - `test_only_ads_none_은_전량과_같다` — 같은 rows 로 `only_ads=None` 과 인자 생략이 동일한 `plans` 를 낸다
    - `test_필터해도_streak_은_전량기준` — `update_streaks` 를 몽키패치로 가로채 **전량 adId 집합**이 넘어왔음을 단언한다. 필터 뒤 `plans` 는 1건인데 `zero_ids` 는 전체 건수여야 한다 (T-1-04)
    - `test_only_ads_깨지면_실패한다` — `run_ads.py bids --only-ads webapp/tests/fixtures/targets_broken.json` 의 **exit code 가 1**이고 stdout 에 "폴백하지 않는다"가 들어 있다 (T-1-05)
    - `test_항목단위_결과` — `nvad.call` 을 몽키패치해 한 건은 200, 한 건은 500 을 돌려주게 하고, 결과 plans 가 `result` 에 "성공"/"실패"/"스킵"을 각각 갖고 `error` 에 사유가 들어 있음을 단언한다 (FLOW-05)
    - `test_preview_out_이_전량을_쓴다` — plans 11건 이상일 때 `--preview-out` 산출물의 plans 수가 stdout 의 10건 접기와 무관하게 전량이다 (D-02)
  </behavior>
  <action>
    `webapp/tests/test_cli_patch.py` 를 pytest 스타일로 쓴다. 파일 최상단에서 `sys.path.insert(0, str(SCRIPTS_DIR))` 로 `.claude/skills/naver-ads-weekly/scripts` 를 넣고 `import bids` 한다. **이 sys.path 삽입은 테스트 프로세스에만 허용된다** — 한국어 주석으로 못박아라: "웹앱 런타임(`webapp/*.py`)은 절대 이걸 하지 않는다. `bids`·`collect`·`nvad` 는 네임스페이스 없는 최상위 이름이라 stdlib 을 가린다 (RESEARCH §5.6 에서 `inspect` 로 실제 재현됨). 여기서만, 테스트 격리 목적으로 허용한다." `bids.py` 는 stdlib + `ledger` + `nvad` 만 import 하므로 `.venv-web` 에서 정상 동작한다(확인됨).

    `test_bids.py:83-104` 의 몽키패치 패턴을 그대로 가져온다 — `bids.nvad.call` 을 가짜 함수로 바꾸고 `bids.time.sleep` 을 끈다(`time.sleep(0.08)` × 다건이면 테스트가 실제로 기다린다). pytest `monkeypatch` 픽스처로 복원을 맡긴다.

    `test_bids.py:217-239` 의 tempfile run-dir 패턴을 그대로 쓴다 — **`tmp/runs/<회차>` 2단 구조를 반드시 만들어라.** `bids.py:130` 의 `run_dir.parent.parent/"ledger"` 규약 때문에 이걸 빼먹으면 테스트가 실제 ledger 파일을 오염시킨다. `accounts/<alias>/ads.json` 과 `stats_7d.json` 도 같이 심는다.

    `test_필터해도_streak_은_전량기준` 은 `bids.update_streaks` 를 가짜로 바꿔 넘어온 `zero_ids` 를 기록하게 한다. rows 3건 중 `only_ads` 로 1건만 남겼을 때 기록된 `zero_ids` 의 크기가 **3** 이어야 한다. 이 테스트 하나가 OQ-1 확정(필터를 `cmd_bids` 가 아니라 `run_bids` 안쪽에 건다)의 유일한 자동 방어선이다 — 주석으로 그 사실을 남겨라.

    `test_only_ads_깨지면_실패한다` 는 `subprocess.run([".venv/bin/python3", "<run_ads.py>", "bids", "--run-dir", <tmp회차>, "--only-ads", "<targets_broken.json>"], capture_output=True, text=True)` 로 **실제 프로세스를 띄워** returncode 를 본다. 이게 CLI 계약(exit 1)을 증명하는 유일한 방법이다. `--commit` 은 **절대 붙이지 마라** — `webapp/tests/no_commit_guard.sh` 가 이 파일도 스캔한다.

    `test_항목단위_결과` 는 `bids.nvad.call` 을 `seq.pop(0)` 방식(테스트 관례)으로 `(200, {})` 과 `(500, "서버 오류")` 를 번갈아 돌려주게 하고, rows 에 `action` 이 "인상"이 될 행 2개 + 쿨다운에 걸려 "최근인상"이 될 행 1개를 섞는다. 단언: 성공 1 · 실패 1 · 스킵 1, 그리고 스킵 plan 의 `error` 가 `"최근인상"` 이다. **PUT 을 한 번도 안 내보낸다**(몽키패치 덕분).

    테스트 이름은 `01-VALIDATION.md` 의 Automated Command 열과 **글자 그대로** 같아야 한다.
  </action>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_cli_patch.py -x -q && bash webapp/tests/no_commit_guard.sh</automated>
  </verify>
  <acceptance_criteria>
    - 아래 명령이 exit 0 으로 통과한다:
      ```
      .venv-web/bin/pytest webapp/tests/test_cli_patch.py -x -q && bash webapp/tests/no_commit_guard.sh
      ```
    - `01-VALIDATION.md § Per-Task Verification Map` 의 해당 행이 지목한 **테스트 파일**이 위 명령에 들어 있고,
      그 행의 `핵심 노드` 테스트가 이 실행 안에서 돈다 (VALIDATION 은 파일 단위로 적고, 노드 ID 는 병기만 한다 —
      태스크는 파일 전체를 돌린다. 노드 하나만 돌리는 것보다 회귀 방어가 낫기 때문이다)
  </acceptance_criteria>
  <done>`test_cli_patch.py` 5개 테스트 전량 통과, `--commit` 리터럴 0건, CLI 100 tests 여전히 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 웹앱이 만든 targets 파일 → CLI 프로세스 | 웹앱이 고른 대상이 파일로 건너간다. 이 파일을 잘못 읽으면 실행 범위가 통째로 바뀐다 |
| CLI 프로세스 → 네이버 광고 API | `--commit` 시 실제 `PUT /ncc/ads/{adId}` — 되돌릴 수는 있지만 돈이 움직인다 |
| `~/.eroom/naver-ads.json` → stdout | `accounts` 서브커맨드가 이 파일을 읽어 표준출력으로 내보낸다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-04 | Tampering | `bids.run_bids` 의 `only_ads` 필터 위치 | mitigate | 필터를 `update_streaks(led, {r["adId"] for r in rows}, ...)` **뒤**, `plans = [plan_raise(...)]` **앞**에 단 한 줄로 건다. `cmd_bids` 에는 절대 걸지 않는다. 자동 방어선: `webapp/tests/test_cli_patch.py::test_필터해도_streak_은_전량기준` 이 몽키패치로 `zero_ids` 크기를 단언한다 |
| T-1-05 | Tampering (자해, **최악**) | `run_ads._load_only_ads` | mitigate | 읽기 실패 시 `print(f"--only-ads 읽기 실패 — 전량 실행으로 폴백하지 않는다: {type(e).__name__}: {e}")` 후 **`return 1`**. 빈 집합으로도, `None` 으로도 폴백하지 않는다. 자동 방어선: `test_only_ads_깨지면_실패한다` 가 실제 프로세스의 exit code 를 본다 |
| T-1-03b | Info Disclosure | `run_ads.cmd_accounts` | mitigate | `nvad.load_accounts()` 결과를 **화이트리스트 투영**(`alias`·`customer_id` 만 새 dict). `api_key`/`secret_key` 문자열을 이 함수에 쓰지 않는다 — 키가 추가돼도 안 샌다. 자동 방어선: `! grep -n "secret_key\|api_key" run_ads.py` |
| T-1-13 | Tampering | `run_ads._dump_preview` 부분 쓰기 | mitigate | `tmp → os.replace` 원자적 쓰기. 반쪽 JSON 을 다음 회차가 `--only-ads` 로 읽는 사고를 막는다. 쓰기 실패는 exit 1 (조용히 성공하면 화면이 빈 결과를 정상으로 읽는다) |
| T-1-14 | Tampering | `bids.run_bids` 백업 병합 (Important 1) | accept | `only_ads` 로 `plans` 가 줄면 이번 백업에 안 담기는 adId 가 생긴다. 기존 키가 보존되므로 되돌리기 가능성은 유지된다 — 이게 D-12~D-14 가 성립하는 근거다. 로직 무변경 |
| T-1-15 | Repudiation | `plans[i]["error"]` 길이 | mitigate | 저장소 관례대로 `str(err)[:150]` 로 잘라 담는다. 원문 예외를 통째로 산출물·화면에 싣지 않는다 |
</threat_model>

<verification>
- `for t in nvad reports ads_rules ledger bids prune; do .venv/bin/python3 .claude/skills/naver-ads-weekly/scripts/test_$t.py || exit 1; done` — **100 tests 불변**
- `.venv-web/bin/pytest webapp/tests/test_cli_patch.py -x -q`
- 실데이터 스모크(네트워크 0, 0.066초):
  `echo '["nad-a001-02-000000495390006"]' > /tmp/t.json && .venv/bin/python3 .claude/skills/naver-ads-weekly/scripts/run_ads.py bids --run-dir 2026-08-30 --only-ads /tmp/t.json --preview-out /tmp/p.json && .venv-web/bin/python -c "import json;d=json.load(open('/tmp/p.json'));print({k:len(v.get('plans',[])) for k,v in d.items()})"`
  기대: `{'cy728': 0, 'cy7728': 0, 'ownway1': 1, 'pogeunae': 0}`
- `git diff --stat` 이 `bids.py`·`run_ads.py` 두 파일 합쳐 **+30줄 미만**
</verification>

<success_criteria>
- `--only-ads` 로 대상이 좁혀지고, `update_streaks` 는 여전히 전량을 본다
- `--only-ads` 파일이 깨지면 exit 1 (전량 실행 0건)
- `--preview-out` 산출물이 `plans` 전량을 담고, commit 경로에서 항목별 `result`/`error` 를 갖는다
- `run_ads.py accounts` 가 시크릿 없이 계정 목록을 낸다
- CLI 테스트 100개가 그대로 통과한다
</success_criteria>

<output>
Create `.planning/phases/01-board-bid-raise/01-02-SUMMARY.md` when done
</output>
