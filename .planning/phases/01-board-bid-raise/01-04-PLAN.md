---
phase: 01-board-bid-raise
plan: 04
type: execute
wave: 2
depends_on: ["01-03"]
files_modified:
  - webapp/board.py
  - webapp/routes/board.py
  - webapp/templates/board.html
  - webapp/static/board.js
  - webapp/tests/test_board.py
autonomous: false
requirements: [BOARD-01, BOARD-02]

must_haves:
  truths:
    - "D-04: 보드에 6규칙 전부가 실린다. 버튼은 나중에 ① 하나만 열린다 — ①행에는 imp/clk/ctr 가 아예 없어서 ①만 실으면 BOARD-01 이 요구하는 열이 전부 빈칸이 되어 검증이 불가능하다"
    - "D-05: 보드 한 줄 = 상품이다. 접기 키는 (계정 alias, mallProductId) 이고, 판정값을 새로 만들지 않고 모으기만 한다"
    - "C-2 해소: 같은 소재(adId)가 ②와 ③에 동시에 들어가도 노출·클릭이 두 번 세지지 않는다 — 규칙을 순회하기 전에 adId 로 먼저 dedupe 한다"
    - "①·⑥ 행에 imp/clk/ctr/rank/cost 가 없어도 접기와 렌더가 깨지지 않는다 (실데이터 2,274행이 그렇다)"
    - "BOARD-02: 계정 목록·규칙 목록이 result.json 에서만 온다. 가짜 5번째 계정 zzfake 가 픽스처에 있는데도 화면에 나타난다"
    - "D-16: 보드 상단에 회차 날짜 · 경과일 · 실제 통계기간이 함께 보이고, 경과일이 임계값(기본 8일)을 넘으면 경고색이다"
    - "사용자가 계정·규칙으로 거르고, 열을 눌러 정렬하고, 상품명으로 검색한다 (Tabulator 클라이언트 사이드)"
    - "보드는 result.json 만 읽는다. accounts/*/ads.json(2.6MB×2)은 열지도 않고 브라우저로 보내지도 않는다"
    - "웹앱은 입찰가를 계산하지 않는다. bid/useGroupBid/groupBid 를 읽어 표시만 한다"
  artifacts:
    - path: "webapp/board.py"
      provides: "fold_products(result) -> list[dict] · rule_counts() · account_list() — 읽기 전용 순수 투영"
      min_lines: 60
      contains: "mallProductId"
    - path: "webapp/routes/board.py"
      provides: "GET / 가 보드 데이터·신선도·계정/규칙 필터 목록을 템플릿에 넘긴다"
    - path: "webapp/static/board.js"
      provides: "Tabulator 초기화 · 컬럼 정의 · 필터 위젯 · 검색"
    - path: "webapp/tests/test_board.py"
      provides: "접기·중복계상·계정 비하드코딩·신선도 검증"
  key_links:
    - from: "webapp/board.py"
      to: "run-dir/result.json"
      via: "paths.run_dir_path(name) / 'result.json' 을 json.loads. CLI 를 실행하지 않는다"
      pattern: "result.json"
    - from: "webapp/static/board.js"
      to: "Tabulator 6.5.3"
      via: "/static/vendor/tabulator-6.5.3.min.js — CDN 아님"
      pattern: "static/vendor"
    - from: "webapp/routes/board.py"
      to: "webapp/paths.py freshness()"
      via: "신선도 배너 데이터"
      pattern: "freshness"
---

<objective>
**사용자가 브라우저에서 광고 판정 결과를 한 줄 = 상품 1개로 본다.** 성공기준 1 이 여기서 끝난다.

Plan 01-03 이 만든 빈 셸에 실제 데이터를 채운다. 2,726 판정행이 2,637 상품 줄로 접히고,
계정·규칙으로 걸러지고, 정렬·검색이 된다. 상단에 회차 신선도가 뜬다.

**이 플랜은 CLI 를 한 번도 실행하지 않는다.** `run-dir/result.json` 을 읽어 투영만 한다
(STACK.md "세 번째 길"). 크레딧도 광고비도 네트워크도 0 이다.

Purpose: 사용자가 "고를 수 있는 화면"이 있어야 이후 모든 버튼이 대상을 받는다.
Output: `webapp/board.py` 투영 함수 + Tabulator 보드 + 신선도 배너 + 테스트 5종
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-board-bid-raise/01-VALIDATION.md
@.planning/phases/01-board-bid-raise/01-SKELETON.md

<interfaces>
이 플랜이 만드는 계약. Plan 01-06 의 선택 UI 와 대상 확정이 여기에 올라탄다.

webapp/board.py:
  fold_products(result: dict) -> list[dict]
      반환 행: {
        "acct": str,                  # 계정 alias
        "mallProductId": str | None,
        "title": str,
        "rules": str,                 # "①③" 처럼 규칙 기호를 정렬해 이어붙인 문자열
        "rule1_ads": list[str],       # 이 상품의 규칙① 소재 adId 목록  ← 버튼이 적용될 대상 (D-06)
        "rule1_count": int,           # "이 버튼이 N건에 적용됩니다" 의 N
        "bid": int | None,            # 대표 ①소재의 현재 적용 입찰가 (effective_bid 결과를 읽기만)
        "useGroupBid": bool | None,
        "groupBid": int | None,
        "imp": int | None, "clk": int | None, "ctr": float | None,
        "purCnt": int | None, "purAmt": int | None, "cost": int | None,
      }
  account_list(result: dict) -> list[str]     # sorted(result["accounts"].keys())
  rule_list(result: dict) -> list[str]        # result 안에 실제로 등장한 규칙 키를 순서대로

webapp/routes/board.py 가 템플릿에 넘기는 컨텍스트:
  request · token · run_dirs · run_dir · freshness · accounts · rules · rows_json

read_first 로 확인할 result.json 실측 스키마 (RESEARCH §2.2):
  {"generated": "2026-08-30", "accounts": {"<alias>": {"summary": {...}, "rules": {"①노출0": [...], ...}}}}
  ①노출0 · ⑥삭제대상 행 필드: adId · adGroup · title · mallProductId · bid · useGroupBid · groupBid  (imp/clk/ctr/rank/cost 없음)
  ②③④ 행: 위 + imp · clk · ctr · rank · cost
  ⑤효자확정 행: 위 + purCnt · purAmt
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: webapp/board.py — 상품 단위 접기 (adId 선 dedupe)</name>
  <files>webapp/board.py, webapp/tests/test_board.py</files>
  <read_first>
    - `.claude/skills/naver-ads-weekly/scripts/report_md.py` 17-45행(`_table` 의 `.get(k, "")` 과 bool 을 int 보다 먼저 분기하는 함정), 43-45행(`_count_label` — 잘렸으면 잘렸다고 말한다), 57-59행과 73-75행(**"규칙별 행을 합산하지 않는다 — 같은 소재가 ②와 ③에 동시에 들어가 두 번 세진다"** 경고와 계정 비하드코딩 순회)
    - `.claude/skills/naver-ads-weekly/scripts/sheets_out.py` 28-48행 — `PREFIX` 와 규칙별 `COLS` 정의. **①에 imp/clk 가 없다는 사실이 이 표에 이미 박혀 있다.** 보드 컬럼 정의가 베낄 정본
    - `.claude/skills/naver-ads-weekly/scripts/ads_rules.py` 22-31행 `effective_bid` docstring("useGroupBidAmt=True 면 adAttr.bidAmt 는 잠자는 값이고 그룹 기본가가 적용된다 — 헷갈리면 올리려다 내린다"), 34-57행 `ad_info` 가 만드는 판정행 스키마
    - `.planning/phases/01-board-bid-raise/01-RESEARCH.md` §2.2 · §2.3 · §2.4 · § Pattern 2 `fold_products` · § Anti-Patterns · Pitfall 8
    - `.planning/phases/01-board-bid-raise/01-PATTERNS.md` §C-2 — 이 태스크가 해소해야 할 충돌
    - `webapp/tests/fixtures/result_min.json` (Plan 01-01 산출물)
  </read_first>
  <behavior>
    - `test_imp_없는_행도_접힌다` — ①행만 있는 상품이 접기에서 예외 없이 나오고 `imp` 가 `None` 이다 (BOARD-01)
    - `test_계정을_코드에_박지_않는다` — 픽스처의 가짜 5번째 계정 `zzfake` 가 `account_list()` 와 접힌 행에 나타난다 (BOARD-02)
    - `test_같은_소재가_두_규칙에_있어도_노출이_두번_세지지_않는다` — ②와 ③에 동시에 들어간 adId 의 `imp` 가 한 번만 반영된다 (PATTERNS §C-2)
    - `test_대표_입찰가는_읽기만_한다` — `useGroupBid=true` 행의 `bid` 가 픽스처의 `groupBid` 와 같고, 웹앱이 산술을 하지 않는다
    - `test_접기_키는_계정과_상품이다` — 계정이 다른 같은 `mallProductId` 는 두 줄로 남는다
    - `test_rule1_count_는_규칙1_소재수다` — ①소재 2개 + ③소재 1개인 상품의 `rule1_count` 가 3이 아니라 2다 (D-06)
    - `mallProductId` 가 없는 행(이론상)도 `KeyError` 없이 처리된다
  </behavior>
  <action>
    `webapp/board.py` 를 쓴다. 모듈 docstring 에 **"읽기 전용이다. CLI 를 실행하지 않고 판정값을 만들지 않는다. `result.json` 만 읽는다 — `accounts/*/ads.json` 은 2.6MB × 2 라 열지도 않는다(Pitfall 8)"** 를 적는다.

    `fold_products(result)` 를 RESEARCH § Pattern 2 의 구조로 쓰되, **PATTERNS §C-2 의 충돌을 해소한 형태**로 좁힌다. 핵심은 두 단계다.

    1단계: `result["accounts"]` 를 순회하며 `(alias, adId)` → `{"row": <행>, "rules": set()}` 로 **소재를 먼저 dedupe** 한다. 같은 adId 가 ②와 ③에 동시에 들어오면 규칙 기호만 set 에 더하고, 지표 필드(`imp`·`clk`·`ctr`·`rank`·`cost`·`purCnt`·`purAmt`)는 **처음 본 값을 유지**한다(더하지 않는다). `report_md.py:59` 의 경고 "규칙별 행을 합산하지 않는다 — 같은 소재가 ②와 ③에 동시에 들어가 두 번 세진다" 를 주석으로 그대로 인용해라.

    2단계: dedupe 된 소재를 `(alias, mallProductId)` 로 접는다. 상품 행의 `imp`·`clk`·`purCnt`·`purAmt`·`cost` 는 **그 상품에 속한 서로 다른 소재들의 합**이다(소재 단위 중복은 1단계에서 이미 제거됐다). `ctr` 은 합산하지 말고 `clk/imp*100` 으로 재계산하거나, 합산 불가하면 `None` 으로 둬라 — 비율을 더하면 거짓말이 된다. `rules` 는 소재들의 규칙 기호 합집합을 정렬해 이어붙인 문자열(`"①③"`). `rule1_ads` 는 ① 규칙에 속한 adId 목록, `rule1_count` 는 그 길이.

    **`.get(field)` 로만 만져라.** ①·⑥ 행에는 `imp`/`clk`/`ctr`/`rank`/`cost` 키가 **아예 없다**(실데이터 2,274행). `row["imp"]` 를 쓰면 거기서 깨진다.

    `bid`·`useGroupBid`·`groupBid` 는 **그 상품의 ① 소재 중 첫 번째 것에서 읽어** 그대로 싣는다. 웹앱이 `bid + 10` 같은 산술을 하면 안 된다 — ①행의 92%가 `useGroupBid=true` 라 재계산하면 거의 전량 틀린다(실측: 잠자던 값 50 vs 실제 적용 70). 그 근거를 `effective_bid` docstring 인용과 함께 주석으로 남겨라.

    접기 키를 **반드시 `(alias, mallProductId)`** 로 해라. `mallProductId` 는 스마트스토어 상품ID라 계정 간 중복 가능성을 배제할 수 없다(RESEARCH Assumption A1). 어느 쪽이든 안전한 선택이다.

    `account_list(result)` 는 `sorted(result.get("accounts", {}).keys())`. **계정 alias 를 코드·템플릿·SQL 어디에도 리터럴로 쓰지 마라** — `~/.eroom/naver-ads.json` 에 항목을 더하는 것만으로 계정이 4개에서 6개로 늘어난다.

    `rule_list(result)` 는 실제로 등장한 규칙 키를 `①②③④⑤⑥` 순서로 돌려준다.

    `webapp/tests/test_board.py` 를 behavior 7개 항목에 1:1 대응하게 쓴다. `fake_result_json` 픽스처(Plan 01-01)를 쓴다 — 파일 IO 없이 순수 함수만 검증한다. 테스트 이름은 `01-VALIDATION.md` 의 Automated Command 열과 글자 그대로 같아야 한다.
  </action>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_board.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 아래 명령이 exit 0 으로 통과한다:
      ```
      .venv-web/bin/pytest webapp/tests/test_board.py -x -q
      ```
    - `01-VALIDATION.md § Per-Task Verification Map` 의 해당 행과 명령이 글자 그대로 일치한다
  </acceptance_criteria>
  <done>test_board.py 전량 통과. `grep -c 'ads.json' webapp/board.py` 가 0 이고, `grep -cE '"(cy728|cy7728|ownway1|pogeunae)"' webapp/` 가 픽스처 밖에서 0 이다.</done>
</task>

<task type="auto">
  <name>Task 2: GET / 에 보드 데이터·신선도 배너 연결 + Tabulator 렌더</name>
  <files>webapp/routes/board.py, webapp/templates/board.html, webapp/static/board.js</files>
  <read_first>
    - `webapp/routes/board.py` (Plan 01-03 이 만든 토큰 게이트 — 그 구조를 유지한 채 컨텍스트만 채운다)
    - `webapp/templates/board.html` (Plan 01-03 산출물 — hx-headers 와 include 자리)
    - `webapp/board.py` · `webapp/paths.py` · `webapp/settings.py`
    - `.claude/skills/naver-ads-weekly/scripts/sheets_out.py` 28-48행 — 규칙별 컬럼 정의를 **데이터로 두는** 구조. Tabulator 컬럼 정의가 이 모양을 따르면 ①의 빈칸이 설계상 정상값이 된다
    - `.claude/skills/naver-ads-weekly/scripts/report_md.py` 17-40행 — **bool 을 int 보다 먼저 분기**하는 함정(`useGroupBid` 렌더에 그대로 적용된다)
    - `.planning/phases/01-board-bid-raise/01-RESEARCH.md` §2.4 (페이로드 크기와 GZip) · §2.6 (신선도 — 회차명은 통계기간보다 항상 2일 신선해 보인다) · Pitfall 7 · § Tabulator 선택 모델
  </read_first>
  <action>
    `webapp/routes/board.py` 의 `GET /` 를 채운다. Plan 01-03 이 만든 토큰/쿠키 게이트 구조는 **그대로 두고**, 쿠키가 맞을 때 렌더하는 분기만 채운다.

    회차 선택: 쿼리 파라미터 `run_dir` 이 오면 `paths.run_dir_path(name)` 화이트리스트를 통과시킨다(`ValueError` 면 400). 없으면 `paths.scan_run_dirs()[0]`(최신). 회차가 하나도 없으면 "먼저 새로 수집을 돌려라"는 안내 화면을 낸다 — 빈 표를 띄우지 마라.

    `result.json` 읽기는 **try-except 로 감싸되 폴백하지 않는다** — 읽기 실패는 화면에 사유를 띄우고 빈 보드를 보여준다(이 실패는 돈으로 이어지지 않으므로 exit 이 아니라 화면 메시지다. 저장소 S-2 판별 기준). 예외 문자열은 `f"{type(e).__name__}: {e}"` 로 축약한다.

    템플릿 컨텍스트에 `run_dirs`(회차 선택 드롭다운), `run_dir`(현재), `freshness`(`paths.freshness()` 결과), `accounts`(`board.account_list()`), `rules`(`board.rule_list()`), `rows_json`(`board.fold_products()` 를 `json.dumps(..., ensure_ascii=False)` 한 문자열)을 넘긴다. 핸들러는 `async def` 가 아니라 `def` 로 선언한다.

    `rows_json` 은 547KB 다. **GZipMiddleware 가 이미 켜져 있어야 95KB 로 내려간다**(Plan 01-03 Task 2). `<script type="application/json">` 안에 넣고 JS 가 `JSON.parse` 로 읽게 한다 — Jinja2 자동 이스케이프가 걸린 채로 script 안에 들어가면 깨지므로, `tojson` 필터를 쓰거나 별도 `GET /board-data?run_dir=...` 읽기 라우트(쓰기 아님, 토큰 불필요)로 분리해라. 둘 중 하나를 고르고 이유를 주석에 남겨라.

    `webapp/templates/board.html` 을 채운다. 상단에 **신선도 배너**: `2026-08-30 회차 · 20일 전 · 통계 기간 08-22~08-28` 형태로 셋을 같이 보여준다(D-16). `freshness.stale` 이면 경고색(Pico 의 대비색 또는 인라인 스타일). 배너 문구 옆에 "3주 묵은 판정으로 입찰가를 올리는 게 이 단계의 실질 위험이다"에 해당하는 반말 안내를 한 줄 붙인다. 회차 드롭다운을 배너 옆에 둔다.

    필터 위젯: 계정 select(다중), 규칙 select(다중), 상품명 검색 input. **옵션은 전부 서버가 넘긴 `accounts`/`rules` 로 렌더한다** — 템플릿에 계정 alias 를 박지 마라.

    `webapp/static/board.js` 를 쓴다. Tabulator 6.5.3 을 `#board` 에 초기화한다. `layout: "fitColumns"`, `height: "60vh"`(가상 DOM 이 켜지려면 높이가 고정돼야 한다), `index` 는 `acct + "|" + mallProductId`.

    컬럼 정의를 **데이터 구조로** 둔다(`sheets_out.py:28-48` 관례). 컬럼: 계정 · 규칙(`rules`) · 상품명(`title`, 넓게) · 노출(`imp`) · 클릭(`clk`) · CTR(`ctr`) · 구매완료(`purCnt`) · 구매금액(`purAmt`) · 광고비(`cost`) · 현재입찰(`bid`) · 그룹입찰따름(`useGroupBid`) · ①소재수(`rule1_count`) · 상품ID(`mallProductId`).
    - `imp`/`clk` 등 수치 컬럼의 formatter 는 **`null` 을 빈칸으로** 낸다. ①·⑥행에 그 필드가 없는 게 정상이다 — `undefined` 가 화면에 찍히면 안 된다.
    - `useGroupBid` formatter 는 **bool 을 먼저 분기**해 `예`/`아니오` 로 낸다. 파이썬에서 bool 이 int 의 하위 타입이라 순서를 바꾸면 1/0 으로 찍혀 사람이 못 읽는 함정이 `report_md.py:17-40` 에 있다. JS 에서도 같은 실수를 하지 마라.
    - **`cost` 컬럼 헤더를 "매출"로 쓰지 마라.** 광고비다(`ads_rules._with_stat:50` 주석).
    - 정렬 기본값은 규칙 → 노출 내림차순.

    필터 위젯을 Tabulator 의 `setFilter` 에 연결한다. 상품명 검색은 `like` 매칭. **서버사이드 페이징·가상 스크롤 설정을 켜지 마라** — 2,637행은 클라이언트 사이드 한계(약 5,000행) 안이고 Out of Scope 다.

    선택(체크박스) 컬럼은 **이 플랜에서 넣지 않는다** — Plan 01-06 이 "보이는 것만 vs 필터 전체"(BOARD-03/D-07)와 함께 붙인다. 지금 넣으면 확인 배너 없는 전체 선택이 먼저 생긴다.
  </action>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_board.py webapp/tests/test_security.py -x -q && .venv-web/bin/python -c "
from fastapi.testclient import TestClient
from webapp import settings, security
from webapp.main import app
c = TestClient(app, base_url=f'http://127.0.0.1:{settings.PORT}')
c.cookies.set(security.COOKIE_NAME, security.BOOT_TOKEN)
r = c.get('/')
assert r.status_code == 200, r.status_code
assert 'undefined' not in r.text
print('PASS')
" && ! grep -rqn '매출' webapp/static/board.js</automated>
  </verify>
  <acceptance_criteria>
    - 아래 명령이 exit 0 으로 통과한다:
      ```
      .venv-web/bin/pytest webapp/tests/test_board.py webapp/tests/test_security.py -x -q && .venv-web/bin/python -c "
      from fastapi.testclient import TestClient
      from webapp import settings, security
      from webapp.main import app
      c = TestClient(app, base_url=f'http://127.0.0.1:{settings.PORT}')
      c.cookies.set(security.COOKIE_NAME, security.BOOT_TOKEN)
      r = c.get('/')
      assert r.status_code == 200, r.status_code
      assert 'undefined' not in r.text
      print('PASS')
      " && ! grep -rqn '매출' webapp/static/board.js
      ```
    - `01-VALIDATION.md § Per-Task Verification Map` 의 해당 행과 명령이 글자 그대로 일치한다
  </acceptance_criteria>
  <done>토큰 쿠키를 가진 GET / 가 200 이고 본문에 `undefined` 가 없다. board.js 에 "매출" 이라는 헤더 문자열이 없다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 실데이터 2,637행 보드 육안 확인</name>
  <files>webapp/templates/board.html</files>
  <read_first>
    - `.planning/phases/01-board-bid-raise/01-VALIDATION.md` § Manual-Only Verifications 의 "회차 신선도 배너" 행
    - `.planning/phases/01-board-bid-raise/01-RESEARCH.md` §2.2 규칙별 행 수 표(cy728 9/39/22/30/6/0 · ownway1 1,195/33/13/20/9/20 등)
  </read_first>
  <action>
    서버를 띄우고 실제 run-dir `2026-08-30`(result.json 879KB, 판정 2,726행)로 보드를 그린 뒤 사람이 확인한다. 자동 테스트는 픽스처만 보므로 실데이터 규모·인코딩·빈 필드가 실제로 버티는지는 여기서만 안다.
  </action>
  <what-built>
    `result.json` 을 읽어 (계정, mallProductId) 로 접은 2,637행 Tabulator 보드. 계정·규칙 필터, 상품명 검색, 열 정렬, 회차 드롭다운, 신선도 배너.
  </what-built>
  <how-to-verify>
    1. `CT_DEV_TOKEN=devtoken123 ./webapp/run-webapp.sh` 로 서버를 띄우고 기동 로그 URL 로 들어간다.
    2. 상단 배너에 `2026-08-30 회차 · N일 전 · 통계 기간 2026-08-22~2026-08-28` 가 뜨고, N 이 8을 넘으므로 **경고색**인지 확인한다.
    3. 행 수를 센다 — 보드 하단/헤더의 건수가 **2,637 근처**여야 한다(실측 distinct (계정, mallProductId)).
    4. 계정 필터를 `ownway1` 로 좁힌다 → 규칙 ① 행이 압도적으로 많아야 한다(실측 1,195).
    5. 규칙 필터를 ① 로 좁힌다 → **노출·클릭·CTR 열이 전부 빈칸**이고 `undefined`·`NaN` 이 한 칸도 없어야 한다. 이게 이 단계의 가장 흔한 깨짐이다.
    6. 규칙 필터를 ⑤ 로 좁힌다 → 노출·클릭·구매완료·구매금액에 실제 숫자가 보여야 한다.
    7. 상품명에 한글·중국어가 깨지지 않는지 본다(`ensure_ascii=False` 확인).
    8. `그룹입찰따름` 열이 `예`/`아니오` 로 보이는지(1/0 이 아니라).
    9. 상품명 검색에 아무 단어나 넣어 필터가 즉시 반응하는지, 노출 열을 눌러 정렬되는지 확인한다.
    10. 브라우저 Network 탭에서 `/` 응답이 **gzip 으로 100KB 내외**인지 본다(압축이 안 걸리면 547KB).
  </how-to-verify>
  <verify>
    <automated>.venv-web/bin/pytest webapp/tests/test_board.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv-web/bin/pytest webapp/tests/test_board.py -x -q` 통과
    - 실데이터 보드 행 수가 2,600~2,700 범위 (사람 확인)
    - 규칙 ① 필터에서 노출·클릭·CTR 칸이 빈칸이고 `undefined`/`NaN` 0건 (사람 확인)
    - 신선도 배너에 회차 날짜 · 경과일 · 통계기간 셋이 모두 보이고 경고색이다 (사람 확인)
    - `/` 응답이 gzip 압축돼 200KB 미만 (사람 확인)
  </acceptance_criteria>
  <resume-signal>"approved" 라고 답하거나, 몇 번 항목이 어떻게 달랐는지 알려줘</resume-signal>
  <done>실데이터 10단계 육안 확인 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run-dir 파일 → 웹앱 프로세스 | 디스크의 JSON 이 파싱돼 메모리에 들어온다 |
| 웹앱 → 브라우저 | 상품명(중국어 원문·특수문자 포함)이 HTML/JSON 으로 나간다 |
| 사용자 입력(run_dir 쿼리) → 파일 경로 | 회차 이름이 경로 조합에 쓰인다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-07 | Tampering (진실이 둘) | `webapp/board.py` 의 입찰가 필드 | mitigate | 웹앱은 `bid`·`useGroupBid`·`groupBid` 를 **읽기만** 한다. `bid + 10` 같은 산술을 하지 않는다 — ①행의 92%가 `useGroupBid=true` 라 재계산하면 거의 전량 틀린다(실측: 잠자던 값 50 vs 적용값 70). 검증: `test_대표_입찰가는_읽기만_한다` |
| T-1-11 | Tampering | `run_dir` 쿼리 파라미터 | mitigate | `paths.run_dir_path(name)` 화이트리스트만 통과. `scan_run_dirs()` 에 없는 이름은 `ValueError` → 400. 경로를 문자열 결합으로 만들지 않는다 (ASVS V12) |
| T-1-17 | Tampering (XSS) | 상품명 렌더 | mitigate | Jinja2 자동 이스케이프 + Tabulator 기본 텍스트 렌더. `| safe` 와 `formatter: "html"` 를 쓰지 마라 — 상품명에 중국어 원문·특수문자가 섞여 들어온다 |
| T-1-19 | Info Disclosure / DoS (자해) | `accounts/*/ads.json` | mitigate | 보드는 `result.json` **만** 읽는다. `ads.json` 은 2.6MB × 2 이고 소재 원본이라 열지도, 브라우저로 보내지도 않는다(Pitfall 8). 검증: `grep -c 'ads.json' webapp/board.py` == 0 |
| T-1-20 | Repudiation (오판 유도) | 신선도 미표시 | mitigate | 20일 묵은 판정으로 입찰가를 올리는 것이 이 단계의 실질 위험이다. 회차 날짜 + 경과일 + `prep_summary.json` 의 실제 통계기간 셋을 같이 보여주고, `stale_days`(기본 8) 초과면 경고색 (D-16 / Pitfall 7) |
| T-1-21 | Tampering (지표 왜곡) | 규칙 중복 계상 | mitigate | adId 선 dedupe 후 상품 접기. 같은 소재가 ②·③에 동시 등장해도 `imp`/`clk` 가 두 번 세지지 않는다. `ctr` 은 합산하지 않는다. 검증: `test_같은_소재가_두_규칙에_있어도_노출이_두번_세지지_않는다` |
</threat_model>

<verification>
- `.venv-web/bin/pytest webapp/tests -q` 전량 green
- 실데이터 스모크: `.venv-web/bin/python -c "import json,sys;sys.path.insert(0,'.');from webapp import board,paths;r=json.loads((paths.run_dir_path('2026-08-30')/'result.json').read_text());rows=board.fold_products(r);print(len(rows), sum(x['rule1_count'] for x in rows))"` — 기대: 행 수 2,637 근처, ①소재 합계 2,242
- 사람 확인 10단계(Task 3)
- `for t in nvad reports ads_rules ledger bids prune; do .venv/bin/python3 .claude/skills/naver-ads-weekly/scripts/test_$t.py || exit 1; done` — CLI 기준선 불변
</verification>

<success_criteria>
- 보드 한 줄 = 상품 1개, 접기 키 `(alias, mallProductId)`
- 6규칙 전부가 실리고 ①·⑥의 없는 필드가 빈칸으로 정상 렌더된다
- 계정·규칙 필터 목록이 `result.json` 에서만 오고 코드에 alias 리터럴이 없다
- 정렬·검색·필터가 클라이언트 사이드로 즉시 반응한다
- 신선도 배너에 회차 날짜 · 경과일 · 통계기간이 함께 뜨고 임계값 초과 시 경고색
</success_criteria>

<output>
Create `.planning/phases/01-board-bid-raise/01-04-SUMMARY.md` when done
</output>
