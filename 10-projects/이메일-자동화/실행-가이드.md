# 이메일 자동관리 — 실행 가이드 (방식 B 확정)

> 작성: 2026-07-13. **2026-08-19 재설계 반영**(③④⑤번 및 처리 규칙 갱신 — 아래 참조).
> 이 파일 = 세팅할 때 이거 하나만 보면 됨.

## 2026-08-19 갱신 요약
- **판단 기준 확장**: 삭제 3규칙은 그대로 두되, "확인 대상"을 고정 키워드가 아니라 **심각도/긴급성 기준**으로 판단하게 함. 클린위반·지재권·KC인증·계정정지경고는 **예시**일 뿐 전부가 아님 — 새 유형이 계속 생겨도 대응 가능해야 함.
- **실행 방식 확정**: Claude Code 클라우드 스케줄이 아니라 **이 PC에서 상시 실행**(Windows 작업 스케줄러 + 로컬 Python 스크립트, Claude API 직접 호출) — PC가 꺼져 있으면 자동화도 멈춘다는 트레이드오프 인지하고 선택함.
- **로그 추가**: 처리내역을 구글시트에 기록해 오분류 사후검증 가능하게.
- **실패 감지 추가**: 인증 만료(7일 주기, Testing 모드 시) 등으로 스크립트가 조용히 죽는 걸 감지하는 장치.

## 지금까지 완료된 것 ✅

- `@googleworkspace/cli`(gws) 설치 완료 — v0.22.5 (`C:\Users\woori\AppData\Roaming\npm\gws.ps1`)
  - ※ 워크스페이스 문서에 있던 `gws-cli`는 **틀린 패키지명**이었음. 진짜는 `@googleworkspace/cli`.
- Google Cloud SDK(gcloud) 설치 완료 — v575.0.1 (`C:\Program Files (x86)\Google\Cloud SDK\...`)
- gws Gmail 명령 구조 파악 완료 (triage/read/forward/trash)
- 자동화 설계 확정 (아래)

## 최종 설계 (방식 B — 수집함 통합)

```
[사업자 18개 계정]
   │  각 계정에서 "자동전달" 1회 켜기 (용팀장님, 계정당 1회)
   ▼
[cy728728@gmail.com]  ← gws 연동(수집·처리 허브)
   │  Claude가 스케줄로 실행:
   ├─ 정크(정산입금·주문현황·제안광고 등) → 휴지통 (30일 복구 가능)
   └─ 확인필요 메일 → cy728@daum.net 으로 전달 + 읽음처리
        (※ Daum = 용팀장님 메인. 원 요청대로 확인메일만 여기로)
```

핵심: **판단은 키워드 필터가 아니라 Claude가 내용을 읽고 의미로** 함 → "정산해서 잘 입금 해 드리겠습니다" 같은 풀어쓴 문장도 잡힘.

---

## ① 인증 — 2026-08-19 재확인: 이미 대부분 되어 있음

> **실측 확인(2026-08-19)**: gws는 `cy728728@gmail.com`으로 이미 인증되어 살아있음
> (`token_valid: true`, refresh token 있음, project `gws-cli-cy728728-01`, 실제 unread 201건 조회 성공).
> 7/15 세팅이 지금까지 안 끊긴 상태 — `gcloud auth login` / `gws auth setup`은 **다시 할 필요 없음**.
>
> **단, 스코프에 `gmail.send`가 빠져있어 전달(+forward)이 막힘** — dry-run으로 확인함
> (`+forward`는 `messages/send` 엔드포인트를 씀 → send 스코프 필수. 휴지통 이동은 `gmail.modify`로 이미 커버됨, 문제없음).

**할 일은 이것 하나뿐:**
```
! gws auth login --services gmail
```
→ Gmail 권한 승인 화면에서 **보내기(send)** 스코프가 포함되도록 승인. cy728728로.

**확인**: 아래 dry-run이 `gmail.send` 관련 에러 없이 위와 같은 요청 미리보기를 뱉으면 성공.
```
! gws gmail +forward --message-id <최근메일ID> --to cy728@daum.net --dry-run
```

인증 끝나면 저한테 "인증 됐어"만 알려주세요.

---

## ② 18개 계정 자동전달 켜기 (계정당 1회, 아침 이후 천천히)

각 Gmail 계정에서:
1. 설정(⚙️) → 모든 설정 보기 → **전달 및 POP/IMAP**
2. "전달 주소 추가" → `cy728728@gmail.com` → 저장
3. cy728728에 온 **확인 메일 승인**
4. **"수신 메일 전달"** 라디오 ON → "Gmail 사본 보관"(또는 원하는 처리) 선택 → 저장

- 네이버(cy728@naver.com, 1번): 네이버 메일 → 환경설정 → POP3/IMAP/자동전달 → 전달주소 cy728728 등록.
- 인증메일 18통이 cy728728에 몰림 — 하나씩 승인.
- ※ 전달만 켜면 됨. 원본 계정 필터·삭제는 안 만들어도 됨(삭제는 cy728728에서 Claude가 함).

---

## ③ 자동 실행 — 이 PC 상시 실행 (2026-08-19 확정)

Claude Code 클라우드 스케줄이 아니라, **이 PC에서 Windows 작업 스케줄러 + 로컬 Python 스크립트**로 돈다.
- 스크립트가 gws로 cy728728의 신규(안읽은) 메일을 조회 → 각 메일을 **Claude API(Anthropic API 직접 호출)** 로 위 "처리 규칙" 기준 판단 → 정크는 휴지통, 확인 필요는 Daum 전달 + 읽음처리 + "확인필요" 라벨.
- 주기: **09/12/15/18시 하루 4회 확정** (2026-08-19).
- **이 방식은 Claude Code 세션과 무관하게 도는 별도 스크립트**라 자체 **ANTHROPIC_API_KEY**가 필요함(메일 1건당 소액 과금, 원 단위). `.env`에 추가 예정.
- 트레이드오프: PC가 꺼져 있거나 절전 상태면 그 시간 동안 자동화도 멈춤(합의됨).

수동 실행도 가능: "이메일 정리해줘" 한마디면 Claude Code 세션에서 즉시 1회 처리(gws 직접 호출, API 스크립트와 별개 경로).

### 처리 규칙 (Claude 판단 기준) — 2026-08-19 갱신

**삭제(휴지통)** — 다음 의미면 삭제:
- 정산금액/정산대금이 **입금·지급됐다는 알림**
- **주문현황/주문상태**를 알려주는 알림
- **제안·홍보성 광고** (일익번창, 협업/입점/광고 제안, 프로모션, 뉴스레터 등 명백한 광고)

**Daum 전달(cy728@daum.net)** — 위 삭제 조건에 안 걸리는 나머지 전부. 판단 기준은 고정 목록이 아니라
**"방치하면 계정정지·법적문제·매출타격 등 사업에 실질적 피해로 이어지는가"**(심각도·긴급성).
아래는 **예시**일 뿐 전부가 아님 — 새 유형이 나와도 같은 기준으로 스스로 판단:
- 스마트스토어 클린위반 경고, 지재권(상표·저작권) 침해 신고/이의, KC인증 관련 공지
- 계정정지·이용제한·판매중지 경고, 세금/정산 이의·문의, 실제 CS·클레임 등

**애매하면 삭제하지 말고 전달** (놓침보다 오삭제가 더 나쁨). 삭제는 휴지통이라 30일 복구 가능.

## ④ 로그 (사후검증용, 2026-08-19 추가)

처리내역을 구글시트에 기록 — 시간 / 원계정 / 제목 / 발신자 / 판단결과(정크·확인·애매) / 액션(휴지통·전달).
오분류 의심되면 나중에 훑어보기 위함. 시트 위치는 구현 시 확정.

## ⑤ 실패 감지 (2026-08-19 추가)

gws OAuth가 Testing 모드면 **토큰이 7일마다 만료**된다(위 인증 단계에서 프로덕션 게시 권장 이유).
스크립트가 조용히 실패하면 정크만 쌓이고 중요메일도 놓칠 위험 → 매 실행 시 **"마지막 성공 실행시각"을 로컬 파일에 기록**,
하루 이상 갱신 안 되면 다음 daily-note/daily-review에서 경고 뜨도록 연동(구현 시 확정).

---

## 미결정 / 확인 부탁

1. ~~확인메일 도착지~~ → **cy728@daum.net 확정** (2026-08-19 재확인).
2. ~~자동 실행 주기~~ → **09/12/15/18시 하루 4회 확정** (2026-08-19).
3. 처음 며칠은 **삭제 대신 '삭제후보' 보여주기(미리보기)**로 돌려서 오삭제 없나 검증 후 실삭제 전환하는 걸 권장. 바로 실삭제 원하시면 그렇게.
4. **ANTHROPIC_API_KEY 발급** — 워크스페이스 `.env`에 아직 없음(확인함, 2026-08-19). console.anthropic.com에서 발급 필요 — 구현 계획의 첫 단계로 안내.
5. **gws OAuth 프로덕션 게시 여부** — ①단계에서 "프로덕션"으로 안 하면 7일마다 재인증 필요(위 ⑤ 실패감지 참고).

---

## 참고 — Claude가 쓰는 실제 명령 (기록용)

```
gws gmail +triage --query 'is:unread in:inbox' --format json   # 목록
gws gmail +read --id <ID> --headers                            # 본문 읽기
gws gmail users messages trash --params '{"userId":"me","id":"<ID>"}'   # 삭제(휴지통)
gws gmail +forward --message-id <ID> --to cy728@daum.net        # Daum 전달
```
