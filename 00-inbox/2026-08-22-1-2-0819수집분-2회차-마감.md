# 1-2 마켓그룹 8/19 수집분 98건 — 2회차 마감 (2026-08-22)

지시: "1-2 8/19 수집분 인계문서 읽고 이어가자" (1회차 인계 = `2026-08-22-1-2-0819수집분-작업인계.md`)

## 한 줄 요약

**4단계 전부 pending 0 · 보류 0으로 닫았다.** 1회차가 남긴 판정 큐 31건(옵션 12 · 썸네일 8 ·
카테고리 6 · 상품명 5)을 전건 소진했고, 사람이 판단해야 할 잔여는 **없다.**

| 단계 | 완료 | 상품삭제 | 해당없음 | 보류 |
|---|---|---|---|---|
| 카테고리 | **94** | 0 | 4 | **0** |
| 상품명 | **81** | 11 | 6 | **0** |
| 옵션 | **81** | 15 | 2 | **0** |
| 썸네일 | **82** | 9 | 7 | **0** |

**이번 회차 크레딧 45** (생성 9장 × 5 · 원본대체 1건은 0)

## 고정 좌표

| 항목 | 값 |
|---|---|
| 마켓그룹 | `1번_용쌤1-2` / groupId **1001185** |
| 계정 | **부킹(용쌤)** — `clwkdzjavjsl@gmail.com` |
| 그룹 시트 | `1RkxOCaAXSdo5GNzg6thXKzToqloy-nGlzrTGXC7yZNo` |
| 옵션 run | `~/python_work/data/option-cleanup/runs/1-2_0822` (+ `b` 수납장 · `c` 왕복정리) |
| 썸네일 run | `~/python_work/data/thumbnail/runs/1-2_0822` (+ `d` 물걸레헤드 재생성) |
| 상품명 run | `~/python_work/data/product-name/runs/1-2_0822b` (+ `_nokw` `_nokw2` `_nokw3`) |
| 통다운 | **260819** — 1회차 인계는 "260811, 갱신 필요"라 적었으나 **8/19에 이미 받아져 있었다** |

**8/19 수집분 재계산법** — 상품id(ULID)의 `U` 다음 26자 중 앞 10자가 Crockford Base32
타임스탬프다. 디코드해 `2026-08-19` 인 것이 정확히 98건.

```python
B32="0123456789ABCDEFGHJKMNPQRSTVWXYZ"
n=0
for c in pid[1:][:10]: n = n*32 + B32.find(c)
datetime.fromtimestamp(n/1000, timezone(timedelta(hours=9)))
```

## 이번에 한 일

### 옵션 16건 (1회차 잔여 12 + 이관 3 + 보류 1)

15건 팬아웃 → 저장, 그 뒤 `보류(저장실패)` 로 묶여 있던 수납장 협탁 1건까지 풀었다.
축 이름 8축 반영. 삭제후보 0.

**저장 실패 2건의 원인이 같았다 — 워커가 유지분만 이름을 줬다.** 손대지 않은 기존 이름이
25자를 넘어 저장이 **통째로** 거부된다(§알려진 함정에 있는 그것). 중국어 원문 기준으로
전 옵션을 다시 지어 통과시켰다:

| 상품 | 옵션 수 | 무엇이 문제였나 |
|---|---|---|
| 인조잔디 `P-CWeDHJrHsPDNx23JXw_` | 156 | 기존 번역이 `1.5cm`→`.5cm` 로 앞자리가 잘려 있었고, `特密`(특밀)·`超密`(초밀)을 둘 다 "초밀도"로 뭉개 **이름이 중복**이었다 |
| 수납장 협탁 `nZznp919eFKwfWn4piKDc` | 38 | 안내문 2행 + 3색×구성×치수 36행. 색상(奶咖/纯白/枪灰)을 짧게 갈라 25자 안에 넣었다 |

> **원문을 안 봤으면 못 잡는다.** 한국어 표시명만 보면 `.5cm` 가 잘린 건지 원래 그런 건지
> 구분이 안 되고, 특밀/초밀 중복도 안 보인다.

### 썸네일 9건 → 생성 9장 · 반영 8 · 원본대체 1 · 기존대표 유지 1

- **왕복 종결 장치가 이번엔 발화했다.** 옵션이 `실물기준없음` 을 이력 열에 박았고 prep 이
  그걸 읽어 3건을 대표옵션 기준에서 뺐다(1회차엔 이게 안 닫혔다)
- 기준적격에서 2건 승격(사육장 = 구성품 3칸 그리드 · 공구함 = 2색 비교컷)
- **크레딧 태우기 전에 2건을 걸렀다** — 후보에도 대표옵션 실물이 없었다

### 상품명 6건 전건 반영

| 상품 | 바뀐 것 |
|---|---|
| 회전 책장 | `…이동식카트 트롤리 카트` → `원목 360도회전책장 이동식 회전책장원형 바퀴형 기본형` |
| 케이크 스탠드 | `3단` 제거 → `스텐 케이크받침대 원형 케익스탠드 기본형`(썸네일로 원형·단일 확인) |
| 물걸레 헤드 | `다이슨 V7/V8 로봇청소기` → `샤오미 1C K10 무선청소기 호환 소모품 기본형` |
| 철제 파티션 | 원본이 이미 정확 → `기본형` 마커만 |
| 드럼클램프 | `지게차 드럼클램프 포크 집게 단일독수리부리 기본형` |
| 팔레트 집게 | 원본이 이미 정확 → `기본형` 마커만. 1회차가 `재진입금지` 로 닫아둔 건인데, **카테고리는 이미 확신도 100%로 저장돼 있었고** 상품명 뷰에 직결어가 없었을 뿐이라 무키워드로 풀렸다 |

### 카테고리 6건 — 전부 "이미 맞았다"

1회차가 판정 큐로 남긴 6건인데, 열어보니 **고칠 게 없었다.**

| 상품 | 1회차 상태 | 재조회 결과 |
|---|---|---|
| 링컨 센서 | 부분일치확인요 | **이미 삭제된 상품**(품목불일치) — 카테고리 열만 안 닫혀 있었다 |
| 질소커피 | 변경대상 41% | **이미 삭제된 상품** — 위와 같음 |
| 대기실 의자 | 변경대상 39% | `의자>일반의자` 100%·89% = **현재와 동일.** 1회차 제안(야외벤치)이 오답 |
| 철제 파티션 | 매칭불가 | `인테리어파티션/가벽` 100%×2 — 불사자 leaf 이름만 다르고 같은 자리 |
| 물걸레 헤드 | 매칭불가 | `청소기부속품` 100% — 위와 같음 |
| 전시다이 | 변경대상 61% | 변형끼리 불일치(62%/69%) — 현재 `스탠드선반/진열대` 유지 |

> **`매칭불가` 는 "카테고리가 틀렸다"가 아니라 "네이버 leaf 이름이 불사자에 없다"였다.**
> 둘 다 현재 카테고리가 정답이었는데 1회차가 판정 큐로 넘겼다. 재조회 4회로 닫혔다.

## 이번 회차에서 드러난 결함

### ① 옵션 워커가 실물을 "도면"으로 오판한다 — 같은 유형 세 번째 (수정함)

1회차 인계 §③에서 이룸님이 지적한 그 유형이 **또** 났다. 그때는 썸네일 쪽 판정기준만
고치고 **옵션 워커 프롬프트는 안 고쳤다.**

| 상품 | 무슨 일이 |
|---|---|
| 자석 픽업바 `ipwlluaR0lLTaPS3-a3E0` | 대표옵션(1.2m 픽업바 **실물 사진** + 치수 화살표)을 "치수 도면·일러스트"로 오판 → `실물기준없음` 으로 되돌림 → 썸네일이 후보에서 생성 **2회 실패(10크레딧)** → `fallback` 이 결국 **처음 그 대표옵션 이미지**로 되돌아왔다 |
| 물걸레 헤드 `KIwHwDRbQZ12sVuJMEqzY` | 같은 오판. 이력 열의 종결 토큰을 지우고 대표옵션 기준으로 다시 태우니 **한 번에 통과** |

**수정**: `bulsaja-option-cleanup/references/옵션-워커-프롬프트.md` §2-10 —
판정 문장이 **절 끝 참고**에 있어 절차만 읽는 워커가 놓쳤다. **절차 2번 안으로 옮기고**
"판정 근거가 '도면이라서'로 시작하면 그 판정은 틀린 것"을 명시했다.

> 일반화: **워커가 절차를 읽고 내려간다면, 판정 기준은 절차 단계 안에 있어야 한다.**
> 절 끝 보충 문장은 없는 것과 같다.

### ② `matrix` 라이브러리가 두 벌이다 — 잘못 잡으면 조용히 0행 (운영 주의)

| 경로 | 무엇 |
|---|---|
| `.claude/lib/eroomlib` | **정본.** 스킬 스크립트가 `.claude` 앵커로 찾아 쓴다 |
| `~/Documents/bulsaja-kit/shared/eroomlib` | 수강생 배포판. 저장 계층이 다르다 |

배포판을 잡으면 `matrix.read()` 가 **에러 없이 빈 dict** 를 돌려준다. 이번에 이관 flag
하나가 그렇게 유실될 뻔했다(찍었다는 출력까지 정상으로 나왔다).
**인라인 파이썬으로 현황판을 만질 땐 `sys.path.insert(0, '<워크스페이스>/.claude/lib')` 를 박는다.**

### ③ 실행 환경 두 가지

- **`python` 이 PATH 에 없다.** 워크스페이스 `.venv/bin/python` 절대경로로 부른다.
  1회차 인계문서의 명령을 그대로 붙여넣으면 `nohup: python: No such file or directory` 로
  **즉시 죽는데, nohup 이라 화면에 안 보인다**
- **zsh 는 `$IDS` 를 단어분리하지 않는다.** `--ids ${=IDS}` 를 쓴다. 안 그러면 id 9개가
  한 문자열로 가서 "조회 실패 1건 / 실제 대상 0건" 으로 조용히 끝난다

### ④ 상품명 prep 은 그룹 전체를 집는다 — 범위를 반드시 좁힌다

`--ids` 없이 돌리면 **그룹 전체 pending 176건**이 대상이 된다(현황판 재작업 자동편입 150건 포함).
8/19 수집분 5건만 하려던 회차가 176건짜리 워커 비용으로 갈 뻔했다. 중단하고 `--ids` 로 다시 돌렸다.

## 남은 것 — 이 그룹의 **다른 수집분**

8/19분은 끝났지만 그룹 전체(977건)에는 아직 남아 있다. **별도 회차 몫이다.**

| 수집일 | 건수 | 비고 |
|---|---|---|
| 2026-05-08~09 | 943 | |
| 2026-08-16 | 300 | |
| 2026-08-14 | 4 | |

상품명 pending 이 **174건**(대부분 옵션·정합검사가 되찍은 `재작업`)이다. 다음 회차 진입점:

```
"1-2 마켓그룹 8/16 수집분 상품명 재작업 돌려줘"
```

**잠금 4건은 그대로다** (1회차 인계 §④ — 이룸님이 앱에서 건 삭제 잠금, 스킬이 못 푼다).

---
**다음 세션 진입점**: 8/19분은 닫혔다. 이 그룹을 더 하려면 위 §남은 것 참조.
