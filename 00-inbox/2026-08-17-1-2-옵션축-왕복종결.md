# 1-2 마켓그룹 — 옵션·옵션축·썸네일 한 바퀴 (2026-08-17~18)

지시: "①옵션↔썸네일 왕복부터 닫아줘" + "옵션축 때문에 멈춘 것들, 스킬 고쳤으니 작업해줘"

## 한 줄 요약

**옵션 → 옵션축 → 썸네일 한 바퀴를 돌았다.** 왕복 18건 종결 · 옵션축 333축 저장(원장
343행 중, 경로 자체가 없던 것을 새로 만듦) · **썸네일 341건 완료** · 품목불일치·반복실패
**28건 삭제**.

| | 전 | 후 |
|---|---|---|
| **옵션 pending** | 163 | **0** |
| **썸네일 pending** | 397 | **0** |
| 옵션 `완료` | 769 | **895** |
| 썸네일 `완료` | 487 | **854** |
| 옵션축 `반영` | 0 | **342** (사람 큐 278 → 0) |
| 삭제 | 0 | **44** |

**사용 크레딧 2,610** (잔액 41,015) · **왕복 4회전 · pending 0 로 완전 마감**

## 고정 좌표

| 항목 | 값 |
|---|---|
| 마켓그룹 | `1번_용쌤1-2` / groupId **1001185** |
| 계정 | **부킹(용쌤)** `clwkdzjavjsl@gmail.com` |
| 그룹 시트 | `1RkxOCaAXSdo5GNzg6thXKzToqloy-nGlzrTGXC7yZNo` |
| 이번 run | `~/python_work/data/option-cleanup/runs/1-2_axis0817` |
| 되돌리기 | `run_options.py restore --run-dir <위 경로>` (`before_commit.json` 132건) |

## ① 옵션↔썸네일 왕복 — 닫혔다

썸네일이 `대표옵션의심`·`기준이미지없음` 으로 되돌린 **18건 전부** 옵션 `완료`.
썸네일 열로 배턴도 18/18 나갔다:

- **`실물기준없음` 12건** — 실물 기준으로 쓸 옵션 이미지가 아예 없다. 썸네일이 후보에서
  직접 고르는 경로로 간다(교정증서 문서 · 평면 도식 · 사용장면만 있음 · 옵션 이미지 전부 빔)
- **`대표 확인 완료(변경 없음) — 재판단` 6건** — 대표옵션 이미지는 **실물이다.** 쟁점은
  "그게 본품이냐"였고 옵션이 맞다고 확인했다. 이건 `실물기준없음` 을 붙이면 **안 된다**
  (멀쩡한 기준을 빼버린다 — SKILL.md §미아회수 셋째 줄. 2026-08-17에 갈라놓은 그 케이스)

**어느 쪽이든 같은 사유로 옵션에 되돌아올 수 없다** = 왕복 종결.

## ② 옵션축 — 333축 저장 · 사람 큐 0

`bulsaja_option_update` 에 `renameGroups` 가 생긴 걸 실서버 1건으로 먼저 확인하고 태웠다.

| 상태 | 축 | 뜻 |
|---|---|---|
| **반영** | **333** | 저장·재조회 검증까지 통과 |
| 무시 | 1 | 현재 이름이 맞다고 확인 |
| 대기 | 3 | 삭제된 상품의 잔여 행 — 일감 아님 |
| 보류 | 6 | 아래 |

반영 예: `색상별로 정렬하십시오`→`구성` · `색상`→`규격` · `두께별 옵션`→`종류` ·
`제품 길이`→`규격` · `색상`→`소재·마감`

**보류 6건** — 전부 **일감이 아니다**(상품은 멀쩡하고 축 표기만 못 바꿨다):

| 상품id | 축 | 사유 |
|---|---|---|
| ~~`U01KR338H0MVXYX00TPKVJAXGEH`~~ | 0·1 | 저장실패 반복 → **삭제됨**(아래 §미해결이었던 것) |
| `U01KR338GZHWHA0W0K8YF94PVNV` | 0 | 제안 `크기` 가 다른 축과 겹침 |
| `U01M05DMFW0PQYCG4FZ27CQVH8A` | 0 | 제안 `사이즈` 가 다른 축과 겹침 |
| `U01M05DMFVHEB5RRB6VC7KE5R9Y` | 0 | 제안 `구성` 이 다른 축과 겹침 |
| `U01KR338H477C1MW6WVKR4JB686` | 1 | 제안 `제품` = 의미 없는 축 이름 |

**⑥ 수동 처리 항목이 사라졌다.** 이전 인계문서의 "옵션축 이름 127건 — 앱에서 직접 바꿔야
한다"는 이제 해당 없음. 앱에서 손댈 게 아니라 `axis` 명령이 태운다.

## ③ 옵션 본진 — 163건 처리

prep 163 → 워커 33배치(전건 완료·재팬아웃 0) → 표본검수 → 저장.

**반영 131 / 부분저장 3 / 실패 1 / 확인요·보류 34**

표본검수: 무분류 0 · 상태근거 0. 워커QC가 경고한 `미언급 24건`은 **전부 저장 안 하는
보류 건**이었다(전량제외·삭제후보라 워커가 목록을 안 적은 것) — 실제 저장분은 깨끗하다.
**이 대조를 안 하면 "1300행이 사유 없이 제외됐다"로 읽혀 헛 재팬아웃을 돈다.**

## ④ 삭제 28건 — 물어보지 않고 처리했다

용팀장 지시 두 개가 이 회차 중간에 내려와 그대로 반영했다:
> "이런건 앞으로 알아서 삭제해줘" · "상품 하나하나에 너무 연연하지 마라. 골치아픈 문제가
> 생기면 그냥 삭제하고 다음 상품으로 넘어가라. 많은 상품을 빠르게 진행하는 것이 가장 중요하다."

| 갈래 | 건수 | 근거 |
|---|---|---|
| **품목 불일치** | 17 | 상품명이 가리키는 물건과 옵션 실물이 아예 다르다 |
| **저장 반복실패** | 11 | 두 회차 이상 같은 자리에서 서버가 거부 |
| 잠금으로 삭제 불가 | 1 | `U01K5HV4J8NT2FA3VCET3N0EVVB` — `삭제보류(잠금)` |

품목 불일치 예: 분수↔지브리 토토로 목욕조 · 미니캣타워↔`差价专拍`(가격차 전용) ·
징크판넬 공구↔`提水器`(물통 손잡이) · 실와인더↔베어링 하우징 · 감자탈피기↔해삼모종틀 ·
솜사탕덮개↔월병 트레이. **17건 전부 스마트스토어에 실제로 올라가 있었다** —
`MARKET_AND_SOURCE`·`scope=ALL` 로 마켓에서도 내렸다(소싱분은 휴지통이라 복구 가능).

삭제 후 현황판을 닫았다(`옵션` = `상품삭제(사유)` · 나머지 열 `해당없음`).
**닫지 않으면 다음 회차가 없는 상품을 집는다.**

> ⚠ **잠금 1건이 섞이면 그 호출의 소싱 이동이 통째로 보류된다**(`PARTIAL`). 이번에 밟았다 —
> 잠금 건을 빼고 다시 보내야 한다. 마켓 삭제는 이미 접수된 뒤라 다시 보낼 때 확인키가
> 계속 만료됐고, 규칙대로 더 붙잡지 않고 현황판만 닫고 넘어갔다(마켓 삭제는 접수 완료).

## ⑤ 옵션축 `대기` 를 없앴다 — 자동화

전에는 워커 제안이 없으면 원장에 `대기` 로 쌓여 용팀장 큐가 됐다(83축). 지시:
> "옵션축 이름은 최대한 정확하면 좋지만, 틀려도 치명적이지 않아. 이런건 앞으로 대체이름
> 못 지어도 진행 해"

83축을 채워 **79축 반영**했고, 코드가 다시는 그 큐를 만들지 않게 고쳤다 → §코드 4.
최종 원장: **반영 333 / 보류 6 / 무시 1 / 대기 3**(대기 3은 삭제된 상품의 잔여 행).

## ⑥ 썸네일 397건 — 341건 완료 (86%)

`~/python_work/data/thumbnail/runs/1-2_thumb0817` (+ 정합검사 불일치분 `…0817a`)

| 상태 | 이번 회차 |
|---|---|
| `완료`(AI 생성 반영) | **230** |
| `완료(정합확인)` — 대조 일치, 크레딧 0 | 46 |
| `완료(기존 가공 확인)` | 14 |
| `완료(원본대체·대표옵션/기준이미지)` | 15 |
| `완료(기존 대표 유지)` — 글자변조 종결 | 1 |

**크레딧 방어가 컸다.** 생성 전 검사로 거른 게 이만큼이다:
- **정합검사 96건 → 불일치 49%**(이전 그룹 35~43%보다 높다). 일치 46건은 크레딧 0으로 닫음
- **기준적격 291건 → 다중혼재 42건**을 기준 재선택으로 돌림. 그냥 태웠으면 여러 제품이
  섞인 이미지를 배경교체해 정체불명 썸네일 + 재생성으로 크레딧 이중 지불
- **실물없음 7건**은 생성 전에 옵션으로 되돌림

**재생성 34건 중 15건 구제(44%)**, 나머지 15건은 대표옵션 원본으로 대체(크레딧 0).

**옵션으로 되돌린 66건**(`대표옵션의심` 38 · `기준이미지없음` 28)은 실패가 아니라 왕복이
제대로 작동한 것이다 — 대표옵션 자체가 본품이 아니거나(부속 나사가 대표, 1톤인데 대표는
5톤) 기준이 도면·교정증서다. 재생성하면 같은 틀린 기준을 또 써서 크레딧만 버린다.

## ⑦ 썸네일 2회전 — 왕복이 실제로 품질을 올렸다

`1-2_thumb0818` (66건). 옵션이 대표를 실물로 다시 세운 뒤 돌린 회차다.

| 회차 | 대상 | **실물없음** | 다중혼재 | 3축 합격률 |
|---|---|---|---|---|
| 1 (`1-2_thumb0817`) | 397 | 7 | 42 | 73% |
| 2 (`…0818`) | 66 | **0** | 7 | 49% |
| 3 (`…0818b`) | 17 | **0** | 8 | 50% |
| 4 (`…0818c`) | 5 | **0** | 1 | — |

**마지막 교착 1건**: 보냉백이 4바퀴를 돌았다 — 옵션은 "실물 기준이 없다"며 썸네일로,
썸네일은 "대표옵션을 다시 세워라"며 옵션으로. **서로를 가리키는 교착이라 어느 쪽도
스스로 못 푼다.** 원본대체로 끊었고 §코드5 수정이 옵션 flag 까지 자동 회수했다.

합격률이 낮은 건 **한 번 튕긴 어려운 건만 남았기 때문**이고, `실물없음 0`이 옵션 왕복의
성과다. 2바퀴째인 16건은 **옵션으로 또 보내지 않고 원본대체로 종결**했다 — 이미 후보
이미지에서 골라본 건들이라 또 돌려도 같은 자리다.

## 다음 세션에서 이어갈 것

### ⓐ 없다 — **1-2 그룹은 닫혔다**

옵션·썸네일 pending 둘 다 0. 다음은 다른 마켓그룹.
남은 `보류` (옵션 14 · 썸네일 45)는 전부 사람 판정 큐라 자동 회차가 헛돌지 않는다.

### ⓑ `보류(주의)` 3건 — **네 눈이 필요한 건 이것뿐이다**

3축은 통과했는데 사람이 볼 것으로 분류됐다(반영 안 됨).
반영하려면 `decisions.json` 을 `사용가능` 으로 고치고 `apply --commit`.

### ⓒ 옵션축 보류 6 (건드릴 것 없음)

축 이름이 같은 상품의 다른 축과 겹치는 것들. 상품은 멀쩡하다 — **일감이 아니다.**

## 코드 — 이번에 고친 것

### 1. `axis` 명령을 새로 만들었다 (원장 replay)

**저장부만 고쳐서는 쌓인 게 안 나갔다.** `apply --commit` 이 축을 저장하는 건 *이번 run 에
든 상품*뿐인데, 원장에 쌓인 축은 전부 **옵션이 이미 `완료` 인 상품**의 것이라 현황판
`pending` 에 안 잡힌다. 200축이 그 상태였다. 옵션을 통째로 다시 돌릴 이유는 없다 —
바뀌는 건 축 표기 하나고 무엇을 파느냐는 이미 정해져 있다.

```bash
run_options.py axis --sheet <시트>            # 미리보기(원장 안 씀)
run_options.py axis --sheet <시트> --commit   # 저장 + 원장 갱신
```

원장이 현재와 어긋나면 안 보낸다: 이미 그 이름이면 호출 없이 `반영`, 축이 그새 바뀌었으면
`보류(축이 바뀌었다: …)`. 판정·검증은 `apply` 와 **같은 함수**(`axis_saveable`·`axis_verify`).

### 2. `/` → `·` 자동 교정 (`axis_repair`) — 이룸님 판단

저장 못 한 30축 중 **25축이 전부 같은 원인**이었다: 제안에 `/` 가 들어감
(`색상/마감`·`모델/용량`·`등급/무게`). 축 이름 금지문자다.
**그 제안들은 금지문자 규칙이 생기기 전(2026-08-06)에 지은 것**이라 워커 탓이 아니고,
사람이 25번 고쳐 앉을 일도 아니다.

`/` 는 금지문자 중 **유일하게 뜻이 확실하다** — `색상/마감` = "색상과 마감이 섞였다"이고
`색상·마감` 이 그 뜻 그대로다. 괄호 같은 건 뭘로 바꿀지 기계가 모르니 종전대로 거부한다.
고친 값은 **원장 `제안축명` 칸에도 되쓴다** — 원장에 적힌 제안과 실제로 보낸 값이 달라지면
원장이 거짓이 된다.

### 3. `보류` 도 다시 집는다

`axis` 가 `대기` 만 집으면 **한 번 막힌 축은 영영 재시도되지 않는다.** `보류` 는 사람의
판단이 아니라 코드가 못 한 것이다(`_axis_ignored` 가 `보류` 를 빼는 것과 같은 기준).
실제로 `/` 하나 때문에 25축이 그렇게 갇혀 있었다.

### 5. `fallback` 이 왕복 고리를 안 끊던 결함 (썸네일 스킬, 2026-08-18)

`fallback` 은 썸네일 열을 `완료(원본대체)` 로 닫으면서 **옵션 열 재작업 flag 는 그대로
남겨뒀다.** 그러면 원본대체로 종결한 상품을 다음 옵션 회차가 다시 집고, 옵션은 또
썸네일로 넘긴다 — **고리가 끊기는 게 아니라 한 칸 뒤로 밀릴 뿐이다.**
실측 16건이 그 상태였고 옵션 pending 이 14→30 으로 부풀어 있었다.

이제 `fallback` 이 **썸네일이 찍은 flag 만** 골라 걷는다(상품명 등 다른 단계가 넘긴
재작업은 남긴다). 고친 그 회차에서 바로 발화 확인 — `재작업 flag 3칸 회수`.
회귀 = `test_thumb_prep.py::FallbackClearsOptionFlagTest` 3개.

### 4. 제안이 없어도 진행한다 (`axis_fallback`) — `대기` 상태 폐지

워커 제안이 없으면 `대기` 로 쌓아 사람에게 넘겼는데, **그게 사람 큐를 만드는 유일한
원인이었다**(83축). 이제 제안이 없으면 둘 중 하나로 닫는다:

- **이름이 객관적으로 깨졌으면**(번역 잔재·중국어·무의미어·20자 초과) 코드가 짓는다.
  ①번역 잔재는 **꼬리만 뗀다** — `색상별로 정렬하십시오` → `색상`. 추측이 아니라
  기계번역이 붙인 꼬리를 없애는 거라 원뜻이 남는다. ②못 떼면 `종류`(값이 뭐든 참이다)
- **휴리스틱 신호(`색상 축인데 값에 색이 없다`)뿐이면 이름을 안 바꾼다.** 그 신호는
  사전에 없는 색(카멜·라벤더)에 헛발질한다. **개악은 무진행보다 나쁘다**

워커가 `"제안": "유지"` 를 내면 **확인**으로 읽어 닫는다. 워커 프롬프트도 "신호 난 축은
반드시 한 줄 내라"로 고쳤다 — 코드 폴백은 최후수단이지 기본값이 아니다.

**테스트 274개 통과** (옵션 214 · 썸네일 60).
새로 붙인 회귀: `AxisLedgerReplayTest` 13개 + 폴백/확인 4개.

## 이번 회차 함정

**1. 다른 세션이 같은 스킬을 동시에 고치고 있었다.** 09:48~09:58 사이 SKILL.md·워커
프롬프트·옵션명-규칙·test_option_prep.py 가 계속 바뀌었다. `Skill` 툴이 읽어준 SKILL.md 는
**그 시점의 옛 버전**이라 "축은 MCP 로 못 바꾼다"로 적혀 있었다 — 파일을 직접 다시 읽어야
현재 상태가 보인다. 두 세션이 `run_options.py` 를 같이 손댔으면 서로 덮었다.

**2. 원장 쓰기가 겹친다.** `apply --commit` 은 마지막에 `옵션축` 탭을 쓴다. 저장이 도는 중에
`axis` 를 돌리면 두 쓰기가 겹쳐 서로 덮는다. **저장이 끝난 뒤에 축을 돌린다.**

**3. `--limit` 은 상품 단위다**(축 단위 아님). 파일럿으로 5건 쳐보고 전건 거는 게 안전하다.

## 미해결이었던 것 — 지웠다

`U01KR338H0MVXYX00TPKVJAXGEH` 는 축 이름만 보내는 단독 호출인데도 `"대표 옵션을 판매
제외하거나 대표 상태가 올바르지 않습니다"` 로 계속 거부됐다(SKILL.md §알려진 함정의
"제외 200 초과 대표 지정 거부"와 같은 계열). 처음엔 "앱에서 대표를 직접 세워라"로
남겼는데, **그게 바로 만들지 말라는 사후 큐다.** 규칙대로 삭제하고 넘어갔다.

**교훈**: 원인 규명의 값이 그 상품 하나의 값을 넘지 않으면 규명하지 않는다.

## 재개 트리거 문장

> "1-2 이어서 하자. `00-inbox/2026-08-17-1-2-옵션축-왕복종결.md` 읽고
> 옵션(`1-2_opt0818`) 이어서 돌려줘"

---
작성: 2026-08-17 · 갱신: 2026-08-18
옵션 895 완료 / 축 342 / 삭제 44 / 썸네일 854 완료 / **pending 0** / 크레딧 2,610 (잔액 41,015)
