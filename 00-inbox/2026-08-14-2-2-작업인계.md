# 2-2 마켓그룹 작업 인계 (2026-08-14)

그룹 `2번_용쌤2-2` (groupId **1001186**) · 시트 **1wMKUsOxy3wSzejGXLk0G6o22DLf8zcBDg3_6sbVXBZo**
계정 **부킹(용쌤)** — 작업 전 `bulsaja_my_profile` 로 확인할 것.

## 지금 무슨 상태인가

| 단계 | 완료 | pending | 비고 |
|---|---|---|---|
| 카테고리 | 980 | 1 | 실질 0 (남은 1건은 삭제대상 상품) |
| 상품명 | 459 | 19 | 빈칸 14(통다운 공백) + 재작업 5 |
| 옵션 | 700 | 17 | 빈칸 2 + 재작업 15 |
| 썸네일 | 267 | 436 | **생성 진행 중** |

삭제 268건은 전부 처리 끝(제외카테고리 267 + 품목불일치 7 + 원본404 6 = 실행분).

## ★ 지금 백그라운드로 돌고 있는 것

**썸네일 생성 218건** — `2-2_0814` run-dir. 마지막 확인 시점 23/218, 크레딧 5/건.

```bash
R=~/python_work/data/thumbnail/runs/2-2_0814
# 진행 로그 — generate_live.log 는 지금 도는 프로세스로 걸린 심볼릭 링크(살아 있으면 계속 늘어난다)
grep -c '\[생성\]' $R/generate_live.log ; grep -oE '^\[[0-9]+/218\]' $R/generate_live.log | tail -1
# 링크가 끊겼으면(세션 종료) 스냅샷본을 본다: $R/generate.log
```

- 죽었으면 아래 재개 명령을 그냥 다시 친다
  (`--generate` 는 재개 모드라 이미 태운 건을 건너뛴다 — 크레딧 중복 없음).
- ~~**세션을 클리어해도 이 프로세스는 계속 돈다.**~~ **틀렸다(2026-08-14 실측).**
  이전 세션에서 띄운 백그라운드는 예외 없이 프로세스+부모셸이 통째로 사라진다
  (14:49 에 30/218 지점에서 소멸, 로그에 트레이스백 없음). **세션을 넘길 거면
  진행 상황을 믿지 말고 `ps` 로 생존을 먼저 확인해라.**
- ~~실패 4건은 전부 **HTTP 429** 다. 재개하면 다시 잡힌다.~~ **재개(재생성)가 아니라
  `recover` 가 답이다.** 429 6건을 `recover` 로 조회하니 **전부 서버에서 생성 완료**돼
  있었다 — 회수 크레딧 0. 재생성했으면 30크레딧을 두 번 낼 뻔했다.
  폴링에서 난 429 는 접수가 이미 성공한 것이라 타임아웃과 같은 부류다.
  → SKILL.md §결과 미수신 회수 참조. **어떤 실패든 `recover` 를 먼저 돌려라.**

## 이어서 할 일 — 순서대로

### ① 썸네일 생성 완주 → 판정 → 반영 (run-dir `2-2_0814`)

```bash
R=~/python_work/data/thumbnail/runs/2-2_0814
SH=1wMKUsOxy3wSzejGXLk0G6o22DLf8zcBDg3_6sbVXBZo
P=".venv/bin/python .claude/skills/bulsaja-thumbnail/scripts/run_thumbs.py"

# 1) 회수부터 (크레딧 0). 접수 뒤에 난 실패는 타임아웃이든 429든 전부 회수 대상 —
#    재개(재생성)보다 이게 **먼저다**. 회수할 게 없으면 그냥 없다고 끝난다
PYTHONPATH=.claude/lib $P recover --run-dir $R

# 2) 그 다음 생성 재개(멱등 — 회수분까지 건너뛴다)
PYTHONPATH=.claude/lib $P apply --run-dir $R --sheet $SH --generate

# 3) 3축 판정 — 9건 이상이면 반드시 팬아웃
PYTHONPATH=.claude/lib $P verdict --run-dir $R
PYTHONPATH=.claude/lib $P pending --run-dir $R --verdict
#    → Workflow {name:"thumb-fanout", args:<출력 그대로>}  (mode:verdict)
PYTHONPATH=.claude/lib $P verdict --run-dir $R --commit    # exit 3 이면 누락 — 무시하지 말 것

# 4) 반영
PYTHONPATH=.claude/lib $P apply --run-dir $R --sheet $SH --commit
```

**불합격 자동 처리**(묻지 않는다 — SKILL.md §검수 불합격 자동 처리):
대표옵션의심/기준이미지없음 → 옵션 열로 반려 · 글자변조 → 재생성 없이 기존 유지 ·
그 외 → 재생성(누적 2회) · 2회째도 불합격 → `fallback` 으로 원본대체(크레딧 0).

### ② 불일치 158건 세트 (run-dir `2-2_0814a`) — ①과 같은 순서

정합검사에서 "기존 대표 ≠ 대표옵션"으로 걸린 건들. **기준적격·기준이미지 선택까지 이미 끝나 있다.**
바로 생성부터 가면 된다. 예상 크레딧 ≈ 790.

```bash
Ra=~/python_work/data/thumbnail/runs/2-2_0814a
PYTHONPATH=.claude/lib $P apply --run-dir $Ra --sheet $SH --generate
# 이후 recover → verdict(팬아웃) → verdict --commit → apply --commit  (①과 동일)
```

> ⚠ **①과 ②를 동시에 돌리지 마라.** 불사자 AI 생성 큐가 하나라 경합하면 타임아웃이 는다
> (2026-08-11 25-2 에서 큐가 4시간 멈춘 전례).

### ③ 남은 상품명 5건 · 옵션 15건 (옵션↔상품명 이관 잔여)

썸네일 반영이 끝나면 이관 flag 가 더 쌓인다. **그때 한 번에 모아서** 돌리는 게 싸다.

```bash
# 상품명
PYTHONPATH=.claude/lib .venv/bin/python .claude/skills/product-name/scripts/run_names.py \
  prep --group-id 1001186 --group-name "2번_용쌤2-2" --run-dir ~/python_work/data/product-name/runs/2-2_0815
#   → pname-fanout → fix-r9 --commit → check → append → 표본검수 → rename --commit

# 옵션
PYTHONPATH=.claude/lib .venv/bin/python .claude/skills/bulsaja-option-cleanup/scripts/run_options.py \
  prep --sheet $SH --run-dir ~/python_work/data/option-cleanup/runs/2-2_0815
#   → pending → optclean-fanout → apply(미리보기) → apply --commit
```

## 사람이 판정해야 하는 큐 (자동화 밖)

| 건수 | 내용 |
|---|---|
| **14** | 상품명 미착수 — 카테고리 5종(인테리어파티션·사무캐비닛·파라솔·PC마이크·기타사무교구)이 **셀러라이프 통다운에 아예 없다.** 뷰가 안 만들어져 무키워드 경로(`--nokw-mode`)도 안 돈다(그 경로도 뷰를 전제한다). 통다운 갱신 때 재시도 |
| **2** | 옵션 저장 거부 — 판매행 600·585개짜리. 서버가 대표 상태를 거부한다 |
| **1** | 삭제 잠금 `U01K5HZBBKY4ST0A11WC85VR851`(좌식 의료용자전거) — 원본404라 삭제 대상인데 **앱에서 잠금을 걸어둬서** 스킬이 못 지운다 |
| **1** | 상품명 표본의심 `U01KS1WY74AMQPD5PC7BRYJS8SE` — 실물은 팬틸트 헤드 단품인데 이름이 '삼각대'를 2회 지칭. 원복할지 폐기할지 판단 필요 |
| **18** | 옵션 표본의심 — 워커가 ±50% 판매범위를 선점해 정상 사양 변형까지 제외한 건(최악: 가스통 카트 48개 중 42개 제외). 현황판에 사유와 함께 남아 있음 |
| **271** | `옵션축` 탭 — 축 이름(`색상`인데 값은 모델·규격)은 MCP에 변경 API가 없다. **불사자 앱에서 수동 반영** |
| **113** | 상품명 `보류(카테고리의심)` · **108** `검증실패` — 지난 회차 누적분. 재교정 1회 한정 규칙에 걸려 자동 재진입 안 함 |

## 이번에 고친 스킬 (커밋 전, 테스트 53건 통과)

`bulsaja-option-cleanup/scripts/run_options.py` + `test_option_prep.py` + SKILL.md + `_shared/스킬-계약.md`

1. **`--no-sheet` 가 현황판·이관까지 막던 것** → 원장 탭(`옵션`·`옵션축`)만 막게 분리.
   현황판·이관은 항상 나간다. 끄려면 새 플래그 `--no-matrix`(검증 전용).
   25-2(254건)·2-2(664건) 두 번 밟은 함정이라 계약(`_shared/스킬-계약.md`)에 올렸다.
2. **`committed.json` 재개 구현** — SKILL.md엔 있었는데 코드엔 없었다. 이제 건별 즉시 기록,
   재개 회차는 MCP 0회로 현황판만 채운다. `--ignore-committed` 로 전건 재저장.
3. **새로 찾은 결함**: 저장이 2단계(①이름 → ②판매·대표·순서)인데 ①만 넘어가고 죽으면
   불사자엔 새 이름, 스냅샷엔 옛 이름이 남아 **다음 회차가 낡은 상태로 계획**을 세운다.
   같은 상품이 매 회차 같은 사유로 실패했다(1회차 마커 실패 9 → 2회차 4 재발).
   → 실패해도 스냅샷을 실제 값으로 되맞추게 수정. 되맞추고 재시도하니 7건 중 4건이 바로 풀렸다.

**아직 커밋 안 했다.** 검토 후 `깃푸쉬`.

## 오늘 한 일 요약

- 삭제대기 267 + 신규 3 → **전량 삭제**, 잔여 0
- 카테고리 재작업 4 + 상품명발 재교정 12 → 저장 11, 수동잔여 1
- 상품명 **75건 반영**(1차 32 + 2차 43), 불량 0. 표본검수 62건 중 의심 1
- 옵션 **713건 저장**(1차 664 + 2차 49). 표본검수에서 안티패턴 19건 적발해 보류
- 썸네일 크레딧 0 구간 완료: 정합검사 436(일치 267·불일치 158) · 기준적격 372 · 원본404 삭제 6
- 품목불일치 7건 삭제(옵션 워커가 "상품명과 실물이 딴 물건"으로 잡은 건)

## run-dir

```
~/python_work/data/category-fix/runs/2-2_gate0812 · 2-2_gate0812b · 2-2_redo1 · 2-2_redo2
~/python_work/data/product-name/runs/2-2_0812 · 2-2_0812_redo1 · 2-2_0814
~/python_work/data/option-cleanup/runs/2-2_0812 · 2-2_0814 · 2-2_0814b
~/python_work/data/thumbnail/runs/2-2_0814 · 2-2_0814a     ← review.html 여기
```

되돌리기: 옵션 `before_commit.json` · 썸네일 `before_generate.json` · 상품명 원본은 시트 E열.
