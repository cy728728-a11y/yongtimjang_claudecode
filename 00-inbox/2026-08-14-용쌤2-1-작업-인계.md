# 2026-08-14 · 2번_용쌤2-1 작업 인계 (컨텍스트 clear 대비)

groupId **1001115** · 그룹시트 `1T3qkLlE4vkX_1KyaSAic-QgaA3bG6JNPUxUJh6kfWF4`
직전 인계: `00-inbox/2026-08-09-용쌤2-1-작업-인계.md`

## 이번 세션에 끝낸 것

| 작업 | 결과 |
|---|---|
| 카테고리 재교정 | redo1 23건 → 저장 21 · redo2 16건 → 저장 15 · redo3 108건 → **저장 74**(자동 62·유도 4·consensus 8) · 이미정확 27 |
| 상품명 | **불사자 반영 131건** (본 라운드 59 + `_kw1` 1 + `_redo1` 11 + `_nokw` 12 + `_kw2` 12 + `_redo3` 59 − 표본의심 2 − 중복정정 1) |
| 옵션 마커 이관 | 19건 중 **17건 저장** · 보류 2 |
| 제외카테고리 삭제 | **297건 실제 삭제**(휴지통 → 복구 가능) · 잔여 삭제대기 3 |

**현황판 최종**: 상품명 완료 645 · 카테고리 완료 1022 · 그룹 상품 수 1001 → **약 700건**(삭제 반영)

run-dir
- 카테고리: `~/python_work/data/category-fix/runs/2-1_redo1` · `_redo2` · `_redo3`
- 상품명: `~/python_work/data/product-name/runs/용쌤2-1_260813`(+`_kw1` `_nokw` `_redo1` `_redo2`) ·
  `용쌤2-1_260814_kw2` · `용쌤2-1_260814_redo3`
- 옵션: `~/python_work/data/option-cleanup/runs/용쌤2-1_260814_mark`
- 통다운: 260811 사용

## 남은 것 (우선순위 순)

### 1. 보류(카테고리의심) 34건
재교정 **1회 한정** 규정에 걸린 잔여다(`_redo` run-dir 에서 또 의심이 나온 건).
그중 5건은 consensus `실물판정의심` — 검색어가 아니라 실물 판정이 틀렸을 가능성이 높다.
`run_all.py recheck` (이미지 사다리)로 실물을 확정한 뒤에 재교정해야 한다. 그냥 다시 돌리면 같은 답이 나온다.

### 2. 검증실패 19건
이번 세션 이전부터 있던 백로그. 손대지 않았다. `--ids --redo` 로 새 run-dir 에서 재작업.

### 3. 상품명 재작업 17건 · 옵션 재작업 11건
이번 옵션 작업이 새로 찍은 이관 플래그. 다음 런이 `_matrix_redo` 로 자동 편입한다 — `--ids --redo` 를 손으로 칠 필요 없다.

### 4. 옵션 보류 2건 (앱에서 직접)
- `U01KREA821BK5Q41TBF91GGP6T7` 네일 진열장 — **판매 가능한 메인 옵션이 0개**(옵션 뒤집힘).
  본품이 전량 판매제외고 부속만 판매중이다. 저장하면 안 되는 상태라 스크립트가 막았다.
- `U01K5BR2XZECFB775XCGQP5XV9D` 교탁 — 옵션명 2개가 27자·26자(상한 25). `보류(이름규칙)`.

그 밖에 옵션 저장실패 4건 · 보류(삭제대상) 19건 · **옵션축 수동 반영**(MCP에 축 이름 변경 API 없음).

### 5. 썸네일 재작업 242건
현황판 `썸네일` 열. `bulsaja-thumbnail` 런이 자동으로 잡는다.

### 6. 카테고리 매칭불가 5 · 변경대상 3 — 수동지정 큐
대표적으로 `모닝휠 알루미늄 휠`: 어떤 키워드로도 불사자 `타이어/휠>휠` 에 자동매칭이 안 닿는다
(`알루미늄휠`→휠체어, `자동차 휠`→카오디오 앰프로 튄다). 기존 값이 `타이어/휠>휠용품` 이라
무해해서 손대지 않았다. 경로를 직접 알려주면 `category_steer.py` 1단계로 넣을 수 있다.

## 이번 세션에 고친 스킬

1. **R11 신설(product-name `name_check.py`)** — 유통 브랜드 20종 사전.
   원본 상품명에 없는데 붙었으면 **실패**, 원본에 이미 있었으면 경고.
   발단: 중국산 계단카트에 채택 키워드 `코스트코접이식카트`(상표 리스크).
2. **`prep --nokw-mode` 결함 수정** — 통다운밖(`not_found`) 상품은 카테고리 항목이 없어
   뷰도 배치도 0개가 됐다. `--nokw-mode` 는 **이미 만들어진 배치에 플래그만 얹는** 코드라
   정작 제 대상에 안 걸렸다. 이제 뷰 없는 배치를 만든다(평상시 라운드에서는 안 만든다).
3. **워커 프롬프트 보강** — "품목어를 원본에서 승계하지 마라, 옵션 원문이 정한다" +
   실측 3건 표(오지칭 2 + 정상 정정 1) + 유통 브랜드 금지.
4. 회귀 테스트 **126건 통과**(`test_run_names.py`).

### ★ R10은 만들었다가 당일 폐기했다 — 다시 만들지 마라

"상품명 term 이 원본·원문·옵션·키워드 어디에도 없으면 경고" 규칙. **실측 표본 35건 중 23건 오탐.**
증거의 절반이 중국어 원문이고 상품명은 한국어라 문자열 대조로 `고속`↔`高速` ·
`북유럽`↔`北欧` · `3단`↔`三层` 을 못 넘는다. 발단 사례('발광글자')조차 원문 `广告发光字` 에
근거가 있어 **잡으려던 건 자체가 오탐**이었다.
폐기 사유는 `name_check.evidence_text` docstring + `SKILL.md` §검증 규칙에 남겼다.

→ **실물 오지칭(미용실의자→거울)은 기계로 못 막는다. 표본검수가 마지막 방어선이다.**

## 판정 큐 (이룸님 몫)

`보류(표본의심)` 2건 — 상태 원복(재반영) 또는 폐기를 정해야 한다.
- `U01KREA823EY0KB4N8D09QSVSSP` 미용실의자 → 실물은 **미용실 거울**
  (원문 `理发店镜台…美发镜子落地挂墙`, 옵션 전부 `款式+색상+单面/双面` = 거울 사양)
- `U01KREA823DAF1JMQ53RE8YA287` 계단카트 → 무관한 타사 브랜드 `코스트코` 채택
  (원문 `买菜小推车…爬楼梯` = 장보기 계단 손수레)

※ 철판펀칭기(`U01KREA821XVD1GYR5K7N2QZQ36`)는 의심 판정을 **철회하고 반영했다** —
`工业级`·`重型`·`广告发光字`·`铁皮字打孔器` 에 근거가 다 있었다.

## 주의

- **불사자 저장 ≠ 마켓 반영.** 스마트스토어 게시분은 **수정 업로드**를 따로 해야 판매 화면이 바뀐다.
- **`auto` 는 끝에서 제외카테고리를 자동 삭제한다**(2026-08-11 이룸님 "묻지 말고 삭제").
  이번 세션 297건이 이 경로로 나갔다. 막으려면 `--no-autodelete`.
- **캡챠는 릴레이 대기(5분) 중에 저절로 풀리는 패턴**이 이번 세션 4회 반복됐다.
  Aside 가 `needs_human` 을 줘도 단건 조회로 상태만 확인해 보면 이미 풀려 있다.
  오답을 넣지 말고 **단건 조회로 확인 → `--resume`** 이 실전 순서다.
- 조회 재개할 땐 `--sellha-sleep 8 --sellha-sleep-max 18` 정도로 휴식을 준다
  (`--sellha-rest-every` 는 현 엔진에서 미사용).
- 제외카테고리 일괄삭제 요청 시트(잔여분용):
  https://docs.google.com/spreadsheets/d/1LweJBPpwXw2-dMXP-k6-koHW2Kfh663JWjLOFksJO10/edit

---
그룹 시트: https://docs.google.com/spreadsheets/d/1T3qkLlE4vkX_1KyaSAic-QgaA3bG6JNPUxUJh6kfWF4/edit
