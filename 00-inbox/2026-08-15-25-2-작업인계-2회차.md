# 25-2 작업 인계 (2회차) — 2026-08-15

마켓그룹 **5번_용쌤25-2** (groupId `1001822`) · 계정 **부킹(용쌤)** `clwkdzjavjsl@gmail.com`
그룹 시트 `1DbR2upLX_DXjgjXpGPdide2SUifli_X6DTqhUTr9Syk`

> ⚠ **같은 날 25-2 인계문서가 두 개다. 둘 다 읽어라 — 축이 다르다.**
> · **이 문서** = **옵션·카테고리·상품명 축**
> · [`2026-08-15-25-2-작업인계-2.md`](2026-08-15-25-2-작업인계-2.md)
>   = **썸네일 축** (정합검사 불일치 108건 재생성 완료 · 미완결 3건 종결 ·
>   불량률 기저치 35% · 큐 정체 대응). 이 문서 §4-A "썸네일 1,400건" 은 그쪽이 정본이다.
>   이름이 비슷하니 하나만 읽고 넘어가지 말 것.

> 앞 회차: [`2026-08-15-25-2-신규958-작업인계.md`](2026-08-15-25-2-신규958-작업인계.md)
> 이 문서는 그 인계문서의 **§3-A/B/C 를 실제로 처리한** 기록이다.

새 세션은 이렇게 시작하면 된다:
```
00-inbox/2026-08-15-25-2-작업인계-2회차.md 읽고 이어서 작업해
```

**세션 클리어 시점 상태**
- **도는 백그라운드 프로세스 없음.** 전부 정상 종료
- 계정 확인: `bulsaja_my_profile` → **부킹**이어야 한다
- 크레딧 **58,325** (이번 회차도 AI 생성 0 — 조회성 차감만)
- **워킹트리에 미커밋 변경분이 있다**(§8). 커밋 전에 §8 을 먼저 읽어라
- 아래 수치는 스냅샷이다. 현황판을 다시 뽑아라:
  ```bash
  PYTHONPATH=.claude/lib .venv/bin/python -c "
  from eroomlib import matrix
  from collections import Counter
  m=matrix.read('1DbR2upLX_DXjgjXpGPdide2SUifli_X6DTqhUTr9Syk')
  for t in ('카테고리','상품명','옵션','썸네일'):
      c=Counter((r.get(t) or '(빈칸)').strip() or '(빈칸)' for r in m.values())
      print('===',t,'| pending',len(matrix.pending(m,t)))
      for k,v in c.most_common(7): print('   ',v,'\t',k[:54])
  "
  ```

---

## 0. 이번에 한 것

| 축 | 결과 |
|---|---|
| 카테고리 consensus | 76건 변형조회 → 채택 62 · **저장 39** · 수동잔여 14(실물판정의심) |
| 제외카테고리 삭제 | **115건 삭제** · 잔여 33(그중 **32건은 판매이력 잠금**이라 삭제 불가) |
| 상품명 | 본 55건 + 재작업 24건 → **불사자 반영 28건** · 불량 0 / 오류 0 |
| 옵션 | 337건 팬아웃 → **반영 299건** · 부분저장 2 · 실패 8 |
| 삭제(지시) | 옵션 저장실패 **10건** · 품목불일치 **21건** — 둘 다 용팀장 지시로 삭제 |
| 스킬 수정 | `run_options.py`·`option_rules.py` 저장실패 2대 원인 제거(§2) |

**현황판 이동**: 옵션 pending **337 → 14** · 상품명 pending 98 → 70 · 카테고리 pending 0 유지

---

## 1. run-dir

```
카테고리  ~/python_work/data/category-fix/runs/25-2_new0814        (consensus·gate 이어서 씀)
          └ gate-retry/                                            (잔여 삭제대기 재큐)
상품명    ~/python_work/data/product-name/runs/25-2_0815            (본 라운드 55건)
          ~/python_work/data/product-name/runs/25-2_0815_redo1      (카테고리 재교정발 24건)
옵션      ~/python_work/data/option-cleanup/runs/25-2_0815          (337건)
```

**되돌리기**: 옵션 `25-2_0815/before_commit.json` · 상품명 원본은 시트 `상품명` 탭 E열

---

## 2. 옵션 저장실패 — 원인 2종을 찾아 고쳤다 (앞 인계문서 §3-A 숙제)

앞 회차 실패 10건을 파보니 원인이 **두 가지**였고 둘 다 스크립트 결함이었다.

### ① 제외행 200개 초과 상품이 통째로 죽는다 (9건 = 실패의 90%)

`excludeSkuIds` 는 MCP 스키마 상한이 200이라 제외가 많으면 앞 덩어리를 따로 보내는데,
**그 덩어리에 현재 대표행이 섞이면** 서버가 "대표 옵션을 판매 제외하거나 대표 상태가
올바르지 않습니다"로 거부한다. 그 호출엔 대표를 실을 수 없어(순서를 같이 실으면 아직 안 뺀
행 때문에 또 거부) 빠져나갈 길이 없었다.

실패 9건의 제외 수: 292 · 552 · 447 · 418 · 242 · 241 · 353 · 226 · 251 — **전부 200 초과**.

**수정**: `_promote_main()` — 제외 청크를 보내기 **전에** 새 대표를 판매중·대표로 먼저 세운다.
그 뒤로는 옛 대표가 어느 덩어리에 들어가든 상관없다.

```python
mcp.option_update(pid, includeSkuIds=[plan["대표"]], mainSkuId=plan["대표"])
```

> **실전 검증됨**: 이번 337건 커밋에서 **대표 상태 오류 실패 0건**. 앞 회차의 9건 유형이
> 하나도 안 나왔다.

### ② 기존 이름끼리의 중복 (앞 인계문서 §2⑥ 그대로)

워커가 손도 안 댄 **기존 옵션명끼리** 겹쳐서 저장이 거부되던 건. 서버는 판매 여부를 안 가리고
전 옵션의 최종 이름을 본다.

**수정**: `option_rules.with_dedup_cleanup()` — 같은 차원 안 중복을 기계적으로 갈라놓는다.
첫 번째는 그대로, 뒤엣것에 ` 2`·` 3`. `기본형` 마커가 붙은 이름은 첫 번째로 고정해 번호를
면한다. 25자 상한을 넘으면 앞을 자른다. 실측: 중복 17그룹 → 19개 조정 → 중복 0.

**테스트**: `test_option_rules.py`·`test_option_prep.py` 149개 통과(신규 5개 포함).
```bash
cd .claude/skills/bulsaja-option-cleanup/scripts
PYTHONPATH=<워크스페이스>/.claude/lib:. <워크스페이스>/.venv/bin/python -m unittest test_option_rules test_option_prep
```

### 이번 회차에 새로 남은 실패 8건은 **다른 사유**다

```
4건  옵션값을 못 찾았다: ['1']          ← 워커가 rename 키를 잘못 지정
1건  옵션값을 못 찾았다: ['1_기본형참고']  ← 같음
1건  같은 옵션값의 이름을 두 번 지정      ← 워커 산출물 중복 키
2건  이름 저장 후 검증 실패: 중국어 잔존   ← 워커가 이름을 안 바꿈
```
전부 **워커 산출물 품질** 문제지 저장 경로 결함이 아니다. 재팬아웃하면 대개 풀린다.

---

## 3. 이번에 새로 확인한 함정

### ① 삭제는 `--resume` 때문에 재제출이 막힌다
`autodelete` 를 같은 run-dir 로 다시 돌리면 `delete_progress.jsonl` 의 배치키가 겹쳐
**"제출할 배치 0개"** 로 끝난다(앞 인계문서가 경고한 그 함정). 잔여는 이렇게 푼다:
```bash
category_gate.py retry --run-dir "<R>/gate-retry" --only "5번_용쌤25-2"   # 현황판에서 새 큐
category_gate.py autodelete --run-dir "<R>/gate-retry"
```

### ② 제외카테고리인데 **잠금**된 상품은 못 지운다 (32건)
판매 이력이 있어 잠가 둔 상품이다(`bulsaja-product-lock`). 삭제 응답이 `잠금` 으로 온다.
지우려면 **불사자 앱에서 잠금을 먼저 풀어야** 한다 → 용팀장 판단 몫.

### ③ 하네스가 대량 삭제 명령을 막는다
149건 삭제를 `nohup … category_gate.py autodelete` 로 띄우니 자동승인 분류기가 차단했다.
**용팀장에게 물어 승인받으면 같은 명령이 그대로 통과한다.** 10건짜리 삭제는 안 막혔다.

### ④ 상품명 prep 은 `재작업(카테고리…)` 을 자동으로 안 집는다
자동 편입되는 건 `재작업(옵션: …)` 뿐이다(`_matrix_redo`). consensus 로 카테고리가 바뀐
27건은 **`--ids … --redo` 로 별도 run-dir(`_redo1`)에서** 돌려야 한다. 같은 run-dir 로
돌리면 workdata 캐시가 옛 카테고리 뷰를 만든다.

### ⑤ 시트 `보류(저장실패)` 가 원장과 어긋나 있었다
10건 중 **7건이 실제로는 저장 성공**(r2 `committed.json` 기준 6 완료 + 1 부분저장)인데
시트만 실패로 남아 있었다. 앞 회차 apply 미리보기가 찍은 값을 커밋이 못 덮은 자리로 보인다.
**원장(`committed.json`)이 진실이다** — 어긋나면 원장 기준으로 시트를 되맞춘다.

### ⑥ 옵션 prep 로그는 끝나야 나온다
`nohup` 으로 띄우면 버퍼링 때문에 로그가 0바이트로 한참 있는다. `-u` 를 안 붙였다면
프로세스 생존(`pgrep -f "bin/python.*run_options.py prep"`)으로 판단해라.

---

## 4. 남은 일 — 우선순위 순

### A. 썸네일 1,400건 — 크레딧 큰 지출 (유일하게 남은 큰 축)
직전 실측 270장 ≈ 1,350크레딧 → 1,400건이면 **약 7,000크레딧** 예상.
**착수 전 예상 크레딧 보고 + 용팀장 승인 필수**(썸네일 스킬 규약). 크레딧 58,325 보유.
그중 `재작업(옵션: 대표 동률…)` 13건은 대표옵션과 썸네일을 맞추라는 이관분이다.

### B. 상품명 `보류(카테고리의심)` 175건
카테고리 뷰에 직결어가 0인 건들. `holds --catfix-run` 으로 갈라보니 **무키워드대상 0 ·
카테고리수동큐 175** 였다 — catfix 가 `이미정확` 이라고 답한 건이 없어서 무키워드 라운드로
못 보낸다. **카테고리를 더 정확히 잡는 게 선행**이다(sellha조회실패 33 · 매칭불가 25 ·
변경대상 20 · 부분일치확인요 11).

### C. 카테고리 저확신 잔여 114건
```
sellha조회실패 33 · 매칭불가 25 · 변경대상 20 · 현행유지(검색어오해) 15 ·
부분일치확인요 11 · 보류(정체불명) 10
```
consensus 는 이미 한 바퀴 돌았다(실물판정의심 14건은 재변형 금지 — Step 3b `recheck` 몫).
**조회실패 33건은 조회를 다시 붙이면 풀릴 가능성이 높다.**

> ⚠️ **`sellha조회실패` 는 셀하와 무관하다** — 2026-08-04 에 셀하가 막혀 **Aside(네이버
> 가격비교)로 전환**됐고(`run_all.py` CAT_SCRIPT → `aside-category/scripts/aside_category.py`),
> 지금 이 값은 **Aside 조회 실패**를 뜻한다. 파일명(`sellha.json`)·시트열(`sellha경로`)·
> 플래그(`--skip-sellha`)도 같은 이유로 옛 이름을 유지한다 — **`--resume` 과 시트 재개
> 로직이 그 이름에 물려 있어 바꾸면 깨진다.** 이름만 보고 셀하를 손보러 가지 마라.
> (2026-08-15: 쓰지 않는 `sellha-category` 스킬 디렉토리는 삭제했다 — §8)

### D. 옵션 잔여 14건 + 저장실패 11건
저장실패 11건 중 8건이 이번 회차 워커 품질 문제(§2). 재팬아웃 1회면 대개 풀린다.

### E. 옵션축 대기 (앱에서 수동, MCP 불가)
시트 `옵션축` 탭에 이번 회차 **신규 170 · 갱신 15** 가 쌓였다. 축 이름(`색상`인데 값은
모델·규격)은 MCP로 못 바꾼다. 저장을 막지는 않는다.

### F. 삭제 못 한 30건 (제외카테고리 · 잠금)
§3② 참조. 잠금 해제가 선행돼야 한다.

### G. 시트에만 있고 그룹엔 없는 235건
앞 회차부터 그대로. 작업 이력이 날아가서 안 지웠다. 정리는 용팀장 판단.

---

## 5. 이번 회차 실측치 (다음 견적용)

| 단계 | 규모 | 워커 | 소요 |
|---|---|---|---|
| 카테고리 consensus | 152회 조회(76건×변형2) | — | ~28분 (7건 청크 × 22회, 캡챠 0) |
| 제외카테고리 삭제 | 149 + 48건 | — | ~25분 (라운드 3회 + 재큐) |
| 상품명 prep | 74건 | — | ~4분 (통다운 캐시 적중) |
| 상품명 팬아웃 | 배치 48 | 12명 | 11분 · 1.15M 토큰 |
| 상품명 재팬아웃 | 배치 10 | 2명 | 8분 |
| 옵션 prep | 337건 · 옵션 9,159 · 이미지 3,872 | — | ~35분 |
| 옵션 팬아웃 | 배치 68 | 68명 | **30분** · 5.74M 토큰 |
| 옵션 커밋 | 305건 | — | ~25분 |

**크레딧**: 세 축 모두 AI 크레딧 0. 썸네일만 큰 지출.

---

## 6. 다음에 그대로 쓸 명령 (이번에 검증된 형태)

```bash
# ── 카테고리 consensus (저확신 → 변형 2개 → 2/3 합의)
R=~/python_work/data/category-fix/runs/<이름>
run_all.py consensus prep --run-dir "$R"     # → consensus_targets.json (Claude가 변형 2개 작성)
#   변형 작성: consensus_variants.json = [{"productId": "...", "변형": ["변형1","변형2"]}]
run_all.py consensus run  --run-dir "$R"     # → 2/3 통과분 자동 저장 + 게이트 + 상품명 재작업 플래그

# ── 제외카테고리 삭제 (잔여가 남으면 반드시 새 run-dir 로 재큐)
category_gate.py autodelete --run-dir "$R/gate"
category_gate.py retry      --run-dir "$R/gate-retry" --only "5번_용쌤25-2"
category_gate.py autodelete --run-dir "$R/gate-retry"

# ── 상품명 (--sheet 는 서브커맨드 **뒤**)
R=~/python_work/data/product-name/runs/<이름>
run_names.py prep --group-id 1001822 --group-name "5번_용쌤25-2" \
  --sheet 1DbR2upLX_DXjgjXpGPdide2SUifli_X6DTqhUTr9Syk --run-dir "$R" --sleep 0.5 [--ids ${=IDS} --redo]
#   → pending 계산(named_* 만 센다) → Workflow{name:"pname-fanout",
#      args:{runDir, promptPath, batchNums, viewKB, budgetKB:30}}
#   → fix-r9 --commit → check → (실패배치 named 지우고) 재팬아웃 1회(retried:true) → check
#   → append → audit_sample → mark(의심) → rename --commit

# ── 옵션 (--sheet 는 서브커맨드 뒤)
R=~/python_work/data/option-cleanup/runs/<이름>
run_options.py prep --group-name "5번_용쌤25-2" \
  --sheet 1DbR2upLX_DXjgjXpGPdide2SUifli_X6DTqhUTr9Syk --run-dir "$R" --sleep 0.5 --max-option-images 16
run_options.py pending --run-dir "$R"        # → 그 JSON을 그대로 Workflow args 로
#   → Workflow{name:"optclean-fanout", args:<pending 출력>}
#   → apply (미리보기, --sheet·--group-name 둘 다) → apply --commit --no-sheet --no-review
```

**긴 작업은 세션에서 분리해 띄운다**:
```bash
nohup env PYTHONIOENCODING=utf-8 PYTHONPATH=.claude/lib .venv/bin/python <명령> > "$R.log" 2>&1 &
disown
until ! pgrep -f "bin/python.*<스크립트명>" > /dev/null; do sleep 60; done   # 파이썬만 좁혀서 대기
```

**zsh 주의**: `--ids $IDS` 는 한 덩어리 문자열로 넘어간다. 반드시 `${=IDS}`.

---

## 7. 표본검수 기록 (2026-08-15)

본 라운드 29건 전건 + 재작업 라운드 19건 전건. **의심 1건**:

| 상품id | 새 상품명 | 기준 | 사유 |
|---|---|---|---|
| `U01KZZYPC0AQSEXE76TTTQYH43Y` | RS485 숫자카운터기 TTL 카운터기 PLC모듈 기본형 | (b) | 실물은 값을 **표시**하는 LED 모듈인데 '카운터기'는 계수 기능을 지칭 → `보류(표본의심)` |

썸네일로 확인해 **살린 건 2건** — `접이식`(썸네일에 `可折叠设计` 명시) · `베어링`(썸네일에
`进口轴承` 명시). 원문·옵션만 보면 근거가 없어 보이지만 **썸네일 안 문구가 근거**인 경우가
있다. 판정 전에 반드시 이미지를 연다.

`책상1000500` 같은 숫자 규격 키워드는 오타처럼 보여도 **통다운에 실재하는 저상품수
직결어**다(상품수 109 · 검색량 50). 뷰에서 확인하고 살린다.

---

## 8. 커밋 전에 볼 것 — 워킹트리에 **다른 세션 작업분이 섞여 있다**

이 세션이 도는 동안 **다른 세션이 같은 저장소에 커밋 3개**를 올렸고(`197cb2b`·`cf30707`·
`fbc0498` — 스킬 배포 파이프라인), 옵션 스킬 문서에 **미커밋 변경분**도 남겼다.
`git commit -a` 로 뭉뚱그리면 남의 작업이 딸려 들어간다. 나눠서 커밋해라.

**이 세션이 바꾼 것** (2026-08-15 옵션 저장실패 수정 — §2):
```
M  .claude/skills/bulsaja-option-cleanup/scripts/option_rules.py    (with_dedup_cleanup 추가)
M  .claude/skills/bulsaja-option-cleanup/scripts/run_options.py     (_promote_main 추가)
M  .claude/skills/bulsaja-option-cleanup/scripts/test_option_rules.py (신규 테스트 5개)
D  .claude/skills/sellha-category/**                                 (쓰지 않는 스킬 삭제)
?? 00-inbox/2026-08-15-25-2-작업인계-2회차.md                          (이 문서)
```

**다른 세션 작업분** (건드리지 않았다):
```
M  .claude/skills/bulsaja-option-cleanup/SKILL.md                   (§워커준수 검사 신설)
M  .claude/skills/bulsaja-option-cleanup/references/옵션-워커-프롬프트.md
M  .claude/skills/bulsaja-option-cleanup/references/옵션명-규칙.md
M  00-inbox/2026-08-15-25-2-신규958-작업인계.md
M  00-inbox/2026-08-15-용쌤2-1-썸네일-완료.md
?? mismatch_ids.txt                                                 (루트에 생긴 108줄 id 목록)
```

> 내용상 충돌은 없다 — 그쪽은 **워커 산출물 품질**(제외 사유를 안 쓰는 문제)을 잡았고,
> 이쪽은 **저장 경로 결함**을 잡았다. 서로 다른 층이다. `test_option_rules.py` 는 양쪽
> 변경이 한 파일에 겹쳤지만 106개 전건 통과를 확인했다(SKILL.md 의 `106건` 수치와 일치).

### `sellha-category` 스킬을 지웠다 (2026-08-15 용팀장 지시)

실행 경로는 2026-08-04 에 이미 Aside 로 옮겨졌는데(§4-C 경고 박스) 스킬 디렉토리만
남아 있었다. 지우기 전에 확인한 것: `sellha.py`·`sellha-category` 를 부르는 활성 코드가
없다(잡힌 참조는 전부 `.claude/worktrees/shipfee-skill-install/` 안의 옛 문서 주석).
**catfix 파이프라인은 영향 없다.**

---

**Last updated**: 2026-08-15
