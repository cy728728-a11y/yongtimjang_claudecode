# 2026-08-14 · 2번_용쌤2-1 작업 인계 (2회차 · 컨텍스트 clear 대비)

groupId **1001115** · 그룹시트 `1T3qkLlE4vkX_1KyaSAic-QgaA3bG6JNPUxUJh6kfWF4`
직전 인계: `00-inbox/2026-08-14-용쌤2-1-작업-인계.md`

## 이번 세션에 끝낸 것

| 작업 | 결과 |
|---|---|
| 보류(카테고리의심) 34건 소진 | **27건은 재교정 결과 `이미정확`**(의심 기각) · 7건만 실제 미해결이었다 |
| 카테고리 7건 재확정 | 자동저장 4 · 유도저장 1 · 이미정확 1 · 매칭불가 1(모닝휠) |
| 상품명 `_nokw2` 라운드 | 28건 전건 생성·검증 통과 → **27건 반영**(표본의심 1 제외) |
| 상품명 `_r5` 라운드 | 재작업 5 + 검증실패 34 = 39건 → **36건 반영**(표본의심 1 제외) |
| 상품명 `_nokw3` 라운드 | 2건 → **2건 반영** |
| 제외카테고리 잔여 삭제 | **0건 성공** · 잠금 2 · 서버거부 3(3라운드 전부) |

**현황판 최종**: 카테고리 완료 1028 · 상품명 완료 **710**(645→710, +65) · 옵션 완료 694

run-dir
- 카테고리: `~/python_work/data/category-fix/runs/2-1_redo4`
- 상품명: `~/python_work/data/product-name/runs/용쌤2-1_260814_nokw2` · `_r5` · `_nokw3`
- 통다운: 260811 사용

## 남은 것 (우선순위 순)

### 1. 그룹 이탈 17건 — 마켓그룹 목록에 없다 ★ 이룸님 확인 필요

현황판 상품명 열이 `재작업(카테고리: …)` 인 17건이 **`bulsaja_market_group_products` 조회에
안 잡힌다.** 상품 자체는 살아 있고(`bulsaja_product_detail` 로 1건 확인 — 정상 응답),
시트1 G열도 `자동저장완료`/`저장완료`다. 마켓에서 내려가 그룹 조회 대상에서 빠진 것으로 보인다.

**그룹에 없으면 `prep` 이 대상으로 못 잡으므로 앞으로 매 라운드 이 17건이 계속 남는다.**
불사자 앱에서 그룹 재할당·재업로드가 필요한지 확인해야 한다.

```
U01KREA81Y6G1PKTV4AXAGQFNVM U01KREA81Y72W37RFFS49P2SQ35 U01KREA81Y84ZHRAFF9S3ERQFN4
U01KREA81YTJ2AKETASNDAF5N1C U01KREA81Z0MSKFRF8J6P890C34 U01KREA81Z3JG3269T5531EEF32
U01KREA81Z4YFYWFCRHZ8ZS8QWG U01KREA81Z6N6F2X1CRGZ01NWCE U01KREA81ZGKDWQRDG491S58RP0
U01KREA8208KHXQ1CBM27PFSBDK U01KREA821415VY66VFCXP1CY6X U01KREA821PVBKGAMZJ0P70WB72
U01KREA821RGPJQV22VDS8QZ1CN U01KREA821V58K39S80YQDMJCY3 U01KREA82353633NY241XR6CAA1
U01KREA8236PCWHEHXXSJX5BE16 U01KREA823BFQ6Q10KMGRMFQ3VD
```

### 2. 썸네일 재작업 — 현황판 `썸네일` 열 (미착수 491 + 재작업 다수)

`bulsaja-thumbnail` 런이 자동으로 잡는다. 이번 세션에서 손대지 않았다.

### 3. 옵션 재작업 14건 + 보류 27건

- `재작업(상품명: 기본형 부착 — 대표옵션명 보완 필요)` **14건** (이번 rename 이 +4)
  → 다음 옵션정리 런이 `_matrix_redo` 로 자동 편입한다.
- `보류(삭제대상)` 19 · `보류(저장실패)` 4 · `삭제대기(제외카테고리)` 3 · 미착수 1

### 4. 옵션 보류 2건 (앱에서 직접) — 직전 인계에서 이월

- `U01KREA821KREA821BK5Q41TBF91GGP6T7` 네일 진열장 — 판매 가능한 메인 옵션 0개(옵션 뒤집힘)
- `U01K5BR2XZECFB775XCGQP5XV9D` 교탁 — 옵션명 27자·26자(상한 25)

### 5. 카테고리 매칭불가 3 — 수동지정 큐

대표: **모닝휠**(`U01KREA8236ABYMNCQEJFJ66Z8W`). 조회 경로 `생활/건강>자동차용품>타이어/휠>휠`
확신도 100%인데 **불사자 스마트스토어 후보에 그 경로가 아예 없다**(`steer` = `미발견`).
기존 값 `타이어/휠>휠용품` 이라 무해해서 그대로 뒀다. 경로를 직접 알려주면 1단계로 넣을 수 있다.

※ 이 상품은 **상품명도 오지칭**이다 — 상품명은 "모닝휠 19인치"인데 원문은
`适用于15寸本田新老飞度…铝合金轮毂` = **혼다 피트용 15인치 휠**이다.

### 6. 제외카테고리 삭제 5건 — 스킬이 못 넘긴다

3라운드 전부 실패. `삭제대기` 로 남겼다(비용 0).
- **잠금 2건**: `U01K5BQ9NAX0STEWG78V5XJENT3` · `U01K5BPG8THM4NK34PTZGBEM04R` — 앱에서 잠금 해제 필요
- **서버 거부 3건**(`업로드에 실패했습니다`): `U01K5B58XQYPGTRDJK8Z2MP301T` ·
  `U01K5B4HM20M6A1XMPJ89MSG404` · `U01K5B1X5AA50E46Z6PBN9KSKXG`
  → 일괄삭제 요청 시트: https://docs.google.com/spreadsheets/d/1LweJBPpwXw2-dMXP-k6-koHW2Kfh663JWjLOFksJO10/edit

## 판정 큐 (이룸님 몫) — 4건

`보류(표본의심)`. 상태 원복(재반영) 또는 폐기를 정해야 한다.

**이번 세션에 새로 잡은 2건**
- `U01KREA8239THWTZBRVJV89C1YJ` 스키복 → **(a) 실물 오지칭**.
  옵션 원문 전량이 `滑雪套装`(상하 분리 세트)이고 썸네일도 재킷+팬츠 분리인데
  상품명은 "일체형스키복 원피스보드복 … 점프슈트". **원본유지 경로로 나왔으나 원본 자체가 틀렸다.**
- `U01KREA81YAQZXJGG1QSZKDWBAH` 원탁 → **(b) 증거에 없는 속성 '좌식'**.
  썸네일이 의자와 함께 쓰는 일반 높이 카페 상담 테이블이고 원문
  (`洽谈桌椅…双层小圆桌`)에도 근거가 없다. 원본의 '좌식'을 그대로 승계한 건.

**직전 세션에서 이월 2건**
- `U01KREA823EY0KB4N8D09QSVSSP` 미용실의자 → 실물은 미용실 거울
- `U01KREA823DAF1JMQ53RE8YA287` 계단카트 → 무관한 타사 브랜드 `코스트코` 채택

## 이번 세션에 겪은 사고 — 되돌렸다

**`finish --overwrite-done` 이 keywords.json 에 없는 건까지 덮어쓴다.**

`prep` 은 34건을 대상으로 잡았는데 `keywords.json` 에는 7건만 넣고 `finish --overwrite-done`
을 돌렸더니, `merge` 가 34건 전건으로 `merged.json` 을 만들고 `apply --force` 가
**나머지 27건을 `sellha조회실패` 로 덮었다**(D·E·F 열도 비워짐). 불사자 저장은 무사했고
**원장만 거짓**이 됐다.

→ `2-1_redo3/decisions.json` 의 `이미정확` 27건으로 시트1 27행(`sheet_log.SheetLogger`)과
현황판 27칸(`matrix.mark_many`)을 복원했다. 검증 완료.

**교훈**: `--overwrite-done` 은 원장 보호를 뚫는 플래그다. **`keywords.json` 을 손으로 쓸 때는
`prep` 대상 전건을 넣거나, 부분 집합이면 `--overwrite-done` 을 빼야 한다.**
(`prep --ids` 를 처리할 건만으로 좁히는 쪽이 더 안전하다.)

## 현황판 정합성 — 두 번 정정했다

`run_names.py mark` 는 **원장(상품명 탭)만 쓰고 현황판(`00_진행`)은 안 쓴다.**
그래서 `보류(표본의심)` 로 마킹한 건이 현황판에 `진행중(미반영)` 으로 남는다.

- 세션 시작 시 이미 17칸이 이 사유로 어긋나 있었다(원장은 검증실패 15 + 표본의심 2).
- 이번 세션 마킹분 2칸도 같은 증상 → 둘 다 `matrix.mark_many` 로 직접 정정했다.

**`matrix.rebuild` 는 쓰지 마라** — 다른 축(옵션·썸네일)의 재작업 플래그가 `preserve_redo` 로
살아남긴 하나, 삭제분 297건의 `상품삭제(제외카테고리·자동)` 표기가 `해당없음` 으로 뭉개진다.

## 주의 (직전 인계에서 유지)

- **불사자 저장 ≠ 마켓 반영.** 스마트스토어 게시분은 **수정 업로드**를 따로 해야 판매 화면이 바뀐다.
- 불사자 MCP 는 연속 호출 시 **HTTP 429** 가 난다(이번 세션 `steer` 1회). 2~3분 쉬면 풀린다.
- zsh 에서 `--ids $IDS` 는 단어 분할이 안 된다 — `${=IDS}` 또는 `$(cat ids.txt)` 를 쓴다.
- 제외카테고리 자동삭제는 `auto` 출구에만 붙어 있다. `finish`/`steer` 로 쪼개 돌리면
  `category_gate.py autodelete` 를 따로 불러야 한다.

---
그룹 시트: https://docs.google.com/spreadsheets/d/1T3qkLlE4vkX_1KyaSAic-QgaA3bG6JNPUxUJh6kfWF4/edit
