# 2-2 잔여 전량 마감 (2026-08-22, 6회차 후반)

> ★ **2-2 의 단일 진입점이다. 다음 세션은 이 문서만 읽으면 된다.**
> 같은 날 앞 문서 두 개(`…카테고리재교정-72건.md` · `…상품명-재작업-54건반영.md`)의
> "남은 일" 표는 **전부 낡았다** — 그 뒤 잔여 축을 다 처리했다. 앞 문서들은 **원인 규명
> 기록**으로만 참고한다(코드 손볼 거리는 §8에 합쳐 놨다).
>
> **2-2 는 세 축 모두 대량작업 종료.** 다음 회차는 **다른 그룹**으로 넘어간다.
> 2-2 에서 할 일이 다시 생기는 조건은 둘뿐이다 — ①통다운 갱신(빈칸 14건) ②잠금 6건 판정(§5).

## 0. 다음 세션 진입점

```
그룹   2번_용쌤2-2  (groupId 1001186)
계정   부킹(용쌤) clwkdzjavjsl@gmail.com
시트   1wMKUsOxy3wSzejGXLk0G6o22DLf8zcBDg3_6sbVXBZo
run-dir 상품명: 2-2_0822cat / _nokw / _fix6 / _jam
        옵션:   2-2_0822rem / _rt
        썸네일: 2-2_0822rem / _rt
        카테고리: 2-2_redo4
python  .venv/bin/python  (PYTHONPATH=.claude/lib)
통다운  260819
```

**2-2 는 세 축 모두 대량작업이 끝났다.** 남은 건 전부 판정 대기이거나 "그대로 두기로 한 것"이다.
**다음 회차는 다른 그룹으로 넘어가도 된다.**

### ⚠ 하지 말 것

1. **썸네일 `보류(제외)` 25건 재생성 금지** — 세부사양(색상·단수) 어긋남은 AI 배경교체가
   못 고친다. 기존 대표가 살아 있어 판매 지장 없다.
2. **`보류(카테고리의심)` 9건 · `보류(실물불명)` 1건 재진입 금지** — `_redo` 1회 한정 소진.
3. **잠금 6건 삭제 재시도 금지** — 스킬이 못 푼다(§5).
4. **썸네일 `보류(실물기준없음·왕복종결)` 7건 다시 집지 마라** — 왕복이 닫힌 상태다.

## 최종 현황 (994건)

| 축 | 완료 | 삭제/해당없음 | 남은 것 |
|---|---|---|---|
| **상품명** | **677** | 293 | 빈칸 14(통다운밖) · 보류 10 |
| **옵션** | **772** | 222 | **0** |
| **썸네일** | **686** | 260 | 보류 48 |

**옵션 축은 완전히 비었다** — 보류·재작업 0.

## 1. 옵션 11건 — 두 회차 이상 막혀 있던 `저장실패` 4건 전부 해소

| 갈래 | 건수 | 결과 |
|---|---|---|
| `재작업(상품명: 기본형 부착)` | 6 | 반영 |
| `보류(저장실패)` | 4 | **전건 반영** |
| `재작업(썸네일: 기준이미지없음)` | 1 | 반영 (대표를 세트로 재확정) |

**반영 11/11 · 부분저장 0 · 실패 0.** 워커준수: 사유 쓴 제외행 433개 · 무분류 0.

### 1-1. ★ `저장실패` 의 원인은 둘 다 "미해결"로 적혀 있던 것과 달랐다

**(가) 큰 3건(옵션 378·585·600)** — SKILL §알려진 함정이 "제외 200 초과 대표 지정 거부
**(미해결)** · 재팬아웃해도 안 풀린다 · 삭제하라"로 못박아 둔 프로필이었다.
실제 원인은 **이름 충돌**이었고, 현황판에 사유를 실어 재팬아웃하니 전건 저장됐다:

```
저장실패 재시도(옵션 N) — … 전 옵션에 원문 기준 고유 이름을 줘라(이름중복이면 확인키가 안 나온다)
```

> **일반화**: 함정 표의 "미해결"은 **그때 그 상품에서 못 풀었다**는 기록이지 **원인 규명이
> 끝났다**는 뜻이 아니다. 옵션 수가 크다는 표면 특징이 같다고 같은 원인이라 단정하면
> 멀쩡히 풀릴 상품을 삭제한다. **삭제 규칙을 적용하기 전에 한 라운드는 사유를 실어 태운다** —
> 비용은 워커 1명이고, 2-3 실측(5건 중 4건 복구)이 이미 그 값을 보여줬다.

**(나) 회 써는 기계(옵션 56)** — 08-15 문서가 "네 군데 다 확인했는데 중복이 없다"로 남긴 건.
이름은 이번에 고쳐졌는데 저장이 `RuntimeError: 옵션값을 못 찾았다: ['6','2','3','5','4']`
로 죽었다. 원인은 **워커가 `vid고유: false` 상품에 맨 vid 키를 쓴 것**이다.
차원 좌표(`@0:6` …)로 고치니 즉시 통과 — 대표 `저전력 단면형, 2.5인치 기본형`.

### 1-2. ★ 같은 vid 키 실수가 11건 중 2건에서 났다

`vid고유: false`(차원이 둘 이상이고 vid 가 겹침)인데 워커가 `"이름": {"1": …, "2": …}` 로 준다.
증상이 둘로 갈린다 — **어느 쪽도 "워커가 틀렸다"고 안 찍힌다**:

| 상품 | 증상 |
|---|---|
| VR6EFYAX (3차원) | 같은 이름이 **크기 축에도** 붙어 표시명 중복 → `보류(이름규칙)` |
| HVQQ13XJ (2차원) | 키가 아예 해석 안 됨 → `RuntimeError` 로 저장 실패 |

SKILL 은 `"@차원:vid"` 규칙을 적어 뒀고 배치에도 `vid고유` 플래그를 싣는데, 워커가 안 본다.

> **손볼 곳**: `apply` 가 결과를 읽을 때 **`vid고유: false` 인데 맨 vid 키가 오면
> 거부하지 말고 자동 교정**한다(차원 후보가 유일하면 그 차원으로 고정, 둘 이상이면 보류).
> `_repair_truncated_ids`·`_fill_prefix_names` 와 같은 부류의 코드 방어다 —
> 지시문으로는 안 막히는 것이 이미 실측으로 두 번 나왔다.

## 2. 썸네일 6건 — 생성 6건 · **30크레딧** · 전건 사용가능

두 라운드로 돌았다(옵션 왕복이 중간에 한 번 끼었다).

| 라운드 | 생성 | 크레딧 | 결과 |
|---|---|---|---|
| 1회차 | 4 | 20 | 전건 사용가능 |
| 2회차(왕복 복귀분) | 2 | 10 | 전건 사용가능 |

**교정 내용 — 전부 옵션이 지적한 그대로였다:**

| 상품 | 전 | 후 |
|---|---|---|
| 원단 샘플행거 | 옷걸이형 다중로드 거치대 | **5단 보드형 랙**(대표옵션과 일치) |
| 미용실 카트 | 금색 다기능 카트 | **스테인리스 2단 카트** |
| 소스 볶음기 | 모터·교반날 헤드 단독(부속) | **냄비+모터 일체형 세트** |
| 회 슬라이서 | (제품 동일) | 배경만 정리 |
| 드로잉테이블 | 책상+스툴 합성컷 | **테이블 단품** |
| 워치와인더 | 보관함 2개 합성컷 | **1대 단품**(손 제거) |

### 2-1. ★ `prescreen` 이 크레딧 3건분(15)을 막았다

생성 전 기준적격 검사에서 **6건 중 3건이 `다중혼재`** 로 걸렸다 — 대표옵션 이미지에
물건이 둘씩 들어 있었다(책상+스툴 · 보관함 2개 · 슬라이서+사용법 6칸 그리드).
그대로 태웠으면 배경교체가 뭘 살릴지 모르는 이미지를 냈고 재생성까지 갔을 자리다.

**셋 다 옵션정리가 "대표 동률이라 원본 순서로 정했다"며 되돌린 건**이었다 —
즉 **옵션이 "대표를 정하긴 했는데 확신은 없다"고 넘긴 신호가 실제로 위험했다.**
prescreen 이 그걸 잡아 후보에서 다시 고르게 했고, 2회차 prescreen 은 전건 `단일특정` 이었다.

### 2-2. ★ 옵션↔썸네일 왕복이 설계대로 닫혔다 (2026-08-19 수리분 실증)

`이력` 열 방식(작업 열이 아니라 어떤 저장도 덮지 않는 자리)이 이번에 처음으로 완주했다:

```
썸네일 → 옵션(대표옵션의심·기준이미지없음) → 옵션이 대표 재확정
  → [미아회수] 대표 바뀜 1건(재판단) · 대표 그대로 1건('실물기준없음' 종결)
  → 썸네일 prep 이 종결분을 비전 배치로 보냄(대표옵션 기준 제거)
  → prescreen 전건 단일특정 → 생성 → 사용가능
```

3회차·4회차에 같은 건들이 되돌아오던 루프가 **1왕복으로 끝났다.**

### 2-3. 내 오진 1건 — 옵션명 기계번역에 끌려갔다

드로잉테이블(7HT5XKFP)의 대표옵션명이 `미니멀 노르딕 **스툴** 기본형` 이라 "상품명은 책상인데
대표가 의자다 = 대표옵션의심" 으로 판정해 옵션으로 되돌렸다. 옵션 워커가 이미지를 열어 보니
**그 옵션은 책상 단품이고 '스툴'은 오역**이었다(교정 후 `미니멀 노르딕 단품 기본형`).

> **일반화**: 옵션명은 기계번역이라 품목을 단정하는 근거로 쓰면 안 된다 — SKILL 이
> 여러 곳에 적어 둔 규칙인데 **판정하는 쪽(썸네일 검수)에서 내가 그걸 어겼다.**
> 결과적으로 왕복 1회가 늘었지만 손해는 아니다: 옵션이 옵션명을 고쳤고(오역 제거)
> 대표도 재확정됐다. 다만 **품목 판정은 원문·이미지로만** 한다.

## 3. 상품명 잔여 — 표본의심 7건 처리 + 이관 1건

### 3-1. `보류(표본의심)` 7건 (용팀장님 판정: 죽은 1건 삭제 · 6건 이름 재작업)

- **삭제 1건** — `BF8BYN8T`(종이 전시대). 썸네일 축이 이미 `해당없음(원본404·삭제)` 인
  죽은 상품이라 이름을 다시 지을 값이 없다. 삭제 완료(이미 휴지통이었다).
- **재작업 6건 — 전건 반영.** 사유를 현황판에 실어 R12 게이트를 태웠다:

| 상품 | 전 | 후 |
|---|---|---|
| 사무실 파티션 | 사무실 파티션 이동식 **스크린** (앞 회차엔 '슬라이딩책장') | **유리 회사파티션** 사무실칸막이파티션 |
| 석등 | 돌 조각품 야외 **옥상등** 정원 **석탑** 이발소 바버폴 | **석등** 전기정원등 석재 태양열정원등 |
| 우드슬랩 테이블 | **대형** 우드슬랩 **클래식** 카페 **인용** | 회의용 긴식탁 **월넛** 업소용테이블 |
| 크리스마스트리 | **하프트리 반쪽트리 벽트리** | **120cm** 크리스마스트리 솔방울 PVC |
| 대리석 테이블 | 거실 대리석 **협탁** 원목 좌탁 | 거실 **쇼파** 마블링대리석테이블 모던 |
| 캔버스 텐트 | 농막 **투명 온실 그늘막 소형** | 방수 **몽골천막** 캔버스 장박천막 텐트 |

### 3-2. ★ `재작업사유` 가 워커에게 도달한 적이 없었다 (코드 수리)

`_mk_product` 가 배치를 조립할 때 키를 하나하나 꼽아 쓰면서 **`재작업사유` 만 빠뜨렸다.**
`prep` 은 `pending.json`·`targets.json` 까지 사유를 멀쩡히 실어 놓는데 마지막에 떨어뜨린다.

이 필드에 의존하는 게 셋인데 **셋 다 실전에서 한 번도 발동한 적이 없다**:
- SKILL.md §Step 2 "사유를 배치의 `재작업사유` 로 워커에게 실어 보낸다"
- 워커 지시서 §1-1 "재작업사유가 있으면 그것부터 처리한다(생략 불가)"
- **R12 재작업 게이트**(2026-08-15 신설)

08-15 에 25-2 표본검수에서 "워커가 지목된 낱말을 그대로 뒀다"는 의심 3건이 나와 R12 를
만들었는데, **진짜 원인은 지시서가 아니라 필드가 애초에 안 갔던 것**이었다. 옵션정리·
썸네일·정합검사가 넘긴 사유가 전부 여기서 증발했다.

수리 후 즉시 효과가 나왔다 — 6건 전부 `재작업반영` 에 "대표옵션 id19, 제외:false, 최저가
3x2m라 소형 근거 없음" 식으로 **옵션 실물을 짚어** 답했다.
회귀 테스트 통과(`test_run_names.py` · `test_name_check.py`).

> **일반화**: **"필드를 실어 보낸다"고 적힌 계약은 수신 쪽에서 실물을 확인해야 한다.**
> 세 문서가 한목소리로 그 필드를 전제하는데 아무도 배치 JSON 을 열어 보지 않았다.
> 계약 검증 테스트가 있었으면 R12 를 만들 필요조차 없었다.

### 3-3. 옵션 이관 4건 중 3건은 워커 오판이었다

옵션 워커 3명이 `이관(상품명): "상품명 끝에 '기본형' 오기재 — 대표옵션명 표기와 중복,
제거 필요"` 를 냈다. **`기본형` 은 R8 이 요구하는 짝 지목 마커다** — 상품명·대표옵션명
양쪽에 같은 단어가 있어야 썸네일·대표옵션·상품명이 한 옵션을 가리킨다(규칙 17 ↔ R8).
무효 처리하고 `완료` 로 되돌렸다.

> **손볼 곳**: 옵션 워커 지시서에 **"상품명 끝의 `기본형` 은 중복이 아니다 — 이관하지 마라"**
> 를 명시한다. 11건 중 3건(27%)에서 났으니 우연이 아니다. 안 막으면 상품명 축에
> 헛 재작업이 회차마다 쌓인다.

**정당한 이관 1건** — 소스 볶음기(28C6QDSM). 옵션뒤집힘 복구로 본품이 헤드→세트로 바뀌어
상품명을 다시 지었다: `세트 업소용자동볶음기 소스교반기 기본형`.
원본 '잼 만드는기계'는 오기재였다(원문 `全自动小型火锅底料炒料机麻辣烫…` = 훠궈 육수·마라탕
양념 볶음기. '잼'은 원문 어디에도 없다).

## 4. 404 자동 삭제 — 2건 중 1건

| 상품id | 결과 |
|---|---|
| `U01KS1WY74A3WKAENEFW7TQ5HHB` (대형러그) | 삭제 완료 → `mark-deleted` 로 확정 |
| `U01K5HZBBKY4ST0A11WC85VR851` (어르신 실내자전거) | **잠금** — `보류(원본404·삭제보류(잠금))` 로 표시 |

## 5. 판정 필요 (용팀장님) — 잠금 6건

게이트 잠금 5건 + 위 404 잠금 1건 = **6건.** 스킬이 못 푼다(앱에서 직접 걸어야 해제).
잠금은 보통 **판매된 상품**에 건다 → 삭제가 맞는지 자체가 의심스럽다.

> **권고: 그대로 둔다.** 두기로 하면 현황판 `삭제대기` 를 다른 값으로 바꿔야
> 매 런 게이트가 재큐하는 걸 멈춘다.

## 6. 남은 것 — 전부 "그대로 두기로 한 것"

### 상품명 24건

| 건수 | 상태 | 성격 |
|---|---|---|
| 14 | (빈칸) | 통다운밖 — **통다운 갱신 전엔 못 한다** |
| 9 | `보류(카테고리의심)` | 재진입 금지(1회 한정 소진) |
| 1 | `보류(실물불명)` | 재진입 금지 |

### 썸네일 48건

| 건수 | 상태 | 판정 |
|---|---|---|
| 25 | `보류(제외)` | **재생성 금지** — 세부사양 어긋남은 AI가 못 고친다 |
| 9 | `보류(기준이미지없음)` | 대표옵션 이미지가 비제품이고 후보에도 실물 없음 |
| 7 | `보류(실물기준없음·왕복종결)` | 왕복 닫힘 — 다시 집지 않는다 |
| 5 | 대표옵션 일치 후보 없음(4단·화이트 어린이용·스툴형 짙은녹색·색상) | 개별 판정 |
| 1 | `보류(주의)` | 3축 통과했으나 사람이 볼 것 — **미반영** |
| 1 | `보류(재작업사유 미충족)` | |

## 7. 이번 세션 총계

| 축 | 처리 |
|---|---|
| 상품명 | 반영 **61건** (54 + 6 + 1) · 삭제 1 |
| 옵션 | 반영 **13건** (11 + 2) |
| 썸네일 | 반영 **6건** · 생성 6 · **30크레딧** |
| 카테고리 | 재교정 1 → 제외카테고리 삭제 |
| 현황판 정리 | 그룹 밖 삭제분 9 · 무효 이관 3 · 404 확정 1 |

크레딧 잔액 30,790. 워커 토큰 약 1.9M.

## 8. 문서/코드 손볼 거리 (누적)

| # | 내용 | 근거 | 상태 |
|---|---|---|---|
| 1 | **`_mk_product` 가 `재작업사유` 를 배치에 싣게** | §3-2 | **수리 완료** |
| 2 | `apply` 가 `vid고유:false` + 맨 vid 키를 자동 교정 | §1-2 (11건 중 2건) | 미해결 |
| 3 | 옵션 워커 지시서에 "상품명 끝 `기본형` 은 이관 대상 아님" 명시 | §3-3 (11건 중 3건) | 미해결 |
| 4 | `_recover_group_orphans` 가 `already_gone` 건을 `_mark_gone_column` 에 넘기게 | 앞 문서 §2 | 미해결 |
| 5 | 상품명 규격어·크기어를 대표옵션과 대조하는 경고 규칙 | 앞 문서 §4-1 | 미해결 |
| 6 | `apply` 가 `이미정확` + 상품명 없음 → `재작업` 을 찍게 | 08-22 카테고리 문서 §2 | 미해결(이월) |
| 7 | 대량 `--ids` 용 `--ids-file` 인자 | 08-22 카테고리 문서 §5 | 미해결(이월) |
| 8 | 함정 표의 "미해결"에 **원인 규명 여부**를 구분해 적기 | §1-1 | 미해결 |

---
Sources: run-dir `2-2_0822cat`/`_nokw`/`_fix6`/`_jam` · `option-cleanup/2-2_0822rem`/`_rt` ·
`thumbnail/2-2_0822rem`/`_rt` · `category-fix/2-2_redo4`
시트: https://docs.google.com/spreadsheets/d/1wMKUsOxy3wSzejGXLk0G6o22DLf8zcBDg3_6sbVXBZo/edit
