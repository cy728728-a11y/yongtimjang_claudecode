# 용쌤 3-1 작업 인계 (2026-08-14)

그룹 시트: `1aTVHM43YDcwN_P46l-0AkDJl-Ti9i3a1pFa8w-lfufA` (카테고리교정_3번_용쌤3-1)
불사자 groupId: `1001116` · 계정: **부킹(용쌤)** clwkdzjavjsl@gmail.com

## 한 줄 요약

시작 시 1033건. **4단계 전부 한 바퀴 완주했다.** 제외카테고리 266건 삭제로 그룹이 정리됐고,
썸네일만 생성 큐 정체로 일부(약 55건)를 남겼다.

## 완료된 것

| 단계 | 오늘 처리 | 그룹 완료 | 비고 |
|---|---|---|---|
| 카테고리 재교정 | **저장 88** (자동 74 + 유도 14) · 이미정확 51 | 1002 | run-dir `3-1_redo1` |
| **상품명** | **반영 200건** · 불량 0 · 오류 0 | 658 | 1R 64 + 키워드부족 22 + 재진입 114 |
| **옵션** | **반영 605** · 부분저장 1 · 실패 11 | 666 | run-dir `3-1_0813` |
| **썸네일** | **반영 400건** | 415 | run-dir `3-1_0814` |
| 삭제 | 제외카테고리 **266** + 품목불일치 22 | — | 잔여 9(잠금 추정) |

### 썸네일 상세

- prep 567 → prescreen으로 부적격 54 걸러냄(크레딧 0) → 생성 대상 557
- **생성 502건 회수 후 중단** — 불사자 큐 정체(10시간 41분 지점에 시간당 3건 수준).
  중단 시 **미승인 이미지가 대표로 남은 것 없음**(원본 복원 확인). 크레딧 약 2,510
- 3축 판정 465건 → **사용가능 402 / 불합격 63** → **반영 400**
  - 제외(변조) 29 · 대표옵션의심 31(옵션 재작업 flag) · 기준이미지없음 8 · 글자변조 1 · 주의 2

### 이어서 할 것

```bash
R=~/python_work/data/thumbnail/runs/3-1_0814
# ① 미회수 약 55건 — 큐 풀린 뒤 (크레딧 재소모 0)
python .claude/skills/bulsaja-thumbnail/scripts/run_thumbs.py recover --run-dir "$R"
# ② 정합검사 159건 (크레딧 0) — 아직 안 돌림
python .claude/skills/bulsaja-thumbnail/scripts/run_thumbs.py audit --run-dir "$R"
```

**팬아웃 함정 하나** — verdict 워커가 상품id 끝 3자를 잘라 3건이 통째로 버려졌다.
`verdict --commit` 이 "판정 누락"으로 차단해 줘서 잡혔다(차단이 정상 동작).
해당 배치만 지우고 "id를 한 글자도 줄이지 말라"는 emphasis 로 재팬아웃하면 복구된다.

## 사후 판정 큐 (자동 처리 소진 · 사람 판단 몫)

- **상품명**: 검증실패 3(배치 5·7) · 보류 12(카테고리의심 11 · 실물불명 1)
- **카테고리**: 매칭불가 13 · 정체불명 3 · **consensus 18건**(네이버 캡챠로 조회 못 함)
- **옵션**: 실패 11(이름중복 6 · 대표무효 2 · 전옵션제외 1 · 옵션값못찾음 2) · 보류 6
- **썸네일**: 기준이미지 부적격 54 · 실물없음 4 · 타오바오 404 삭제대상 2
- **삭제 잔여**: 제외카테고리 9 · 품목불일치 미접수 5
- **다음 회차 몫**: 상품명 재진입이 새로 찍은 **옵션 재작업 25칸**

## 겪은 일 3가지 (재발 방지용)

### 1. 불사자 계정이 세션 도중 바뀌어 옵션 616건 전건 저장 실패

`~/.claude.json`의 불사자 토큰이 작업 중간에 **용팀장 계정**으로 교체됐다.
세션 MCP 연결은 옛 연결(부킹)을 물고 있어서 **내 MCP 호출은 되는데 스크립트만 실패**했다
(스크립트는 파일을 매번 새로 읽는다). 3-1은 부킹 소유라 용팀장 토큰으로는 상품이 안 보인다
(`해당 상품을 찾을 수 없습니다`).

- 증상: `확인키 미발급: 옵션 수정에 실패했습니다` 가 **전건**에 뜬다
- **판별법**: 스크립트 전송 계층으로 프로필을 직접 조회한다 (MCP 도구로 조회하면 안 된다 —
  둘이 다른 연결이라 문제를 못 본다)
  ```bash
  PYTHONPATH=.claude/lib .venv/bin/python3 -c "
  from eroomlib.snapshot import ProductMCP
  m=ProductMCP(); m.open()
  print(m.call_tool('bulsaja_my_profile',{}).get('요약')); m.close()"
  ```
- 되돌린 뒤 `apply --commit` 재실행으로 605건 전량 회복(멱등)
- 중간에 `option_rules.py` 의 `vid` 자료형(정수/문자열)을 원인으로 잘못 짚어 수정했다가
  **원복했다** — 같은 페이로드가 MCP로는 통과하는 걸 보고 전송 계층 문제로 판명

### 2. 구글 인증 만료 — 7일마다 재발한다

`gws` OAuth 앱이 Google Cloud에서 **"테스트" 게시 상태**라 refresh token이 7일마다 죽는다.
`invalid_grant: Token has been expired or revoked` → `gws auth login` 으로 재인증.
**근본 해결**: https://console.cloud.google.com/apis/credentials/consent 에서
게시 상태를 **프로덕션으로 전환**하면 7일 만료가 사라진다 (미인증 앱 경고는 "고급→계속"으로 통과).

### 3. 시트 쓰기 쿼터(분당 60회)

상품명 `append` 가 배치 42개 × 2회 쓰기로 429를 낸다. **재실행하면 이어붙으므로**(멱등)
70초 대기 루프로 감싸면 통과한다.

## 병렬 세션 주의

작업 중 **다른 세션이 3-2·25-2 썸네일을 동시에** 돌리고 있었다(불사자 생성 큐·크레딧 공유).
크레딧 잔액으로 소모량을 역산하면 틀린다.

## run-dir

```
~/python_work/data/category-fix/runs/3-1_redo1      # 카테고리 재교정 (+ gate/)
~/python_work/data/category-fix/runs/3-1_gate0814   # 삭제 잔여 큐
~/python_work/data/product-name/runs/3-1_r0813      # 상품명 1R
~/python_work/data/product-name/runs/3-1_kw1        # 키워드부족
~/python_work/data/product-name/runs/3-1_r2         # 재진입
~/python_work/data/option-cleanup/runs/3-1_0813     # 옵션 (before_commit.json)
~/python_work/data/thumbnail/runs/3-1_0814          # 썸네일 (before_generate.json · review.html)
```

되돌리기: 옵션 `before_commit.json` · 썸네일 `before_generate.json` · 상품명 원본은 시트 E열
