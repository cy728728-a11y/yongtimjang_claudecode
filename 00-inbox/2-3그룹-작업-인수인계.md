# 2번_용쌤2-3 작업 인수인계

**작성**: 2026-08-09 작업분 / **그룹**: 2번_용쌤2-3 (groupId `1001994`)
**시트**: [카테고리교정_2번_용쌤2-3](https://docs.google.com/spreadsheets/d/1B_Q4tVrCGUKNcJXYoxIhTRZeISBXiKrqgGHr3VroA1M/edit)
(id `1B_Q4tVrCGUKNcJXYoxIhTRZeISBXiKrqgGHr3VroA1M`)

## 끝난 것 (2026-08-09)

| 축 | 반영 | 비고 |
|---|---|---|
| 상품명 | **179건** | 1회차 66 + 2회차 123 (표본의심 10건 제외) |
| 옵션정리 | **235건** | 1회차 87 + 2회차 160 |
| 썸네일 | **239건** | 신규생성 + 정합확인 + 원본대체. **크레딧 675 소모** |
| 삭제 | **273건** | 삭제후보 13 + 제외카테고리 260 |
| 카테고리 재교정 | 11건 | 자동저장 4 · 이미정확 3 · 변경대상 2 · 부분일치 2 |

## 끝난 것 (2026-08-12)

| 항목 | 결과 |
|---|---|
| 잠금 11건 삭제 | **완료** — 마켓 삭제 + 소싱 휴지통 이동 전건 확인 |
| 카테고리 재교정 18건 | **완료** — 저장 11(자동 9 + consensus 2) · 이미정확 7 · 보류 0 |
| 제외카테고리 게이트 | 신규 삭제 4건 (큐 281건 중 나머지는 이미 삭제된 재고) |
| 상품명 재작업 표시 | 7건 (카테고리 바뀐 상품 — 현황판에 pending) |

## 이어서 할 것

### ~~1. 카테고리 재교정 18건~~ — 2026-08-12 완료
run-dir: `/Users/choiyongsmacbook/python_work/data/category-fix/runs/2-3_r3_redo1`
증거 판별(18건)까지 끝났고 **네이버 조회에서 캡챠**로 멈춤. 조회 1~2건마다 뜨는 상태였음
(누적 총량 한도). 하루 지났으니 풀렸을 것.

```bash
.venv/bin/python3 .claude/skills/bulsaja-category-fix/scripts/run_all.py \
  --sheet 1B_Q4tVrCGUKNcJXYoxIhTRZeISBXiKrqgGHr3VroA1M auto \
  --run-dir /Users/choiyongsmacbook/python_work/data/category-fix/runs/2-3_r3_redo1 \
  --overwrite-done --sellha-sleep 4 --sellha-sleep-max 9 --sellha-rest-every 4
```
- 이미 조회된 건은 `--resume`이 자동으로 건너뜀
- **Aside 캡챠 릴레이 감시 루프가 응답 안 함**(2회 다 타임아웃) → 캡챠 뜨면 용팀장이 Aside 앱 탭에서 직접 풀어야 함

### ~~2. 상품명 재진입~~ — 2026-08-14 완료 (10건 반영)
실제 대상은 4건이 아니라 **13건**이었다(카테고리 재작업 6 + 옵션 재작업 7).
run-dir `2-3_r4` · 무키워드분 `2-3_r4_nokw`. 결과: 반영 10 · 표본의심 보류 2 · 그룹이탈 1.

### ~~3. 잠금 10건 삭제~~ — 2026-08-12 완료 (11건 전건 휴지통)
```
U01KBME0AVDB3VT2HZ39Y68KX5G   U01KBMCFFEYA1DSHHG31VFZ60SE
U01KBMDPAP9JSQ8273480ZW8XRS   U01KBMC1BBZSY5F3692SJM3Z0H5
U01KBMDBYFESTZK46W47PY4BXH2   U01KBMBW57TB8MDNPB980G455JA
U01KBMCFY6A27R2EHK484RRFBFG   U01KBMBPGJJMMHK4CTJX6MF3N2B
U01KRZBANWEBKWZMWH81DKWV4YV   U01KBMBK94X13F99YV9WS0KKQQ4
U01KBMDZ5Z7TRX3KVV9KMR71E1G   (원본404 · 위 10건과 별개)
```

### 4. 옵션축 111건 — 불사자 앱에서 수동 반영
축 이름이 `색상`인데 값은 모델·규격. MCP에 축 이름 변경 기능이 없음. 시트 `옵션축` 탭 참조.

### 5. ⚠️ 수정 업로드
상품명·옵션·썸네일 전부 **불사자에만 저장**됨. 이미 마켓에 올라간 상품은 수정 업로드해야
판매 화면이 바뀜.

## 끝난 것 (2026-08-14 2차) — 상품명 검증실패 146건

**팬아웃 비용 0원으로 118건 반영.** 재생성이 필요 없었다 — 아래 §디스크에 이름이 남아 있었다 참조.

| 단계 | 결과 |
|---|---|
| run-dir `2-3` 현행 규칙 재검증 | 146건 중 108이 이미 `생성완료` (append 를 못 돌린 채 끝난 라운드였음) |
| `fix-r9 --commit` | R9 단독 22건 기계 재배열 → 생성완료 108 → **119** |
| append (필터 run-dir `2-3_r5fix`) | 146행 기록 · 관련어 572행 · J열 backfill 145건 |
| 표본검수 40건(육안 20건) | **의심 1건** — `U01KRZBANR80DF0K6SWVWZXAB2C` 옷걸이 수납카트를 '행거'로 오지칭 |
| `rename --commit` | **118건 반영 · 불량 0 · 오류 0** |

부작용: 현황판 `옵션` 열에 **반쪽 마커 이관 118칸**이 새로 찍혔다(상품명에만 `기본형`이
붙어 대표옵션명 보완 필요) → 옵션 재작업 총 **438건**으로 늘었다.

## 끝난 것 (2026-08-14 3차) — 상품명 보류(카테고리의심) 107건 재교정

run-dir `2-3_r5_redo1` · product-name 발(發) 자동 재교정 모드(SKILL Step 5 ②).
**캡챠 0회** — 조회 107건 + consensus 변형 32건 + 애드혹 5건 전부 무발생.

| 단계 | 결과 |
|---|---|
| prep | 스냅샷 **107/107 적중** → 불사자 조회 0회 · 썸네일 107장 |
| catfix-fanout (배치 14개) | 107건 전건 판정 · 정체불명 0 · 오류 0 · 재팬아웃 0 (2분) |
| `auto --overwrite-done` | 자동저장 **54** · 이미정확 34 · 유도저장 2 |
| `consensus prep/run` (16건) | 채택 7 → **저장 3** · 실물판정의심 9 |
| 근접저장(3단계) 3건 | 파라솔 2(`캠핑>파라솔`→`캠핑>캠핑가구>파라솔`) · 파티션 1 — **세분화 차이만** |
| Claude 판정 저장 3건 | row104 전동공구액세서리 · row547 DIY화장품재료 · row780 썬팅필름 |
| row22 재조회 저장 | 조회실패분. 실물=탁상 유리 진열케이스 → 주얼리 보관함/케이스 (2/3 합의) |
| 제외카테고리 게이트 | **삭제 15건** (3라운드, 전건 성공 · 잔여 0) |

**대상 107건 귀결**: 카테고리 변경 **52** · 삭제 **14** · 카테고리 그대로(이미정확) **41**

### 남긴 판정 큐 4건 (수동)

| row | 상품 | 왜 남겼나 |
|---|---|---|
| 155 | 공압 니들밸브형 UV글루 디스펜서 | 변형 3개가 3방향. 최고 87% `공구>접착용품>접착제`(소모품 카테고리라 도구와 어긋남). 기존 `기타수작업공구`도 41.3%로 근거 있어 바꿀 이득 불확실 |
| 549 | 304 스텐 버터롤러(햄버거집용) | 최고 63% `주방용품>식기>양식기`. 기존 `디지털/가전>주방가전`은 확실히 틀림(전기 아님)인데 대안 확신도가 낮음 |
| 726 | 양 사육용 플라스틱 분뇨 누설판 | 최고 56.5% `공구>안전용품>기타안전용품`(엉뚱). 축산용품에 맞는 네이버 카테고리가 없다 |
| 374 | LG 로봇청소기 호환 배터리 | 조회경로 `청소기>청소기부속품` 이 **불사자에 없음**(references 기존 실증과 동일). 근접 후보가 곧 현재 카테고리(로봇청소기)라 근접저장도 무의미 |

### 카테고리는 맞다고 검증됐는데 상품명이 `보류(카테고리의심)`로 남은 41건

**재교정을 다시 돌리면 안 된다** (SKILL §1회 한정 — `_redo` run-dir 발 의심은 재진입 금지).
카테고리가 옳다고 확인된 것이므로 **product-name 이 그대로 상품명을 만들면 된다.**
`run_names.py holds` 가 `[재진입금지]` 를 찍는 경로를 태울 것.

## 끝난 것 (2026-08-14 4차) — 상품명 99건 (재작업 58 + 카테고리확정 41)

run-dir `2-3_r6` (본 라운드) · `2-3_r6_nokw` (무키워드). 통다운 **260811**(3일차 신선분).
**재팬아웃 0회 · 상품명 반영 83건 · 불량 0 · 오류 0.**

| 단계 | 본 라운드 | 무키워드 라운드 |
|---|---|---|
| 대상 | 95 → 그룹매칭 93 → 배치수록 90 | 24 (holds 기계선별 15 + 재진입완료 9) |
| prep 스냅샷 | **93/93 적중** (조회 0회) | 24/24 |
| 팬아웃 | 워커 17 · 배치 61 | 워커 6 · 배치 17 |
| 1차 검증 | 통과 50 · 실패 11 · 보류 27 | 통과 23 · 실패 1 · **보류 0** |
| `fix-r9 --commit` | R9 단독 **9건** 기계 재배열 → 통과 59 | R9 단독 0건 |
| 잔여 실패 수복 | 2건 (term분해 누락) — **재팬아웃 없이** | 1건 (동일) |
| append | 88행 · 관련어 243행 · J열 backfill 5 | 24행 |
| 표본검수 | 35건 중 **의심 2 (5.7%)** | **전건 24 · 의심 0** |
| `rename --commit` | **59건 반영** | **24건 반영** |

**상품명 완료 558 → 641 (77% → 88%).**

### 검증실패 14건의 정체 — 대부분 이름이 아니라 term분해 형식 오류다

이번 라운드 실패 12건 중 **9건이 R9 단독**(기계 재배열로 해결, 비용 0), **3건이 R1/R2**였다.
R1 3건은 전부 같은 모양이다 — **상품명에는 있는 어절이 `term분해`에서 빠졌다**(`정원`·`원형`·`방진`).
넣으면 term 7이 되어 R2에 걸리는 경우가 있는데, 그건 **워커가 그 부가어를 버리려 했다는 뜻**이므로
상품명 쪽에서 빼면 맞아떨어진다. 셋 다 이렇게 수복했고 `check` 가 통과시켰다.

```python
# 삽입 위치는 결합 문자열이 상품명과 일치하는 지점을 탐색해 찾는다(어절≠term이라 인덱스 추정 금지)
for i in range(len(t)+1):
    if ''.join(t[:i]+[miss]+t[i:]) == 새상품명.replace(' ',''): ok = t[:i]+[miss]+t[i:]
# 반드시 확인: ok[-1] == '기본형' (R8)
```
**재팬아웃보다 이게 먼저다** — 재팬아웃은 같은 배치의 통과한 이름까지 다시 만들어 오히려 불안정하다
(§`--ids --redo` 결함 이력 참조).

### 표본검수 의심 2건 — 공통 패턴은 "제목 앞머리 승계"

| 상품 | 기준 | 사유 |
|---|---|---|
| `U01KRZBANTKHC8M9YHJD03FEHX6` 유압식앙카 드릴거치대 | (b) | 옵션 전량이 `电动升降`/`手动升降`, 원문도 `电锤支架…升降式`. **유압(液压) 증거 0** — 원본 '유압식앙카'를 승계 |
| `U01KRZBANVFKSRXSXEWZBV17JVC` 자동차 스모그테스터기…형광오일 | (a)(e) | 판매 옵션 4개 **전부 연기오일**. 원문도 `烟雾检漏仪**专用烟雾油**`(검진기 **전용 오일**). 살 수 없는 본체를 앞세움 |

둘 다 **원본/원문 제목의 앞머리 단어를 실물 확인 없이 가져온 것**이다. 무키워드 라운드 emphasis 에
이 두 사례를 그대로 실었더니 **의심 0건**이 나왔다 — 다음 그룹에도 실을 것.

## 끝난 것 (2026-08-15) — 옵션 576건 (재작업 521 + 빈칸 55)

run-dir `2-3_r4` (본 라운드 576건) · `2-3_r5` (마무리 27건).
**반영 570건 · 재팬아웃 0회 · 옵션 완료율 20% → 98.6%.**

| 단계 | r4 (576건) | r5 (27건) |
|---|---|---|
| prep | 스냅샷 **576/576 적중** · 신규조회 0 · 이미지 5,484장 | 27/27 적중 · 328장 |
| 팬아웃 | 배치 116 · 워커 116 · 오류 0 · **재팬아웃 0** (55분) | 배치 6 · 워커 6 (7분) |
| 감사 | 잘린 productId **5건 자동복구 + 1건 수동교정** · 환각 1건 폐기 | 누락·환각 0 |
| 미리보기 | 정리대상 553 / 확인요·보류 23 | 정리대상 25 / 보류 2 |
| 표본검수 | 28건(5%) — **의심 0** | 복구 8건 집중 — **의심 0** |
| 저장 | **반영 548 · 부분저장 1 · 실패 6** (52분) | **반영 22 · 실패 3** |

**옵션 완료 146 → 715 / 725 (98.6%).** 잔여 10건은 아래 §수동 큐.

### 실패 사유를 현황판에 flag 로 실으면 워커 재판정이 확 좋아진다 ★

r4 에서 **보류 `남길 옵션 없음` 이 12건** 나왔다. 그대로 재팬아웃하면 워커가 같은 판정을
반복한다 — 배치에 실리는 건 현황판 문자열뿐이라 `보류(...)` 만으론 왜 틀렸는지가 안 간다.
`matrix.flag_many` 로 **구체 지시를 옵션 열에 직접 찍고** r5 를 돌렸더니:

| 실은 사유 | 결과 |
|---|---|
| `전량제외 판정 재검토 — 현재제외를 근거로 쓰지 마라. 가격계단·원문 접두한자(加/送/单独)·상품명 핵심어로 본품을 직접 판정할 것` | 14건 중 **13건 복구** (남은 1건은 옵션이 실제로 1개뿐) |
| `저장실패(이름중복) — 유지분만 고치지 말고 전 옵션에 원문 기준 고유 이름을 줄 것` | 5건 중 **4건 복구** |
| `25자 초과로 이름 미저장 — 본문을 줄일 것` | 3건 전건 복구 |

복구 사례가 그대로 SKILL §알려진 함정의 실증이다 — PE관은 원문이 전부 `얇은 모델 100m`
라 겹치던 걸 **구경(20/25/32…mm)을 앞에 붙여** 갈랐고, 우편함은 `빨간 우편함` 2개를
**꽃무늬 유무**로, 라운지소파는 `MA 블루` 2개를 **동봉 테이블 종류**로 갈랐다.
**다음 그룹에서도 보류·실패는 재팬아웃 전에 사유부터 flag 한다.**

### 저장실패 3건 — 스크립트로 못 미는 구조적 건 (수동)

| 상품id | 상품 | 오류 | 진단 |
|---|---|---|---|
| `U01KRZBANWB88A5Z7822JW6BT00` | 중량랙 철제선반 (옵션 378) | 대표 상태 무효 | r4·r5 **두 번 다 실패**. 제외 225개로 200 상한을 넘어 분할 경로를 타는데, `_commit` 의 대표오류 fallback(이름+대표+포함/제외 동시 전송)까지 갔는데도 거부됐다 |
| `U01KRZBANR9H8Y91N0JEDPF2V1B` | 카운터 안내테이블 (옵션 585) | 대표 상태 무효 | 동일. **둘의 공통점 = 제외 200개 초과 → `includeSkuIds` 분할 경로**. 다른 그룹에서도 재발할 후보다 |
| `U01KRZBANWVC5P5B16SRP5ZYEWD` | 이동식 사이드테이블 | 이름 겹침 1가지 2개 | 손대지 않은 기존 이름 `화이트보드 40x60` 이 T·X 두 자리에 그대로 있다. 전 옵션 이름 부여가 필요 |

앞 2건은 `보류(저장실패)` 라 `pending` 에 안 잡힌다 — 앱에서 대표를 직접 세우거나
`--ids` 로 지목해 다시 돌려야 한다.

### 옵션을 정리하면 썸네일·상품명 빚이 늘어난다 ★

r4+r5 저장이 **썸네일 재작업 124건 · 상품명 재작업 59건**을 새로 찍었다. 대표옵션이
바뀌었으니 그 기준으로 만든 썸네일·이름이 다시 검토 대상이 되는 게 맞다 — 설계된 동작이다.

| 축 | 작업 전 | 작업 후 |
|---|---|---|
| 썸네일 완료 | 150 | **67** (83건이 재작업으로 내려감) |
| 상품명 완료 | 641 | **590** (51건 내려감) |

**순서가 중요하다는 증거다.** 8/14 인계문서가 "상품명을 더 돌릴수록 옵션 빚이 커진다"고
적은 것의 대칭이다 — 반대 방향으로도 같은 일이 일어난다.
**옵션 → 썸네일 → 상품명 순으로 굳혀야 왕복이 끝난다.**

### 워커 productId 절단은 여전히 난다 (6건/576 = 1.0%)

5건은 `_repair_truncated_ids` 가 접두사 대조로 자동 복구했지만, **1건은 못 잡았다** —
`U01KRZBANVAR72SRNRG0MKRWQG1` 을 `U01KRZBANVAR72SRNRG0MKRW1` 로 적어 **가운데를 빼고
끝 글자는 남겼다.** 접두사 대조는 뒤가 잘린 것만 잡으므로 이 형태는 환각으로 분류돼
`감사` 가 `--commit` 을 막는다. 배치 정본과 내용(상품명↔메인상품)을 대조해 손으로 고쳤다.

```bash
# 감사가 "미완 배치 N개 / 누락 상품 M건"을 찍으면 pending 이 비어 있어도 재팬아웃이 아니다
python3 -c "import json; b=json.load(open('batches/batch_NNN.json')); r=json.load(open('results/result_NNN.json'))
print(set(p['productId'] for p in b['products']) - set(p['productId'] for p in r['products']))"
# 내용(상품명 ↔ 메인상품)이 일치하면 result 의 id 만 고친다. 재팬아웃 불필요(비용 0)
```

## 끝난 것 (2026-08-15 2차) — 썸네일 569건

run-dir `2-3_r4` (본 라운드) · `2-3_r4a` (정합검사 불일치 합류분).
**반영 213건 · 정합검사 394건(크레딧 0) · 크레딧 1,350 소모.**

**569건이 통째로 생성 대상이 아니었다** — prep 이 이렇게 갈랐다(옵션을 먼저 끝낸 덕이다):

| 갈래 | 건수 | 크레딧 |
|---|---|---|
| 정합검사(가공됨+대표옵션 있음) | 398 | **0** |
| 기작업 백필(가공됨+대표옵션 없음) | 42 | 0 |
| 실제 생성 | 129 | 645 |
| 원본 404 삭제 | 1(+audit 4) | — |

| 단계 | r4 | r4a |
|---|---|---|
| prescreen(기준적격·크레딧 0) | 115건 → 단일특정 98 · 다중혼재 11(승격) · 실물없음 6(옵션 반환) | 139건 → 127 · 11 · 1 |
| 생성 | 접수 120 → **회수 113 + 인라인 6 = 119** · 실패 1 | 접수 136 → **회수 136 · 실패 0** |
| verdict 3축 | 사용가능 95 / 제외 24 | 사용가능 108 / 제외 28 |
| 반영 | **95** | **108** |
| 재생성(제외분) | 15건 → **10건 통과**(67% 구제) → 반영 | — |

**썸네일 완료 67 → 608 / 725 (9% → 84%).**

### 정합검사 불일치 35% — 25-2 그룹과 독립 재현 ★

394건 대조: 일치 247 · **불일치 139(35.3%)** · 대조불가 4 · 대표옵션의심 4.
25-2 세션의 순수 cdn 군이 **65/184 = 35.3%** 로 **소수점까지 같았다.**

| 대표 호스트 | 표본 | 불일치율 |
|---|---|---|
| `cdn.bulsaja.com` | 2-3 139/394 · 25-2 65/184 | **둘 다 35.3%** |
| `fal.media` | 25-2 43/101 | **42.6%** (2-3 엔 0건) |

두 모수 다 **옵션 변경 영향을 안 받은 군**이다(2-3 은 옵션 재작업 123건이
`run_thumbs.py:292` 규칙으로 생성 경로에 빠져 audit 모수에 안 들어갔다).
**견적에 쓸 것**: 가공됨 N건 → 재생성 ≈ **0.35N × 5크레딧**, audit 자체는 0.

### 접수모드가 인라인보다 3~4배 빠르다 (큐가 건강해도)

인라인은 상품마다 서버 생성 시간을 혼자 기다려 **건당 약 80초**였다.
`THUMB_POLL_TIMEOUT=20` 으로 접수만 하고 `recover` 로 수확하니 같은 분량이 크게 줄었다.
**손실 0** — r4 회수 113/114(실패 1은 서버측 진짜 생성 실패), r4a **136/136 실패 0**.

**중단·재개는 순서가 있다**: ①짝 없는 `[기준]` → `restore` ②`recover` ③재개.
②를 건너뛰면 taskId 만 있는 접수분이 **다시 접수돼 크레딧을 두 번** 낸다(실측 1건 회피).
⚠ 짝 탐지 시 `[기준]` 로그 두 종류를 반드시 가른다 — `올림` 만 세라.
`올릴 것 없음(워커선택)` 을 같이 세면 **멀쩡한 상품이 미승인으로 잡힌다**(r4a 실측 오탐 6건).

### 이번 회차에 밟은 함정 (전부 스킬 수정 지시서로 넘김)

`00-inbox/2026-08-15-썸네일옵션-스킬-수정지시서.md` — A~H2 항목.

- **워크플로 반환값 ≠ 디스크**: `run`(r4) 이 "완료배치 7"인데 `result_002.json` 이 없었다.
  단 **"결과 0건" 을 보면 곧바로 재팬아웃하지 마라** — r4a verdict 에서 0건을 보고 17배치를
  재팬아웃했는데, 실은 **완료 알림이 디스크 쓰기보다 먼저 온 것**이었다(같은 task-id 로
  완료 알림이 두 번 왔다). 워커 17개를 헛돌렸고 **판정이 흔들려 사용가능이 110→108 로 바뀌었다**
- **`verdict --ids` 는 이전 회차 배치와 섞인다**: 재생성 15건인데 `vbatch_003~015`(이전 104건)가
  남아 누락으로 잡힌다. 잔재를 옮기고 돌려야 한다(`decisions.json` 은 앞 라운드를 유지·병합한다)
- **productId 가운데 누락**: verdict 에서 `…TV6P4N`(정본 `…TV6PB4N`). `_heal_pids` 는 접두
  매칭이라 못 잡는다 — **누락 1 + 환각 1 이 짝으로** 나타나면 이 유형이다. 옵션 팬아웃에 이어 2번째
- **24자 접두 절단은 자동 복구된다** — prescreen 에서 1건 발견해 손으로 고쳤는데 **불필요했다**
  (`_heal_pids` 가 이미 처리). 잘린 id 를 보면 인덱스 파일 존재부터 확인할 것

## ★ 그룹 완료 기준 잔여 (2026-08-15 2차 실측)

현황판 1020행 중 **295건 삭제** → 살아있는 건 **725건**.

| 축 | 완료 | 잔여 |
|---|---|---|
| 카테고리 | 717 | 변경대상 6 · 매칭불가 1 · 빈칸 1 |
| 상품명 | 590 / 725 (81%) | **재작업(옵션발) 59** · 키워드부족 26 · 빈칸 14 · 검증실패 14 · 표본의심 6 등 |
| 옵션 | 695 / 725 (96%) | 대표옵션의심(썸네일발) 16 · 삭제대상 5 · 저장실패 3 등 |
| **썸네일** | **608 / 725 (84%)** | 빈칸 51 · 보류(제외) 30 · 대표옵션의심 16 · 기준이미지없음 15 · 글자변조 3 · 원본404 2 |
| 상세·지재권·배송비·업로드 | 0 | **각 빈칸 725 = 전건 미착수** |

**다음에 잡을 것 우선순위**:
1. **상품명 재작업 59건**(옵션발) + 키워드부족 26 + 빈칸 14 + 검증실패 14 — 이제 최대 덩어리다.
   옵션·썸네일이 굳었으니 지금이 상품명을 확정할 자리다. **검증실패 14 는 먼저 `check` 로
   디스크 대조**(§디스크에 이름이 남아 있었다) — append 누락이면 비용 0
2. **썸네일↔옵션 왕복 잔여 31건** — 대표옵션의심 16 + 기준이미지없음 15. 옵션정리가 대표를
   실물로 다시 세우면 썸네일로 되돌아온다(왕복 종결 조건은 `실물기준없음` 낱말)
3. **썸네일 빈칸 51건** — 이번 회차에서 새로 생긴 분(재생성 제외분 등). 약 255크레딧
4. **옵션축 대기 237건** — MCP 에 축 이름 변경 기능이 없다. 불사자 앱에서 수동 (시트 `옵션축` 탭)
5. **상세·지재권·배송비·업로드 725건 전건 미착수** — 그룹 마감의 실제 남은 일
6. 옵션 저장실패 3건 (제외 200 초과 대표 지정 — 앱에서 수동)

### 디스크에 이름이 남아 있었다 — 다음 그룹에서도 먼저 확인할 것

`2-3` 라운드는 **재팬아웃까지 돌고 append 를 못 돌린 채 끝나 있었다**(named/ mtime 17:50 >
checked/ 17:39). 그래서 시트는 8/6 시점의 `검증실패` 를 그대로 들고 있었고, 디스크에는
고쳐진 이름 108개가 그냥 놓여 있었다. 인수인계 초판은 이걸 "재작업 155건"으로 읽어
**팬아웃 재생성(≈$60)을 계획**했지만 실제로 필요한 건 `check` → `fix-r9` → `append` 였다.

**새 라운드 시작 전 반드시 이 순서로 확인한다** (비용 0):
```bash
python .claude/skills/product-name/scripts/run_names.py check --run-dir <옛 run-dir>
# → 시트 상태 vs checked/ 상태를 대조. 시트만 실패면 append 누락이다
python .claude/skills/product-name/scripts/run_names.py fix-r9 --run-dir <R> --commit
```
`append` 를 옛 run-dir 로 그대로 돌리면 **현황판이 오염된다**(그 run-dir 의 다른 상품
`반영완료` 가 `진행중(미반영)` 으로 내려간다). 대상 id 만 남긴 **필터 사본 run-dir** 를
만들고(named/ 를 대상 id 로 걸러 복사 + `redo.json` = 대상 id + `jk_map.json` 은 라이브
시트로 갱신) 거기서 append 한다. 이번 회차의 `2-3_r5fix` 가 그 사본이다.

## 판정 대기 큐

**판정 페이지(2026-08-14 생성)**: https://claude.ai/code/artifact/546bd3e8-1943-4224-a86e-d0cdef2aef34
— 상품ID·썸네일 비교(생성 전 원본 ↔ AI 생성본) 포함. 건별 판정 후 하단에서 결과 복사.
갱신하려면 생성 스크립트가 세션 스크래치패드에 있으므로 **다시 만들어야 한다**(원본 소실됨).
데이터 출처는 시트 `상품명` 탭 Z열 = `보류(표본의심)` · `썸네일` 탭 J열 = `보류(...)`.

- **표본의심** — 8/9분 9건(삭제 1건 제외) + 8/14분 2건 + 8/14 2차분 1건
  (`U01KRZBANR80DF0K6SWVWZXAB2C` 옷걸이 수납카트 → '행거' 오지칭. 옷걸이수납함 계열로 재작명)
- **썸네일 보류 12건** — 인수인계 초판의 "기준이미지없음 10 · 대표옵션의심 6"은 틀림.
  **시트 실제값 = 기준이미지없음 4 · 대표옵션의심 3 · 제외 6** (삭제분 1 제외)
- **삭제복구 "확인 필요" 3건** — 2026-07-31/08-04/08-08자 묵은 건. 앱에서 확인

### 8/14 새로 생긴 판정 건 2개 (페이지에는 없음 — 페이지 생성 후 발생)

| 상품id | 문제 |
|---|---|
| `U01KRZBANTJNPGMPXDZWBVD9Q5F` | 이름에 '교반기'가 있으나 **판매 옵션은 통 단독 4종뿐** (모터 조합 전량 제외). 썸네일은 교반기 장착본 → 살 수 없는 구성을 광고. **옵션 쪽 제외 판정 재검토가 먼저** |
| `U01KRZBANTQQYAQWFP5Y1NW2C9D` | 이름은 '스텐청소도구함'인데 최저가 대표옵션은 **냉연강 도장 캐비닛**. `기본형` 마커가 옵션 3개에 중복. **201/304 옵션을 대표로 세우는 옵션 작업이 먼저** |

둘 다 이름만 다시 지어선 안 풀린다 — 옵션 축을 먼저 손봐야 한다.

### 마켓그룹 이탈 1건 — 앱 확인 필요

`U01KRZBANVHHT7034K5JKDB5R10` — 불사자에 살아있는데 **마켓그룹 1001994 목록에 없다**
(그룹 배정이 풀렸거나 다른 그룹으로 이동). 게다가 원장이 서로 다른 물건을 가리킨다:
- 상품명 = `병뚜껑 밀봉기 캡핑기 뚜껑 페트병 참기름병 업소용`
- 8/9 카테고리 교정 실물판정 = `220V 전동 스크류드라이버(산업용)` → `공구>전동공구>충전드릴` (확신도 100%)

둘 중 하나가 오기재다. 앱에서 실물 확인 필요.

## 알아둘 것 (다음 회차에 도움)

- **셀러라이프 통다운 260803은 8일 경과** → 신선도 7일 넘김. product-name prep에서
  `--source-date` 생략하면 자동으로 새로 받음(크롬 창 뜸)
- **워커가 productId를 자르는 사고가 5회** 발생(썸네일 3·옵션 1·카테고리 0). 매번 스크립트
  안전장치가 잡았지만, 팬아웃 `emphasis`에 "productId는 배치 JSON에서 한 글자도 빠짐없이
  복사" 문구를 넣으면 줄어듦. **워커 지시서에 상설 반영 검토할 것**
- **상품명 표본검수 의심률 23% → 5.7%로 개선**됨. 2회차 emphasis에 넣은 문구가 효과:
  *"원본 한국어 상품명은 정답지가 아니라 용의자다 — 이름 짓기 전에 중국어 원문명·옵션
  원문으로 실물부터 확정할 것"*. 실측 오지칭 사례: 낚시그물→식물덩굴망, 위스키잔→디캔터,
  오수통→청수통, 등받이→팔걸이. **워커 지시서에 상설 반영 권장**
- **정합검사 불일치율 43%→29%** — 기존 "가공됨" 썸네일이 대표옵션과 어긋나 있던 게 구조적
  문제였음. 다른 그룹도 같을 가능성 높음
- **불사자 삭제 큐는 동시 처리 한도가 있음** — 20건씩 넣으면 12~18건만 접수됨. 큐가 비면
  20/20 성공. 미업로드 상품은 즉시 휴지통행이라 빠름
- **삭제가 PARTIAL 로 끝나면 소싱 휴지통 이동이 보류된다** — 마켓 삭제만 큐에 들어가고
  소싱은 안 간다. 큐가 빈 뒤 `deleteAction: SOURCE_ONLY` 로 한 번 더 밀면 된다
  (이미 간 건 `이미 휴지통에 있는 소싱 상품을 확인함` 으로 멱등)
- **셀러라이프 통다운 260811 이 신선분** (8/14 기준 3일). `python -m eroomlib.gdrive runs` 로
  확인 — `PYTHONPATH=.claude/lib` 를 붙여야 모듈이 잡힌다
- **네이버 조회 캡챠는 하루 지나면 풀린다** — 8/9에 1~2건마다 뜨던 게 8/12엔 18건 연속 무발생,
  **8/14엔 144건(조회 107 + consensus 32 + 애드혹 5) 연속 무발생**. 8/9이 예외적으로 나빴던 것
- **재교정 run-dir 의 prep 은 스냅샷 적중률이 100%다** — 이미 상품명·카테고리 작업을 거친
  상품이라 `workdata`·썸네일이 전부 캐시에 있다. **prep 비용 0** (107/107 적중, 신규조회 0)
- **제외카테고리 게이트는 재교정 저장 직후가 진짜 일하는 자리다** — 카테고리가 바뀌면서
  비로소 블랙리스트에 걸리는 건이 나온다(실측: 수제비누 절단기 → `뷰티소품`,
  유리 진열케이스 → `주얼리`). 저장할 때마다 게이트가 돌므로 **저장 후 `autodelete` 를 다시
  돌려야 큐가 빈다**(이번엔 3라운드에 걸쳐 15건)
- **근접저장(3단계)의 안전한 패턴 = 세분화 차이** — 조회 `캠핑>파라솔`(3단) vs 불사자
  `캠핑>캠핑가구>파라솔`(4단)처럼 **leaf 가 같고 상위가 포함관계**면 자동 저장해도 된다.
  반대로 `청소기>청소기부속품` 처럼 **근접 후보가 곧 현재 카테고리**면 저장할 이득이 없다 → 수동 큐
- **consensus `실물판정의심` 9건 중 3건은 "최고 확신 변형 = 기존 카테고리"였다** — 즉 이미
  정확한데 합의 실패로 분류된 것. 의심 큐를 볼 때 **기존카테고리와 대조부터** 하면 3분의 1이 즉시 걸러진다

### 2026-08-14 고친 결함 2개 (테스트 추가함)

- **`run_names.py` — `--ids --redo` 가 재작업사유를 죽였다.** prep 이 `done_ids - want` 를
  `_matrix_redo` 보다 먼저 해서, 손으로 지목한 건이 사유 조회 후보에서 통째로 빠졌다.
  자동 편입과 수동 지목이 배타적으로 동작한 셈. **증상: 재작업 라운드가 첫 라운드와 똑같은
  이름을 다시 만든다**(2-3 실측 — 옵션이 "대표옵션은 냉연강인데 상품명이 '스텐'"이라 남긴
  사유가 안 실려 그대로 재발). `_redo_candidates()` 헬퍼로 분리 + `RedoCandidatesTest`
- **`category_gate.py` — 성공을 실패로 셌다.** 마커가 `이미 휴지통` 하나뿐이라 형제 문구
  `업로드된 마켓이 없어 소싱 상품을 휴지통으로 이동함` 이 안 걸렸다. **증상: 실제로는 지워졌는데
  `삭제대기` 로 남아 잔여 건수가 계속 부풀고 라운드를 헛돈다.** `_is_gone()` 으로 분리
  (부정형 `이동 실패` 를 먼저 거른다) + `[_is_gone]` 8케이스
- 회귀 테스트: `test_run_names.py` 130 tests OK · `test_category_gate.py` 전부 통과.
  **두 스크립트를 손대면 이걸 먼저 돌린다.** git 커밋은 안 했다(다른 PC발 미커밋 변경과 섞여 있음)

## run-dir 위치

```
상품명   /Users/choiyongsmacbook/python_work/data/product-name/runs/2-3 (8/6 1회차 — named 원본),
         2-3_r2b, 2-3_r3, 2-3_r4 (8/14 재진입 12건), 2-3_r4_nokw (8/14 무키워드 1건),
         2-3_r5fix (8/14 2차 — 검증실패 146건 필터 사본. checked.bak_/named.bak_20260814 는 2-3 안),
         2-3_r6 (8/14 4차 — 재작업+카테고리확정 95건), 2-3_r6_nokw (8/14 4차 무키워드 24건)
옵션     /Users/choiyongsmacbook/python_work/data/option-cleanup/runs/2-3_r2b_100, 2-3_r3,
         2-3_r4 (8/15 본 라운드 576건), 2-3_r5 (8/15 마무리 27건)
         ※ 되돌리기: run_options.py restore --run-dir <R> [--ids ...] (before_commit.json)
썸네일   /Users/choiyongsmacbook/python_work/data/thumbnail/runs/2-3_r2b, 2-3_r2ba, 2-3_r3, 2-3_r3a,
         2-3_r4 (8/15 본 라운드 129건 + 재생성 15), 2-3_r4a (8/15 정합검사 불일치 139건)
         ※ 검수 페이지: 2-3_r4/review.html · 2-3_r4a/review.html (사후 검토용)
카테고리 /Users/choiyongsmacbook/python_work/data/category-fix/runs/2-3_r2b_redo1, 2-3_r3_redo1,
         2-3_r5_redo1 (8/14 3차 — 카테고리의심 107건 재교정)
```

---
**Last updated**: 2026-08-15 2차 (썸네일 569건 — 반영 213 · 정합검사 394 · 크레딧 1,350 · 완료율 9→84%)
