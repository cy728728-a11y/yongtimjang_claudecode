# 2-2 상품명 `검증실패` 107건 종결 (2026-08-17)

그룹 `2번_용쌤2-2` (groupId **1001186**) · 시트 **1wMKUsOxy3wSzejGXLk0G6o22DLf8zcBDg3_6sbVXBZo**
계정 **부킹(용쌤)**. 통다운 **260811**(6일, 신선).

## 결과

**`검증실패` 큐를 비웠다. 107 → 1.** 현황판 상품명 `완료` 458 → **554**(+96).

| 단계 | 수치 |
|---|---|
| 대상 | 107 → **106** (1건은 마켓그룹에서 사라짐) |
| 워커 생성 | 104 (보류 2 = 키워드부족) |
| 검증 | `fix-r9` 5건 기계교정 후 **전건 통과**(실패 0) |
| 시트 기록 | 106행 — 대상 수 일치, 누락 0 |
| 표본검수 | 35건 중 **의심 8건** |
| **불사자 반영** | **96건 · 불량 0 · 오류 0** |
| `_kw1` 라운드 | 대상 2 → 생성 1(그마저 의심) · 2회째 부족 1 |

run-dir:
```
~/python_work/data/product-name/runs/2-2_0817_fail107       본 라운드
~/python_work/data/product-name/runs/2-2_0817_fail107_kw1   키워드부족 2건
```

## 1. 지난 회차가 왜 107건 전멸이었나 — 규칙 문제가 아니라 타이밍 문제였다

인계문서(08-15)는 `검증실패` 107건을 "제일 유력한 덩어리"로 지목하면서도 **사유는 몰랐다.**
107건이 **전부 2026-08-05 회차 산출물**인데 그 run-dir이 지워졌고, **실패 사유는 시트에
안 남는다**(`checked/`에만 있다). 그래서 시트 `상품명`·`관련어` 탭으로 `name_check.check_one`
입력을 재구성해 다시 돌렸다.

| 위반 | 건수 | 성격 |
|---|---|---|
| **R4** 키워드 1개 | **54** | **전건이 `키워드확장` 증빙 없음** |
| **R9** 배치 어긋남 | **50** | 결정론적 |
| R1·R2·R3·R5·R6·R7 | 18 | 단순 위반 |

R4 54건이 핵심이다. **2026-08-05는 R4 예외(1개여도 `키워드확장` 증빙 남기면 통과)가
도입된 바로 그날이다.** 워커가 그 필드의 존재를 모른 채 돌아 54건이 기계적으로 죽었다.
규칙이 잘못된 게 아니라 규칙과 워커가 어긋난 하루였던 것 — 그래서 **재작업만으로 풀렸다.**

> **일반화**: `검증실패`가 무더기로 나오면 **그날 규칙이 바뀌었는지부터 본다.**
> 그리고 사유가 run-dir 에만 사는 구조 때문에 run-dir 을 지우면 진단이 불가능해진다
> (→ 아래 §개선 제안 1).

## 2. ★ 표본검수 35건 중 8건(23%) — 체계적 결함이다

의심 8건이 **전부 같은 뿌리**다. 두 갈래.

### (A) 판매제외 옵션을 근거로 사양을 지목한다 — 4건

| 상품id | 상품명 | 실제 |
|---|---|---|
| `U01KS1WY755CE042A6TXBK0SAQ4` | 디지털버니어캘리퍼스**300mm** | 30cm 옵션은 판매제외. 대표는 15cm |
| `U01KS1WY74CYHX1HJ75WWXZXP4F` | 수입 엔진체인톱 **대마력** | 大马力=4행정 라인 29개 **전량 제외**, 판매중은 2행정 |
| `U01KS1WY74ZME854KCDJ5KWM9PT` | **다단** 업소용스텐작업대 | 대표옵션은 `单层`(1단) |
| `U01KS1WY765J62CNH57QBZEG2NW` | 레스토랑 와인정리대 우드와인랙 | 본품(1600mm 진열대) 전량 제외. 판매중·썸네일은 **나무 상자**(450×385×150) |

### (B) 원본 상품명의 과장을 검증 없이 승계한다 — 4건

| 상품id | 상품명 | 증거 |
|---|---|---|
| `U01KS1WY751965M0JGE055SGS2S` | 베어링 **유압**기어풀러 | 원문·옵션 전부 스크류식. **메모엔 "유압은 원문 근거 없어 상품명에서 제외"라 써놓고 상품명엔 들어갔다** |
| `U01KS1WY76HD2WXS5A3ZM8AHKZ2` | **충전식** 유압집게 | 원문 `手动`(수동), 썸네일도 수동 레버식 |
| `U01KS1WY76K0NAG0YE8PXDQGH21` | 철제 거실**코너**선반 | 썸네일은 일자형 벽면 책장. 원문에 코너 없음 |
| `U01KS1WY75Y0RHCEWD9GMVDSNFW` | **분리형**개모차 | 대표옵션은 `不可分离`(분리 불가) |

`_kw1` 라운드의 1건(`U01KS1WY74A49RVTDHDB64PMTX9`)도 같은 부류다 —
실물은 **치즈 웨지 모형**인데 키워드 `빵모형`을 채택했다. 총 **의심 9건.**

### 근본 원인 — 규칙끼리 충돌한다

기어풀러 건이 이 결함의 표본이다. **워커는 '유압'이 틀린 걸 알고 있었다.** 그런데
채택 키워드가 `유압기어풀러`였고, **R5가 "키워드는 원문 그대로 연속 문자열로 넣어라"고
강제**한다. 적합성(상한)과 R5(형식)가 충돌할 때 **형식이 이겼다.**

`빵모형` 건도 똑같다. 뷰에 `치즈` 직결어가 0이라 대형키워드 `빵모형`을 골랐고, R5 때문에
그대로 박혔다.

> **막는 방법은 키워드 채택 단계에 있다.** 상품명을 지은 뒤 고칠 수 없다(R5가 막는다).
> **실물과 어긋나는 속성어가 박힌 키워드는 애초에 고르면 안 된다** — 이 문장이 워커
> 지시서 §3-2(키워드 선택)에 없다.

### (B)는 이번 emphasis 의 부작용일 수 있다

R4 54건을 살리려고 emphasis 에 **"1개뿐이면 원본 상품명·원문·옵션명 단어로 자리를 채워라"**
를 강하게 실었다. **원본 단어를 쓰라고만 했지 그 진위를 검증하라고는 안 했다.**
`_kw1` 라운드에는 금지 문안을 넣었는데도 `빵모형`이 나왔다 — **emphasis 만으로는 안 막힌다.
지시서 본문을 고쳐야 한다.**

## 3. 개선 제안 (아직 손대지 않음 — 판단 필요)

1. **검증실패 사유를 시트에 남긴다.** 지금은 `checked/`에만 있어 run-dir 을 지우면
   진단이 불가능하다. `_build_row` 에서 `검증` 위반 목록을 메모 앞에 `[검증실패] R4 …`
   로 박으면 된다(`[키워드1개]`·`[무키워드]` 태그와 같은 방식). 비용 0.
2. **워커 지시서 §키워드 선택에 적합성 게이트를 넣는다.**
   "키워드에 실물과 어긋나는 속성어(소재·구동방식·형태·규격)가 박혀 있으면 상품수가
   낮아도 채택하지 않는다. R5가 상품명에서 그 단어를 뺄 수 없게 만들기 때문이다."
3. **기계 규칙 R13 후보 — 판매제외 옵션 근거 금지.** (A) 4건은 전부
   `상품명의 규격어가 판매중 최저가 옵션에 없다`로 잡힌다. `spec_match.py` 가 이미
   규격어를 뽑고 옵션 매칭을 한다 → **경고 수준**으로 넣을 수 있다(R12 전례).
   R10 폐기 전례가 있으니 **오탐률을 먼저 재고 결정한다.**

## 4. 2-2 에 남은 일

| 건수 | 내용 | 상태 |
|---|---|---|
| 109 | `보류(카테고리의심)` | **통다운 갱신 전엔 안 풀린다**(재교정 1회 소진). 260811 로도 안 됨 — 다음 통다운 |
| 26 | `보류(키워드부족)` | 같은 이유. `_kw1` 1회 한정 소진분 25 + 2회째 부족 1 |
| 14 | (빈칸) | `prep` 이 집는 정상 대상. 다음 회차에 그냥 돌리면 된다 |
| 14 | `재작업(옵션 이관)` | 옵션정리가 찍은 flag. prep 이 자동 편입 |
| **10** | `진행중(미반영)` | **= 표본의심 9 + 기존 1.** `mark` 는 현황판을 안 내리므로 사유가 현황판에 안 보인다 → 상품명 탭 Z열이 `보류(표본의심)` |
| 1 | `검증실패` | `U01KS1WY77P881VPTBFZN2XTFGH` — 마켓그룹에서 사라짐(삭제 추정) |
| 1 | `보류(실물불명)` | 이미지 사다리 대상 |
| 8 | `상품삭제(품목불일치)` | 종결 |
| 276 | 옵션축 | MCP에 변경 API 없음 → 앱에서 수동 |

**썸네일 쪽**(08-15 인계문서)은 그대로 유효하다 — 옵션 열 반려 7 · 확인요 5 ·
`보류(남길 옵션 없음)` 2 · 저장 실패 1 · 삭제 잠금 1.

## 5. 이룸님 판정 대기

**`보류(표본의심)` 9건.** 상품명 탭에서 상태로 찾을 수 있다. 원본은 E열에 남아 있다.
- 반영하려면 상태를 `생성완료`로 되돌리고 `rename --commit`
- 폐기하려면 그대로 두고 다음 통다운 때 `--ids … --redo`

되돌리기: 원본 상품명 = 상품명 탭 E열.
