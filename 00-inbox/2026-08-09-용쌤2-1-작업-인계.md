# 2026-08-09 · 2번_용쌤2-1 작업 인계 (컨텍스트 clear 대비)

groupId **1001115** · 그룹시트 `1T3qkLlE4vkX_1KyaSAic-QgaA3bG6JNPUxUJh6kfWF4`

## 끝난 것

| 작업 | 결과 |
|---|---|
| 상품명 | 불사자 반영 **46건** (38 + 표본의심 원복분 8) |
| 옵션정리 | 불사자 반영 **713건** · 부분저장 2 · 저장실패 2 |
| 삭제 | 삭제대상 **27건 전부 접수** (마켓 대기열 15 + 휴지통 이동 12) |

run-dir
- 상품명: `~/python_work/data/product-name/runs/용쌤2-1_260809` (+ `_kw1`)
- 옵션: `~/python_work/data/option-cleanup/runs/용쌤2-1_260809` (+ `_fix`)
- 카테고리 재교정: `~/python_work/data/category-fix/runs/2-1_redo1`

## 남은 것 (우선순위 순)

### 1. 카테고리 재교정 23건 — 캡챠로 중단, Aside 대기 중
용팀장 지시로 **대기**. Aside 살아나면 이어서:
```bash
.venv/bin/python .claude/skills/bulsaja-category-fix/scripts/run_all.py \
  --sheet 1T3qkLlE4vkX_1KyaSAic-QgaA3bG6JNPUxUJh6kfWF4 auto \
  --run-dir ~/python_work/data/category-fix/runs/2-1_redo1 --overwrite-done
```
- 판정(Step 3a)은 23건 전건 완료돼 있음. 남은 건 네이버 조회+저장뿐.
- 캡챠 릴레이(`captcha-relay`)는 **못 씀** — `~/.aside/u/0/captcha-relay/responses/` 가 비어 있고
  2026-08-09 보낸 ping 3건(01:45·03:29·17:42) 전부 무응답 = Aside 감시 루프 죽어 있음.
- 재개할 땐 `--sellha-rest-every 5` 류로 휴식을 같이 준다(캡챠는 간격이 아니라 누적 총량에 반응).

### 2. 상품명 재작업 57건 — 다음 상품명 런이 자동으로 집는다
현황판 `상품명` 열에 `재작업(옵션: …)` 로 찍혀 있음. `prep` 이 `_matrix_redo` 로 자동 편입하므로
`--ids --redo` 를 손으로 칠 필요 없다. 그냥 다음 런을 돌리면 됨.

그중 **13건은 내(Claude)가 검수표 기계대조로 찾아 추가한 품목 불일치**다:
평판프레스 인쇄기→퀵클램프 지그 · 미용실의자→벽걸이 거울 · 하이볼기계→와인 디스펜서 랙 ·
구리스주입기→전동 흡유펌프 · 회전간판→LED 메뉴 라이트박스 · 잔반처리대→개수대 캐비닛 ·
갯벌삽→낙양삽 · 스카이어닝→파라솔 · 전기톱→헤지트리머 · 실내자전거→사이클 리프트 테이블 ·
와인 하드케이스→발광 진열 스탠드 · 직선 벽램프→곡선형 벽등 · 품목어 없는 상품명→LED 펜던트 조명

### 3. 옵션 저장 실패 2건 — 불사자가 계속 거부
`U01KREA81YHN1V3SD217VVMTSHV` · `U01KREA821ZC89N4MXM18WKTWYQ`
사유: "대표 옵션을 판매 제외하거나 대표 상태가 올바르지 않습니다."
재팬아웃 1회 했는데도 같은 거부 → **불사자 앱에서 직접 확인 필요.** 현황판 `보류(저장실패)`.

### 4. 옵션축 수동 반영 대기 263건
그룹시트 `옵션축` 탭. MCP에 축 이름 변경 API가 없어 **불사자 앱에서 수동**으로만 가능.

### 5. 썸네일 재작업 이관 139건
현황판 `썸네일` 열. `bulsaja-thumbnail` 돌릴 때 자동으로 잡힌다.

### 6. 통다운밖 11건
260803 통다운에 해당 카테고리 키워드가 없어 배치에 안 실린 건. 무키워드 경로(`--nokw-mode`) 대상.

## 이번 런에서 발견한 스킬 결함 → **전부 수정 완료 (2026-08-10)**

1. ~~**`대표지정` 열이 생성 47건 전부 비어 있다.**~~ **오진이었다.** 시트를 직접 읽어보니
   2026-08-09 분 76행 중 **5행이 채워져 있었다**(빈 값은 시트가 뒤 열을 잘라 저장해서
   행 길이가 29로 보인 것). 다만 대조하다 `spec_match` 실제 결함 2개가 나와 고쳤다:
   - **뒤 경계가 숫자까지 막았다.** 비교 전에 공백을 지우므로 `500kg 7.6m` 이 `500kg7.6m`
     이 되는데, 뒤에 붙은 `7` 을 다른 단위로 오인해 매칭이 통째로 죽었다 → 전동윈치500kg 이
     (가)가 아니라 (나)로 떨어졌다. 이제 **글자만** 막는다.
   - **`m`(미터)이 단위 목록에 없었다** → `인조잔디2m`·`25m` 이 규격어로 안 잡혔다.
     `米` 한자 대응도 같이 넣었다.
   → 같은 76건을 다시 돌리니 지정이 **5건 → 7건**. 회귀 테스트 3개 추가.
2. ~~**옵션 워커가 productId를 잘라서 반환**(6건)~~ →
   - 프롬프트: "27자를 그대로 복사" 경고 + **예시의 `U01…` 줄임표기를 실제 27자 id로 교체**
     (예시가 절단을 가르치고 있었다).
   - 스크립트: `_repair_pid()` — 잘린 id를 배치 정본의 **접두사로 유일하게 걸릴 때만** 복원.
     감사가 `잘린 상품id N건(접두사로 복원 …)` 으로 따로 찍는다(환각과 구분).
3. ~~**품목 불일치를 워커가 27건만 잡았다**~~ → **품목대조를 스크립트로 상시화.**
   `apply` 가 상품명 ↔ 워커 `메인상품` 을 전수 대조해 한 낱말도 안 겹치면 `상품명` 이관을
   자동으로 붙이고 검수표·요약에 찍는다. **저장은 막지 않는다**(개집↔도그하우스 같은
   동의어 위양성이 섞인다). 이번 런 데이터 756건에 25건 발화, 인계에 적힌 13건 중 10건 포함.
   워커 프롬프트에도 놓친 8건을 표로 넣어 감도 보정.
4. ~~**`optclean-fanout` 인라인 args가 152배치에 25KB**~~ → 원본에 `compact` 지원을 합치고
   사본(`optclean-fanout-compact.js`) 삭제. `pending` 이 기본으로 압축형을 찍는다.
   실측 **22,492바이트 → 2,124바이트**.

검증: `test_option_prep`+`test_option_rules` 132건 · `test_spec_match`+`test_run_names` 155건 통과.

남은 것: 2번 spec_match 수정으로 새로 지정되는 2건(전동윈치500kg `U01K5AAADHT4WV00SY6T4JVVJYV`,
인조잔디25m `U01KREA820ZQVZ8V3RKF5WGY77P`)은 이미 옵션정리가 끝난 뒤라 시트에 소급 반영은
안 했다. 상품명 재작업 57건 런에서 자연히 다시 채워진다.

## 주의

- **불사자 저장 ≠ 마켓 반영.** 이미 스마트스토어에 올라간 상품은 **수정 업로드**를 따로 해야
  판매 화면이 바뀐다.
- 표본의심 8건 중 `U01KREA81YJMEGXGF9HWYZ6R13P`(애완동물 산소 측정기)는 상품명 원복 반영과
  삭제대상 27건에 **둘 다 들어 있었다** → 이름은 바뀌었지만 상품은 휴지통행. 무해하지만 알아둘 것.
