# 2-2 썸네일 완료 보고 (2026-08-17)

그룹 `2번_용쌤2-2` (groupId **1001186**) · 시트 **1wMKUsOxy3wSzejGXLk0G6o22DLf8zcBDg3_6sbVXBZo**
계정 **부킹(용쌤)**.

## 결과

**썸네일 큐를 비웠다.** 인계문서(08-15)에 남아 있던 ①6건 · ②158건을 전부 종결했다.

| 항목 | 건수 |
|---|---|
| 생성본 반영(`완료`) | **136** |
| 원본대체(`완료(대표옵션 원본)`) | **17** |
| 옵션 열로 반려 | 7 (대표옵션의심 2 · 기준이미지없음 5) |
| 기존 대표 유지(글자변조) | 2 |
| 주의 — 용팀장 판정 대기 | 1 |
| 서버 하드 실패 | 1 |

**사용 크레딧 875** (① 30 · ② 765 + 재생성 80).

### ① `2-2_0814` 재생성 6건

- **반영 2** — `U01KS1WY75H4Q3NH0VM8J0WDTPH`(피아노 방음판: 대표옵션 `하얀색 기본형`에 맞게 검정→흰색 교정됨) · `U01KS1WY74A78J0Q5V83PQ86ZR4`(라벨 열프레스기)
- **원본대체 4** — 재생성 2회 상한. 넷 다 **재작업 사유가 그대로 재현**됐다:
  - `U01KS1WY76NHEMFAP0RFSE2KM7J` 캠핑테이블 — 대표옵션 `라이트폴 미포함`인데 조명봉+랜턴 장착, 인물·주전자·컵 추가
  - `U01KS1WY76DT0PRN2S2CZV5Z0XN` 카페테이블 — 대표옵션 `1.2m 박스형원탁 단독`인데 직사각+의자 4개
  - `U01KS1WY75JMS552A9HRKJCCG77` 씽크대 — 대표옵션 `수전없음`인데 수전 장착
  - `U01KS1WY73J3D8V4B22GTTYJVE5` 베이킹세트 — 대표옵션 `가전미포함`인데 전동 핸드믹서·전자저울 포함

### ② `2-2_0814a` 불일치 세트 153건

1차 생성 153 → 회수 152(하드실패 1) → 판정 152: 사용가능 **132 반영** · 제외 16 · 대표옵션의심 2 · 글자변조 1 · 주의 1
재생성 16 → 회수 16 → 재판정: 사용가능 **2 반영** · 제외 13 **원본대체** · 글자변조 1

- 서버 하드 실패: `U01KS1WY744NQEEXAD0ZSTBJ458`
- 주의(용팀장 판정 대기): `U01KS1WY766HR2KM60VFP3PQY1G` — **반영 안 됨**. `반영`이면 decisions.json을 `사용가능`으로 고쳐 재커밋
- 글자변조 종결: `U01KS1WY76MW354Q83ETRVN9MV7` · `U01KS1WY73REVHMX0RFEH00GF6F`(RIDGID 각인 소실)
- 옵션 열 반려: 대표옵션의심 `U01KS1WY75P2Z65A15NE7D8THFN` · `U01KS1WY77E1Z3HR12THG4CSN2C`
- 기준이미지없음 5(생성 대상 밖): `U01KS1WY752VXPS6Y5TJ0X3FVKF` `U01KS1WY75J2VPHAVK02GFKYT87` `U01KS1WY75R3V1KNV07NK7S49C4` `U01KS1WY775WY7JSCYKX9JT8ZQK` `U01KS1WY78FNNWVEGBHV0HN5SJS`

## 이번에 밝혀낸 것

### 1. 인라인/접수모드 기준은 건수가 아니라 큐 상태다

스킬은 `50건 이하는 인라인`이라고 하는데, 그 근거인 "인라인 80초/건"은 **큐가 정상일 때** 값이다.
①의 6건을 인라인으로 돌렸더니 **건당 300초를 꽉 채워 32분**이 걸렸고 전건 타임아웃이었다.
②의 재생성 16건은 접수모드로 돌려 **5분**에 끝났다.

→ **큐가 밀렸으면 소량도 접수모드**(`THUMB_POLL_TIMEOUT=20`)로 간다. 판단은
"연속 3건 타임아웃"을 기다릴 게 아니라 **직전 회차가 타임아웃으로 끝났는지**로 미리 정할 수 있다.

### 2. `apply --commit` 은 run-dir 전량을 재커밋한다 — 소수 반영엔 별도 run-dir

`_commit` 이 `generated.json` 전체를 순회한다(스킬 함정표에 있는 그대로).
①은 이미 157건이 반영돼 있어서 그대로 돌렸으면 **전부 재저장 → 429 → `보류(저장실패)`** 로
멀쩡한 상태를 되돌릴 뻔했다. 해당 건만 담은 run-dir을 만들어 반영했다:

```
~/python_work/data/thumbnail/runs/2-2_0814_ok2     (사용가능 2건)
~/python_work/data/thumbnail/runs/2-2_0814a_r2     (사용가능 2 + 글자변조 1)
```

만드는 법 = 원본에서 `results`·`batches` 해당 행 + `generated.json`·`decisions.json` 해당 키만 복사.
`batches` 가 비어도 `--commit` 은 막히지 않는다(누락 차단은 `--generate` 전용).

### 3. 폴링 타임아웃은 고장이 아니라 접수모드의 정상 동작이다

25-2가 993건을 "성공 0 / 실패 993"으로 찍은 걸 처음엔 환경 문제로 봤는데,
`THUMB_POLL_TIMEOUT=20` 접수모드의 **의도된 결과**였다. `recover` 가 991건을 0크레딧으로 걷었다.
2-2도 같았다(①6건·②153건·재생성16건 전부 타임아웃 → 전량 회수).

→ **로그의 `###GENERATE### 성공 N / 실패 M` 은 이번 배치가 아니라 `generated.json` 누적 집계다.**
이번 회차 성적으로 읽으면 안 된다. 실제 성적은 `[생성]`·`[실패]` 줄 수를 세라.

### 4. 큐는 여러 세션이 공유한다 — 회수 0이 이어지면 남의 회차를 의심해라

새벽에 회수가 3회 연속 0이었다. 크레딧이 5,125 빠졌는데 내 몫은 795뿐이라
25-2가 새 회차(`25-2_0815f`, 866건)를 얹은 걸 알아냈다. 손해는 없다(taskId가 남아 있어
`recover` 가 결국 걷는다) — **재생성만 안 하면 이중 지불도 없다.**

## ★ 상품명 263건은 "또 남은" 게 아니라 큐에서 나간 적이 없다 (2026-08-17 규명)

회차마다 "상품명 돌렸다"고 하는데 숫자가 안 줄어서 현황판(`00_진행`)을 직접 셌다.

| 상품명 상태 | 건수 | `prep` 이 집나 |
|---|---|---|
| 완료 | 458 | — |
| 상품삭제(제외카테고리·자동) | 256 | — |
| `보류(카테고리의심)` | 109 | **✗** |
| `검증실패` | 107 | **✗** |
| `보류(키워드부족)` | 25 | **✗** |
| (빈칸) | 14 | ○ |
| `재작업(옵션 이관)` | 8 | ○ |
| 상품삭제(품목불일치) | 8 | — |

**원인은 대상선정 규칙이다.** `matrix.pending`(`.claude/lib/eroomlib/matrix.py:132`)은
**빈칸과 `재작업(...)` 만** 집는다 — 주석 그대로 *"보류·진행중처럼 그 밖의 값이 있는 건
사람이 이미 판단한 것이라 자동 재처리하지 않는다."*

→ `prep` 을 몇 번을 돌려도 **빈칸 14 + 재작업 8 = 22건만** 대상이 된다.
나머지 **241건은 구조적으로 자동 재진입이 안 된다.** 지난 회차들이 이 22건 부류만
처리하고 "상품명 완료"로 보고해 온 것이다.

### 갈래별 성격이 다르다 — 같이 묶어 보면 안 된다

| 갈래 | 건수 | 진단 |
|---|---|---|
| `검증실패` | 107 | **여기가 제일 유력하다.** R1~R7 위반이라 SKILL.md:142 는 `Step 3 재작업` 대상이라고 정의하는데, 상태값이 `재작업(...)` 형식이 아니라 `pending` 에 안 잡힌다. **규칙 문제가 아니라 상태값↔대상선정 불일치**다. `prep --ids … --redo` 로 직접 태우면 상당수 풀릴 가능성 |
| `보류(카테고리의심)` | 109 | 자동 재교정 **1회 한정**(SKILL.md:375, 루프가드로 2회째 금지)을 이미 쓰고 떨어진 건. 게다가 SKILL.md:417 이 *"재교정을 또 돌려도 안 나온다 — 통다운에 그 카테고리 키워드가 없는 것"* 이라고 못박아 뒀다 → **셀러라이프 통다운 갱신 전엔 안 풀린다** |
| `보류(키워드부족)` | 25 | `_kw1` **1회 한정**(SKILL.md:393) 소진분. 같은 이유로 통다운 대기 |

### 다음 회차 권고 순서

1. **`검증실패` 107건부터.** 사유별로 묶어 유형을 보고, 고칠 수 있는 위반이면
   `prep --ids <…> --redo` 로 재진입. 규칙 변경 없이 손댈 수 있는 유일한 덩어리다
2. `카테고리의심`·`키워드부족` 134건은 **통다운 갱신 후**에 다시 본다
3. 1회 한정 규칙 자체를 풀지는 **아직 결정하지 말 것** — 1을 해보면 실제 회수 가능량이
   나오고, 그 뒤에 판단하는 게 싸다

> **일반화**: `보류(...)`·`검증실패` 처럼 `재작업(...)` 이 아닌 상태로 세운 건은
> **스스로 큐로 못 돌아온다.** 썸네일 쪽 `보류는 미아가 된다`(옵션 `[미아회수]` 가 되찍어야
> 복귀)와 **같은 구조**다. 상품명에는 그 회수 장치가 없다.

## 2-2 에 남은 일 (썸네일 밖)

| 건수 | 내용 | 왜 막혔나 |
|---|---|---|
| 263 | 상품명 | **위 절 참조** — 241건이 `pending` 대상 밖 |
| 276 | 옵션축 | MCP에 변경 API 없음 → 앱에서 수동 |
| 7 | 이번에 옵션 열로 반려된 썸네일 건 | 옵션정리가 대표를 복구하고 `이관(썸네일)` 되찍으면 재진입 |
| 5 | 옵션 `확인요` | 의도적 보존(인조나무 등) |
| 2 | 옵션 `보류(남길 옵션 없음)` | 재실행하면 풀릴 가능성 |
| 1 | 회 써는 기계 저장 실패 | 스크립트가 실제 전송하는 값을 계측해야 잡힘 |
| 1 | 삭제 잠금 상품 | 앱에서 잠금 해제 필요 |

## run-dir

```
~/python_work/data/thumbnail/runs/2-2_0814        ① · review.html
~/python_work/data/thumbnail/runs/2-2_0814_ok2    ① 사용가능 2건 반영용
~/python_work/data/thumbnail/runs/2-2_0814a       ② · review.html
~/python_work/data/thumbnail/runs/2-2_0814a_r2    ② 최종 3건 반영용
```

되돌리기: `before_generate.json` + `restore --run-dir <R> --ids …`
