# 25-2 작업 인계 — 2026-08-15 (2차 · **이 문서 하나만 읽으면 된다**)

마켓그룹 **5번_용쌤25-2** (groupId `1001822`)
계정 = **부킹(용쌤)** `clwkdzjavjsl@gmail.com` — 작업 전 `bulsaja_my_profile` 로 확인할 것.
그룹 시트 `1DbR2upLX_DXjgjXpGPdide2SUifli_X6DTqhUTr9Syk`

> 직전 인계본 두 개는 **다시 열 필요 없다**. 결말난 것은 §7 에 옮겼다.
> · [`2026-08-15-25-2-작업인계.md`](2026-08-15-25-2-작업인계.md) (썸네일·상품명 축)
> · [`2026-08-15-25-2-신규958-작업인계.md`](2026-08-15-25-2-신규958-작업인계.md) (8/14~15 신규 수집분)

새 세션은 이렇게 시작하면 된다:
```
00-inbox/2026-08-15-25-2-작업인계-2.md 읽고 이어서 작업해
```

**세션 클리어 시점 상태**
- **도는 백그라운드 프로세스 없음.** 전부 정상 종료했다
- 크레딧 잔액 **57,600** · 이 그룹 누적 소모 **1,140 / 승인상한 2,475**
- 아래 수치는 8/15 저녁 실측이다. 그래도 시작 전에 현황판을 다시 뽑아라(§9 쿼리)

---

## 0. 현황판 실측 (2,295행)

| 축 | 완료 | 미착수 | 삭제·제외 | 보류 | 재작업 |
|---|---|---|---|---|---|
| 카테고리 | 2,176 | **0** | — | 116(저확신) | — |
| 상품명 | 1,678 | 70 | 328 | 211 | 32 |
| 옵션 | 1,869 | 19 | 389 | 16 | 14 |
| 썸네일 | 603 | **1,286** | 353 | 51 | 249 |

카테고리·상품명·옵션은 사실상 다 돌았다. **썸네일이 유일한 큰 미완결 축**이다
(빈칸 1,037 + 옵션발 재작업 249).

---

## 1. 이번 회차에 끝낸 것

### ★ 정합검사 불일치 108건 재생성 **완료** (535크레딧)

run-dir `thumbnail/runs/25-2_0815_audit_r1`

```
108건 (직전 회차 audit --commit 이 찍은 재작업 flag)
├─ prep         배치 0개 — 전부 대표옵션 확정 선기록(옵션정리가 끝나 있어 비전 판단 0)
├─ prescreen    단일특정 101 · 다중혼재 7(승격) · 실물없음 0
├─ run 팬아웃   선택 6 · 보류(기준이미지없음) 1
├─ 생성         107/107 · 실패 0     ← §4① 큐 정체를 뚫은 방법이 핵심
└─ verdict      사용가능 86 / 제외 21
```

| 단계 | 결과 |
|---|---|
| **`apply --commit`** | **반영 86건** · 현황판 썸네일 **107칸** · 옵션 재작업 flag 4칸 |

제외 21 내역: 제외 16 · 대표옵션의심 3 · 글자변조 1 · 기준이미지없음 1.
**같은 회차 재투입은 여기까지다**(1회 한정 규칙). 또 나온 불일치는 다음 회차로.

### 썸네일 미완결 3건 — 전부 **종결**

| 항목 | 결과 |
|---|---|
| `25-2_new0811_r2` 7건 판정 | `recover` 로 1건 회수 → **7/7 생성본 확보**(8/11에 나간 35크레딧 전액 회수). 반영 **3** / 제외 4 |
| 대표옵션 404 삭제 2건 | 삭제 완료 · 4개 축 `해당없음` |
| **저장실패 9건** | **재저장 불필요였다** — 실물 대조 결과 미저장 0건. 현황판 9칸만 `완료` 로 교정 (§4③) |

**r2 제외 4건과 사유** (판정은 Claude 직접 — 7건이라 팬아웃 기준 9건 미만):

| 상품명 | 판정 | 사유 |
|---|---|---|
| 전동 긴풍선 펌프 | 제외(글자변조) | 스펙 라벨 `Model:B231`→`B321`, 제조사 `BORO`→`BENGFU` |
| 원단 배경지거치대 | 제외 | 도면·옵션명 모두 걸이봉 **2개**인데 생성본은 **3개** |
| 금속 녹제거브러쉬 | 제외 | 헤드가 금색 컵휠 1개 → 와이어 브러시 휠 2개 + 가드판 |
| LED 워킹배너 | 제외(대표옵션의심) | 상품명은 "백팩형 워킹배너"인데 대표옵션 이미지가 40x40 벽걸이 라이트박스 |

> **사고 상품 확인 완료**: 직전 문서가 "반영 전 라이브를 한 번 더 보라"고 지목한
> `U01KZQCP57DHRGV76087WDVDNDA` 는 현재 대표(0번)가 `before_generate.json` 의
> 생성 전 원본과 **정확히 일치**한다. 8/11 복원이 유지되고 있고 미승인 잔류 없다.

---

## 2. 남은 일 — 우선순위 순

### ★ A. 카테고리 저확신 116건 → `consensus` (**크레딧 0 · 레버리지 최대**)

```
sellha조회실패 32 · 매칭불가 25 · 변경대상 20 · 현행유지(검색어오해) 15
부분일치확인요 11 · 보류(정체불명) 10 · 매칭불가(불사자미지원) 3
```
```bash
R=~/python_work/data/category-fix/runs/25-2_new0814
run_all.py consensus prep --run-dir "$R"   # → consensus_targets.json, Claude가 변형 2개 작성
run_all.py consensus run  --run-dir "$R"   # → 2/3 합의 통과분 자동 저장
```
**이게 풀리면 상품명 `보류(카테고리의심)` 175건이 자동 재진입한다.**
⚠ 다른 세션이 8/15 낮에 `consensus run` 을 돌리다 끝냈다. `consensus_result.json`(11:54)이
있으니 **어디까지 됐는지 먼저 읽어라** — 처음부터 다시 돌리지 말 것.

### B. **수정 업로드** — 계속 밀리고 있다 (직전 문서 §2-A 부터)

**이 그룹은 99.3%가 이미 판매 중인데 `--commit` 은 불사자 데이터만 바꾼다.**
지금까지 쌓인 판매 화면과의 격차:

| 축 | 저장한 건수 |
|---|---|
| 상품명 | 70(8/14) + 835(958분) |
| 썸네일 | 107(8/14) + 89(8/15 = 86+3) |
| 옵션 | 510(8/14) + 735(958분) |

묶어서 한 번에 거는 게 효율적이다. **판매 화면을 실제로 건드리는 일이라 규모·시점을
용팀장과 정하고 들어갈 것.**

### C. 썸네일 1,286건 — 큰 지출, **별도 승인 필요**

- **옵션발 재작업 249건**이 먼저다. 뿌리가 옵션축이라 옵션정리를 돌려야 풀린다
  (순서: **옵션 → 썸네일**. 2-3 실측에서 569 → 128건으로 줄었다)
- 빈칸 1,037건은 실측 기준 **약 5,000크레딧** 규모. 승인상한 2,475 를 넘는다

### D. 상품명 잔여

| 사유 | 건수 |
|---|---|
| 미착수(pending) | 70 |
| 빈칸 | 38 |
| `보류(카테고리의심)` | 175 → **A 가 풀면 자동 재진입** |
| `보류(키워드부족)` | 33 (셀러라이프 통다운 갱신 후) |
| 재작업 | 32 |

A 다음에 한 바퀴로 도는 게 싸다.

### E. 옵션 잔여

- 미착수 19 · 보류 16(그중 `보류(저장실패)` 11) · 재작업 14
- **저장실패의 원인 하나는 스킬 결함이다** — `run_options.py` 가 **제외행 포함 전 옵션**의
  이름 중복을 강제 해소해야 한다. 팬아웃 재호출로는 안 풀린다(958 문서 §2⑥ 실측)

### F. 그대로 두기로 한 것 (다시 묻지 말 것)

- **삭제잠금 32건** — 2026-08-14 용팀장 결정.
  `~/python_work/data/category-fix/runs/25-2_miup/gate/locked_2026-08-14.txt`
- **옵션축 409건** — 앱 UI 수동. MCP 로 못 바꾼다. 저장은 안 막힌다
- **시트에만 있고 그룹엔 없는 235건** — 작업 이력 보존용. 정리는 용팀장 판단
- 썸네일 보류 3종: `보류(기준이미지없음)` 13 · `보류(대표옵션의심)` 13 · `보류(제외)` 22

---

## 3. 손대지 않은 것 — 다른 세션 잔여물

`bulsaja_upload_tasks` 조회 중 발견. **내 작업과 무관하다.**

```
삭제 후처리 확인 필요 5건 — "BACKFILL-20260813 삭제 그룹 미완결 종결(원인: BLOCKED)"
  U01KN93G2XZV34KKY00VGNEATAK  U01KN93FWFFJQRHD9FDP7JMBKG4
  U01KN93FWBZYRCBTAGW9038780H  U01KN93FW86PNQZ8GYT0FDAAE1Q
  U01KN93FW9EZXCYT9R730JGZC4Z
삭제 실패 1건 — U01KN93G2X5XZKAKJ3VN45GNVBT (taskId 41319369)
```

---

## 4. 이번에 겪은 함정 (다음 세션이 반드시 볼 것)

### ① **불사자 AI 큐가 통째로 막히는 시간대가 있다 — `THUMB_POLL_TIMEOUT=60` 으로 갈아타라**

기본 설정(폴링 300초)으로 돌렸더니 **5건 연속 타임아웃, 성공 0**. 첫 접수는 **55분이 지나도
"생성 중"** 이었다. 그대로 뒀으면 107건 × 5분 = **9시간에 결과물 0** 이었다.

해법은 스킬 주석(`run_thumbs.py:80-86`)에 이미 있었다 — 2026-08-12 25-2 에서 겪은 것과
같은 함정이다:

```bash
# 접수만 빠르게 돌리고 recover 로 수확한다
THUMB_POLL_TIMEOUT=60 ... run_thumbs.py apply --run-dir <R> --generate --ids ${=IDS}
... run_thumbs.py recover --run-dir <R>     # 크레딧 0, 멱등, 몇 번이고 다시 불러도 된다
```

실측: **인라인 성공 65 + 회수 42 = 107건 · 크레딧 손실 0.**
큐는 도중에 풀렸다(초반 타임아웃 27% → 후반 34%로 오르내림). **타임아웃은 실패가 아니라
"아직"이다.**

### ② **재개 모드는 접수만 된 건을 다시 태운다 — 크레딧 이중 지출**

`generate_plan` 은 `"생성본"` 이 있는 건만 건너뛴다(`thumb_rules.py:125`). 접수돼서
`taskId` 만 있는 건은 **재개하면 또 접수된다.**

> **중단 후 재개할 때는 `--ids` 로 아직 접수 안 된 것만 지목하라.** 이번엔 6건(30크레딧)을
> 그렇게 피했다. 아니면 `recover` 를 먼저 돌려 `생성본` 을 채운 뒤 재개해도 된다.

### ③ **`보류(저장실패)` 는 "저장이 안 됐다"가 아니다 — 재저장 전에 실물부터 대조하라**

직전 문서의 "저장실패 9건 → 그 run-dir 로 커밋하면 229건 통째 재저장 위험" 이라는
숙제는 **실제로는 없는 일이었다.**

```
사용가능 판정 + 생성본 보유 232건 전수 대조 (현재 대표(0번) vs 생성본)
  → 일치 232 · 미저장 0 · 조회오류 0
  → 그런데 현황판엔 9건이 `보류(저장실패)` 로 남아 있었다
```

진짜 원인: commit3~5 가 반영을 끝냈고, **commit6 이 *이미 저장된 값*을 다시 쓰려다
HTTP 429(율제한)로 실패**했다. 그 실패가 시트에 굳었을 뿐 데이터 공백은 없었다.

> **232건 조회 5분으로 229건 재저장을 통째로 아꼈다.** 대조 스크립트는 재사용 가치가 있다 —
> `decisions.json` 의 `사용가능` × `generated.json` 의 `생성본` 을 `workdata()['썸네일'][0]`
> 과 비교하면 된다.

### ④ **안전하게 kill 하는 법 — 폴링으로 창을 잡으려 하지 마라**

`[기준]`/`[방어]` 개수가 같아지는 순간을 5초 폴링으로 잡으려 했는데, **방어 직후 바로 다음
`[기준]` 이 찍혀서 창을 계속 놓쳤다**(두 번 실패).

> **그냥 끊고, 짝 없는 `[기준]` 을 찾아 `restore --ids <pid>` 하는 쪽이 확실하다.**
> 실제로 1건(`U01KN93FW86YD2BP5VCBH9XMN62`)이 미승인 이미지를 대표로 달고 있었고 복원했다.
```bash
python3 -c "import re;L=open(LOG).read().splitlines();b=[m.group(1) for l in L if (m:=re.search(r'\[기준\] (\w+):',l))];g={m.group(1) for l in L if (m:=re.search(r'\[방어\] (\w+):',l))};print([x for x in b if x not in g])"
```

### ⑤ **404 삭제는 옵션 이미지를 여러 장 확인하고 지워라**

`audit_delete_404.json` 을 그대로 믿지 않고 재확인했다:
- **브라우저 UA 로도 404** (Python-urllib UA 차단이 아님 — 그 함정은 별개로 존재한다)
- **옵션 이미지 6장 전부 404** → 한 장만 죽은 게 아니라 타오바오 원본이 통째로 내려간 것

한 장만 죽었다면 소싱 원본 소멸이 아니므로 상품을 지우면 안 된다.

### ⑥ **워크플로 반환 요약은 계속 틀린다 — 디스크로 세라**

이번 회차에도 두 번 다 틀렸다. prescreen 반환은 "선택 108 · 보류 7"(합 115 > 대상 108)
이었고 **디스크 실제는 단일특정 101 · 다중혼재 7**. verdict 도 마찬가지였다.
매 단계 결과 파일을 직접 세고 **배치 원본과 id 집합을 대조**했다(id 길이 27 검사 포함 —
이번 회차엔 잘린 id 0건).

### ⑦ **불일치 재작업 합류는 `audit --commit` 이 먼저다** (2-3 세션 제보, 2026-08-15)

`audit --commit` 으로 현황판에 `재작업(정합검사: …)` flag 가 찍혀야 `prep --ids` 가 그걸
**생성 대상**으로 잡는다. flag 없이 prep 하면 `가공됨 + 대표옵션 있음` 분기로 떨어져
**도로 `audit_targets.json` 으로 분류되고 실제 대상 0건**이 나온다.

근거는 `run_thumbs.py:292` 주석이다 — "**재작업 flag 가 있으면 절대 빼지 않는다**".

> **SKILL.md §정합검사 "불일치 재작업 합류(1회 한정)" 에 이 전제가 빠져 있다.**
> 이번 108건은 직전 세션이 `audit --commit` 을 이미 쳐둔 상태를 이어받아 우연히 안 걸렸다.
> 2-3 은 그대로 걸렸다.

### ⑧ 하네스 잡기술

- `sleep N; <명령>` 조합은 차단된다. `run_in_background: true` 를 쓰거나 `until` 루프로
- 긴 대기 루프는 하네스가 **6~7분에 끊는다.** 프로세스는 안 죽으니 다시 걸면 된다
- `pgrep -f "run_thumbs.py"` 는 남의 zsh 껍데기까지 잡는다 →
  **`pgrep -f "bin/python.*run_thumbs.py"`** 로 좁혀라

---

## 5. run-dir

```
썸네일   thumbnail/runs/25-2_0815_audit_r1        ★ 이번 회차 (107 생성 · 86 반영)
         thumbnail/runs/25-2_new0811_r2           ★ 종결 (7/7 회수 · 3 반영)
         thumbnail/runs/25-2_new0811              저장실패 9건 = 유령이었음(§4③)
         thumbnail/runs/25-2_0814_main420         직전 회차
         └ audit_delete_404.json    (2건 — 이번에 삭제 완료)
         └ upload_census_420.json   (업로드 전수조사) · upload_census.py
         thumbnail/runs/25-2_{miup,miup_r2,ndm7,new0811_r2,redo_0814}
상품명   product-name/runs/25-2_{0814_fail80,new0814,new0814_r2,miup,ndm7_*,new0811*}
옵션     option-cleanup/runs/25-2_{0814_redo489,0814_r2,new0814,new0814_r2}
카테고리 category-fix/runs/25-2_new0814           ← A 작업 대상 (consensus 중간까지 됨)
         category-fix/runs/25-2_{miup,ndm7_*,new0811*}
```

**되돌리기**: 썸네일 `before_generate.json` · 옵션 `before_commit.json` ·
상품명 원본은 시트 `상품명` 탭 E열 · 삭제분은 불사자 휴지통

---

## 6. 크레딧

| 항목 | 값 |
|---|---|
| 이번 회차 소모 | **535** (썸네일 107장 × 5) |
| 회수분 | 42건(210) + r2 1건(5) — **손실 0** |
| 그룹 누적 | 605 + 535 = **1,140** |
| 용팀장 승인 상한 | 2,475 |
| 잔액 (8/15 저녁) | **57,600** |

> ⚠ **잔액으로 내 소모분을 검산하지 마라.** 계정을 공유한다(다른 세션 + 앱/웹 직접 작업).
> 이번에도 내가 아무것도 안 돌리는 사이에 −150 이 빠진 구간이 있었다.
> **내 소모는 로그의 접수 건수로만 센다.**

---

## 7. 직전 문서들에서 결말난 것

**`2026-08-15-25-2-작업인계.md`**
- §2-A **정합검사 불일치 108건** → **완료** (107 생성 · 86 반영)
- §2-B **r2 7건 판정** → **완료** · **404 삭제 2건** → **완료** ·
  **저장실패 9건** → **완료**(재저장 불필요, §4③)
- §3 상품명 표본검수 의심 3건 → **그대로 대기 중**(아래)

**`2026-08-15-25-2-신규958-작업인계.md`**
- §3-B 카테고리 consensus → 다른 세션이 8/15 낮에 착수, **중간까지 진행됨**(§2-A 참조)
- 나머지(A 옵션 저장실패 10 · C 옵션재작업 46+상품명 113 · D 옵션축 · E 썸네일 · F 235건)
  → **그대로**

**아직 그대로인 것**: 수정 업로드 · 옵션축 409 · 삭제잠금 32 ·
`run_thumbs.py` 재개(`committed.json`) 기능 부재 · `run_options.py` 이름중복 강제해소 미구현

### 상품명 표본검수 의심 3건 — 용팀장 판정 대기 (이월)

셋 다 불사자 미반영 확정. 시트 상태 `보류(표본의심)`.

| 상품id | 새상품명(보류) | 왜 |
|---|---|---|
| `U01KN93FWF7R0S5NMCTK3Y20J60` | 휴대용 정비리프트 받침대 이동식자동차리프트 기본형 | 실물은 **플라스틱 경사로**. 리프트가 아니다 |
| `U01KY9Y9TGZG89MWXGE77KXM1DC` | 학생 접이식1인책상 … **높이조절** 소형 기본형 | 원문에 `升降` 없음 · 이미지도 고정 다리 |
| `U01KN93G2Y2V15FAS5FD4TH7HMT` | 거실 원목원형스툴 쌓는 **4개세트** 기본형 | 옵션은 **색상 4종**. 4개 묶음 판매가 아니다 |

> **다음 회차 팬아웃 emphasis 에 넣을 것**: "증거(원문명·옵션 원문·썸네일)에 명시되지 않은
> 속성은 쓰지 않는다. 특히 **높이조절·세트 수량·기능어**는 원문에 근거 문자열이 있을 때만.
> 키워드에 그 낱말이 있다는 것은 근거가 아니다."

---

## 8. 다른 세션과의 조율

**계정(부킹) 크레딧과 율제한을 공유한다.** 작업 전 반드시:

```bash
pgrep -fl "bin/python.*run_(thumbs|options|names|all)\.py"   # 비어 있어야 시작한다
```

- 8/15 낮에 카테고리 `consensus run` 이 돌고 있었다(축이 달라 병행 진행함)
- **`apply --commit` 은 현황판 열 통짜 쓰기다** — 다른 세션이 같은 시트를 만지고 있으면
  마지막에 몰아서 한 번만 친다
- 용팀장 지시(2026-08-14): "썸네일 시작할 때 다른 그룹이 돌고 있으면 그 그룹을 멈추고
  25-2 우선"
- ⚠ 다른 세션 프로세스를 죽일 때는 §4④ 대로 — kill 후 짝 없는 `[기준]` 확인 → `restore`

### 2-3 세션 제보 (2026-08-15 저녁)

- **정합검사 불량률이 그룹 문제가 아니다 — 기저율 ≈ 35%.**
  2-3 394건에서 불일치 139(**35.3%**). 25-2 는 108/285(37.9%)였는데, 이관 전 백업
  (`audit_targets.json.bak`)으로 두 군을 갈라보니 이렇게 나뉜다:

  | 군 | 건수 | 불일치 | 비율 |
  |---|---|---|---|
  | `cdn.bulsaja.com` (원래 audit 대상) | 184 | 65 | **35.3%** |
  | `fal.media` (이관분) | 101 | 43 | **42.6%** |
  | 합계 | 285 | 108 | 37.9% |

  **내 순수 cdn 군 35.3% 가 2-3 의 35.3% 와 소수점까지 같다.**

  두 모수 다 **교란이 없다** (2-3 세션 확인). 2-3 은 옵션정리로 대표옵션이 바뀐
  재작업 123건이 `run_thumbs.py:292` 규칙에 따라 **생성 경로로 빠져나가서**
  audit 394건에는 안 들어 있다 — 즉 양쪽 다 "손도 안 댄 채 가공만 돼 있던" 군이다.
  성격이 같은 모수끼리 독립적으로 같은 값이 나온 것이므로 근거가 깨끗하다.

  **쓸 수 있는 형태로:**
  - `cdn.bulsaja.com` 가공됨 + 대표옵션 있음 → 불일치 **≈ 35%**
    (2-3 139/394 · 25-2 65/184 — **독립 재현**)
  - `fal.media` 가공됨 → **≈ 43%** (25-2 43/101 — 단일 관측)
  - 견적: 가공됨 N건 → 재생성 `0.35N × 5크레딧` + audit 비용 0

### ★ 스킬 개선 과제 — `fal.media` 를 가공됨으로 인정 (근거 확보됨)

`fal.media` 군이 **42.6%** 로 cdn 군(35.3%)보다 나쁘다. 지금 판별은 이 호스트를
"미가공 원본"으로 봐서 **재생성 대상으로 잡는다** — 분류 오류일 뿐 아니라
**더 불량한 군을 audit 없이 통과시키는 것**이다.

```
.claude/skills/bulsaja-thumbnail/scripts/thumb_rules.py:20
    PROCESSED_HOST = "cdn.bulsaja.com"        # ← 단일 문자열. 집합으로 바꿔야 한다
    is_processed_url()  (:38)  ·  product_status()  (:43)
회귀 테스트: scripts/test_thumb_rules.py (62건 — is_processed_url 관련 4건 포함)
```

⚠ **이관 자체는 여전히 이득이다** — 42.6% 가 불일치여도 **57.4% 는 멀쩡**하니
전건 재생성보다 audit 이 싸다. 이번에 이 경로로 **505크레딧** 아꼈다.
규칙을 고치면 그 이관이 자동화된다.
- **24자 절단이 2-3 에서도 났다** — prescreen 139건 중 1건
  (`U01KRZBANSMT7XB06WCBRZQH` → 실제 `…QHBRP`). 비율은 낮아도 디스크 대조를 안 했으면
  판정 없이 생성됐을 건이다. §4⑥ 대로 **매번 id 집합·길이(27) 대조**.
- **워크플로 반환값 불일치도 재현됐다** — run 팬아웃이 "완료배치 7" 이라 반환했는데
  디스크에 `result_002.json` 이 없었다. `apply` 감사가 "누락 4건" 으로 잡아줬고
  `pending` 이 그 배치만 정확히 다시 내놨다. **재팬아웃 1회로 복구.**
- 2-3 진행: r4 생성 120건(600크레딧) 진행 중 · r4a 139건 대기

---

## 9. 현황판 다시 뽑기

```bash
PYTHONPATH=.claude/lib .venv/bin/python -c "
from eroomlib import matrix
from collections import Counter
m=matrix.read('1DbR2upLX_DXjgjXpGPdide2SUifli_X6DTqhUTr9Syk')
def bucket(v):
    v=(v or '').strip()
    if not v: return '(빈칸)'
    for k in ('재작업','보류','완료','상품삭제','삭제대기','해당없음','진행중'):
        if v.startswith(k): return k
    return v[:30]
print('전체 행수:', len(m))
for t in ('카테고리','상품명','옵션','썸네일'):
    c=Counter(bucket(r.get(t)) for r in m.values())
    print(f'=== {t} | pending {len(matrix.pending(m,t))}')
    for k,v in c.most_common(): print(f'   {v}\t{k}')
"
```

---

**Last updated**: 2026-08-15 (2차)
