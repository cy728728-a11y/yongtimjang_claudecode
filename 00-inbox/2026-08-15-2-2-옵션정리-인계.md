# 2-2 옵션정리 인계 (2026-08-15)

그룹 `2번_용쌤2-2` (groupId **1001186**) · 시트 **1wMKUsOxy3wSzejGXLk0G6o22DLf8zcBDg3_6sbVXBZo**
계정 **부킹(용쌤)** — 작업 전 `bulsaja_my_profile` 확인.

## 딱 하나 남았다

**썸네일 재생성 6건.** ① 세트(`2-2_0814`)에서 제외 32건 중 26건은 끝났고 6건이 남았다.

```bash
R=~/python_work/data/thumbnail/runs/2-2_0814
SH=1wMKUsOxy3wSzejGXLk0G6o22DLf8zcBDg3_6sbVXBZo
P=".venv/bin/python .claude/skills/bulsaja-thumbnail/scripts/run_thumbs.py"

# 남은 대상은 매번 계산해서 뽑는다(이미 태운 건 재과금되니 --ids 를 손으로 적지 마라)
IDS=$(.venv/bin/python -c "
import json
R='$HOME/python_work/data/thumbnail/runs/2-2_0814'
dec=json.load(open(R+'/decisions.json')); gen=json.load(open(R+'/generated.json'))
ex=[k for k,v in dec.items() if v.get('판정')=='제외']
print(' '.join(p for p in ex
    if int((gen.get(p) or {}).get('재생성횟수') or 0)<2 and not (gen.get(p) or {}).get('taskId')))")

PYTHONPATH=.claude/lib $P apply --run-dir $R --sheet $SH --generate --ids ${=IDS} --allow-missing
#   → recover → verdict(팬아웃) → verdict --commit → apply --commit
#   2회째도 불합격이면 fallback(원본 유지, 크레딧 0)
```

> ⚠ **`--ids` 는 재개 모드가 아니다.** 이미 성공한 건도 다시 태워 크레딧을 또 낸다.
> 위처럼 `재생성횟수>=2` 와 `taskId` 있는 건(회수 대기)을 빼고 넘겨라.

> ⚠ **zsh 는 `$IDS` 를 단어 분리하지 않는다.** 반드시 `${=IDS}`. 안 그러면 문자열 1개로
> 들어가 "매칭 0건"이 되고, 스킬이 걸러줘서 크레딧 손실은 없지만 헛돈다(2026-08-15 실측).

**큐 경합 주의** — 2026-08-15 17:10 기준 다른 세션이 `2-3_r4` 생성 114건 중 30건째를 돌고
있었다. 불사자 AI 생성 큐가 하나라 붙이면 429가 는다(실측: 경합 시 30건에 6건 → 단독 시
68건에 1건). 6건뿐이니 큐가 빌 때 붙여라.

## 오늘 옵션 쪽에서 한 일 — 62건 저장

옵션 원장 **975 / 991 정리대상**.

| 갈래 | 건수 | 내용 |
|---|---|---|
| 워커판정 미반영 복구 | 20 | 2인용 자전거 0→104 · 24인치 자전거 0→54 · 자이언트플라워 0→32 · 인버터 0→21 |
| 구버전 잔재 재실행 | 41 | 45건 재실행분 |
| 나무조명 대표 교정 | 1 | 아래 참조 |
| 품목불일치 삭제 | 8 | 전부 **이미 휴지통**이었다 — 현황판 9개 열만 `상품삭제(품목불일치)`로 맞춤 |

## 오늘 밝혀낸 것 (같은 함정 다시 밟지 마라)

### 1. 워커 판정 없이 계획하면 조용히 전량 제외된다

28개 상품이 옵션 전량 `비상품/메인상품 아님`으로 떨어져 **유지 0**이 돼 있었다.
워커가 과하게 거른 게 아니라 **판정이 원장에 닿지 않은 것**이다.

- **진단법**: 원장의 `메인상품`·`대표옵션`·`기준가`가 **전부 빈칸**이면 이 경로다.
  워커가 실제로 판정한 건은 그 칸이 차 있다.
- 원인: run `2-2` 는 배치 199개에 **결과 0개**(워커 미실행)인데 그걸로 apply 가 돌았다.
  결과가 있는 run 은 `2-2u`(969건)였는데 반영된 적이 없었다.
- `[워커준수]` 감사가 "미언급 N건"을 경고로 찍지만 **저장을 막지는 않는다.**
  → 이걸 코드로 막을지는 미결. 썸네일 쪽 id 잘림처럼 fail-closed 로 바꾸는 게 맞아 보인다.

### 2. 08-07 스킬 교체 이전 보류는 판정거리가 아니다

옵션 보류 58건 중 **45건이 구버전 잔재**였다(작업일 전부 `2026-08-06`,
스킬 교체 커밋 `317981f` 가 08-07).

- `보류(기본형 수동판단)` 18건 — **폐지된 규칙.** 현재 코드에 문자열조차 없고
  `test_마커수동은_폐지됐다` 가 같은 입력에 `정리대상` 이 나오는지 못박아 뒀다.
  주석에 "용쌤1-1 에서 75건이 그랬다"는 전례까지 있다.
- `보류(기본형)` 20건 — 당시엔 전부-아니면-전무라 통째로 막혔다. 지금은 부분저장 + 재작업.
- **재실행하니 45건 중 42건이 계획됐고 41건이 저장됐다.** 사람 판정 0건.

→ **다른 그룹에서도 08-06 이전 보류가 쌓여 있으면 사람에게 올리기 전에 재실행부터 해라.**

### 3. 계획끼리 비교하지 말고 "지금 팔리는 상태"와 비교해라

`확인요` 6건을 "대표를 올리면 몇 개 팔리나"로만 비교해서 틀린 권고를 했다.
실제 라이브를 보니 **계획을 저장하면 오히려 줄어드는** 건이었다.

| 상품 | 지금 라이브 | 계획 저장 시 |
|---|---|---|
| 나무조명 LED | **8개 판매중** | 1개 (7개 감소) |
| 겨울 인조나무 | **18개 판매중** | 9개 (9개 감소) |

**`확인요` 로 저장이 막힌 건 사고가 아니라 방어였다.** 규칙이 제대로 작동한 것.

- **나무조명은 해결함**: 45,300원 `핑크 꽃가지 75cm`가 기준가를 끌어내리고 있었다.
  원문이 `粉色花枝`(꽃**가지**)로 본품 `樱花树灯`(나무등, `高1.5米`)과 **다른 품목**이라
  유지에서 빼니 대표가 `1.5m 480구 86,100원`이 되고 판매 8개 = 라이브와 동일.
  워커도 대표후보로 그 옵션(6번)을 이미 찍어놨었다 — 최저가가 아니라 스크립트가 무시했을 뿐.
- **인조나무는 의도적으로 손대지 않음**(용팀장 승인 2026-08-15). 0.5m~3.5m 가 전부 같은
  상품의 크기 변형이라 뺄 게 없고, 라이브가 최저가의 1.96배까지 팔고 있어 1.5배 규칙
  자체를 벗어나 있다. 18개 유지하려면 대표를 113,900원으로 올려야 하는데 진입가가 49% 뛴다.
  **`확인요` 로 두면 저장이 안 돼서 안전하다.**

## 남은 보류 16건

| 상태 | 건수 | 처리 |
|---|---|---|
| `보류(삭제대상)` | 8 | **완료** — 이미 휴지통, 현황판 정리됨 |
| `확인요` | 5 | 인조나무 포함 **의도적 보존**. 나머지는 최저가 대표가 이미 최선 |
| `보류(남길 옵션 없음)` | 2 | 오징어 롤러 · 실험용 건조 오븐 — 워커가 전 행 미언급, 재실행 필요 |
| `보류(저장실패)` | 1 | 아래 |

### 미해결: 회 써는 기계 저장 실패

`U01KS1WY76XQBXD9B7AHVQQ13XJ` — 서버가 거부한다:
`확인키 미발급: 최종 옵션 이름이 서로 겹쳐 저장할 수 없습니다. 겹치는 이름 1가지, 관련 옵션 2개.`

**네 군데를 다 확인했는데 중복이 없다** — 워커 교정명(1건뿐) · 차원별 최종명(7·8개) ·
SKU 조합명(56개) · 불사자 현재 이름. 스크립트가 **실제로 보내는 값**을 계측해야 잡힌다.
`--emit` 은 파일을 안 남겼다(미리보기에선 안 쓰이는 듯).

## 판정 대기 화면

https://claude.ai/code/artifact/bfc9bec3-e972-4abb-a5bd-4432a155832e
로컬 사본: `50-resources/attachments/2-2_판정대기큐_20260815.html`

썸네일 뺀 나머지 큐가 정리돼 있다. **상품명 260건**(카테고리의심 113 · 검증실패 108 ·
키워드부족 25 · 통다운없음 14)이 제일 큰 덩어리인데, 앞 221건은 **재교정 1회 한정 규칙**에
걸려 자동 재진입이 안 된다 — 그 규칙을 풀지 말지가 미결.
**옵션축 276건**은 MCP에 변경 API가 없어 앱에서 수동.

## run-dir

```
~/python_work/data/option-cleanup/runs/2-2_0815redo   (미반영 28건 복구)
~/python_work/data/option-cleanup/runs/2-2_0815stale  (구버전 잔재 45건)
~/python_work/data/option-cleanup/runs/2-2_0814       (나무조명 교정)
~/python_work/data/thumbnail/runs/2-2_0814            ← 남은 6건 여기
~/python_work/data/thumbnail/runs/2-2_0814a           (불일치 158건, 미착수 ≈790크레딧)
```

되돌리기: 옵션 `before_commit.json` · 썸네일 `before_generate.json`
