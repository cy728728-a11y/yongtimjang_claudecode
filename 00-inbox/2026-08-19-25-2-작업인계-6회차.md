# 25-2 작업 인계 (6회차) — 2026-08-19

마켓그룹 **5번_용쌤25-2** (groupId `1001822`) · 계정 **부킹(용쌤)** `clwkdzjavjsl@gmail.com`
그룹 시트 `1DbR2upLX_DXjgjXpGPdide2SUifli_X6DTqhUTr9Syk`

> 앞 회차: [`2026-08-17-25-2-작업인계-5회차.md`](2026-08-17-25-2-작업인계-5회차.md)
> 그 문서의 **§2-A(상품명 pending 56)** 과 **§2-B(보류 165)** 를 둘 다 끝내고,
> 5회차가 남긴 **§3① 결함**까지 고쳤다. 판정 큐는 전건 삭제로 비웠다.

새 세션은 이렇게 시작하면 된다:
```
00-inbox/2026-08-19-25-2-작업인계-6회차.md 읽고 이어서 작업해
```

**★ 다음에 할 일은 §2 다. 앞 네 축은 닫혔고, 이제 뒤 네 축이다.**

**세션 클리어 시점 상태**
- **도는 백그라운드 프로세스 없음.** 전부 정상 종료
- 계정 확인: `bulsaja_my_profile` → **부킹**이어야 한다
- 크레딧 **38,235** (이번 회차 **0 소모** — 상품명·옵션은 크레딧을 안 쓴다)
- 워킹트리 깨끗함. 스킬 수정 6건은 다른 세션 커밋 `f5eaf7d` 에 함께 들어갔다
- 통다운 **260819** 신선(0일차). 260811 은 만료
- 아래 수치는 스냅샷이다. 현황판을 다시 뽑아라:
  ```bash
  PYTHONPATH=.claude/lib .venv/bin/python -c "
  from eroomlib import matrix
  from collections import Counter
  import re
  m=matrix.read('1DbR2upLX_DXjgjXpGPdide2SUifli_X6DTqhUTr9Syk')
  def n(s):
      s=(s or '').strip()
      return re.split(r'[:(]', s,1)[0].strip() if s else '(빈칸)'
  for t in ('카테고리','상품명','옵션','썸네일','상세','지재권','배송비','업로드'):
      print(f'{t:5s} pend {len(matrix.pending(m,t)):5d} |',
            ' · '.join(f'{k} {v}' for k,v in Counter(n(r.get(t)) for r in m.values()).most_common(5)))
  "
  ```

---

## 0. 이번에 한 것 — 앞 네 축을 닫았다

| 축 | pending 이동 | 결과 |
|---|---|---|
| **상품명** | 54 → **1** | **반영 201건** / 불량 0 / 오류 0 |
| **옵션** | 16 → **0** | 반영 13건 · 축 8개 · 부분저장 0 / 실패 0 |
| 카테고리 | 0 → 0 | (앞 회차에 종결) |
| 썸네일 | 1 → 5 | 옵션 저장이 되찍은 미아 (다음 회차 몫) |

**판정 큐 10건을 용팀장 지시로 전건 삭제했다** — §4-A.
**스킬 결함 6건을 고쳤다** — §3. 그중 4건은 5회차 이전부터 조용히 새던 것이다.

크레딧 **0**. 이 회차는 상품명·옵션만 돌아 AI 생성이 없었다.

---

## 1. run-dir

```
상품명  ~/python_work/data/product-name/runs/25-2_0817p        미아회수 12건 + 통다운밖
        ~/python_work/data/product-name/runs/25-2_0817p_nokw   무키워드 23건
        ~/python_work/data/product-name/runs/25-2_0819b        §2-B 본체 178건(배치 91)
        ~/python_work/data/product-name/runs/25-2_0819b_nokw   §2-B 무키워드 101건(배치 57)
옵션    ~/python_work/data/option-cleanup/runs/25-2_0819o1     16건
```

**되돌리기**: 옵션 `before_commit.json` → `restore --run-dir <R>` · 상품명 원본은 시트 `상품명` 탭 E열

---

## 2. ★ 다음 작업 — 뒤 네 축이 통째로 남았다

앞 네 축이 닫히면서 **상세·지재권·배송비·업로드가 각 1,886건씩 빈칸**으로 드러났다.
이게 지금 압도적으로 큰 덩어리다.

| 축 | pending | 성격 |
|---|---:|---|
| **상세** | 1,886 | AI 상세페이지 생성 — **크레딧이 든다**(§2-A) |
| **지재권** | 1,886 | 상표·디자인 위험 검사 |
| **배송비** | 1,886 | 배송비 추천·설정 |
| **업로드** | 1,886 | 마켓 업로드 |

### A. 상세페이지는 규모 판단을 먼저 받아라

`bulsaja-detail-page` 스킬은 기본 10장 · 일반화질이다. **1장당 5크레딧** →
**1,886건 × 10장 × 5 = 94,300 크레딧**인데 잔액은 **38,235** 다. 전량은 못 돌린다.

**접수 전 예상 크레딧 보고가 스킬 의무다.** 용팀장에게 물을 것:
- 몇 건을 돌릴지(잔액으로는 약 760건이 한계)
- 장수를 줄일지(10장 → 5장이면 1,520건)
- 크레딧을 더 채울지

### B. 지재권·배송비는 크레딧 0이다

먼저 돌려도 손해가 없다. **상세보다 이쪽을 먼저 태우는 게 맞다** — 크레딧 결정을
기다리는 동안 진도가 나간다.

### C. 수집 pending 954건은 **표기 누락이다 — 확인했다**

`수집` 열이 954건 빈칸이라 미수집처럼 보이지만, **그 954건 중 936건은 카테고리가
`완료`** 이고 상품명 802 · 옵션 773 · 썸네일 745가 이미 `완료` 다. 나머지도 대부분
`상품삭제(제외카테고리)` 다. 즉 **뒤 축이 다 돌아간 상품인데 `수집` 열만 안 찍힌 것**이다.

뒤 네 축 작업에 영향이 없으니 급하지 않다. 정리하려면 한 줄이면 된다:
```python
from eroomlib import matrix
m = matrix.read(SHEET)
tgt = {p: '완료' for p in m
       if not (m[p].get('수집') or '').strip()
       and (m[p].get('카테고리') or '').startswith('완료')}
matrix.mark_many(SHEET, '수집', tgt)   # 실측 936건
```

---

## 3. 이번에 고친 스킬 결함 6건 (전부 실측에서 나왔다)

> 전 스위트 통과(product-name 162 · thumb_rules 70 · 나머지 11개 파일).
> 커밋 `f5eaf7d` 에 포함됨.

### ① 상품명에 미아 회수 장치가 없었다 — `_recover_group_orphans`

`prep` 의 모집단은 `collect_group.py` 가 받아온 그룹 목록 하나뿐인데,
**불사자 그룹 목록 API 가 살아 있는 상품을 빠뜨린다.** 25-2 실측: pending 54건 중
**13건이 그룹 1879건에 없었고**, 조회하면 멀쩡히 존재했다(옵션·썸네일은 `완료`).

`prep` 은 이걸 조용히 빼고 `[2/6] 대상 N건` 만 찍는다 — **에러도 경고도 없다.**
이번엔 `group.json` 에 손으로 넣어 뚫었다. 옵션정리엔 같은 장치(`_recover_orphans`)가
있는데 이 축에만 없었다.

### ② 삭제 게이트가 큐를 안 비웠다 — `_mark_gone_column`

`_drop_gone` 은 워커 비용을 막아주지만 **현황판 pending 은 그대로 둔다.**
다른 7개 열엔 `상품삭제(…)` 가 찍혔는데 `상품명` 열만 빈칸인 건이
**회차마다 "남은 일감"으로 다시 세어졌다** — 25-2 pending 54건 중 **18건**이 이것이라
5회차 인계문서가 실제 작업량을 잘못 적었다.

이제 **다른 열이 쓰는 삭제 상태를 그대로 복사**해 찍는다(사유를 새로 짓지 않는다).

### ③ ★ 카테고리교정 시트 J열 156건이 다른 상품 것이었다 — `_drop_stale_jk`

**워커 3명이 각각 독립적으로** "J열이 원문명·옵션명과 전면 상충한다"고 보고해서 전수
대조했더니, 같은 상품id가 두 곳에서 완전히 다른 물건이었다:

| 상품id | 불사자 현재 | 카테고리시트 J열 |
|---|---|---|
| `…FW9848P2C…` | 모니터 받침대 | 2인용 패브릭 소파 |
| `…FW7M2BEE…` | 캠핑카 테이블 다리 | 로보락 직배수 키트 |
| `…FW7BXR9B…` | 접이식 사다리 | 지하수위 측정기 |

전부 `U01KN93FW…` 대역에 몰려 있어 **상품id 재할당**으로 보인다.
지시서는 "J열이 있으면 그게 실물"이라는 신뢰 원칙이라, 그대로 따르면 모니터 받침대에
소파 이름이 붙는다. **이번엔 워커가 눈치채고 보류로 뺐지만 그건 재량이지 장치가 아니다**
(그래서 3건이 작업 손실).

`prep` 이 시트 B열 ↔ 현황판 `상품` 열을 bigram 대조해 **어긋나면 J·K를 둘 다 버린다.**
버려도 증거 4종으로 직접 판정하는 구버전 경로를 탈 뿐이라 손해가 없다 — **의도한 비대칭**이다.
지시서에도 같은 방어를 넣었다(이중). 버린 목록은 `<run-dir>/jk_stale.json`.

### ④ 옵션↔썸네일 왕복 카운터 — `close_roundtrip` (5회차가 남긴 숙제)

종결 장치가 `prep` 경로에만 있고 `verdict` 에는 없어 무한 왕복이 돌았다.
`verdict --commit` 이 옵션 열에 이미 `실물기준없음` 이 있으면 되돌리지 않고 종결시킨다.
**실측 검증: 5회차가 손으로 끊은 그 25건을 코드가 자동으로 잡는다.**

### ⑤ 원본유지가 품질 검사를 통째로 면제했다 (지시서)

`원본유지사유` 게이트가 R2·R3를 면제하는데, **원본이 나쁘면 그대로 통과한다.**
실측: `전동 호이스트 체인 가방 체인 호이스트 리프팅 체인 보관 가방 전동 호이스트
액세서리 방청 체인 가방` — `체인`×4 · `호이스트`×3 · `가방`×3 에 60자.
워커 판단("실물 지칭이 맞다")은 옳았다. 빈틈은 **"이름으로 쓸 만한가"를 안 물은 것.**

→ 지시서에 판정 기준 추가: **같은 낱말 3회 이상 또는 45자 초과면 원본유지 금지.**
13건 중 12건은 정상이었으니 규칙은 유효하고 경계만 좁혔다.

### ⑥ `mark` 가 현황판을 안 고쳐 두 저장소가 갈렸다

표본검수로 뺀 3건이 `상품명` 탭엔 `보류(표본의심)` 인데 현황판엔 `진행중(미반영)` 로
남았다. **`진행중` 은 pending 이 아니라 다음 prep 이 집지도 않아 영영 굳는다** —
현황판을 원장으로 읽는 다음 세션이 "반영 대기"로 오해할 자리다.
이제 `cmd_mark` 가 현황판도 함께 찍는다(`--no-matrix` 로 끌 수 있다).

---

## 4. 남은 일 — 우선순위 순

### A. 판정 큐 10건 → **용팀장 지시로 전건 삭제 완료** (2026-08-19)

화면으로 보고한 뒤 전건 삭제 지시를 받았다. `scope:ALL` · `MARKET_AND_SOURCE`.
**현황판 9개 열 전부에 `상품삭제(용팀장 지시·2026-08-19)` 를 찍었다**(90칸).

| 갈래 | 건수 | 무엇 |
|---|---:|---|
| 상품명 표본의심 | 4 | 스노우보드백→스키복세트 · 카펫→"사진배경천" · 회전붐대→"고압호스" · 호이스트 체인가방(60자 스팸) |
| 옵션 판매0 | 3 | 1인용 라운지체어(옵션이 전부 2인세트) · 아치형 욕실거울(방담 추가옵션뿐) · 스텐 작업대 1.8m |
| 옵션 근거부족 | 3 | 화장대 · 다기세트 · 미용카트 |

**결과**: 스마트스토어 대기열 4건(`41841257`~`41841260`) · 소싱 휴지통 4건 · 이미 휴지통 2건.
**대기열 4건은 다음 세션에서 `bulsaja_upload_tasks(mode=list)` 로 완료를 확인해라.**

### B. 썸네일 pending 5건 — 옵션 저장이 되찍은 미아

이번 옵션 13건 저장이 `_recover_orphans` 로 썸네일 열을 되찍은 결과다(설계대로).
한 바퀴 돌리면 닫힌다. **크레딧이 든다**(생성 5건 × 5 = 25크레딧 수준).

### C. 종결로 보는 보류 — 손대지 않는다

| 축 | 건수 | 성격 |
|---|---:|---|
| 상품명 | 27 | 카테고리의심 17 · 실물불명 7 · 키워드부족 3 — 전부 재진입 상한 소진 |
| 옵션 | 36 | **왕복종결 25** · 저장실패 8 · 삭제대상 2 · 조회실패 1 |
| 썸네일 | 93 | 기준이미지없음 50 · 제외 22 · 대표옵션의심 13 등 — 실물 사진이 없는 상품군 |

**옵션 `보류(저장실패)` 8건은 재저장 전에 실물부터 대조한다**(스킬 §`보류(저장실패)` 를 믿지 마라).
두 회차 이상 반복되면 스킬 지침대로 삭제한다.

### D. 사람만 할 수 있는 것

- **삭제 잠금 32건** — 불사자 앱에서 용팀장이 풀어야 삭제된다
- **삭제대기 36건** — 그 잠금이 풀리면 자동 처리
- **속성·태그 제안** — 불사자 MCP에 저장 API가 없어 스마트스토어 수동 입력
- **셀러라이프 통다운 갱신** — 자동 갱신이 구글 계정 선택 화면에서 멈춘다(10분 타임아웃).
  이번엔 다른 세션이 260819를 올려 해결했다. 막히면
  `.claude/skills/sellerlife-keyword/scripts/download.py --setup` 으로 로그인 세션을 살린다

### E. 상품명 pending 1건 · 카테고리 잔여

- 상품명 1건: `U01KYCYKS8B488RSPYMMDF9J32C` — `재작업(카테고리 변경)`
- 카테고리: 매칭불가 8 · 변경대상 4 · 보류 2 (전부 앞 회차 종결 판정)

---

## 5. 이번 회차 실측치 (다음 견적용)

| 단계 | 규모 | 워커 | 소요 |
|---|---|---|---|
| 상품명 prep(§2-B) | 197건 지정 → 178건 대상 | — | ~7분 (통다운 스캔 포함) |
| 상품명 팬아웃(§2-B) | 배치 91 / 178건 | 27 | **17분 · 2.55M 토큰** |
| fix-r9 | 8건 교정 | — | 즉시 · **재팬아웃 4배치 절약** |
| 재팬아웃 | 배치 4 | 2 | 7분 · 0.21M |
| append | 178행 | — | **~3분**(2분 타임아웃 초과 → 백그라운드 필수) |
| nokw 팬아웃 | 배치 57 / 101건 | 17 | 6분 · 1.05M |
| 옵션 prep | 16건 · 옵션 277 · 이미지 151 | — | ~2분 |
| 옵션 팬아웃 | 배치 4 | 4 | 3분 · 0.28M |
| 옵션 `apply --commit` | 13건 저장 | — | ~2분 |

**§2-B 의 핵심 수치**: 5회차가 "직결어 0"이라 보류한 166건이 이번엔 **통다운밖 0건** —
전부 뷰가 생겼다. 그 사이 카테고리가 교정됐기 때문이다. 다만 뷰가 있어도 직결어가 없는
니치 상품이 **101건**이었고, nokw 라운드가 **전건 생성(보류 0)** 으로 받아냈다.

**원본 오기재 교정 실적**(nokw 라운드의 본래 목적):
주방카트→쌀통 · 반려견 훈련용품→유아 놀이후프 · 플라스틱 팔레트→양어수조 ·
잭서포트→실험용 XYZ 승강스테이지 · 와인양조기→정유 증류기 · 돈가스 프레스→전병 압착기

---

## 6. 다음에 그대로 쓸 명령

```bash
SHEET=1DbR2upLX_DXjgjXpGPdide2SUifli_X6DTqhUTr9Syk
G="5번_용쌤25-2"

# ── 상세페이지 (크레딧! 접수 전 예상 보고 필수)
run_detail.py prep --group-name "$G" --sheet $SHEET --run-dir "$R" --limit <N>

# ── 옵션 한 바퀴
run_options.py prep  --sheet $SHEET --run-dir "$R" --batch-size 4
run_options.py pending --run-dir "$R"                  # → Workflow{name:"optclean-fanout"}
run_options.py apply --sheet $SHEET --run-dir "$R"      # 미리보기 = 표본검수 입력
run_options.py apply --sheet $SHEET --run-dir "$R" --commit --no-sheet --no-review

# ── 상품명 한 바퀴
run_names.py prep --group-id 1001822 --group-name "$G" --sheet $SHEET --run-dir "$R"   # --source-date 생략 = 최신
run_names.py fix-r9 --run-dir "$R" --commit && run_names.py check --run-dir "$R"       # 재팬아웃 전에 반드시
run_names.py append --run-dir "$R" --group-name "$G" --sheet $SHEET                    # 100건 넘으면 백그라운드
run_names.py rename --run-dir "$R" --group-name "$G" --sheet $SHEET --commit
```

**긴 작업은 셸에서 분리해 띄운다**(5회차 §3④ 그대로 유효):
```bash
nohup env PYTHONIOENCODING=utf-8 PYTHONPATH=.claude/lib .venv/bin/python <명령> > "$R.log" 2>&1 < /dev/null &
disown
```
**zsh 주의**: `--ids $IDS` 는 한 덩어리로 넘어간다. 반드시 `${=IDS}`.
**append 는 2분을 넘긴다** — 100건 이상이면 처음부터 백그라운드로 띄워라.

---

## 7. 종합보고

### 크레딧 0 소모 · 잔액 38,235 · 저장실패 0

**상품명 — 반영 201건 / 불량 0 / 오류 0**
- §2-A(pending 54): 28건 반영 — 미아회수 8 · nokw 20
- §2-B(보류 197): 173건 반영 — 본체 73 · nokw 100
- 삭제 확정분 정리 18건(현황판 마킹만) · 미아 편입 12+14건
- J열 backfill 26+37+4 = **67건**
- 표본검수: §2-A 28건 전건(의심 0) · §2-B 35건(의심 2) · nokw 101건 기계스캔+34건 눈검수(의심 1)
- 검증실패 2건 잔존(재시도 상한 소진 — 종합보고 대상)

**옵션 — 반영 13건 / 부분저장 0 / 실패 0**
- 축 이름 8개 저장(신규 0 · 갱신 9 · 판단완료 유지 1)
- 워커준수: 무분류 0 · 사유 기재 14행
- 제외 사유 실측: 홍보배너(`所有款式有6种颜色，可咨询客服`) · 맞춤제작 문의(`定制联系专拍`) ·
  2인세트 다른모델 · 1.5배 상한 초과
- ⚠️ 모자걸이대는 3층이 대표라 상한 156,000원인데 8층이 **156,100원 — 100원 차이로 잘렸다**.
  규칙대로지만 8·10·12층이 전부 빠져 3층·6층만 남는다

**삭제 — 10건**(§4-A) · 현황판 90칸 마킹

**보고 화면**: https://claude.ai/code/artifact/4df99d2c-b48b-43da-a4c4-4f0ee5e1b70f
(판정 10건 + 잠금 32 + 대기 36. 삭제 지시를 받아 10건은 이미 처리됨)

---

## 8. 커밋 전에 볼 것

**이 세션이 만든 워크스페이스 파일은 이 인계문서 1개뿐이다.**
```
?? 00-inbox/2026-08-19-25-2-작업인계-6회차.md      (이 문서)
```
스킬 수정 6건은 **다른 세션 커밋 `f5eaf7d` 에 이미 들어갔다** — 워킹트리는 깨끗하다.

### 이번 회차가 검증한 것

- **5회차 §2-B 의 판단이 옳았다** — "카테고리가 풀렸으니 재진입 대상"이라 본 166건이
  통다운밖 0건으로 전부 뷰를 얻었다
- **새 수정 3건이 실전에서 즉시 발화했다** — 같은 prep 로그 한 화면에
  `J·K 156건 대조 불일치` · `삭제대기 5건 제외` · `미아 14건 편입` 이 함께 찍혔다
- **fix-r9 가 재팬아웃을 대체한다** — §2-B 에서 8건을 고쳐 실패배치 12→4로 줄였다
- **표본검수가 마지막 방어선이라는 게 다시 확인됐다** — 걸린 3건(카펫→배경천 ·
  붐대→호스 · 60자 스팸)은 전부 R1~R12 를 통과한 것들이다

---

**Last updated**: 2026-08-19
