# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp
*~

# Claude Code local settings (per-user)
.claude/settings.local.json

# Node / scripts
node_modules/
*.log

# Python
__pycache__/
*.pyc
.venv/
venv/
.python-version

# Secrets
.env
.env.local
*.key
*.pem

# Temp / work
00-inbox/*.tmp
.playwright-mcp/
10-projects/이메일-자동화/.state/

# Skill cache
.cache/
NTUSER.DAT*
err.txt

# PC별 경로 오버라이드 (per-machine, 커밋 금지)
workspace.toml

# 이메일 정리 로그 (메일 제목·발신자 포함 — 커밋 금지)
10-projects/이메일-자동화/logs/

# 웹앱(셀러 관제탑) — 로컬 전용 산출물
.venv-web/
webapp.db*
webapp-profile.json
webapp-logs/
# 이 프로젝트는 npm 을 쓰지 않는다. 정적파일은 webapp/static/vendor/ 에 curl 로 박아 둔다.
package.json
package-lock.json

# gsd code-review --fix 의 진행 중 복구 표식 (워크트리 병합되면 사라진다)
.planning/phases/*/.review-fix-recovery-pending.json
